{
  "schema": "lesson-overlay/v1",
  "week": 2,
  "day": 2,
  "parts": {
    "1": {
      "checkpoints": [
        {
          "question": "`const numbers = [1, 2, 3];` followed by `numbers.push(4);` — does that compile?",
          "options": [
            "No — `const` makes the array read-only, so `push` is refused",
            "Yes — `const` stops you pointing `numbers` at a different array; it does not freeze the array's contents",
            "Yes, but only because the array was annotated `number[]`"
          ],
          "answer": 1,
          "explanation": "`const` is about the binding, not the value. `push` mutates the array in place and `numbers` still points at the same array, so nothing is reassigned. This is exactly the shape of a locator: the `const` is fixed, the element it refers to is not."
        },
        {
          "question": "Why does `const emailBox = page.locator(\"input[type='email']\")` need no `: Locator` annotation?",
          "options": [
            "Because TypeScript infers the type from what `.locator()` returns",
            "Because locators are exempt from type checking",
            "Because `const` declarations are never type-checked, only `let` ones are"
          ],
          "answer": 0,
          "explanation": "Inference: TypeScript reads the type off the right-hand side, exactly as it decided `const name = \"Ada\"` was a `string`. The type is still static and still checked — you just did not have to type it out."
        }
      ]
    },
    "2": {
      "at_a_glance": "| | |\n|---|---|\n| **You'll use** | `xpath=//tag[@attr='v']` · `tag[attr='v']` · `expect().toContainText()` · `.toHaveValue()` · `.toHaveURL()` |\n| **Assumes** | [const and type inference](/learn/w2/d2/p1) · [locators](/learn/w1/d4/p2) |\n| **Mode** | mixed — locator syntax runs live, but `expect()` only retries inside the test runner, so assertions go into a spec |\n| **The shift** | [Day 1](/learn/w2/d1/p2) could only prove nothing threw. From here a test can fail for the right reason. |",
      "checkpoints": [
        {
          "question": "Which XPath belongs in code you intend to keep?",
          "options": [
            "`/html/body/div[1]/div[2]/form/input[3]` — it is the most precise",
            "`//input[@type='email']` — it matches anywhere in the document",
            "Either; they are two spellings of the same thing"
          ],
          "answer": 1,
          "explanation": "An absolute path encodes the whole document structure, so one inserted `<div>` anywhere above your element breaks it. A relative XPath matches on the element's own attributes and survives the markup moving. The lesson shows absolute XPath only so you recognise it and avoid it."
        },
        {
          "question": "What does `expect(locator).toContainText(\"Demo\")` do that a plain `if (text.includes(\"Demo\"))` does not?",
          "options": [
            "It retries until the locator matches or it times out",
            "It searches the whole page rather than one element",
            "It is case-insensitive"
          ],
          "answer": 0,
          "explanation": "Web assertions retry for 5 seconds by default, which is what makes them safe against a page that has not finished rendering. A plain `if` checks once, right now, and a page that needed another 200ms fails for no real reason. Retrying is also why `expect()` needs the test runner behind it."
        },
        {
          "question": "The real heading on the page is `\"Automation Demo Site \"` — with a trailing space. Why does `toContainText(\"Automation Demo Site\")` pass anyway?",
          "options": [
            "Because `toContainText` only checks that the text starts with the string",
            "Because Playwright's text matchers normalise whitespace before comparing",
            "Because the assertion retried until the space was removed"
          ],
          "answer": 1,
          "explanation": "Text matchers normalise whitespace, so the trailing space never reaches the comparison. Worth knowing precisely because it is not universal — [Day 3](/learn/w2/d3/p2) shows where that normalisation does *not* happen, and the raw string bites."
        }
      ],
      "recap": "- Relative XPath (`//tag[@attr='v']`) always; absolute XPath never.\n- XPath can go up and sideways in the DOM; CSS can only go down and to the next sibling. That is the one real reason to reach for XPath.\n- `expect()` **retries**; a plain `if` checks once. That is the whole difference, and it only works inside the test runner.\n- `toContainText` for a fragment, `toHaveText` only when you know the exact string. Text matchers normalise whitespace.\n- Assert something observable. \"It didn't throw\" is not an assertion."
    },
    "3": {
      "at_a_glance": "| | |\n|---|---|\n| **New API** | none — every line ran in [Day 2.2](/learn/w2/d2/p2) |\n| **What changes** | three repeated `expect()` calls become one `expectFormFilledCorrectly(page, email)` |\n| **Builds on** | [Day 1.3](/learn/w2/d1/p3)'s helper file, which now holds an action helper *and* a check helper |",
      "checkpoints": [
        {
          "question": "A page object must never assert. So why is `expectFormFilledCorrectly` allowed to be nothing but `expect()` calls?",
          "options": [
            "Because it is a different pattern — a shared assertion helper, where asserting is the entire job",
            "Because the no-assertions rule only applies once you are using classes in week 3",
            "Because `toHaveValue` is exempt; only `toHaveURL` is banned from a page object"
          ],
          "answer": 0,
          "explanation": "The rule is about keeping actions and checks in separate places, not about banning assertions from helpers. A page object holds the actions; a shared assertion helper holds the checks. What would be wrong is one function that fills the form *and* asserts on it."
        },
        {
          "question": "Your suite has three tests that each fill this form and check it was accepted. The Email field is renamed. How many places should need editing?",
          "options": [
            "Three — once per test",
            "Two — the action helper and the assertion helper",
            "Six — each test's fill and its assertion"
          ],
          "answer": 1,
          "explanation": "That is the point of both of today's helpers: the locator appears once in `fillRegistrationForm` and once in `expectFormFilledCorrectly`, no matter how many tests call them. Inline, the same rename would be six edits and you would miss one."
        }
      ],
      "recap": "- Copy-pasted `expect()` calls are the same problem as copy-pasted actions, with the same fix: put them in one function.\n- Name a checker for what it checks (`expectFormFilledCorrectly`), so a reader can tell at the call site that nothing is being *done* to the page.\n- Keep actions and checks in separate functions. Week 3 formalises that split into a class; the discipline is identical today."
    },
    "4": {
      "at_a_glance": "| | |\n|---|---|\n| **Three problems** | XPath by hand, then assertions, then a helper of your own |\n| **Pulls on** | [Day 2.1](/learn/w2/d2/p1) for `const` · [Day 2.2](/learn/w2/d2/p2) for matchers · [Day 2.3](/learn/w2/d2/p3) for the shape |\n| **Watch for** | an assertion that passes for the wrong reason — it is the failure mode this whole day exists to fix |"
    }
  }
}
