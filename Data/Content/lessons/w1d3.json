{
  "schema": "lesson-overlay/v1",
  "week": 1,
  "day": 3,
  "parts": {
    "2": {
      "at_a_glance": "| | |\n|---|---|\n| **Focus** | Running your project's tests from the command line, and reading the results and the report. |\n| **Goals** | Run every test or only some of them, explain why two tests produce six results, and describe what `package.json` and `package-lock.json` hold. |\n| **Prerequisites** | The project that you created in [Day 2 - Fundamentals](/learn/w1/d2/p2). |\n| **Tools** | `npx playwright test`, the options `--project` and `--headed`, and `npx playwright show-report`. |\n| **Environment** | The integrated terminal in VS Code, in your project folder. |",
      "checkpoints": [
        {
          "question": "Your example file contains two tests, but `npx playwright test` reports `6 passed`. Why?",
          "options": [
            "Playwright runs every test three times by default, to be sure of the result.",
            "The configuration file lists three browsers, and each test runs once in each.",
            "Playwright counts every check inside a test as a separate test in the output."
          ],
          "answer": 1,
          "explanation": "`projects` lists Chromium, Firefox, and WebKit, and Playwright runs every test once per project. Two tests in three browsers make six runs. With `--project=chromium`, the same two tests report `2 passed`."
        },
        {
          "question": "Which file do you never edit by hand, and why?",
          "options": [
            "`package.json`, because npm replaces any change that you make to it",
            "`package-lock.json`, because npm writes it to record exact versions",
            "`playwright.config.ts`, because Playwright writes it again on every run"
          ],
          "answer": 1,
          "explanation": "`package.json` is yours: it lists the packages your project needs. `package-lock.json` records the exact version of every installed package, so that a teammate installs the same versions. npm writes the lock file, and you save it with your project."
        },
        {
          "question": "A test result surprises you, and the terminal output does not explain it. What should you open?",
          "options": [
            "The report, with `npx playwright show-report`",
            "The configuration file, to change the `reporter` setting",
            "The `node_modules` folder, to check that Playwright is installed"
          ],
          "answer": 0,
          "explanation": "The report shows every test and each of its steps, which is more than the terminal can show. It is created after every run, so you can open it without running the tests again."
        }
      ],
      "recap": "- The **command line** means typing commands in a terminal. `npx playwright test` runs every test in the project.\n- Every test runs once in each browser that `projects` lists, so two tests in three browsers report six results.\n- Options such as `--project=chromium` and `--headed` change what a command runs and how.\n- `npx playwright show-report` opens the report of the last run in your browser.\n- `package.json` lists your dependencies. npm writes `package-lock.json` to record their exact versions."
    },
    "3": {
      "at_a_glance": "| | |\n|---|---|\n| **Focus** | Every setting in your project's configuration file, and what each one does. |\n| **Goals** | Recognize each setting in `playwright.config.ts`, and explain `retries`, `trace`, and `projects`. |\n| **Prerequisites** | [Fundamentals](/learn/w1/d3/p2), where you ran the tests in your project. |\n| **Environment** | Your project's `playwright.config.ts` in VS Code, and the editor on the right for the example. |\n| **Scope** | This lesson helps you recognize each setting. Timeouts, and `projects`, `workers`, and `retries` in depth, are covered in a later lesson. |\n| **Key takeaway** | Record evidence of every run, and keep it only when a test fails. |",
      "checkpoints": [
        {
          "question": "Why do many teams set `trace`, `screenshot`, and `video` to keep recordings only when a test fails?",
          "options": [
            "Because Playwright cannot record a test that passes",
            "Because it leaves evidence for every failure without filling the disk",
            "Because a recording of a passing test is deleted before you can open it"
          ],
          "answer": 1,
          "explanation": "Recording every run fills the disk with files that nobody opens, and recording nothing leaves a failure that you cannot investigate. Keeping recordings only for failures gives you evidence exactly when you need it."
        },
        {
          "question": "`retries` is set to `process.env.CI ? 2 : 0`. What happens when a test fails on your computer?",
          "options": [
            "Playwright runs it two more times, as it does on a CI server.",
            "Playwright reports the failure at once, without running the test again.",
            "Playwright skips the test on your computer and runs it only on a CI server."
          ],
          "answer": 1,
          "explanation": "On your computer, `process.env.CI` is not set, so `retries` is 0. You see a failure the first time it happens, which is what you want while you write a test. A CI server runs a failed test up to two more times, which absorbs short network problems."
        }
      ],
      "recap": "- **CI**, or continuous integration, is a server that runs tests automatically. `process.env.CI` is true there and false on your computer.\n- `retries` is 0 on your computer and 2 on a CI server, so a failure on your computer shows itself at once.\n- The `use` block holds settings that every test receives. `baseURL` lets a test open short addresses such as `'/'`.\n- A **trace** is a step-by-step recording of a test. Keep traces, screenshots, and videos only for failed tests.\n- Every test runs once in each project that `projects` lists."
    }
  }
}
