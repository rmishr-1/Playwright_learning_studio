{
  "p2_yt1": "Same steps, your own marker: use a different first name as well as the generated last name, and log the `empNumber` out of the URL the app redirects you to. Keep `lastName` and `idPart` in scope — every cell below needs them, and Example 4 is what deletes this employee again.",
  "p2_yt2": "Same row, one axis at a time: print the match count for each stage of the XPath separately — `//div[text()='...']` on its own, then with `/parent::div` appended, then with `/following-sibling::div[last()]`. Watching where the number changes shows you the `parent::div` trap directly, instead of taking it on trust.",
  "p2_yt3": "Same verification, pointed the other way: once the reload has confirmed the new last name, go back to the Employee List and search for the OLD one. Print the row count. Whether you get 0 or 1 tells you how far behind the list's search index is right now — which is exactly why Example 4 anchors on the random id instead of either name.",
  "p2_yt4": "Same cleanup, one more proof: after the delete, assert that the \"No Records Found\" message is actually visible, not only that the row count is 0. Two ways of saying the same thing — and an empty table with no message would quietly pass the first check while failing the second.",
  "p3_yt1": "Same three helpers, a different caller: write a short flow that creates an employee, uses `findRow` only to COUNT the buttons in its actions cell, and then deletes it — never clicking the pencil at all. That is the whole reason `findRow` hands back a locator instead of doing the click for you."
}
