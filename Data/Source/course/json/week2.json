{
  "schemaVersion": "1.0",
  "week": 2,
  "title": "Week 2 — TypeScript Essentials & the Test Runner",
  "days": [
    {
      "schemaVersion": "1.0",
      "day": 6,
      "week": 2,
      "title": "Data Types",
      "subtitle": "The shapes your test data comes in — single values, empty values, lists and records — and the types that describe them precisely",
      "estimatedTime": "3 hours",
      "topics": [
        "Data types (primitives, null and undefined)",
        "Arrays, tuples and objects",
        "Union, literal, any and unknown types"
      ],
      "objectives": [
        "Map the TypeScript type family — primitives, collections and special types",
        "Explain null and undefined, and give fallbacks with `??` and safe access with `?.`",
        "Store and read lists with arrays, and fixed pairs with tuples",
        "Describe records with objects, type aliases and interfaces, including optional properties",
        "Unpack arrays and objects with destructuring — the pattern behind `{ page }`",
        "Use union and literal types, and explain why `any` is a trap",
        "Clean text read from a page and turn it into useful values"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 4: string, number, boolean, annotations and inference",
        "Day 5: let/const, template literals, operators, Number() and String()"
      ],
      "workspace": "pw-course/ts-basics/day6/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d6-prerequisites-1",
              "title": "P1 · Quick recap from Day 5",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "- [ ] Declare values with `const` by default and `let` when they must change (Day 5 · F2)\n- [ ] Build text with template literals: `` `Hello, ${name}` `` (Day 5 · F4)\n- [ ] Compare with `===`, and convert page text with `Number(…)` before comparing (Day 5 · F6)"
                },
                {
                  "type": "quiz",
                  "id": "d6-p1-q1",
                  "quizType": "single",
                  "question": "`let total = 10; total *= 3; total -= 5;` — what is total?",
                  "options": [
                    {
                      "id": "a",
                      "text": "25"
                    },
                    {
                      "id": "b",
                      "text": "15"
                    },
                    {
                      "id": "c",
                      "text": "10"
                    },
                    {
                      "id": "d",
                      "text": "35"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "10 × 3 = 30, then 30 − 5 = 25."
                },
                {
                  "type": "quiz",
                  "id": "d6-p1-q2",
                  "quizType": "single",
                  "question": "Which line causes a type error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const a = 1; console.log(a + 1);`"
                    },
                    {
                      "id": "b",
                      "text": "`` const t = `${1 + 1} items`; ``"
                    },
                    {
                      "id": "c",
                      "text": "`const url = 'https://a.com'; url = 'https://b.com';`"
                    },
                    {
                      "id": "d",
                      "text": "`let n = 1; n++;`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "A `const` can't be reassigned."
                }
              ]
            },
            {
              "id": "d6-prerequisites-2",
              "title": "P2 · The shapes of test data",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Open any test-data spreadsheet and you'll see three shapes of data:\n\n| Shape | Spreadsheet equivalent | Example | TypeScript |\n|---|---|---|---|\n| **A single value** | One cell | `'asha@example.com'`, `3`, `true` | string, number, boolean |\n| **A list** | A column | All the browsers to test on; all the product names on a page | **array** |\n| **A record** | A row with named columns | One user: name, email, age, is admin | **object** |\n\nAnd there's a fourth situation every tester knows: **the empty cell** — a value that's missing on purpose, or hasn't been filled in yet. TypeScript has types for that too."
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart TD\n  T[\"TypeScript types\"] --> P[\"Single values (primitives)<br/>string · number · boolean\"]\n  T --> E[\"Empty values<br/>null · undefined\"]\n  T --> C[\"Collections<br/>array (list) · tuple (fixed list) · object (record)\"]\n  T --> S[\"Special types<br/>union · literal · any · unknown\"]"
                },
                {
                  "type": "markdown",
                  "content": "Today covers each branch of this tree, using test data as the example throughout."
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Choosing the right shape for test data is a skill you already have: a list of browsers is a column, one user is a row. Today you learn how to write those shapes down so TypeScript can check them."
                },
                {
                  "type": "quiz",
                  "id": "d6-p2-q1",
                  "quizType": "single",
                  "question": "You need to store all six product names shown on a search results page. Which shape?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A single string"
                    },
                    {
                      "id": "b",
                      "text": "A list (array)"
                    },
                    {
                      "id": "c",
                      "text": "A boolean"
                    },
                    {
                      "id": "d",
                      "text": "A record (object) with six properties"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Several values of the same kind, in order — that's a list, an array."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d6-fundamentals-1",
              "title": "F1 · The simple types, completed",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 4 you met the three everyday simple types (**primitives**): `string`, `number` and `boolean`. There are a few more facts worth knowing:\n\n| Type | Notes |\n|---|---|\n| `string` | Text, in single quotes, double quotes or backticks |\n| `number` | Whole numbers *and* decimals — there is one number type. Includes the special values `NaN` and `Infinity` (Day 5) |\n| `boolean` | Only `true` or `false` |\n| `null` | \"Empty on purpose\" — see F2 |\n| `undefined` | \"No value (yet)\" — see F2 |\n| `bigint`, `symbol` | Very large whole numbers and unique ids. You won't need them for testing |\n\nPrimitives are **single, simple values**. You can replace a variable's primitive value, but you can't change the value itself: `'asha'.toUpperCase()` gives you a *new* text, `'ASHA'`, and leaves the original alone."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "Other tutorials use `enum` for a fixed set of named values. This course uses literal types instead (F6): they do a similar job, and they also work with Node.js's built-in TypeScript support, which rejects `enum`. (Playwright itself handles `enum` fine, so you may see it in test code.)",
                  "title": "What about `enum`?"
                }
              ]
            },
            {
              "id": "d6-fundamentals-2",
              "title": "F2 · `null` and `undefined` — the empty values",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "JavaScript has two ways of saying \"nothing here\":\n\n| | `undefined` | `null` |\n|---|---|---|\n| Meaning | **No value has been given (yet)** | **Deliberately empty** |\n| Who sets it | Usually JavaScript itself | Usually a programmer, on purpose |\n| When you see it | A `let` declared without a value; an optional property that's missing; reading past the end of a list | A field the user cleared; \"no manager assigned\" |\n| `typeof` | `'undefined'` | `'object'` — a famous old JavaScript mistake, kept for compatibility |\n\nWith TypeScript's strict checks, `null` and `undefined` aren't allowed just anywhere: you must say when a value **might** be missing, using `|` (\"or\"):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let couponCode: string | undefined;     // a string, or not set yet\nlet middleName: string | null = null;   // a string, or deliberately none",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Fallbacks with `??`\n\nThe **nullish coalescing operator** `??` means *\"use this value — or, if it's `null`/`undefined`, use the fallback\"*:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let couponCode: string | undefined;\nconsole.log(couponCode ?? 'no coupon');   // no coupon\ncouponCode = 'WELCOME10';\nconsole.log(couponCode ?? 'no coupon');   // WELCOME10",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`??` only steps in for `null` and `undefined`. A real value such as `0` or empty text `''` is kept — which is usually what a test wants (a quantity of `0` is still a quantity)."
                },
                {
                  "type": "quiz",
                  "id": "d6-f2-q1",
                  "quizType": "single",
                  "question": "`let coupon: string | undefined;` — what does `console.log(coupon ?? 'none')` print?",
                  "options": [
                    {
                      "id": "a",
                      "text": "undefined"
                    },
                    {
                      "id": "b",
                      "text": "null"
                    },
                    {
                      "id": "c",
                      "text": "An error"
                    },
                    {
                      "id": "d",
                      "text": "none"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "coupon was never given a value, so it's undefined, and `??` uses the fallback."
                },
                {
                  "type": "quiz",
                  "id": "d6-f2-q2",
                  "quizType": "single",
                  "question": "Which value is \"deliberately empty\", usually set by a programmer?",
                  "options": [
                    {
                      "id": "a",
                      "text": "0"
                    },
                    {
                      "id": "b",
                      "text": "undefined"
                    },
                    {
                      "id": "c",
                      "text": "NaN"
                    },
                    {
                      "id": "d",
                      "text": "null"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "null means \"empty on purpose\". undefined usually means \"never set\"."
                }
              ]
            },
            {
              "id": "d6-fundamentals-3",
              "title": "F3 · Arrays — ordered lists",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "An **array** is an ordered list of values, written in square brackets:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const browsers: string[] = ['chromium', 'firefox', 'webkit'];   // a list of strings\nconst retryDelays: number[] = [1000, 2000, 4000];                // a list of numbers\nconst flags = [true, false, true];                               // type inferred: boolean[]",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The type `string[]` means *\"a list of strings\"*. (You may also see the long form `Array<string>` — it means exactly the same.) For a list of literal values, give the literal type a name first and add `[]`: `BrowserName[]` (F6).\n\n### Positions start at 0\n\nEach item has a numbered position — its **index** — and counting starts at **0**:\n\n| Index | 0 | 1 | 2 |\n|---|---|---|---|\n| Value | `'chromium'` | `'firefox'` | `'webkit'` |\n\n| You want… | Write | Result |\n|---|---|---|\n| The first item | `browsers[0]` | `'chromium'` |\n| The third item | `browsers[2]` | `'webkit'` |\n| How many items | `browsers.length` | `3` |\n| The last item | `browsers[browsers.length - 1]` | `'webkit'` |\n| An item that doesn't exist | `browsers[5]` | `undefined` — no error, and TypeScript doesn't warn you either (it assumes the item is there) |\n\n### Changing and searching arrays\n\n| Method | What it does | Example | Result |\n|---|---|---|---|\n| `.push(value)` | Adds to the end | `browsers.push('msedge')` | 4 items |\n| `.pop()` | Removes the last item and gives it back | `browsers.pop()` | `'msedge'` |\n| `.includes(value)` | Is it in the list? | `browsers.includes('firefox')` | `true` |\n| `.indexOf(value)` | Where is it? (`-1` if absent) | `browsers.indexOf('webkit')` | `2` |\n| `.join(separator)` | Joins the items into one text | `browsers.join(', ')` | `'chromium, firefox, webkit'` |\n\nTypeScript keeps lists consistent: `retryDelays.push('5000')` is an error — text can't go into a list of numbers."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "`const browsers = [...]` means the **name** always points to the same list. You can still `push` into that list. What you can't do is make `browsers` point to a *different* list.",
                  "title": "`const` arrays can still change"
                },
                {
                  "type": "markdown",
                  "content": "Text has a matching tool: `.split(separator)` cuts text into an array of pieces. `'a,b,c'.split(',')` gives `['a', 'b', 'c']`."
                },
                {
                  "type": "quiz",
                  "id": "d6-f3-q1",
                  "quizType": "single",
                  "question": "`const cities = ['Delhi', 'Kolkata', 'Hyderabad'];` — what is `cities[1]`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'Kolkata'"
                    },
                    {
                      "id": "b",
                      "text": "'Delhi'"
                    },
                    {
                      "id": "c",
                      "text": "'Hyderabad'"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Positions start at 0. Index 0 is 'Delhi', index 1 is 'Kolkata'."
                },
                {
                  "type": "quiz",
                  "id": "d6-f3-q2",
                  "quizType": "single",
                  "question": "`const tags = ['@smoke', '@login'];` — what is `tags[2]`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'@login'"
                    },
                    {
                      "id": "b",
                      "text": "An error"
                    },
                    {
                      "id": "c",
                      "text": "undefined"
                    },
                    {
                      "id": "d",
                      "text": "'@smoke'"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "There's no item at index 2 (the last index is 1). Reading past the end quietly gives undefined — a common source of confusing bugs."
                }
              ]
            },
            {
              "id": "d6-fundamentals-4",
              "title": "F4 · Tuples — lists with a fixed shape",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **tuple** is an array with a **fixed number** of items and a **known type at each position**:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const credentials: [string, string] = ['asha@example.com', 'Secret@123'];   // [email, password]\nconst lineItem: [string, number, boolean] = ['Keyboard', 1499, true];        // [name, price, in stock]\n\nlineItem[1] = 'free';     // ❌ position 1 must be a number",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Use a tuple for small, fixed groups where the position has a meaning — like a pair of login details. For anything bigger, an object (F5) with named properties is clearer.\n\n### Destructuring: unpacking into variables\n\n**Destructuring** takes the items out of a list and puts them into separate variables in one line:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const credentials: [string, string] = ['asha@example.com', 'Secret@123'];\nconst [email, password] = credentials;   // email = 'asha@example.com', password = 'Secret@123'",
                  "mode": "read"
                },
                {
                  "type": "quiz",
                  "id": "d6-f4-q1",
                  "quizType": "single",
                  "question": "`const item: [string, number] = ['Mouse', 799];` — which line causes a type error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const [name, price] = item;`"
                    },
                    {
                      "id": "b",
                      "text": "`item[1] = 'cheap';`"
                    },
                    {
                      "id": "c",
                      "text": "`item[0] = 'Keyboard';`"
                    },
                    {
                      "id": "d",
                      "text": "`item[1] = 1499;`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Position 1 of this tuple must be a number, so 'cheap' is rejected."
                }
              ]
            },
            {
              "id": "d6-fundamentals-5",
              "title": "F5 · Objects — records with named properties",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "An **object** groups related values under **property names** — like a spreadsheet row with named columns:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const user = {\n  name: 'Asha Verma',        // property \"name\" with the value 'Asha Verma'\n  email: 'asha@example.com',\n  age: 29,\n  isAdmin: false,\n};",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Describing an object's shape: type aliases and interfaces\n\nWhen many objects share a shape — every test user, every product — describe the shape **once** and give it a name:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A type alias\ntype User = {\n  name: string;\n  email: string;\n  age: number;\n  isAdmin: boolean;\n  phone?: string;            // ? = optional: may be missing\n};\n\n// An interface — another way to describe the same shape\ninterface Product {\n  name: string;\n  price: number;\n  inStock: boolean;\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "For objects, `type` and `interface` do almost the same job. Many teams use `interface` for object shapes and `type` for everything else; you'll meet both in Playwright code. This course mostly uses `type`.\n\nNow TypeScript checks every object against its shape:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const admin: User = { name: 'Ravi', email: 'ravi@example.com', age: 31, isAdmin: true };   // ✅\nconst broken: User = { name: 'Meera', email: 'meera@example.com', age: '27' };\n// ❌ age must be a number   ❌ isAdmin is missing",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Typos in property names are caught\n\nWith a shape declared, a misspelt property name is an error too — a great safety net for test data:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const typo: User = { name: 'Asha', emial: 'asha@example.com', age: 29, isAdmin: false };\n// ❌ Object literal may only specify known properties … 'emial' does not exist in type 'User'",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Reading and changing properties\n\n| You want… | Write |\n|---|---|\n| Read a property | `user.email` (or `user['email']`) |\n| Change a property | `user.age = 30;` |\n| Read an optional property | `user.phone` → the value, or `undefined` if missing |\n\nJust like arrays, a `const` object's **properties** can change; the name just can't point to a different object.\n\n### Safe access with `?.`\n\nIf a property is optional, TypeScript won't let you use it as if it were definitely there:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const customer: User = { name: 'Meera', email: 'meera@example.com', age: 27, isAdmin: false };\n\nconsole.log(customer.phone.length);        // ❌ error TS18048: 'customer.phone' is possibly 'undefined'.\nconsole.log(customer.phone?.length);       // ✅ ?. — \"if phone exists, its length; otherwise undefined\"\nconsole.log(customer.phone?.length ?? 0);  // ✅ with a fallback: 0 when there's no phone",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The **optional chaining** operator `?.` stops safely at a missing value instead of crashing, and pairs naturally with `??` for a fallback.\n\n### Object destructuring — the key to `{ page }`\n\nObjects can be unpacked by **property name**:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const { name, email } = user;     // two new constants: name and email",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "This is exactly what every Playwright test does. Playwright hands your test an object full of ready-made tools (the fixtures), and `{ page }` unpacks just the page:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('login works', async ({ page }) => { … });\n//                          └──┬──┘\n//         \"from the object Playwright gives me, take the property called page\"",
                  "mode": "read"
                },
                {
                  "type": "quiz",
                  "id": "d6-f5-q1",
                  "quizType": "single",
                  "question": "In `type Product = { name: string; price: number; discount?: number };`, what does the `?` mean?",
                  "options": [
                    {
                      "id": "a",
                      "text": "discount can be null"
                    },
                    {
                      "id": "b",
                      "text": "discount can be any type"
                    },
                    {
                      "id": "c",
                      "text": "discount must be checked with ?. every time it is set"
                    },
                    {
                      "id": "d",
                      "text": "discount is optional — a Product may leave it out"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "A `?` after the property name makes it optional. If it's missing, reading it gives undefined."
                },
                {
                  "type": "quiz",
                  "id": "d6-f5-q2",
                  "quizType": "single",
                  "question": "`const { email } = { name: 'Asha', email: 'asha@example.com' };` — what is `email`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'asha@example.com'"
                    },
                    {
                      "id": "b",
                      "text": "{ email: 'asha@example.com' }"
                    },
                    {
                      "id": "c",
                      "text": "'Asha'"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Object destructuring takes the property with that name and puts its value in a new variable."
                }
              ]
            },
            {
              "id": "d6-fundamentals-6",
              "title": "F6 · Union and literal types",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Union types — \"this OR that\"\n\nA **union type** allows more than one type, joined with `|`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let orderId: string | number;\norderId = 1045;             // ✅\norderId = 'ORD-1045';       // ✅\norderId = true;             // ❌ boolean isn't one of the options",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "A value read from somewhere else might be either:\n\nYou've already used unions for empty values: `string | undefined`, `string | null`.\n\nWith a union, TypeScript only lets you do what's safe for **every** option. To use string-only abilities, check first — TypeScript then knows which one you have:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Imagine this value came from a file: it could be either type\nconst incomingId: string | number = Math.random() > 0.5 ? 'ord-7' : 7;\n\nconst label = typeof incomingId === 'string'\n  ? incomingId.toUpperCase()       // here TypeScript knows it's a string\n  : `ORD-${incomingId}`;           // and here, a number",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "This \"check first, then TypeScript knows\" behaviour is called **narrowing**. You'll use it a lot with `if` on Day 7.\n\n### Literal types — only these exact values\n\nA **literal type** allows only specific values:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type BrowserName = 'chromium' | 'firefox' | 'webkit';\n\nlet target: BrowserName = 'firefox';   // ✅\ntarget = 'safari';                     // ❌ '\"safari\"' is not assignable to type 'BrowserName'",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Literal types turn typos into errors and give you autocomplete. Playwright uses them everywhere — `browserName` is typed exactly as `'chromium' | 'firefox' | 'webkit'`, and roles in `getByRole('button', …)` are a literal union too, so VS Code can list every valid role as you type."
                },
                {
                  "type": "quiz",
                  "id": "d6-f6-q1",
                  "quizType": "single",
                  "question": "Given `type Status = 'passed' | 'failed' | 'skipped';`, which assignment is an error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`let s: Status = 'failed';`"
                    },
                    {
                      "id": "b",
                      "text": "`let s: Status = 'skipped';`"
                    },
                    {
                      "id": "c",
                      "text": "`let s: Status = 'flaky';`"
                    },
                    {
                      "id": "d",
                      "text": "`let s: Status = 'passed';`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "A literal-union type accepts only the listed values; 'flaky' isn't one of them."
                }
              ]
            },
            {
              "id": "d6-fundamentals-7",
              "title": "F7 · `any` and `unknown` — escape hatches",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Sometimes you don't know a value's type — for example, data read from a file. TypeScript has two \"could be anything\" types:\n\n| Type | Meaning | Advice |\n|---|---|---|\n| `any` | \"Stop checking this value\" — anything goes, anything can be done with it | **Avoid.** It silently switches TypeScript off and turns it back into plain JavaScript |\n| `unknown` | \"Could be anything — **check before using it**\" | The safe choice when you truly don't know |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let fromFile: any = 'hello';\nfromFile.toFixed(2);            // no error from TypeScript… crashes when it runs\n\nlet safer: unknown = 'hello';\nsafer.toFixed(2);               // ❌ TypeScript refuses until you check what it is\n\nconst shown = typeof safer === 'string' ? safer.toUpperCase() : 'not text';   // ✅ checked first",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "You'll find `any` in code copied from the internet. Treat every `any` as a question: *\"what type should this really be?\"*"
                },
                {
                  "type": "quiz",
                  "id": "d6-f7-q1",
                  "quizType": "single",
                  "question": "Why is using `any` everywhere a bad idea?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It switches off type checking for those values, so TypeScript can no longer catch mistakes"
                    },
                    {
                      "id": "b",
                      "text": "Playwright refuses to run files that contain it"
                    },
                    {
                      "id": "c",
                      "text": "It makes the program run slower"
                    },
                    {
                      "id": "d",
                      "text": "It forces you to check the value's type before every use"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`any` tells TypeScript to trust you blindly — you lose the main benefit of TypeScript."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [
            {
              "type": "markdown",
              "content": "All files today go in `ts-basics/day6`. Run with `node day6/<file>.ts`, check with `npm run check -- day6/<file>.ts`."
            }
          ],
          "lessons": [
            {
              "id": "d6-implementation-1",
              "title": "I1 · A test-data record",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "One registration test case, described by a type alias and stored as an object:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// The shape of one registration test case\ntype Registration = {\n  fullName: string;\n  email: string;\n  age: number;\n  country: string;\n  acceptsTerms: boolean;\n  phone?: string;              // optional: some users leave it empty\n};\n\nconst newCustomer: Registration = {\n  fullName: 'Meera Iyer',\n  email: 'meera.iyer@example.com',\n  age: 27,\n  country: 'India',\n  acceptsTerms: true,\n};\n\n// Read properties with a dot (or square brackets)\nconsole.log(`Name: ${newCustomer.fullName}`);\nconsole.log(`Email: ${newCustomer['email']}`);\n\n// Change a property: the object is const, but its contents can change\nnewCustomer.age = 28;\nconsole.log(`Age next year: ${newCustomer.age}`);\n\n// An optional property that wasn't given is undefined\nconsole.log(`Phone: ${newCustomer.phone}`);\nconsole.log(`Phone to type: ${newCustomer.phone ?? '(leave empty)'}`);\n\n// Destructuring: take out the properties you need\nconst { fullName, country } = newCustomer;\nconsole.log(`${fullName} lives in ${country}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/registration.ts",
                  "run": "node day6/registration.ts",
                  "expectedOutput": "Name: Meera Iyer\nEmail: meera.iyer@example.com\nAge next year: 28\nPhone: undefined\nPhone to type: (leave empty)\nMeera Iyer lives in India",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Name: Meera Iyer\nEmail: meera.iyer@example.com\nAge next year: 28\nPhone: undefined\nPhone to type: (leave empty)\nMeera Iyer lives in India"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** change `age: 27` to `age: '27'` and check the file. Then remove the `country` line from the object and check again. Read both errors before undoing your changes."
                }
              ]
            },
            {
              "id": "d6-implementation-2",
              "title": "I2 · A list of test cases",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Data-driven tests store many test cases as an **array of objects** — a whole test-data sheet. (On Day 7 you'll loop through them; today you read them by position.)"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// The shape of one login test case\ntype LoginCase = {\n  id: string;\n  username: string;\n  password: string;\n  shouldSucceed: boolean;\n  expectedError?: string;      // only failing cases have one\n};\n\n// The whole test-data sheet: an array of LoginCase objects\nconst loginCases: LoginCase[] = [\n  { id: 'TC-01', username: 'standard_user', password: 'secret_sauce', shouldSucceed: true },\n  { id: 'TC-02', username: 'locked_out_user', password: 'secret_sauce', shouldSucceed: false,\n    expectedError: 'Sorry, this user has been locked out.' },\n  { id: 'TC-03', username: 'standard_user', password: 'wrong', shouldSucceed: false,\n    expectedError: 'Username and password do not match.' },\n];\n\nconsole.log(`${loginCases.length} login cases`);\n\nconst firstCase = loginCases[0];\nconst lastCase = loginCases[loginCases.length - 1];\nconsole.log(`First: ${firstCase.id} (${firstCase.username}) should succeed: ${firstCase.shouldSucceed}`);\nconsole.log(`Last: ${lastCase.id} expects \"${lastCase.expectedError}\"`);\nconsole.log(`TC-01 error: ${firstCase.expectedError ?? 'none expected'}`);\n\n// Add one more case to the sheet\nloginCases.push({ id: 'TC-04', username: '', password: '', shouldSucceed: false,\n  expectedError: 'Username is required' });\nconsole.log(`Now ${loginCases.length} cases; the newest is ${loginCases[3].id}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/login-cases.ts",
                  "run": "node day6/login-cases.ts",
                  "expectedOutput": "3 login cases\nFirst: TC-01 (standard_user) should succeed: true\nLast: TC-03 expects \"Username and password do not match.\"\nTC-01 error: none expected\nNow 4 cases; the newest is TC-04",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "3 login cases\nFirst: TC-01 (standard_user) should succeed: true\nLast: TC-03 expects \"Username and password do not match.\"\nTC-01 error: none expected\nNow 4 cases; the newest is TC-04"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "This is your test-data spreadsheet in code: the type is the column headers, each object is a row, and TypeScript refuses rows with missing or wrongly typed cells."
                }
              ]
            },
            {
              "id": "d6-implementation-3",
              "title": "I3 · A test matrix with literal types and tuples",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Only these three browser names are allowed\ntype BrowserName = 'chromium' | 'firefox' | 'webkit';\n\n// The browsers we test on\nconst browsers: BrowserName[] = ['chromium', 'firefox'];\nbrowsers.push('webkit');                           // add one more\n\n// Login credentials as a tuple: [email, password]\nconst validLogin: [string, string] = ['asha@example.com', 'Secret@123'];\nconst [email, password] = validLogin;              // unpack the tuple\n\n// Pages to check on every browser\nconst pagesToCheck: string[] = ['/home', '/products', '/cart'];\nconst runsNeeded = browsers.length * pagesToCheck.length;\n\nconsole.log(`Browsers: ${browsers.join(', ')}`);\nconsole.log(`Logging in as ${email} (password has ${password.length} characters)`);\nconsole.log(`First page: ${pagesToCheck[0]}, last page: ${pagesToCheck[pagesToCheck.length - 1]}`);\nconsole.log(`Is /cart in the list? ${pagesToCheck.includes('/cart')}`);\nconsole.log(`Total page checks: ${runsNeeded}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/matrix.ts",
                  "run": "node day6/matrix.ts",
                  "expectedOutput": "Browsers: chromium, firefox, webkit\nLogging in as asha@example.com (password has 10 characters)\nFirst page: /home, last page: /cart\nIs /cart in the list? true\nTotal page checks: 9",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Browsers: chromium, firefox, webkit\nLogging in as asha@example.com (password has 10 characters)\nFirst page: /home, last page: /cart\nIs /cart in the list? true\nTotal page checks: 9"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** add `browsers.push('safari');` and check the file. Why does TypeScript refuse? Remove the line afterwards."
                }
              ]
            },
            {
              "id": "d6-implementation-4",
              "title": "I4 · Clean up text read from a page",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 5, `Number('₹1,499')` gave `NaN`. With text methods and arrays you can clean page text into useful values:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Text read from an order page (page text often has stray spaces)\nconst priceText = '  ₹1,499 ';\nconst orderUrl = 'https://shop.example.com/orders/10452?tab=details';\n\n// Clean the price: remove the currency symbol and every comma, then convert\nconst cleaned = priceText.trim().replace('₹', '').replaceAll(',', '');\nconst price = Number(cleaned);\nconsole.log(`Cleaned: \"${cleaned}\" = ${price} (${typeof price})`);\nconsole.log(`With 18% tax: ${(price * 1.18).toFixed(2)}`);\n\n// Split text into an array of parts\nconst parts = orderUrl.split('/');\nconsole.log(parts);\nconst orderId = parts[4].split('?')[0];            // '10452?tab=details' → '10452'\nconsole.log(`Order id: ${orderId}`);\n\n// A value that may be missing\nlet coupon: string | undefined;                    // no coupon applied yet\nconsole.log(`Coupon: ${coupon ?? 'none'}`);\ncoupon = 'WELCOME10';\nconsole.log(`Coupon: ${coupon ?? 'none'}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/clean-text.ts",
                  "run": "node day6/clean-text.ts",
                  "expectedOutput": "Cleaned: \"1499\" = 1499 (number)\nWith 18% tax: 1768.82\n[ 'https:', '', 'shop.example.com', 'orders', '10452?tab=details' ]\nOrder id: 10452\nCoupon: none\nCoupon: WELCOME10",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Cleaned: \"1499\" = 1499 (number)\nWith 18% tax: 1768.82\n[ 'https:', '', 'shop.example.com', 'orders', '10452?tab=details' ]\nOrder id: 10452\nCoupon: none\nCoupon: WELCOME10"
                },
                {
                  "type": "markdown",
                  "content": "| Method | What it did |\n|---|---|\n| `.trim()` | Removed the spaces at both ends (Day 5) |\n| `.replace('₹', '')` | Replaced the **first** `₹` with nothing |\n| `.replaceAll(',', '')` | Replaced **every** comma with nothing (`₹1,00,000` has two) |\n| `.split('/')` | Cut the URL at every `/` into an array (the empty `''` is between the two slashes of `//`) |\n| `parts[4].split('?')[0]` | Took piece 4, cut it at `?`, kept the first half |"
                }
              ]
            },
            {
              "id": "d6-implementation-5",
              "title": "I5 · Type detective — five mistakes in test data",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This file has **five** type mistakes. Check it first:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Five type mistakes in test data. Check this file and read each error.\ntype Product = {\n  name: string;\n  price: number;\n  inStock: boolean;\n  discount?: number;\n};\ntype BrowserName = 'chromium' | 'firefox' | 'webkit';\n\nconst keyboard: Product = {\n  name: 'Mechanical Keyboard',\n  price: '2499',\n  inStock: true,\n};\n\nconst mouse: Product = {\n  name: 'Wireless Mouse',\n  price: 799,\n};\n\nconst browsers: BrowserName[] = ['chromium', 'firefox'];\nbrowsers.push('safari');\n\nconst login: [string, string] = ['asha@example.com', 12345];\n\nconst sizes: number[] = [38, 40, 42];\nsizes.push('44');",
                  "mode": "editor",
                  "file": "ts-basics/day6/detective.ts",
                  "run": "npm run check -- day6/detective.ts",
                  "expectError": true,
                  "expectedOutput": "day6/detective.ts(12,3): error TS2322: Type 'string' is not assignable to type 'number'.\nday6/detective.ts(16,7): error TS2741: Property 'inStock' is missing in type '{ name: string; price: number; }' but required in type 'Product'.\nday6/detective.ts(22,15): error TS2345: Argument of type '\"safari\"' is not assignable to parameter of type 'BrowserName'.\nday6/detective.ts(24,54): error TS2322: Type 'number' is not assignable to type 'string'.\nday6/detective.ts(27,12): error TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npm run check -- day6/detective.ts"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "day6/detective.ts(12,3): error TS2322: Type 'string' is not assignable to type 'number'.\nday6/detective.ts(16,7): error TS2741: Property 'inStock' is missing in type '{ name: string; price: number; }' but required in type 'Product'.\nday6/detective.ts(22,15): error TS2345: Argument of type '\"safari\"' is not assignable to parameter of type 'BrowserName'.\nday6/detective.ts(24,54): error TS2322: Type 'number' is not assignable to type 'string'.\nday6/detective.ts(27,12): error TS2345: Argument of type 'string' is not assignable to parameter of type 'number'."
                },
                {
                  "type": "markdown",
                  "content": "Two words in these messages: an **argument** is the value you hand to a method in brackets — `'safari'` in `push('safari')` — and a **parameter** is the slot the method expects it in. Day 8 covers both.\n\n| Line | Mistake |\n|---|---|\n| 12 | A price given as text |\n| 16 | A required property (`inStock`) is missing — `discount` is optional, so it's fine to leave out |\n| 22 | `'safari'` isn't one of the allowed browser names |\n| 24 | Position 1 of the tuple must be a string (a password) |\n| 27 | Text pushed into a list of numbers |\n\nFix all five, check until silent, and run:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// The same test data, fixed\ntype Product = {\n  name: string;\n  price: number;\n  inStock: boolean;\n  discount?: number;\n};\ntype BrowserName = 'chromium' | 'firefox' | 'webkit';\n\nconst keyboard: Product = { name: 'Mechanical Keyboard', price: 2499, inStock: true };\nconst mouse: Product = { name: 'Wireless Mouse', price: 799, inStock: false, discount: 10 };\n\nconst browsers: BrowserName[] = ['chromium', 'firefox'];\nbrowsers.push('webkit');\n\nconst login: [string, string] = ['asha@example.com', '12345'];\n\nconst sizes: number[] = [38, 40, 42];\nsizes.push(44);\n\nconsole.log(`${keyboard.name}: ₹${keyboard.price}, in stock: ${keyboard.inStock}`);\nconsole.log(`${mouse.name}: ₹${mouse.price}, discount: ${mouse.discount ?? 0}%`);\nconsole.log(`Browsers: ${browsers.length}, login: ${login[0]}, sizes: ${sizes.join('/')}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/detective-fixed.ts",
                  "run": "node day6/detective-fixed.ts",
                  "expectedOutput": "Mechanical Keyboard: ₹2499, in stock: true\nWireless Mouse: ₹799, discount: 10%\nBrowsers: 3, login: asha@example.com, sizes: 38/40/42/44",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Mechanical Keyboard: ₹2499, in stock: true\nWireless Mouse: ₹799, discount: 10%\nBrowsers: 3, login: asha@example.com, sizes: 38/40/42/44"
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d6-practice-1",
              "title": "Quiz · Day 6 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d6-pr-q1",
                  "quizType": "single",
                  "question": "`const tags = ['@smoke', '@regression'];` — what is `tags.length`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "1"
                    },
                    {
                      "id": "b",
                      "text": "0"
                    },
                    {
                      "id": "c",
                      "text": "2"
                    },
                    {
                      "id": "d",
                      "text": "3"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "The array has two items. (Its last index is 1, but its length is 2.)"
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q2",
                  "quizType": "single",
                  "question": "What does `['a', 'b', 'c'].join('-')` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'a-b-c'"
                    },
                    {
                      "id": "b",
                      "text": "'a, b, c'"
                    },
                    {
                      "id": "c",
                      "text": "'abc'"
                    },
                    {
                      "id": "d",
                      "text": "['a-b-c']"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`join` combines the items into one text, with the separator between them."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q3",
                  "quizType": "single",
                  "question": "`type User = { name: string; phone?: string };` and `const u: User = { name: 'Asha' };` — which line is an error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`console.log(u.phone?.length);`"
                    },
                    {
                      "id": "b",
                      "text": "`console.log(u.phone ?? 'none');`"
                    },
                    {
                      "id": "c",
                      "text": "`console.log(u.phone.length);`"
                    },
                    {
                      "id": "d",
                      "text": "`console.log(u.name.length);`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "phone is optional, so it may be undefined. TypeScript requires `?.` or a fallback before using it."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q4",
                  "quizType": "multiple",
                  "question": "Which statements about null and undefined are true? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`undefined` usually means a value was never given"
                    },
                    {
                      "id": "b",
                      "text": "`typeof null` is 'null'"
                    },
                    {
                      "id": "c",
                      "text": "`null` usually means \"deliberately empty\""
                    },
                    {
                      "id": "d",
                      "text": "`??` gives the fallback for both null and undefined"
                    }
                  ],
                  "correct": [
                    "a",
                    "c",
                    "d"
                  ],
                  "explanation": "`typeof null` is 'object' — a long-standing JavaScript quirk."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q5",
                  "quizType": "single",
                  "question": "When should you use a tuple instead of an object?",
                  "options": [
                    {
                      "id": "a",
                      "text": "For any list of more than 10 items"
                    },
                    {
                      "id": "b",
                      "text": "For a small, fixed group where each position has a clear meaning, like [email, password]"
                    },
                    {
                      "id": "c",
                      "text": "When you want to name each value, like email and password"
                    },
                    {
                      "id": "d",
                      "text": "Whenever the values have different types, however many there are"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Tuples suit small fixed pairs or triples. For more, named object properties are clearer."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q6",
                  "quizType": "single",
                  "question": "In a Playwright test, `async ({ page }) => { … }` — what is `{ page }` doing?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Destructuring — taking the `page` property out of the object Playwright passes in"
                    },
                    {
                      "id": "b",
                      "text": "Creating a new object with a page in it"
                    },
                    {
                      "id": "c",
                      "text": "Declaring a tuple"
                    },
                    {
                      "id": "d",
                      "text": "Importing page from another file"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Playwright passes one object full of fixtures; object destructuring picks out `page`."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q7",
                  "quizType": "single",
                  "question": "`let id: string | number = 7;` — which is allowed?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`id = 'ORD-7';`"
                    },
                    {
                      "id": "b",
                      "text": "`id = null;`"
                    },
                    {
                      "id": "c",
                      "text": "`id = ['7'];`"
                    },
                    {
                      "id": "d",
                      "text": "`id = true;`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "The union allows only a string or a number."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q8",
                  "quizType": "single",
                  "question": "Why is `unknown` safer than `any`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It can only hold null or undefined"
                    },
                    {
                      "id": "b",
                      "text": "It's automatically converted to the right type"
                    },
                    {
                      "id": "c",
                      "text": "TypeScript makes you check what the value is before you use it"
                    },
                    {
                      "id": "d",
                      "text": "It lets you call any method without errors"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "`any` switches checking off; `unknown` keeps it on and asks you to check first."
                }
              ]
            },
            {
              "id": "d6-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d6-pr-p1",
                  "title": "Predict — arrays and objects",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "const results = ['pass', 'fail', 'pass'];\nconst summary = { suite: 'Checkout', total: results.length };\nresults.push('skip');\nconsole.log(results.length, summary.total, results[results.length - 1]);",
                  "answer": "`4 3 skip`\n\n`summary.total` stored the length (3) when the object was created; pushing later doesn't change it. The array now has 4 items, and the last one is 'skip'.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d6-pr-p2",
                  "title": "Predict — missing values",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "const sizes = [38, 40, 42];\ntype Person = { name: string; nickname?: string };\nconst user: Person = { name: 'Ravi' };\nconsole.log(sizes[3]);\nconsole.log(sizes[3] ?? 'no size');\nconsole.log(user.nickname ?? user.name);",
                  "answer": "undefined\nno size\nRavi\n\nThere's no index 3 (the last is 2), so it's undefined, and `??` then uses the fallback. nickname is missing, so `??` falls back to the name.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d6-pr-p3",
                  "title": "Predict — split and join",
                  "level": "easy",
                  "prompt": "What is printed?",
                  "code": "const path = '/products/laptops/42';\nconst parts = path.split('/');\nconsole.log(parts.length);\nconsole.log(parts[2]);\nconsole.log(parts.join(' > '));",
                  "answer": "4\nlaptops\n\" > products > laptops > 42\"\n\nThe text starts with `/`, so the first piece is the empty text `''`: `['', 'products', 'laptops', '42']` — 4 pieces. Joining puts `' > '` between them — so the line starts with a space and `>` (after the empty first piece). The quotes above just show where the line begins.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d6-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex1",
                  "title": "A product catalogue",
                  "level": "easy",
                  "prompt": "Create `day6/catalogue.ts`:\n\n1. Define a type alias `Product` with `name` (string), `price` (number) and an optional `badge` (string).\n2. Create an array `products` of type `Product[]` with three products: `Monitor` (8999, badge `Bestseller`), `Webcam` (2499, no badge), `Headset` (1799, badge `New`).\n3. Print:\n```\n3 products\nFirst: Monitor - ₹8999 [Bestseller]\nSecond: Webcam - ₹2499 [no badge]\nLast: Headset - ₹1799 [New]\n```\nUse `??` for the missing badge.",
                  "file": "ts-basics/day6/catalogue.ts",
                  "run": "node day6/catalogue.ts",
                  "hints": [
                    "Optional property: `badge?: string;`",
                    "`${products[1].badge ?? 'no badge'}`"
                  ],
                  "solution": "// The shape of one product\ntype Product = {\n  name: string;\n  price: number;\n  badge?: string;     // optional\n};\n\nconst products: Product[] = [\n  { name: 'Monitor', price: 8999, badge: 'Bestseller' },\n  { name: 'Webcam', price: 2499 },\n  { name: 'Headset', price: 1799, badge: 'New' },\n];\n\nconst first = products[0];\nconst second = products[1];\nconst last = products[products.length - 1];\n\nconsole.log(`${products.length} products`);\nconsole.log(`First: ${first.name} - ₹${first.price} [${first.badge ?? 'no badge'}]`);\nconsole.log(`Second: ${second.name} - ₹${second.price} [${second.badge ?? 'no badge'}]`);\nconsole.log(`Last: ${last.name} - ₹${last.price} [${last.badge ?? 'no badge'}]`);",
                  "expectedOutput": "3 products\nFirst: Monitor - ₹8999 [Bestseller]\nSecond: Webcam - ₹2499 [no badge]\nLast: Headset - ₹1799 [New]",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/catalogue.ts",
                    "expected": "3 products\nFirst: Monitor - ₹8999 [Bestseller]\nSecond: Webcam - ₹2499 [no badge]\nLast: Headset - ₹1799 [New]"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex2",
                  "title": "Login test data",
                  "level": "medium",
                  "prompt": "Create `day6/login-data.ts`:\n\n1. Define a type alias `LoginCase` with `id`, `username`, `password` (all strings), `shouldSucceed` (boolean) and an **optional** `expectedError` (string).\n2. Create two constants of that type:\n   - `validCase`: `TC-01`, `standard_user`, `secret_sauce`, succeeds\n   - `lockedCase`: `TC-02`, `locked_out_user`, `secret_sauce`, fails with `Sorry, this user has been locked out.`\n3. Put both in an array `loginCases` (type `LoginCase[]`) and print:\n```\n2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out.\n```\nThe type check must pass.",
                  "file": "ts-basics/day6/login-data.ts",
                  "run": "node day6/login-data.ts",
                  "hints": [
                    "Optional property: `expectedError?: string;`",
                    "Read items with `loginCases[0]` and `loginCases[1]`."
                  ],
                  "solution": "// The shape of one login test case\ntype LoginCase = {\n  id: string;\n  username: string;\n  password: string;\n  shouldSucceed: boolean;\n  expectedError?: string;   // only needed for failing cases\n};\n\nconst validCase: LoginCase = {\n  id: 'TC-01',\n  username: 'standard_user',\n  password: 'secret_sauce',\n  shouldSucceed: true,\n};\n\nconst lockedCase: LoginCase = {\n  id: 'TC-02',\n  username: 'locked_out_user',\n  password: 'secret_sauce',\n  shouldSucceed: false,\n  expectedError: 'Sorry, this user has been locked out.',\n};\n\nconst loginCases: LoginCase[] = [validCase, lockedCase];\n\nconsole.log(`${loginCases.length} login cases`);\nconsole.log(`${loginCases[0].id} ${loginCases[0].username} expects success: ${loginCases[0].shouldSucceed}`);\nconsole.log(`${loginCases[1].id} ${loginCases[1].username} expects error: ${loginCases[1].expectedError}`);",
                  "expectedOutput": "2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out.",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/login-data.ts",
                    "expected": "2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out."
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex3",
                  "title": "Destructure the test settings",
                  "level": "medium",
                  "prompt": "Create `day6/settings.ts` with this object:\n```ts\nconst credentials: [string, string] = ['qa@example.com', 'Test@123'];\nconst settings = {\n  baseUrl: 'https://staging.shop.example.com',\n  browsers: ['chromium', 'webkit'],\n  retries: 2,\n  credentials,          // short for credentials: credentials\n};\n```\n(An object can hold arrays and tuples as property values, just like any other value.)\nUsing **object destructuring** for the first three properties and **array destructuring** for the credentials, print:\n```\nTesting https://staging.shop.example.com on 2 browsers (chromium, webkit)\nRetries: 2\nUser: qa@example.com\n```",
                  "file": "ts-basics/day6/settings.ts",
                  "run": "node day6/settings.ts",
                  "hints": [
                    "`const { baseUrl, browsers, retries } = settings;`",
                    "`const [user] = credentials;` takes just the first item — you only need the email."
                  ],
                  "solution": "const credentials: [string, string] = ['qa@example.com', 'Test@123'];\nconst settings = {\n  baseUrl: 'https://staging.shop.example.com',\n  browsers: ['chromium', 'webkit'],\n  retries: 2,\n  credentials,          // short for credentials: credentials\n};\n\n// Object destructuring: pull properties out by name\nconst { baseUrl, browsers, retries } = settings;\n// Array destructuring: pull items out by position\nconst [user] = settings.credentials;\n\nconsole.log(`Testing ${baseUrl} on ${browsers.length} browsers (${browsers.join(', ')})`);\nconsole.log(`Retries: ${retries}`);\nconsole.log(`User: ${user}`);",
                  "expectedOutput": "Testing https://staging.shop.example.com on 2 browsers (chromium, webkit)\nRetries: 2\nUser: qa@example.com",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/settings.ts",
                    "expected": "Testing https://staging.shop.example.com on 2 browsers (chromium, webkit)\nRetries: 2\nUser: qa@example.com"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex4",
                  "title": "Clean the order summary",
                  "level": "medium",
                  "prompt": "A test read this text from an order-summary page:\n```ts\nconst summaryText = 'Items: 3 | Total: ₹12,450 | Status: Shipped';\n```\nCreate `day6/summary.ts` that uses `split`, `replace`/`replaceAll` and `Number` to print:\n```\nItems: 3\nTotal: 12450\nStatus: Shipped\nAverage item price: 4150\n```\nwhere `Items` and `Total` are real **numbers** (the average is calculated from them).",
                  "file": "ts-basics/day6/summary.ts",
                  "run": "node day6/summary.ts",
                  "hints": [
                    "`summaryText.split(' | ')` gives `['Items: 3', 'Total: ₹12,450', 'Status: Shipped']`.",
                    "Each piece can be split again at `': '` — the value is at index 1.",
                    "Clean the total with `.replace('₹', '').replaceAll(',', '')` before `Number(…)`."
                  ],
                  "solution": "const summaryText = 'Items: 3 | Total: ₹12,450 | Status: Shipped';\n\n// Cut into the three parts\nconst parts = summaryText.split(' | ');           // ['Items: 3', 'Total: ₹12,450', 'Status: Shipped']\n\n// Take the value after ': ' in each part\nconst itemsText = parts[0].split(': ')[1];        // '3'\nconst totalText = parts[1].split(': ')[1];        // '₹12,450'\nconst status = parts[2].split(': ')[1];           // 'Shipped'\n\n// Convert to numbers\nconst items = Number(itemsText);\nconst total = Number(totalText.replace('₹', '').replaceAll(',', ''));\n\nconsole.log(`Items: ${items}`);\nconsole.log(`Total: ${total}`);\nconsole.log(`Status: ${status}`);\nconsole.log(`Average item price: ${total / items}`);",
                  "expectedOutput": "Items: 3\nTotal: 12450\nStatus: Shipped\nAverage item price: 4150",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/summary.ts",
                    "expected": "Items: 3\nTotal: 12450\nStatus: Shipped\nAverage item price: 4150"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex5",
                  "title": "Challenge: model a test run",
                  "level": "challenge",
                  "prompt": "Create `day6/test-run.ts` that models a finished test run with types:\n\n1. `type Status = 'passed' | 'failed' | 'skipped';`\n2. `type TestResult = { title: string; status: Status; durationMs: number; error?: string };`\n3. `type TestRun = { browser: 'chromium' | 'firefox' | 'webkit'; results: TestResult[] };`\n4. Create a `run` for `firefox` with three results: `login works` (passed, 1200), `search works` (failed, 5300, error `Timeout 5000ms exceeded`), `profile edits` (skipped, 0).\n5. Print (reading by position — no loops yet):\n```\nBrowser: firefox, 3 results\n2nd test: search works - failed after 5.3s\nIts error: Timeout 5000ms exceeded\n1st test error: none\n```\nThen try adding a result with status `'flaky'` and read the check error — then remove it.",
                  "file": "ts-basics/day6/test-run.ts",
                  "run": "node day6/test-run.ts",
                  "hints": [
                    "An object can contain an array of objects: `results: [ { … }, { … } ]`.",
                    "Convert ms to seconds with `/ 1000`.",
                    "`run.results[0].error ?? 'none'`"
                  ],
                  "solution": "// Types that describe a test run\ntype Status = 'passed' | 'failed' | 'skipped';\ntype TestResult = { title: string; status: Status; durationMs: number; error?: string };\ntype TestRun = { browser: 'chromium' | 'firefox' | 'webkit'; results: TestResult[] };\n\n// One finished run\nconst run: TestRun = {\n  browser: 'firefox',\n  results: [\n    { title: 'login works', status: 'passed', durationMs: 1200 },\n    { title: 'search works', status: 'failed', durationMs: 5300, error: 'Timeout 5000ms exceeded' },\n    { title: 'profile edits', status: 'skipped', durationMs: 0 },\n  ],\n};\n\nconst second = run.results[1];\nconsole.log(`Browser: ${run.browser}, ${run.results.length} results`);\nconsole.log(`2nd test: ${second.title} - ${second.status} after ${second.durationMs / 1000}s`);\nconsole.log(`Its error: ${second.error ?? 'none'}`);\nconsole.log(`1st test error: ${run.results[0].error ?? 'none'}`);",
                  "expectedOutput": "Browser: firefox, 3 results\n2nd test: search works - failed after 5.3s\nIts error: Timeout 5000ms exceeded\n1st test error: none",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/test-run.ts",
                    "expected": "Browser: firefox, 3 results\n2nd test: search works - failed after 5.3s\nIts error: Timeout 5000ms exceeded\n1st test error: none"
                  }
                }
              ]
            },
            {
              "id": "d6-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. Which shape would you use for: all product names on a page; one customer; an email-and-password pair?\n2. What's the difference between `null` and `undefined`, and what does `??` do with them?\n3. Why does `user.phone.length` cause an error when `phone` is optional — and how do you fix it?\n4. What does `{ page }` in a Playwright test do?\n5. Why is `'chromium' | 'firefox' | 'webkit'` better than just `string` for a browser name?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Conditions and loops: your code starts making decisions (`if`, `switch`) and repeating work (`for`, `while`) — so you can finally run through that array of test cases one by one.",
                  "title": "Coming up on Day 7"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 18,
        "quizzes": 20,
        "exercises": 8,
        "codeBlocks": 22
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 7,
      "week": 2,
      "title": "Conditions & Loops",
      "subtitle": "Teach your code to make decisions and repeat work — the logic behind every data-driven test",
      "estimatedTime": "3 hours",
      "topics": [
        "Truthy and falsy values",
        "if / else if / else, switch and the ternary operator",
        "for, for...of, while and do...while loops, with break and continue",
        "Array methods forEach, map, filter, find, some and every"
      ],
      "objectives": [
        "Predict whether a value counts as true or false in a condition (truthy and falsy)",
        "Write decisions with if / else if / else, and choose between if, switch and the ternary",
        "Use a check with `if` to narrow a union or optional value, so TypeScript knows what you have",
        "Repeat work with for, for...of, while and do...while, and control loops with break and continue",
        "Filter, transform and question lists with forEach, map, filter, find, some and every",
        "Run a list of test cases through a check and report pass/fail — a mini data-driven test"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 5: comparison and logical operators, the ternary operator, blocks and scope",
        "Day 6: arrays, objects, type aliases, optional properties, `??`, `?.`, union and literal types"
      ],
      "workspace": "pw-course/ts-basics/day7/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d7-prerequisites-1",
              "title": "P1 · Quick recap from Days 5 and 6",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "- [ ] Compare with `===` and `!==`, and combine answers with `&&`, `||` and `!` (Day 5 · F6–F7)\n- [ ] Pick one of two values with the ternary: `condition ? valueIfTrue : valueIfFalse` (Day 5 · F7)\n- [ ] Curly braces `{ }` make a **block**, and `let`/`const` inside it stay inside it (Day 5 · F3)\n- [ ] Store lists in arrays and records in objects; read an optional property with `?.` or `??` (Day 6)"
                },
                {
                  "type": "quiz",
                  "id": "d7-p1-q1",
                  "quizType": "single",
                  "question": "`const isLoggedIn = true; const cartCount: number = 0;` — what is `isLoggedIn && cartCount > 0`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "true"
                    },
                    {
                      "id": "b",
                      "text": "false"
                    },
                    {
                      "id": "c",
                      "text": "0"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`cartCount > 0` is false, and true && false is false."
                },
                {
                  "type": "quiz",
                  "id": "d7-p1-q2",
                  "quizType": "single",
                  "question": "`const results = ['pass', 'fail', 'pass'];` — how do you read the LAST item, however long the list is?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`results[results.length - 1]`"
                    },
                    {
                      "id": "b",
                      "text": "`results.last`"
                    },
                    {
                      "id": "c",
                      "text": "`results[results.length]`"
                    },
                    {
                      "id": "d",
                      "text": "`results[3]`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Indexes start at 0, so the last index is always one less than the length."
                }
              ]
            },
            {
              "id": "d7-prerequisites-2",
              "title": "P2 · Decisions and repetition — you already write them",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Read any manual test case and you'll find two kinds of instruction that a straight list of steps can't express:\n\n| In a manual test case | In code | Today's section |\n|---|---|---|\n| \"**If** a cookie banner appears, dismiss it\" | a **condition** — `if` | F2 |\n| \"**Depending on** the user's role, check the right menu\" | a choice among many — `switch` | F4 |\n| \"**Repeat** steps 2–5 for each browser\" | a **loop** — `for...of` | F6 |\n| \"Refresh **until** the order says *Shipped* (give up after 5 tries)\" | a **loop with a stop condition** — `while` | F7 |\n| \"From the results, **list only** the failed tests\" | an **array method** — `filter` | F8 |"
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart LR\n  S[\"Step\"] --> C{\"Condition<br/>true?\"}\n  C -- yes --> A[\"Do this\"]\n  C -- no --> B[\"Do that\"]\n  A --> N[\"Next step\"]\n  B --> N\n  N --> L{\"More items<br/>in the list?\"}\n  L -- yes --> S\n  L -- no --> E[\"Done\"]"
                },
                {
                  "type": "markdown",
                  "content": "So far your programs ran every line once, top to bottom. Today they learn to **skip** lines (conditions) and **repeat** lines (loops). Together these are called **control flow**."
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "A data-driven test is exactly this: *for each row in the test-data sheet, do the steps; if the result matches the expected value, mark it passed, otherwise failed.* By the end of today you'll write one."
                },
                {
                  "type": "quiz",
                  "id": "d7-p2-q1",
                  "quizType": "single",
                  "question": "\"Repeat the checkout steps for each of the 3 payment methods.\" Which tool from today fits?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A loop over the list of payment methods"
                    },
                    {
                      "id": "b",
                      "text": "An `if` / `else` statement"
                    },
                    {
                      "id": "c",
                      "text": "A ternary operator"
                    },
                    {
                      "id": "d",
                      "text": "A single if statement"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Doing the same steps once for every item in a list is a loop."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d7-fundamentals-1",
              "title": "F1 · Truthy and falsy — what counts as \"true\"",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "An `if` needs a yes-or-no answer. Usually you give it a real boolean — `age >= 18` is `true` or `false`. But JavaScript lets you put **any** value in a condition, and quietly treats it as true or false:\n\n| Falsy — treated as **false** | Truthy — treated as **true** |\n|---|---|\n| `false` | `true` |\n| `0` (and `-0`) | Every other number: `1`, `-5`, `0.5` |\n| `''` (empty text) | Any non-empty text: `'a'`, `' '`, even `'false'` and `'0'` |\n| `null`, `undefined` | Every array and object — even empty ones: `[]`, `{}` |\n| `NaN` | |\n\nThe falsy list is short — **just these six** (`-0` counts as `0`; there's also a rare `0n` you won't meet in tests). Everything else is truthy.\n\nThis is handy for \"is there anything here?\" checks:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const couponCode: string = '';\nif (couponCode) {\n  console.log(`Apply ${couponCode}`);\n} else {\n  console.log('No coupon to apply');      // '' is falsy, so this runs\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### The trap: `0` and `''` are real values\n\n`||` (Day 5) uses the same rule: `a || b` gives `b` whenever `a` is **falsy**. That's a problem when `0` or `''` is a legitimate value:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const retries: number = 0;          // the tester deliberately set 0 retries\nconsole.log(retries || 3);          // 3  ❌ the 0 was thrown away\nconsole.log(retries ?? 3);          // 0  ✅ ?? only replaces null and undefined (Day 6)",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "For a fallback, prefer `??`. Use `||` or a bare `if (value)` only when `0` and `''` really should count as \"nothing\"."
                },
                {
                  "type": "quiz",
                  "id": "d7-f1-q1",
                  "quizType": "multiple",
                  "question": "Which values are falsy? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`''`"
                    },
                    {
                      "id": "b",
                      "text": "`0`"
                    },
                    {
                      "id": "c",
                      "text": "`[]`"
                    },
                    {
                      "id": "d",
                      "text": "`'0'`"
                    }
                  ],
                  "correct": [
                    "a",
                    "b"
                  ],
                  "explanation": "The number 0 and empty text are falsy. The text '0' isn't empty, and every array — even an empty one — is truthy."
                },
                {
                  "type": "quiz",
                  "id": "d7-f1-q2",
                  "quizType": "single",
                  "question": "`const quantity: number = 0;` — what does `quantity || 1` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "0"
                    },
                    {
                      "id": "b",
                      "text": "1"
                    },
                    {
                      "id": "c",
                      "text": "true"
                    },
                    {
                      "id": "d",
                      "text": "false"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "0 is falsy, so `||` falls back to 1. `quantity ?? 1` would keep the 0."
                }
              ]
            },
            {
              "id": "d7-fundamentals-2",
              "title": "F2 · `if`, `else if`, `else`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### `if` — do something only when a condition is true"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "if (condition) {\n  // runs only when condition is true (or truthy)\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The condition goes in round brackets; the code to run goes in a **block** in curly braces.\n\n### `else` — otherwise"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const stockCount: number = 0;\n\nif (stockCount > 0) {\n  console.log('Add to cart is enabled');\n} else {\n  console.log('Show \"Out of stock\"');        // this runs\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### `else if` — more than two possibilities"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const loadTimeMs: number = 1850;\n\nif (loadTimeMs < 1000) {\n  console.log('fast');\n} else if (loadTimeMs < 3000) {\n  console.log('acceptable');                 // this runs\n} else {\n  console.log('too slow');\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The conditions are checked **top to bottom**, and **only the first one that's true** runs. Once a branch runs, the rest are skipped — even if they would also be true. (If you added `else if (loadTimeMs < 5000)` at the end, 1850 would match it too — but it would never get there, because `< 3000` already won.) So put the most specific conditions first.\n\n`else if` and `else` are both optional, and you can have as many `else if` branches as you need.\n\n### Combining and nesting conditions\n\nUse `&&` and `||` to combine checks in one condition, or put an `if` inside another `if` (**nesting**):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const isLoggedIn: boolean = true;\nconst cartCount: number = 2;\n\nif (isLoggedIn && cartCount > 0) {\n  console.log('Show the checkout button');\n}\n\nif (isLoggedIn) {\n  if (cartCount === 0) {\n    console.log('Your cart is empty');\n  }\n} else {\n  console.log('Please sign in');\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Nesting more than two levels deep gets hard to read — like a test case with sub-sub-steps. Combining with `&&` is often clearer."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "Always use `{ }` after `if` and `else`, even for one line, and indent the code inside. JavaScript allows leaving the braces out for a single line, but that's a well-known source of bugs when someone later adds a second line.",
                  "title": "Braces and indentation"
                },
                {
                  "type": "quiz",
                  "id": "d7-f2-q1",
                  "quizType": "single",
                  "question": "`const score: number = 95;` — what does this print? `if (score > 50) { console.log('pass'); } else if (score > 90) { console.log('distinction'); } else { console.log('fail'); }`",
                  "options": [
                    {
                      "id": "a",
                      "text": "pass, then distinction"
                    },
                    {
                      "id": "b",
                      "text": "pass"
                    },
                    {
                      "id": "c",
                      "text": "fail"
                    },
                    {
                      "id": "d",
                      "text": "distinction"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "95 > 50 is true, so the first branch runs and the rest are skipped. To print 'distinction', the `score > 90` check must come first."
                },
                {
                  "type": "quiz",
                  "id": "d7-f2-q2",
                  "quizType": "single",
                  "question": "When does the `else` block run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Always, after the if block"
                    },
                    {
                      "id": "b",
                      "text": "Only when the first condition was true"
                    },
                    {
                      "id": "c",
                      "text": "When the condition causes an error"
                    },
                    {
                      "id": "d",
                      "text": "Only when none of the conditions above it were true"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "`else` is the \"none of the above\" branch."
                }
              ]
            },
            {
              "id": "d7-fundamentals-3",
              "title": "F3 · Conditions that narrow types",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 6 you saw that TypeScript won't let you use a value that **might** be missing, or might be the wrong type. An `if` is the everyday way to check first — and inside the block, TypeScript **knows** what you have. This is called **narrowing**.\n\n### Checking that an optional value is there"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type LoginCase = { id: string; expectedError?: string };\nconst testCase: LoginCase = { id: 'TC-02', expectedError: 'Locked out' };\n\nconsole.log(testCase.expectedError.length);        // ❌ 'testCase.expectedError' is possibly 'undefined'\n\nif (testCase.expectedError !== undefined) {\n  console.log(testCase.expectedError.length);      // ✅ in here it's definitely a string\n}\n\nif (testCase.expectedError) {                       // a truthiness check narrows too\n  console.log(testCase.expectedError.toUpperCase());\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Checking which type of a union you have"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const orderId: string | number = Math.random() > 0.5 ? 'ORD-7' : 7;\n\nif (typeof orderId === 'string') {\n  console.log(orderId.toLowerCase());     // TypeScript knows: string\n} else {\n  console.log(orderId.toFixed(0));        // and here: number\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`typeof value` gives the type's name as text: `'string'`, `'number'`, `'boolean'`, `'undefined'`, `'object'` (Days 4 and 6)."
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Narrowing is the code version of a precondition: \"**given** the error message is shown, **then** check its text\". TypeScript refuses to let you skip the \"given\"."
                },
                {
                  "type": "quiz",
                  "id": "d7-f3-q1",
                  "quizType": "single",
                  "question": "`type User = { name: string; phone?: string };` — inside which block is `user.phone.length` allowed?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`if (user.phone !== undefined) { … }`"
                    },
                    {
                      "id": "b",
                      "text": "`if (user.name) { … }`"
                    },
                    {
                      "id": "c",
                      "text": "`if (typeof user === 'object') { … }`"
                    },
                    {
                      "id": "d",
                      "text": "`if (user.phone === undefined) { … }`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Only a check on phone itself tells TypeScript that phone is a string inside the block."
                }
              ]
            },
            {
              "id": "d7-fundamentals-4",
              "title": "F4 · `switch` — choosing among many fixed values",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "When you compare **one value** against a list of possible values, `switch` reads more cleanly than a long `else if` ladder:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type Role = 'admin' | 'editor' | 'viewer';\ntype User = { name: string; role: Role };\nconst user: User = { name: 'Ravi', role: 'editor' };\n\nswitch (user.role) {\n  case 'admin':\n    console.log('Check the Settings menu is visible');\n    break;\n  case 'editor':\n    console.log('Check the Edit button is visible');     // this runs\n    break;\n  default:\n    console.log('Check the page is read-only');\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Part | Meaning |\n|---|---|\n| `switch (user.role)` | The value being checked |\n| `case 'admin':` | \"If it equals `'admin'`, start running here\" (compared with `===`) |\n| `break;` | \"Stop — leave the switch\" |\n| `default:` | Runs when no `case` matched — like the final `else` (optional) |\n\n### Forgetting `break` — fall-through\n\nWithout `break`, the code **keeps running into the next case**, whether it matches or not. This is called **fall-through**:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const status: string = 'failed';\nswitch (status) {\n  case 'failed':\n    console.log('Investigate');      // runs\n  case 'passed':\n    console.log('All good');         // ALSO runs — there was no break above!\n    break;\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Fall-through is almost always a bug — with one useful exception: stacking cases on purpose so they **share** a block:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "switch (status) {\n  case 'failed':\n  case 'timedOut':                   // 'failed' or 'timedOut' both land here\n    console.log('Open the trace');\n    break;\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Which one to use?\n\n| Situation | Use |\n|---|---|\n| One yes/no decision, or ranges (`< 1000`, `>= 18`) | `if` / `else` |\n| One value compared with several **exact** values | `switch` |\n| Choosing between two **values** in one line | the ternary `? :` (Day 5) |\n\nWith literal types (Day 6), TypeScript even checks your `case` labels: `case 'superuser':` is an error when `user.role` can only be `'admin' | 'editor' | 'viewer'`."
                },
                {
                  "type": "quiz",
                  "id": "d7-f4-q1",
                  "quizType": "single",
                  "question": "What does `break` do inside a `switch`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It stops the whole program"
                    },
                    {
                      "id": "b",
                      "text": "It skips to the default case"
                    },
                    {
                      "id": "c",
                      "text": "It leaves the switch, so the following cases don't run"
                    },
                    {
                      "id": "d",
                      "text": "It restarts the switch from the top"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "Without break, execution falls through into the next case."
                },
                {
                  "type": "quiz",
                  "id": "d7-f4-q2",
                  "quizType": "single",
                  "question": "Which is the best fit for: \"show 'Adult' if age is 18 or more, otherwise 'Minor'\" stored in a variable?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const group = age >= 18 && 'Adult';`"
                    },
                    {
                      "id": "b",
                      "text": "An `if` with no `else`, setting the value only for adults"
                    },
                    {
                      "id": "c",
                      "text": "A ternary: `const group = age >= 18 ? 'Adult' : 'Minor';`"
                    },
                    {
                      "id": "d",
                      "text": "A switch with a case for every age"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "Choosing between two values based on one condition is exactly what the ternary is for. `age >= 18 && 'Adult'` gives false (not 'Minor') for a minor, and an if without else leaves minors with no value."
                }
              ]
            },
            {
              "id": "d7-fundamentals-5",
              "title": "F5 · The `for` loop — counting",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **loop** runs the same block again and again. The classic `for` loop counts:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "for (let i = 0; i < 3; i++) {\n  console.log(`Run ${i + 1}`);\n}\n// Run 1\n// Run 2\n// Run 3",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The brackets hold three parts, separated by semicolons:\n\n| Part | Here | When it runs |\n|---|---|---|\n| **Start** | `let i = 0` | Once, before the loop begins |\n| **Condition** | `i < 3` | Before every round — if it's false, the loop ends |\n| **Update** | `i++` (add 1 to i) | After every round |\n\nTracing it step by step:\n\n| Round | `i` at start | `i < 3`? | Prints | `i` after `i++` |\n|---|---|---|---|---|\n| 1 | 0 | true | Run 1 | 1 |\n| 2 | 1 | true | Run 2 | 2 |\n| 3 | 2 | true | Run 3 | 3 |\n| — | 3 | **false** → stop | | |\n\nEach round of a loop is called an **iteration**. `i` is a traditional name for a counter (short for *index*).\n\n### Looping over an array by index\n\nBecause array indexes start at 0 and the last one is `length - 1`, the standard pattern is:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const browsers: string[] = ['chromium', 'firefox', 'webkit'];\nfor (let i = 0; i < browsers.length; i++) {\n  console.log(`${i}: ${browsers[i]}`);\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Note the `<` — not `<=`. With `i <= browsers.length` the loop runs one extra time and reads `browsers[3]`, which is `undefined`. This **off-by-one** mistake is the most common loop bug of all."
                },
                {
                  "type": "quiz",
                  "id": "d7-f5-q1",
                  "quizType": "single",
                  "question": "How many times does `for (let i = 1; i <= 4; i++) { … }` run its block?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It never stops"
                    },
                    {
                      "id": "b",
                      "text": "5"
                    },
                    {
                      "id": "c",
                      "text": "3"
                    },
                    {
                      "id": "d",
                      "text": "4"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "i takes the values 1, 2, 3 and 4. When i becomes 5, `5 <= 4` is false and the loop ends."
                },
                {
                  "type": "quiz",
                  "id": "d7-f5-q2",
                  "quizType": "single",
                  "question": "`const items = ['a', 'b', 'c'];` — which loop prints `undefined` as its last line?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`for (let i = 0; i <= items.length; i++) { console.log(items[i]); }`"
                    },
                    {
                      "id": "b",
                      "text": "`for (let i = 0; i < 2; i++) { console.log(items[i]); }`"
                    },
                    {
                      "id": "c",
                      "text": "`for (let i = 0; i < items.length; i++) { console.log(items[i]); }`"
                    },
                    {
                      "id": "d",
                      "text": "`for (let i = 1; i < items.length; i++) { console.log(items[i]); }`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`<=` lets i reach 3, and items[3] doesn't exist. (Starting at 1 skips 'a', and `i < 2` stops early — both wrong, but neither prints undefined.)"
                }
              ]
            },
            {
              "id": "d7-fundamentals-6",
              "title": "F6 · `for...of` — one round per item",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Most of the time you don't need the index — you just want **each item**. `for...of` gives you them one by one:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const browsers: string[] = ['chromium', 'firefox', 'webkit'];\n\nfor (const browser of browsers) {\n  console.log(`Testing on ${browser}`);\n}\n// Testing on chromium\n// Testing on firefox\n// Testing on webkit",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Read it as *\"for each `browser` of the list `browsers`\"*. Each round, `browser` holds the next item. It can be a `const` because every round gets a fresh variable. There's no counter to get wrong, so no off-by-one bugs.\n\n`for...of` works on anything that holds a sequence — arrays, and text too (one character per round):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "for (const ch of 'Hi!') {\n  console.log(ch);           // H, then i, then !\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "This is the loop you'll use most in Playwright tests — for example, \"for each product card on the page, check it has a price\".\n\n### Looping over an object's properties\n\nAn object isn't a list, so `for...of` doesn't work on it directly. `Object.entries(obj)` turns it into a list of `[name, value]` pairs, which you can unpack with destructuring (Day 6):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const settings = { retries: 2, workers: 4, timeoutMs: 30000 };\n\nfor (const [key, value] of Object.entries(settings)) {\n  console.log(`${key} = ${value}`);\n}\n// retries = 2\n// workers = 4\n// timeoutMs = 30000",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "There's a look-alike, `for...in`, which gives property **names** (keys) instead of values. On an array, that means the indexes — as **text**: `'0'`, `'1'`, `'2'`. Writing `in` when you meant `of` is an easy slip. For arrays, always use `for...of`.",
                  "title": "`for...in` is not `for...of`"
                },
                {
                  "type": "quiz",
                  "id": "d7-f6-q1",
                  "quizType": "single",
                  "question": "`for (const tag of ['@smoke', '@login']) { console.log(tag); }` — what's printed?",
                  "options": [
                    {
                      "id": "a",
                      "text": "@smoke and @login"
                    },
                    {
                      "id": "b",
                      "text": "The whole array, once"
                    },
                    {
                      "id": "c",
                      "text": "Nothing — for...of needs a counter"
                    },
                    {
                      "id": "d",
                      "text": "'0' and '1'"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "for...of gives the items themselves. (for...in would give the indexes '0' and '1'.)"
                }
              ]
            },
            {
              "id": "d7-fundamentals-7",
              "title": "F7 · `while`, `do...while`, `break` and `continue`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### `while` — repeat as long as a condition holds\n\nUse `while` when you **don't know in advance** how many rounds you need — \"keep checking until it's ready\":"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let attempt = 0;\nlet pageReady = false;\n\nwhile (!pageReady && attempt < 5) {\n  attempt++;\n  pageReady = attempt === 3;       // pretend the page becomes ready on the 3rd check\n}\nconsole.log(`Ready after ${attempt} checks`);   // Ready after 3 checks",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The condition is checked **before** every round. If it's false at the very start, the block never runs at all."
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "If nothing inside the loop ever makes the condition false, it runs forever and your program freezes. Always make sure each round moves towards the end — here, `attempt++` and the `attempt < 5` limit guarantee it stops. If it does happen, press **Stop** (or **Ctrl+C** in the terminal).",
                  "title": "Infinite loops"
                },
                {
                  "type": "markdown",
                  "content": "### `do...while` — run at least once\n\n`do...while` checks the condition **after** each round, so the block always runs at least once:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let tries = 0;\ndo {\n  tries++;\n  console.log(`Try ${tries}`);\n} while (tries < 0);        // false straight away — but \"Try 1\" was already printed",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "It's rarer than `while`; you'll mostly read it rather than write it.\n\n### `break` and `continue`\n\nThese work in every kind of loop:\n\n| Keyword | Effect | Tester's version |\n|---|---|---|\n| `break` | Leave the loop **now** | \"Stop testing — a blocker was found\" |\n| `continue` | Skip the **rest of this round**, go to the next one | \"Skip this row, it's marked N/A\" |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const statuses: string[] = ['passed', 'skipped', 'passed', 'failed', 'passed'];\n\nfor (const status of statuses) {\n  if (status === 'skipped') {\n    continue;                 // ignore skipped tests\n  }\n  if (status === 'failed') {\n    console.log('Failure found - stopping');\n    break;                    // no need to look further\n  }\n  console.log(status);\n}\n// passed\n// passed\n// Failure found - stopping",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "\"Keep retrying until it passes, at most N times\" is a `while` loop with a limit — and it's what Playwright's auto-waiting and web-first assertions (Day 2) do for you behind the scenes, so in real tests you'll rarely write it yourself."
                },
                {
                  "type": "quiz",
                  "id": "d7-f7-q1",
                  "quizType": "single",
                  "question": "What's the key difference between `while` and `do...while`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`do...while` is faster"
                    },
                    {
                      "id": "b",
                      "text": "`do...while` can only count upwards"
                    },
                    {
                      "id": "c",
                      "text": "`do...while` always runs its block at least once; `while` may run it zero times"
                    },
                    {
                      "id": "d",
                      "text": "`while` can't use break"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "`while` checks first; `do...while` runs first and checks afterwards."
                },
                {
                  "type": "quiz",
                  "id": "d7-f7-q2",
                  "quizType": "single",
                  "question": "In a loop over test results, you want to ignore rows whose status is 'skipped' but carry on with the rest. Which keyword?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`continue`"
                    },
                    {
                      "id": "b",
                      "text": "`return`"
                    },
                    {
                      "id": "c",
                      "text": "`break`"
                    },
                    {
                      "id": "d",
                      "text": "`default`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`continue` skips the rest of the current round only. `break` would end the whole loop."
                }
              ]
            },
            {
              "id": "d7-fundamentals-8",
              "title": "F8 · Array methods — loops with a purpose",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Arrays come with methods that do common loop jobs in one line. Each one takes a small **function** that says what to do with one item. You'll learn functions properly on Day 8; for now, you only need to read this shape:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "(result) => result.status === 'failed'\n// \"given one result, give back: is its status 'failed'?\"",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "- `(result)` — a name for the current item (you choose the name)\n- `=>` — \"gives back\"\n- after the arrow — what to work out for that item\n- with two names, `(item, index)`, the method also hands you the item's position\n- with `{ }` after the arrow, the function can hold several statements, like an `if` block\n\nThis is called an **arrow function**. The array method runs it once for every item.\n\n| Method | Question it answers | Gives back |\n|---|---|---|\n| `.forEach(fn)` | \"Do this with every item\" | nothing (`undefined`) |\n| `.map(fn)` | \"Turn every item into something else\" | a **new array**, same length |\n| `.filter(fn)` | \"Which items match?\" | a **new array** with only the matching items |\n| `.find(fn)` | \"Which is the **first** item that matches?\" | that item, or `undefined` |\n| `.some(fn)` | \"Does **at least one** item match?\" | `true` / `false` |\n| `.every(fn)` | \"Do **all** items match?\" | `true` / `false` |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const prices: number[] = [499, 1299, 2599, 799];\n\nconst expensive = prices.filter((price) => price > 1000);     // [1299, 2599]\nconst labels = prices.map((price) => `₹${price}`);             // ['₹499', '₹1299', '₹2599', '₹799']\nconst firstOver2000 = prices.find((price) => price > 2000);    // 2599\nconst anyFree = prices.some((price) => price === 0);           // false\nconst allPositive = prices.every((price) => price > 0);        // true\n\nprices.forEach((price, index) => {                             // forEach also gives the index\n  console.log(`Item ${index + 1}: ₹${price}`);\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "None of these change the original array — `filter` and `map` give you a **new** one."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "Anything these methods do, a `for...of` loop can do too; the methods are just shorter and say their purpose in their name. One important exception for later: in Playwright tests, where each step must **wait** (`await`), use `for...of`, not `forEach` — `forEach` doesn't wait. Day 8 shows why.",
                  "title": "Loop or method?"
                },
                {
                  "type": "quiz",
                  "id": "d7-f8-q1",
                  "quizType": "single",
                  "question": "`const codes = [200, 404, 200, 500];` — what is `codes.filter((c) => c >= 400)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "true"
                    },
                    {
                      "id": "b",
                      "text": "[404, 500]"
                    },
                    {
                      "id": "c",
                      "text": "404"
                    },
                    {
                      "id": "d",
                      "text": "[false, true, false, true]"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "filter keeps the matching items in a new array. (find would give just the first, 404; map would give the true/false list.)"
                },
                {
                  "type": "quiz",
                  "id": "d7-f8-q2",
                  "quizType": "single",
                  "question": "You want to know whether ANY test in a list failed. Which method fits best?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`.forEach()`"
                    },
                    {
                      "id": "b",
                      "text": "`.some()`"
                    },
                    {
                      "id": "c",
                      "text": "`.every()`"
                    },
                    {
                      "id": "d",
                      "text": "`.map()`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`some` answers \"does at least one item match?\" with true or false."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [
            {
              "type": "markdown",
              "content": "All files today go in `ts-basics/day7`. Run with `node day7/<file>.ts`, check with `npm run check -- day7/<file>.ts`."
            }
          ],
          "lessons": [
            {
              "id": "d7-implementation-1",
              "title": "I1 · Rate page-load times, and triage results",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "`if` ladders inside a `for...of` loop, then a `switch` inside another:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Page-load times measured by a performance check (milliseconds)\nconst loadTimesMs: number[] = [420, 1850, 5200];\n\n// for...of: run the block once for each value in the list\nfor (const timeMs of loadTimesMs) {\n  // if / else if / else: the first condition that is true wins\n  let rating: string;\n  if (timeMs < 1000) {\n    rating = 'fast';\n  } else if (timeMs < 3000) {\n    rating = 'acceptable';\n  } else {\n    rating = 'too slow';\n  }\n  console.log(`${timeMs} ms: ${rating}`);\n}\n\n// switch: choose an action for each result status\ntype Status = 'passed' | 'failed' | 'skipped' | 'timedOut';\nconst statuses: Status[] = ['passed', 'failed', 'timedOut', 'skipped'];\n\nfor (const status of statuses) {\n  switch (status) {\n    case 'passed':\n      console.log('passed: nothing to do');\n      break;\n    case 'failed':\n    case 'timedOut':                          // two cases share one block\n      console.log(`${status}: open the trace and investigate`);\n      break;\n    default:                                   // anything not listed above\n      console.log(`${status}: check why it was skipped`);\n  }\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/timing.ts",
                  "run": "node day7/timing.ts",
                  "expectedOutput": "420 ms: fast\n1850 ms: acceptable\n5200 ms: too slow\npassed: nothing to do\nfailed: open the trace and investigate\ntimedOut: open the trace and investigate\nskipped: check why it was skipped",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "420 ms: fast\n1850 ms: acceptable\n5200 ms: too slow\npassed: nothing to do\nfailed: open the trace and investigate\ntimedOut: open the trace and investigate\nskipped: check why it was skipped"
                },
                {
                  "type": "markdown",
                  "content": "Notice `let rating: string;` is declared **without** a value: every branch of the `if` ladder assigns one, and TypeScript checks that. Delete the `else` branch and check the file — TypeScript reports `Variable 'rating' is used before being assigned.`, because 5200 would get no rating.\n\n**Try it:** add `case 'flaky':` inside the switch and check the file. Why is it rejected?"
                }
              ]
            },
            {
              "id": "d7-implementation-2",
              "title": "I2 · A mini data-driven test",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This is Day 6's login test-data sheet, with one more row — and now actually **run**. For each test case, the code works out what the app would do, compares it with what the case expects, and reports PASS or FAIL:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A mini data-driven test: check every login case against the app's rules\ntype LoginCase = {\n  id: string;\n  username: string;\n  password: string;\n  shouldSucceed: boolean;\n  expectedError?: string;\n};\n\nconst loginCases: LoginCase[] = [\n  { id: 'TC-01', username: 'standard_user', password: 'secret_sauce', shouldSucceed: true },\n  { id: 'TC-02', username: 'locked_out_user', password: 'secret_sauce', shouldSucceed: false,\n    expectedError: 'Sorry, this user has been locked out.' },\n  { id: 'TC-03', username: 'standard_user', password: 'wrong', shouldSucceed: false,\n    expectedError: 'Username and password do not match.' },\n  { id: 'TC-04', username: '', password: 'secret_sauce', shouldSucceed: false,\n    expectedError: 'Username is required' },\n  { id: 'TC-05', username: 'standard_user', password: '', shouldSucceed: false,\n    expectedError: 'Password is required.' },\n];\n\nlet passed = 0;\nlet failed = 0;\n\nfor (const testCase of loginCases) {\n  // 1. Pretend to be the app: which error would it show? (undefined = logged in)\n  let actualError: string | undefined;\n  if (testCase.username === '') {\n    actualError = 'Username is required';\n  } else if (testCase.password === '') {\n    actualError = 'Password is required';\n  } else if (testCase.username === 'locked_out_user') {\n    actualError = 'Sorry, this user has been locked out.';\n  } else if (testCase.password !== 'secret_sauce') {\n    actualError = 'Username and password do not match.';\n  }\n\n  // 2. Compare what happened with what the test case expected\n  const loggedIn = actualError === undefined;\n  if (loggedIn === testCase.shouldSucceed && actualError === testCase.expectedError) {\n    passed++;\n    console.log(`PASS ${testCase.id}`);\n  } else {\n    failed++;\n    console.log(`FAIL ${testCase.id}: expected \"${testCase.expectedError ?? 'login'}\", got \"${actualError ?? 'login'}\"`);\n  }\n}\n\nconsole.log(`${passed} passed, ${failed} failed`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/login-runner.ts",
                  "run": "node day7/login-runner.ts",
                  "expectedOutput": "PASS TC-01\nPASS TC-02\nPASS TC-03\nPASS TC-04\nFAIL TC-05: expected \"Password is required.\", got \"Password is required\"\n4 passed, 1 failed",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "PASS TC-01\nPASS TC-02\nPASS TC-03\nPASS TC-04\nFAIL TC-05: expected \"Password is required.\", got \"Password is required\"\n4 passed, 1 failed"
                },
                {
                  "type": "markdown",
                  "content": "TC-05 fails because of a single full stop. Just like in real testing, the question is: is the **app** wrong, or the **test data**? Here the test data has a typo — remove the `.` and run again for 5 passes."
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Everything a data-driven Playwright test does is here: a typed test-data sheet, a loop over its rows, a check per row, and a pass/fail count. On Day 9 the \"pretend to be the app\" part is replaced by a real browser."
                }
              ]
            },
            {
              "id": "d7-implementation-3",
              "title": "I3 · A password-rules checker",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Nested loops, `continue`, and collecting problems in an array:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Check candidate passwords against the sign-up rules\nconst candidates: string[] = ['Secret@123', 'short1A', 'alllowercase1', 'NoDigitsHere', ''];\n\nfor (const password of candidates) {\n  if (password === '') {\n    console.log('(empty) -> skipped: nothing to check');\n    continue;                                  // jump straight to the next password\n  }\n\n  // Look at each character once\n  let hasDigit = false;\n  let hasUpper = false;\n  for (const ch of password) {\n    if ('0123456789'.includes(ch)) {\n      hasDigit = true;\n    } else if (ch !== ch.toLowerCase()) {     // only upper-case letters change when lower-cased\n      hasUpper = true;\n    }\n  }\n\n  // Collect every rule that fails\n  const problems: string[] = [];\n  if (password.length < 8) {\n    problems.push('at least 8 characters');\n  }\n  if (!hasDigit) {\n    problems.push('a digit');\n  }\n  if (!hasUpper) {\n    problems.push('an upper-case letter');\n  }\n\n  const verdict = problems.length === 0 ? 'valid' : `needs ${problems.join(', ')}`;\n  console.log(`${password} -> ${verdict}`);\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/passwords.ts",
                  "run": "node day7/passwords.ts",
                  "expectedOutput": "Secret@123 -> valid\nshort1A -> needs at least 8 characters\nalllowercase1 -> needs an upper-case letter\nNoDigitsHere -> needs a digit\n(empty) -> skipped: nothing to check",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Secret@123 -> valid\nshort1A -> needs at least 8 characters\nalllowercase1 -> needs an upper-case letter\nNoDigitsHere -> needs a digit\n(empty) -> skipped: nothing to check"
                },
                {
                  "type": "markdown",
                  "content": "Two things to notice:\n\n- The rules are three **separate** `if`s, not an `else if` ladder — a password can break several rules at once, and we want to report all of them.\n- The inner loop runs completely for each password: a loop inside a loop is called a **nested loop**.\n\n**Try it:** add a fourth rule — the password must not contain a space (`password.includes(' ')`) — and a candidate `'Has Space 1'` to test it."
                }
              ]
            },
            {
              "id": "d7-implementation-4",
              "title": "I4 · Retry until it works — `while` with a limit",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Retry a flaky check until it succeeds, but never more than maxAttempts times\nconst maxAttempts: number = 5;\nconst serverReplies: string[] = ['timeout', 'timeout', 'ok'];   // what happens on each attempt\n\nlet attempt = 0;\nlet succeeded = false;\n\nwhile (attempt < maxAttempts) {\n  const reply = serverReplies[attempt] ?? 'ok';   // past the end of the list? assume 'ok'\n  attempt++;\n  console.log(`Attempt ${attempt}: ${reply}`);\n  if (reply === 'ok') {\n    succeeded = true;\n    break;                                        // stop looping: we're done\n  }\n}\n\nconsole.log(succeeded ? `Succeeded after ${attempt} attempts` : `Gave up after ${maxAttempts} attempts`);\n\n// do...while: the body always runs at least once\nlet pagesVisited = 0;\ndo {\n  pagesVisited++;\n  console.log(`Visited page ${pagesVisited}`);\n} while (pagesVisited < 1);",
                  "mode": "editor",
                  "file": "ts-basics/day7/retry.ts",
                  "run": "node day7/retry.ts",
                  "expectedOutput": "Attempt 1: timeout\nAttempt 2: timeout\nAttempt 3: ok\nSucceeded after 3 attempts\nVisited page 1",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Attempt 1: timeout\nAttempt 2: timeout\nAttempt 3: ok\nSucceeded after 3 attempts\nVisited page 1"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** change `serverReplies` to `['timeout', 'timeout', 'timeout', 'timeout', 'timeout', 'timeout']` and run again. The loop stops at 5 attempts — the limit is what prevents an infinite loop."
                }
              ]
            },
            {
              "id": "d7-implementation-5",
              "title": "I5 · A test-run report with array methods",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Build a test-run report with array methods\ntype Status = 'passed' | 'failed' | 'skipped';\ntype TestResult = { title: string; status: Status; durationMs: number };\n\nconst results: TestResult[] = [\n  { title: 'login works', status: 'passed', durationMs: 1200 },\n  { title: 'search works', status: 'failed', durationMs: 5300 },\n  { title: 'add to cart', status: 'passed', durationMs: 2100 },\n  { title: 'checkout', status: 'failed', durationMs: 7400 },\n  { title: 'profile edits', status: 'skipped', durationMs: 0 },\n];\n\n// forEach: do something with every item (here: print a numbered list)\nresults.forEach((result, index) => {\n  console.log(`${index + 1}. ${result.title} - ${result.status}`);\n});\n\n// filter: keep only the items that match\nconst failures = results.filter((result) => result.status === 'failed');\n\n// map: turn every item into something else (here: just its title)\nconst failedTitles = failures.map((result) => result.title);\nconsole.log(`Failed (${failures.length}): ${failedTitles.join(', ')}`);\n\n// find: the FIRST item that matches, or undefined\nconst firstSlow = results.find((result) => result.durationMs > 5000);\nconsole.log(`First slow test: ${firstSlow?.title ?? 'none'}`);\n\n// some / every: yes-or-no questions about the whole list\nconst anyFailed = results.some((result) => result.status === 'failed');\nconst allUnder10s = results.every((result) => result.durationMs < 10000);\nconsole.log(`Any failed? ${anyFailed}. All under 10 s? ${allUnder10s}`);\n\n// A plain loop still works for totals\nlet totalMs = 0;\nfor (const result of results) {\n  totalMs += result.durationMs;\n}\nconsole.log(`Total time: ${totalMs / 1000} s`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/report.ts",
                  "run": "node day7/report.ts",
                  "expectedOutput": "1. login works - passed\n2. search works - failed\n3. add to cart - passed\n4. checkout - failed\n5. profile edits - skipped\nFailed (2): search works, checkout\nFirst slow test: search works\nAny failed? true. All under 10 s? true\nTotal time: 16 s",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "1. login works - passed\n2. search works - failed\n3. add to cart - passed\n4. checkout - failed\n5. profile edits - skipped\nFailed (2): search works, checkout\nFirst slow test: search works\nAny failed? true. All under 10 s? true\nTotal time: 16 s"
                },
                {
                  "type": "markdown",
                  "content": "`find` may find nothing, so its result's type is `TestResult | undefined` — which is why the code reads it with `?.` and `??` (Day 6). Change `5000` to `9000` and run: the line becomes `First slow test: none`."
                }
              ]
            },
            {
              "id": "d7-implementation-6",
              "title": "I6 · Spot the loop bugs",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This file type-checks **and** runs — but the output isn't what the comments say it should be. That's the hardest kind of bug: no error message, just wrong behaviour. Run it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Three loop bugs. Run this file and compare the output with the comments.\nconst browsers: string[] = ['chromium', 'firefox', 'webkit'];\n\n// Intended: print each browser once, numbered 0 to 2\nfor (let i = 0; i <= browsers.length; i++) {\n  console.log(`Browser ${i}: ${browsers[i]}`);\n}\n\n// Intended: print the browser NAMES\nfor (const browser in browsers) {\n  console.log(`Testing on ${browser}`);\n}\n\n// Intended: a failed test prints only \"Investigate\"\nconst status: string = 'failed';\nswitch (status) {\n  case 'failed':\n    console.log('Investigate');\n  case 'passed':\n    console.log('All good');\n    break;\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/loop-bugs.ts",
                  "run": "node day7/loop-bugs.ts",
                  "expectedOutput": "Browser 0: chromium\nBrowser 1: firefox\nBrowser 2: webkit\nBrowser 3: undefined\nTesting on 0\nTesting on 1\nTesting on 2\nInvestigate\nAll good",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Browser 0: chromium\nBrowser 1: firefox\nBrowser 2: webkit\nBrowser 3: undefined\nTesting on 0\nTesting on 1\nTesting on 2\nInvestigate\nAll good"
                },
                {
                  "type": "markdown",
                  "content": "| Symptom | Bug | Fix |\n|---|---|---|\n| `Browser 3: undefined` | Off-by-one: `<=` | `i < browsers.length` |\n| `Testing on 0`, `1`, `2` | `for...in` gives indexes | `for (const browser of browsers)` |\n| `All good` after `Investigate` | Missing `break` — fall-through | Add `break;` after `console.log('Investigate');` |\n\nFix all three, then compare with the corrected version:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// The same three loops, fixed\nconst browsers: string[] = ['chromium', 'firefox', 'webkit'];\n\nfor (let i = 0; i < browsers.length; i++) {\n  console.log(`Browser ${i}: ${browsers[i]}`);\n}\n\nfor (const browser of browsers) {\n  console.log(`Testing on ${browser}`);\n}\n\nconst status: string = 'failed';\nswitch (status) {\n  case 'failed':\n    console.log('Investigate');\n    break;\n  case 'passed':\n    console.log('All good');\n    break;\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/loop-bugs-fixed.ts",
                  "run": "node day7/loop-bugs-fixed.ts",
                  "expectedOutput": "Browser 0: chromium\nBrowser 1: firefox\nBrowser 2: webkit\nTesting on chromium\nTesting on firefox\nTesting on webkit\nInvestigate",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Browser 0: chromium\nBrowser 1: firefox\nBrowser 2: webkit\nTesting on chromium\nTesting on firefox\nTesting on webkit\nInvestigate"
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d7-practice-1",
              "title": "Quiz · Day 7 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d7-pr-q1",
                  "quizType": "single",
                  "question": "`const name: string = '';` — what does `` name ? `Hi ${name}` : 'Hi guest' `` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "An error"
                    },
                    {
                      "id": "b",
                      "text": "'Hi '"
                    },
                    {
                      "id": "c",
                      "text": "'Hi undefined'"
                    },
                    {
                      "id": "d",
                      "text": "'Hi guest'"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Empty text is falsy, so the ternary takes the value after the colon."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q2",
                  "quizType": "single",
                  "question": "In an `if / else if / else` chain, how many blocks run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "At most one — the first one whose condition is true (or the else)"
                    },
                    {
                      "id": "b",
                      "text": "Always all of them"
                    },
                    {
                      "id": "c",
                      "text": "The first true one, and then the else as well"
                    },
                    {
                      "id": "d",
                      "text": "Every block whose condition is true"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Once one branch runs, the rest of the chain is skipped."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q3",
                  "quizType": "single",
                  "question": "Which loop is the best fit for \"check each product in the `products` array\"?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`for (let i = 1; i < products.length; i++) { … }`"
                    },
                    {
                      "id": "b",
                      "text": "`for (const product in products) { … }`"
                    },
                    {
                      "id": "c",
                      "text": "`for (let i = 0; i <= products.length; i++) { … }`"
                    },
                    {
                      "id": "d",
                      "text": "`for (const product of products) { … }`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "for...of gives each item in turn. for...in gives the indexes as text; starting at 1 skips the first product; `<=` runs one round too many."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q4",
                  "quizType": "single",
                  "question": "What does `[1200, 5300, 800].map((ms) => ms / 1000)` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "[1.2, 5.3, 0.8]"
                    },
                    {
                      "id": "b",
                      "text": "true"
                    },
                    {
                      "id": "c",
                      "text": "[5300]"
                    },
                    {
                      "id": "d",
                      "text": "7.3"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "map turns every item into a new value and gives back a new array of the same length."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q5",
                  "quizType": "single",
                  "question": "`const users = [{ name: 'Asha', admin: false }, { name: 'Ravi', admin: true }];` — what is `users.find((u) => u.admin)?.name`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "undefined"
                    },
                    {
                      "id": "b",
                      "text": "true"
                    },
                    {
                      "id": "c",
                      "text": "'Ravi'"
                    },
                    {
                      "id": "d",
                      "text": "'Asha'"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "find gives the first item whose check is truthy — Ravi's object — and `?.name` reads its name."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q6",
                  "quizType": "multiple",
                  "question": "Which of these can stop a loop from running forever? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "A condition that eventually becomes false"
                    },
                    {
                      "id": "b",
                      "text": "A `break` that is reached"
                    },
                    {
                      "id": "c",
                      "text": "An attempt limit, like `attempt < maxAttempts`"
                    },
                    {
                      "id": "d",
                      "text": "A `continue` in every round"
                    }
                  ],
                  "correct": [
                    "a",
                    "b",
                    "c"
                  ],
                  "explanation": "`continue` only skips to the next round — the loop keeps going."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q7",
                  "quizType": "single",
                  "question": "`const tags: string[] = [];` — what is `tags.every((t) => t.includes('@'))`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "undefined"
                    },
                    {
                      "id": "b",
                      "text": "false — there are no tags"
                    },
                    {
                      "id": "c",
                      "text": "An error"
                    },
                    {
                      "id": "d",
                      "text": "true — no item breaks the rule"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "every asks \"is there any item that fails the check?\" In an empty list there isn't one, so it's true. (some on an empty list is false.) Worth remembering: an empty list makes `every` true."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q8",
                  "quizType": "single",
                  "question": "In a switch, what happens when no `case` matches and there's no `default`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Nothing — the switch is skipped"
                    },
                    {
                      "id": "b",
                      "text": "An error is thrown"
                    },
                    {
                      "id": "c",
                      "text": "The first case runs"
                    },
                    {
                      "id": "d",
                      "text": "The last case runs"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Without a matching case or a default, none of the switch's code runs."
                }
              ]
            },
            {
              "id": "d7-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p1",
                  "title": "Predict — break and continue",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "for (let i = 1; i <= 5; i++) {\n  if (i === 2) {\n    continue;\n  }\n  if (i === 4) {\n    break;\n  }\n  console.log(i);\n}",
                  "answer": "1\n3\n\ni = 1 is printed. i = 2 is skipped by `continue`. i = 3 is printed. At i = 4, `break` ends the loop, so 4 and 5 are never printed.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p2",
                  "title": "Predict — truthy, falsy and fallbacks",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "const retries = 0;\nconst name = '';\nconsole.log(retries || 3);\nconsole.log(retries ?? 3);\nconsole.log(name ? `Hi ${name}` : 'Hi guest');",
                  "answer": "3\n0\nHi guest\n\n0 is falsy, so `||` falls back to 3. `??` only replaces null or undefined, so it keeps the 0. Empty text is falsy, so the ternary picks 'Hi guest'.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p3",
                  "title": "Predict — a switch without breaks",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "const level: string = 'warning';\nswitch (level) {\n  case 'error':\n    console.log('E');\n  case 'warning':\n    console.log('W');\n  case 'info':\n    console.log('I');\n    break;\n  default:\n    console.log('D');\n}",
                  "answer": "W\nI\n\nThe switch starts at the matching case, 'warning', and prints W. There's no break, so it falls through into 'info' and prints I. The break there stops it before default.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p4",
                  "title": "Predict — filter then map",
                  "level": "easy",
                  "prompt": "What is printed?",
                  "code": "const doubled = [3, 8, 12, 5].filter((n) => n > 4).map((n) => n * 2);\nconsole.log(doubled);\nconsole.log(doubled.length);",
                  "answer": "[ 16, 24, 10 ]\n3\n\nfilter keeps 8, 12 and 5 (3 isn't greater than 4). map doubles each: 16, 24, 10. Node.js prints arrays with spaces inside the brackets.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d7-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex1",
                  "title": "A smoke-test checklist",
                  "level": "easy",
                  "prompt": "Create `day7/smoke.ts` with:\n```ts\nconst smokePages: string[] = ['/', '/products', '/cart', '/account'];\n```\nLoop over the list and print a numbered checklist. The `/account` page needs a login, so add ` (login needed)` to that line only:\n```\nStep 1: open /\nStep 2: open /products\nStep 3: open /cart\nStep 4: open /account (login needed)\n4 pages in the smoke checklist\n```",
                  "file": "ts-basics/day7/smoke.ts",
                  "run": "node day7/smoke.ts",
                  "hints": [
                    "You need both the position and the page: a counting `for` loop, or `forEach((page, index) => …)`.",
                    "Step numbers start at 1, indexes at 0: print `index + 1`.",
                    "A ternary can build the note: `const note = page === '/account' ? ' (login needed)' : '';`"
                  ],
                  "solution": "const smokePages: string[] = ['/', '/products', '/cart', '/account'];\n\nfor (let i = 0; i < smokePages.length; i++) {\n  const page = smokePages[i];\n  const note = page === '/account' ? ' (login needed)' : '';\n  console.log(`Step ${i + 1}: open ${page}${note}`);\n}\n\nconsole.log(`${smokePages.length} pages in the smoke checklist`);",
                  "expectedOutput": "Step 1: open /\nStep 2: open /products\nStep 3: open /cart\nStep 4: open /account (login needed)\n4 pages in the smoke checklist",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/smoke.ts",
                    "expected": "Step 1: open /\nStep 2: open /products\nStep 3: open /cart\nStep 4: open /account (login needed)\n4 pages in the smoke checklist"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex2",
                  "title": "Classify HTTP status codes",
                  "level": "medium",
                  "prompt": "API tests check **status codes**. Create `day7/status-codes.ts` with:\n```ts\nconst codes: number[] = [200, 201, 301, 404, 500, 503];\n```\nFor each code, print its category using an `if / else if / else` ladder:\n\n| Range | Category |\n|---|---|\n| 200–299 | success |\n| 300–399 | redirect |\n| 400–499 | client error |\n| 500 and above | server error |\n\nAlso count how many codes are errors (400 or above) and print:\n```\n200 -> success\n201 -> success\n301 -> redirect\n404 -> client error\n500 -> server error\n503 -> server error\nErrors: 3\n```",
                  "file": "ts-basics/day7/status-codes.ts",
                  "run": "node day7/status-codes.ts",
                  "hints": [
                    "A range check combines two comparisons: `code >= 200 && code < 300`.",
                    "Order your checks from lowest to highest range, so each `else if` only needs the upper limit.",
                    "Declare `let errors = 0;` before the loop and do `errors++` inside an `if`."
                  ],
                  "solution": "const codes: number[] = [200, 201, 301, 404, 500, 503];\nlet errors = 0;\n\nfor (const code of codes) {\n  let category: string;\n  if (code >= 200 && code < 300) {\n    category = 'success';\n  } else if (code >= 300 && code < 400) {\n    category = 'redirect';\n  } else if (code >= 400 && code < 500) {\n    category = 'client error';\n  } else {\n    category = 'server error';\n  }\n\n  if (code >= 400) {\n    errors++;\n  }\n  console.log(`${code} -> ${category}`);\n}\n\nconsole.log(`Errors: ${errors}`);",
                  "expectedOutput": "200 -> success\n201 -> success\n301 -> redirect\n404 -> client error\n500 -> server error\n503 -> server error\nErrors: 3",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/status-codes.ts",
                    "expected": "200 -> success\n201 -> success\n301 -> redirect\n404 -> client error\n500 -> server error\n503 -> server error\nErrors: 3"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex3",
                  "title": "Question the product list",
                  "level": "medium",
                  "prompt": "Create `day7/products.ts` with:\n```ts\ntype Product = { name: string; price: number; inStock: boolean };\nconst products: Product[] = [\n  { name: 'Monitor', price: 8999, inStock: true },\n  { name: 'Webcam', price: 2499, inStock: false },\n  { name: 'Headset', price: 1799, inStock: true },\n  { name: 'Laptop Stand', price: 1299, inStock: true },\n];\n```\nUsing **array methods** (no `for` loops), print:\n```\nIn stock: Monitor, Headset, Laptop Stand\nFirst under 2000: Headset\nAny out of stock? true\nAll priced? true\n```\n\"All priced\" means every price is greater than 0.",
                  "file": "ts-basics/day7/products.ts",
                  "run": "node day7/products.ts",
                  "hints": [
                    "`filter` keeps the in-stock products; `map` turns them into names; `join(', ')` makes one line.",
                    "`find` gives a product or undefined — use `?.name ?? 'none'`.",
                    "Out of stock means `!product.inStock` — use `some`."
                  ],
                  "solution": "type Product = { name: string; price: number; inStock: boolean };\nconst products: Product[] = [\n  { name: 'Monitor', price: 8999, inStock: true },\n  { name: 'Webcam', price: 2499, inStock: false },\n  { name: 'Headset', price: 1799, inStock: true },\n  { name: 'Laptop Stand', price: 1299, inStock: true },\n];\n\nconst inStockNames = products.filter((product) => product.inStock).map((product) => product.name);\nconst firstCheap = products.find((product) => product.price < 2000);\nconst anyOutOfStock = products.some((product) => !product.inStock);\nconst allPriced = products.every((product) => product.price > 0);\n\nconsole.log(`In stock: ${inStockNames.join(', ')}`);\nconsole.log(`First under 2000: ${firstCheap?.name ?? 'none'}`);\nconsole.log(`Any out of stock? ${anyOutOfStock}`);\nconsole.log(`All priced? ${allPriced}`);",
                  "expectedOutput": "In stock: Monitor, Headset, Laptop Stand\nFirst under 2000: Headset\nAny out of stock? true\nAll priced? true",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/products.ts",
                    "expected": "In stock: Monitor, Headset, Laptop Stand\nFirst under 2000: Headset\nAny out of stock? true\nAll priced? true"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex4",
                  "title": "Wait for the order to ship",
                  "level": "medium",
                  "prompt": "A test refreshes an order page until its status is `Shipped`, giving up after `maxChecks` checks. Create `day7/order-status.ts` with:\n```ts\nconst maxChecks: number = 5;\nconst statusOnEachCheck: string[] = ['Pending', 'Pending', 'Processing', 'Shipped'];\n```\nUse a `while` loop to \"check\" the status one at a time (read the next item of the list each round), stopping as soon as it's `Shipped` or after `maxChecks` checks. Print:\n```\nCheck 1: Pending\nCheck 2: Pending\nCheck 3: Processing\nCheck 4: Shipped\nShipped after 4 checks\n```\nIf it never ships within the limit, the last line should instead be `Still not shipped after N checks`, where N is `maxChecks`. Test it by changing `maxChecks` to `3` (you should see `Still not shipped after 3 checks`), then set it back.",
                  "file": "ts-basics/day7/order-status.ts",
                  "run": "node day7/order-status.ts",
                  "hints": [
                    "Keep a counter `let checks = 0;` and a variable for the latest status.",
                    "`statusOnEachCheck[checks] ?? 'Pending'` safely reads past the end of the list.",
                    "After the loop, a ternary on `status === 'Shipped'` picks the last line."
                  ],
                  "solution": "const maxChecks: number = 5;\nconst statusOnEachCheck: string[] = ['Pending', 'Pending', 'Processing', 'Shipped'];\n\nlet checks = 0;\nlet status = '';\n\nwhile (checks < maxChecks && status !== 'Shipped') {\n  status = statusOnEachCheck[checks] ?? 'Pending';\n  checks++;\n  console.log(`Check ${checks}: ${status}`);\n}\n\nconsole.log(status === 'Shipped' ? `Shipped after ${checks} checks` : `Still not shipped after ${checks} checks`);",
                  "expectedOutput": "Check 1: Pending\nCheck 2: Pending\nCheck 3: Processing\nCheck 4: Shipped\nShipped after 4 checks",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/order-status.ts",
                    "expected": "Check 1: Pending\nCheck 2: Pending\nCheck 3: Processing\nCheck 4: Shipped\nShipped after 4 checks"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex5",
                  "title": "Challenge: the release decision",
                  "level": "challenge",
                  "prompt": "Create `day7/release.ts` with this test run:\n```ts\ntype Status = 'passed' | 'failed' | 'skipped';\ntype TestResult = { title: string; status: Status; durationMs: number };\nconst results: TestResult[] = [\n  { title: 'login', status: 'passed', durationMs: 1200 },\n  { title: 'search', status: 'passed', durationMs: 2300 },\n  { title: 'filters', status: 'skipped', durationMs: 0 },\n  { title: 'add to cart', status: 'passed', durationMs: 1900 },\n  { title: 'checkout', status: 'failed', durationMs: 7400 },\n  { title: 'payment', status: 'passed', durationMs: 3100 },\n  { title: 'invoice', status: 'skipped', durationMs: 0 },\n  { title: 'logout', status: 'passed', durationMs: 600 },\n];\n```\n1. In **one** `for...of` loop, count each status with a `switch`, and track the slowest test.\n2. Calculate the pass rate as passed ÷ (passed + failed) × 100 — skipped tests didn't run, so they don't count.\n3. The release is `approved` only if the pass rate is at least 90 **and** no more than 2 tests were skipped; otherwise `blocked`.\n\nPrint:\n```\npassed: 5, failed: 1, skipped: 2\nPass rate: 83.3%\nSlowest: checkout (7.4 s)\nRelease: blocked\n```",
                  "file": "ts-basics/day7/release.ts",
                  "run": "node day7/release.ts",
                  "hints": [
                    "Declare `let passed = 0; let failed = 0; let skipped = 0;` before the loop.",
                    "Track the slowest with `let slowest = results[0];` and replace it inside the loop when `result.durationMs > slowest.durationMs`.",
                    "`.toFixed(1)` rounds the pass rate to one decimal place."
                  ],
                  "solution": "type Status = 'passed' | 'failed' | 'skipped';\ntype TestResult = { title: string; status: Status; durationMs: number };\nconst results: TestResult[] = [\n  { title: 'login', status: 'passed', durationMs: 1200 },\n  { title: 'search', status: 'passed', durationMs: 2300 },\n  { title: 'filters', status: 'skipped', durationMs: 0 },\n  { title: 'add to cart', status: 'passed', durationMs: 1900 },\n  { title: 'checkout', status: 'failed', durationMs: 7400 },\n  { title: 'payment', status: 'passed', durationMs: 3100 },\n  { title: 'invoice', status: 'skipped', durationMs: 0 },\n  { title: 'logout', status: 'passed', durationMs: 600 },\n];\n\nlet passed = 0;\nlet failed = 0;\nlet skipped = 0;\nlet slowest = results[0];\n\nfor (const result of results) {\n  switch (result.status) {\n    case 'passed':\n      passed++;\n      break;\n    case 'failed':\n      failed++;\n      break;\n    case 'skipped':\n      skipped++;\n      break;\n  }\n  if (result.durationMs > slowest.durationMs) {\n    slowest = result;\n  }\n}\n\nconst passRate = (passed / (passed + failed)) * 100;\nconst release = passRate >= 90 && skipped <= 2 ? 'approved' : 'blocked';\n\nconsole.log(`passed: ${passed}, failed: ${failed}, skipped: ${skipped}`);\nconsole.log(`Pass rate: ${passRate.toFixed(1)}%`);\nconsole.log(`Slowest: ${slowest.title} (${slowest.durationMs / 1000} s)`);\nconsole.log(`Release: ${release}`);",
                  "expectedOutput": "passed: 5, failed: 1, skipped: 2\nPass rate: 83.3%\nSlowest: checkout (7.4 s)\nRelease: blocked",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/release.ts",
                    "expected": "passed: 5, failed: 1, skipped: 2\nPass rate: 83.3%\nSlowest: checkout (7.4 s)\nRelease: blocked"
                  }
                }
              ]
            },
            {
              "id": "d7-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. Name the six falsy values. Why is `??` safer than `||` for a fallback?\n2. When would you choose `switch` over an `if / else if` ladder — and what goes wrong without `break`?\n3. What's the difference between `for...of` and `for...in` on an array?\n4. How do you make sure a `while` loop can't run forever?\n5. Which array method would you use to: list only the failed tests; check that every price is positive; get the first out-of-stock product?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Functions: package steps into reusable, named actions (like `login(user)`), learn `async`/`await` — the keyword on every line of a Playwright test — and split your code into modules with `import` and `export`.",
                  "title": "Coming up on Day 8"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 20,
        "quizzes": 25,
        "exercises": 9,
        "codeBlocks": 28
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 8,
      "week": 2,
      "title": "Functions, Async & Modules",
      "subtitle": "Package steps into reusable functions, make code wait for slow work with async/await, handle failures, and share code between files",
      "estimatedTime": "3 hours",
      "topics": [
        "Functions — declaring, calling, parameters and return values",
        "Arrow functions and callbacks",
        "Asynchronous code — Promises, async and await",
        "Errors — throw, try, catch and finally",
        "Modules — export and import"
      ],
      "objectives": [
        "Declare and call functions with typed parameters and return values",
        "Use optional, default and rest parameters",
        "Write arrow functions, and pass functions to other functions as callbacks",
        "Explain why browser automation is asynchronous, and use async/await correctly",
        "Recognise the missing-await bug and why `forEach` doesn't wait",
        "Throw and catch errors with try / catch / finally",
        "Split code across files with export and import — and decode the first line of every Playwright test",
        "Build a mini test runner that works like Playwright Test"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 6: arrays, objects, type aliases, `unknown`, `??` and `?.`",
        "Day 7: if / else, loops (especially for...of), array methods and the arrow shape `(item) => …`"
      ],
      "workspace": "pw-course/ts-basics/day8/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d8-prerequisites-1",
              "title": "P1 · Quick recap from Day 7",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "- [ ] Make decisions with `if / else if / else` and `switch` (Day 7 · F2, F4)\n- [ ] Repeat work for each item with `for...of` (Day 7 · F6)\n- [ ] Filter and question lists: `results.filter((r) => r.status === 'failed')` (Day 7 · F8)"
                },
                {
                  "type": "quiz",
                  "id": "d8-p1-q1",
                  "quizType": "single",
                  "question": "What does `[1200, 4500, 800].filter((ms) => ms > 1000)` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "1200"
                    },
                    {
                      "id": "b",
                      "text": "[1200, 4500]"
                    },
                    {
                      "id": "c",
                      "text": "[4500]"
                    },
                    {
                      "id": "d",
                      "text": "true"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "filter keeps every item that passes the check, in a new array."
                },
                {
                  "type": "quiz",
                  "id": "d8-p1-q2",
                  "quizType": "single",
                  "question": "How many times does `for (const b of ['chromium', 'firefox']) { … }` run its block?",
                  "options": [
                    {
                      "id": "a",
                      "text": "3"
                    },
                    {
                      "id": "b",
                      "text": "1"
                    },
                    {
                      "id": "c",
                      "text": "2"
                    },
                    {
                      "id": "d",
                      "text": "0"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "for...of runs once per item, and the list has two items."
                }
              ]
            },
            {
              "id": "d8-prerequisites-2",
              "title": "P2 · Shared steps and waiting — two ideas you already use",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "**Shared steps.** In a test-management tool, you don't retype \"Log in as a standard user\" in 40 test cases. You write it once as a **shared step** and reference it everywhere. When the login screen changes, you fix one place. In code, a shared step is a **function**.\n\n**Waiting.** When you test manually, you click *Log in* and **wait** for the dashboard before checking it. You'd never check the dashboard while the page is still loading. Code needs to be told to wait for slow work like that — that's what **async/await** is for.\n\n| Manual testing idea | Code | Today |\n|---|---|---|\n| A shared step, like \"Log in as `<user>`\" | A function: `login(user)` | F1–F3 |\n| A step that takes time — page load, save, search | An asynchronous function, with `await` | F4–F5 |\n| \"If the step fails, record the error and move on\" | `try` / `catch` | F6 |\n| A shared library of steps and test data | Modules: `export` and `import` | F7 |\n\nEvery line of a Playwright test uses today's ideas:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';          // a module import      (F7)\n\ntest('user can log in', async ({ page }) => {              // an async arrow function, passed to test()   (F3, F5)\n  await page.goto('https://example.com/login');            // wait for a slow step (F5)\n  await page.getByLabel('Email').fill('asha@example.com');  // calling functions with arguments (F1)\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "By the end of today, you'll be able to read every symbol in that code."
                },
                {
                  "type": "quiz",
                  "id": "d8-p2-q1",
                  "quizType": "single",
                  "question": "Your test case \"Checkout as a guest\" and 12 others all start with the same 5 login steps. In code, what's the best way to handle those 5 steps?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Leave them out; Playwright logs in automatically"
                    },
                    {
                      "id": "b",
                      "text": "Put them in a loop that runs 13 times"
                    },
                    {
                      "id": "c",
                      "text": "Put them in one function and call it from each test"
                    },
                    {
                      "id": "d",
                      "text": "Copy and paste them into all 13 tests"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "A function is a shared step — written once, called anywhere. When the login screen changes, you fix one place."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d8-fundamentals-1",
              "title": "F1 · Functions — named, reusable steps",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **function** is a named block of code that you can run whenever you need it. You've already **used** many built-in functions and methods — `console.log(…)`, `Number(…)`, `text.trim()`, `list.push(…)`. Now you'll write your own.\n\n### Declaring a function"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function buildEmail(name: string, id: number): string {\n  return `${name.toLowerCase()}.${id}@example.com`;\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Part | Here | Meaning |\n|---|---|---|\n| `function` | | \"I'm declaring a function\" |\n| Name | `buildEmail` | What you'll call it (camelCase, usually a verb: `build…`, `parse…`, `check…`) |\n| **Parameters** | `(name: string, id: number)` | The inputs it needs, each with a type |\n| **Return type** | `: string` | The type of value it gives back |\n| Body | `{ … }` | The steps it runs |\n| `return` | `return …;` | Gives the result back to whoever called it |\n\nDeclaring a function **doesn't run it**. It's like writing a shared step in the test-management tool: nothing happens until a test uses it.\n\n### Calling a function"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const email = buildEmail('Asha', 1);     // runs the function: email = 'asha.1@example.com'\nconsole.log(buildEmail('Ravi', 2));      // ravi.2@example.com",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The values you pass in — `'Asha'` and `1` — are the **arguments**. Inside the function, they arrive in the **parameters** `name` and `id`.\n\n| Word | What it is | Example |\n|---|---|---|\n| **Parameter** | The named slot in the declaration | `name: string` |\n| **Argument** | The actual value passed in the call | `'Asha'` |\n\nTypeScript checks every call against the declaration:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "buildEmail('Asha');          // ❌ error TS2554: Expected 2 arguments, but got 1.\nbuildEmail('Asha', '1');     // ❌ error TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### `return` — giving back a result\n\n`return` sends a value back **and ends the function immediately**. Anything after it in the same run is skipped:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function formatDuration(ms: number): string {\n  if (ms < 1000) {\n    return `${ms} ms`;          // for short durations, the function ends here…\n  }\n  return `${ms / 1000} s`;      // …so this line only runs for 1000 ms or more\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Returning early like this — \"if the input is a special case, answer straight away\" — is a common and readable pattern.\n\n### `void` — functions that just *do* something\n\nSome functions perform an action and give nothing back. Their return type is **`void`** (\"nothing\"):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function logStep(step: number, action: string): void {\n  console.log(`Step ${step}: ${action}`);\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Variables inside functions stay inside\n\nA function's body is a block, so `let` and `const` declared inside it only exist inside it (Day 5 · F3). Each call gets fresh variables — two calls can't mess up each other's values."
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "A good function does one clear thing, and its name says what: `parsePrice`, `buildEmail`, `validatePassword`. If you need \"and\" to describe it, it probably wants to be two functions.",
                  "title": "One job per function"
                },
                {
                  "type": "quiz",
                  "id": "d8-f1-q1",
                  "quizType": "single",
                  "question": "In `function greet(name: string): string { … }` and the call `greet('Meera')`, which is the argument?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`string`"
                    },
                    {
                      "id": "b",
                      "text": "`name`"
                    },
                    {
                      "id": "c",
                      "text": "`'Meera'`"
                    },
                    {
                      "id": "d",
                      "text": "`greet`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "The argument is the actual value passed in the call. `name` is the parameter — the slot that receives it."
                },
                {
                  "type": "quiz",
                  "id": "d8-f1-q2",
                  "quizType": "single",
                  "question": "`function check(n: number): string { if (n > 0) { return 'positive'; } return 'not positive'; }` — what does `check(5)` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'positive', then 'not positive'"
                    },
                    {
                      "id": "b",
                      "text": "'not positive'"
                    },
                    {
                      "id": "c",
                      "text": "'positive'"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "return ends the function immediately, so the second return is never reached for 5."
                },
                {
                  "type": "quiz",
                  "id": "d8-f1-q3",
                  "quizType": "single",
                  "question": "What does a return type of `void` mean?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It returns `undefined`, so you can't call it"
                    },
                    {
                      "id": "b",
                      "text": "The function gives no value back — it just performs an action"
                    },
                    {
                      "id": "c",
                      "text": "The function returns null"
                    },
                    {
                      "id": "d",
                      "text": "The function returns an empty string"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "void means \"returns nothing useful\". Calling it still runs its steps."
                }
              ]
            },
            {
              "id": "d8-fundamentals-2",
              "title": "F2 · Parameters: optional, default and rest",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "By default every parameter is **required**, and the call must give exactly one argument per parameter. Three variations make functions more flexible:\n\n### Optional parameters — `?`"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function greet(name: string, title?: string): string {\n  return title ? `Hello, ${title} ${name}` : `Hello, ${name}`;\n}\ngreet('Asha');            // 'Hello, Asha'           — title is undefined\ngreet('Rao', 'Dr.');      // 'Hello, Dr. Rao'",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Like optional properties (Day 6), an optional parameter that isn't passed is `undefined`, and its type is `string | undefined` — so TypeScript makes you check it before using it.\n\n### Default parameters — `= value`"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function buildEmail(name: string, id: number, domain: string = 'example.com'): string {\n  return `${name.toLowerCase()}.${id}@${domain}`;\n}\nbuildEmail('Asha', 1);               // 'asha.1@example.com'  — the default is used\nbuildEmail('Ravi', 2, 'test.org');   // 'ravi.2@test.org'     — the default is overridden",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "A default is often better than optional: inside the function the value is never `undefined`, so there's nothing to check.\n\n### Rest parameters — `...` for \"any number of\""
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function tagTitle(title: string, ...tags: string[]): string {\n  return `${title} ${tags.join(' ')}`;\n}\ntagTitle('checkout works', '@smoke', '@payments');   // tags = ['@smoke', '@payments']",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The three dots collect all the remaining arguments into one array.\n\n### The ordering rule\n\nAn optional (`?`) parameter can't come before a required one — TypeScript reports error TS1016. Put default parameters last too; otherwise callers must pass `undefined` to skip them. A rest parameter must always be **last**. (Otherwise, how would TypeScript know which argument goes where?)"
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "When a function needs many values, pass **one object** instead of a long list — `login(user)` rather than `login(email, password, rememberMe, role)`. You'll see this constantly in Playwright: `page.goto(url, { timeout: 10000 })`, `getByRole('button', { name: 'Log in' })` — the second argument is an options object.",
                  "title": "Objects as parameters"
                },
                {
                  "type": "quiz",
                  "id": "d8-f2-q1",
                  "quizType": "single",
                  "question": "`` function price(amount: number, currency: string = 'INR'): string { return `${amount} ${currency}`; } `` — what does `price(500)` give?",
                  "options": [
                    {
                      "id": "a",
                      "text": "An error: 2 arguments expected"
                    },
                    {
                      "id": "b",
                      "text": "'500'"
                    },
                    {
                      "id": "c",
                      "text": "'500 undefined'"
                    },
                    {
                      "id": "d",
                      "text": "'500 INR'"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "The default value INR is used when no second argument is passed."
                },
                {
                  "type": "quiz",
                  "id": "d8-f2-q2",
                  "quizType": "single",
                  "question": "Which declaration is NOT allowed?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`function f(a?: string, b: number) {}`"
                    },
                    {
                      "id": "b",
                      "text": "`function f(a: string, b?: number) {}`"
                    },
                    {
                      "id": "c",
                      "text": "`function f(a: string, ...rest: number[]) {}`"
                    },
                    {
                      "id": "d",
                      "text": "`function f(a: string, b: number = 1) {}`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "A required parameter can't come after an optional one — TypeScript reports error TS1016."
                }
              ]
            },
            {
              "id": "d8-fundamentals-3",
              "title": "F3 · Arrow functions and callbacks",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Arrow functions\n\nOn Day 7 you used little functions like `(result) => result.status === 'failed'`. These are **arrow functions** — a shorter way to write a function, often stored in a `const`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A function declaration…\nfunction isSlow(ms: number): boolean {\n  return ms > 3000;\n}\n\n// …and the same thing as an arrow function\nconst isSlowArrow = (ms: number): boolean => ms > 3000;",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Shape | Example | Notes |\n|---|---|---|\n| One expression after `=>` | `(ms: number) => ms > 3000` | The result is returned automatically — no `return`, no braces |\n| A block after `=>` | `` (ms: number) => { const s = ms / 1000; return `${s} s`; } `` | Several statements, so you write `return` yourself |\n| No parameters | `() => console.log('done')` | Empty brackets are still needed |\n\nBoth forms are functions; you call them the same way: `isSlowArrow(4500)`. Most Playwright code uses arrow functions.\n\n### Functions are values — callbacks\n\nA function can be stored in a variable and **passed to another function**, just like a number or a string. A function you hand to another function, for it to call later, is a **callback**:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const durations: number[] = [1200, 4500, 800];\n\ndurations.filter(isSlowArrow);              // pass the function itself (no brackets!) → [4500]\ndurations.filter((ms) => ms > 3000);        // or write it on the spot → [4500]",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Note `filter(isSlowArrow)` with **no brackets**: you hand over the function for `filter` to call, once per item. `filter(isSlowArrow())` would call it immediately yourself — not what you want.\n\nHere, TypeScript already knows `ms` must be a number (because `durations` is a `number[]`), so the callback doesn't need a type annotation.\n\n### Function types\n\nTo describe a function as a *type* — \"a function that takes a number and returns a boolean\" — write its shape with an arrow:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type DurationCheck = (ms: number) => boolean;\ntype TestBody = () => Promise<void>;     // takes nothing, returns a Promise",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "You'll meet `Promise` in F4 — for now, read `Promise<void>` as \"finishes later\".\n\n### The Playwright test is a callback\n\nNow you can read a Playwright test's structure:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('user can log in', async ({ page }) => {\n  // …steps…\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`test` is a function with **two arguments**: a title (a string) and a callback (an `async` arrow function, F5). Playwright stores the callback and calls it later, when it runs the test — handing it an object from which `{ page }` is destructured (Day 6)."
                },
                {
                  "type": "quiz",
                  "id": "d8-f3-q1",
                  "quizType": "single",
                  "question": "What does `const double = (n: number) => n * 2;` give for `double(21)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "21"
                    },
                    {
                      "id": "b",
                      "text": "42"
                    },
                    {
                      "id": "c",
                      "text": "An error"
                    },
                    {
                      "id": "d",
                      "text": "undefined — there's no return"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "With a single expression after the arrow, its value is returned automatically."
                },
                {
                  "type": "quiz",
                  "id": "d8-f3-q2",
                  "quizType": "single",
                  "question": "In `test('checkout works', async ({ page }) => { … })`, what is the second argument?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A callback function that Playwright calls when it runs the test"
                    },
                    {
                      "id": "b",
                      "text": "A string describing the steps"
                    },
                    {
                      "id": "c",
                      "text": "The test's result"
                    },
                    {
                      "id": "d",
                      "text": "The page"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "test() receives the title and a function; Playwright calls that function later, passing in the fixtures such as page."
                }
              ]
            },
            {
              "id": "d8-fundamentals-4",
              "title": "F4 · Asynchronous code — why tests must wait",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Synchronous code: one line at a time\n\nEverything you've written so far is **synchronous**: each line finishes completely before the next one starts. Adding numbers, joining text and looping over an array are all instant.\n\n### Asynchronous work: things that take time\n\nBrowser automation is different. Opening a page, clicking a button, waiting for a search result — each takes an unknown amount of time, from milliseconds to seconds. JavaScript doesn't freeze while it waits: it **starts** the slow work and carries on. Code like this is called **asynchronous** (async).\n\nYou can see this with `setTimeout`, which runs a callback after a delay:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "console.log('1. Order placed');\nsetTimeout(() => console.log('3. Order delivered'), 1000);   // run this callback in 1 second\nconsole.log('2. Carry on with other work');\n// Prints 1, 2 … and a second later, 3",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "JavaScript didn't wait for the delivery; it scheduled it and moved on.\n\n### Promises — \"the result will arrive later\"\n\nA slow operation gives you back a **Promise** straight away: an object that stands for a result that isn't ready yet. Think of an order-tracking number — you hold it now, and the parcel arrives later (or doesn't).\n\n| Promise state | Meaning | Order analogy |\n|---|---|---|\n| **pending** | Still working | \"Out for delivery\" |\n| **fulfilled** | Finished, with a value | \"Delivered\" — here's the parcel |\n| **rejected** | Failed, with an error | \"Delivery failed\" — here's why |\n\nIts type says what it will deliver: `Promise<string>` will deliver a string; `Promise<void>` delivers nothing — it just says \"done\".\n\nEvery Playwright action returns a Promise: `page.goto(…)`, `locator.click()`, `expect(locator).toBeVisible()`.\n\n### Decoding the waiting line from Day 2\n\nOn Day 2 you copied this line to make a pretend server slow. Now it makes sense:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "await new Promise((resolve) => setTimeout(resolve, 2000));",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "\"Create a Promise that `setTimeout` fulfils (`resolve`s) after 2000 ms — and `await` it.\" `new Promise(…)` creates a Promise. You give it a function; JavaScript hands that function a `resolve` function, and calling `resolve()` marks the Promise as fulfilled. `setTimeout(resolve, 2000)` calls it after 2 seconds. (`new` always means \"create a fresh object of this kind\" — you'll see `new Error(…)` in F6.)\n\nSo the line is a pause, and it's so useful that today's examples wrap it in a function:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`sleep` isn't marked `async` (F5), but it returns a Promise — and `await` works on any Promise."
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "Fixed waits make real tests slow and flaky (Day 2). Playwright waits automatically for the page; you'll only use `sleep` today to **pretend** that something takes time.",
                  "title": "`sleep` is for learning, not for tests"
                },
                {
                  "type": "quiz",
                  "id": "d8-f4-q1",
                  "quizType": "single",
                  "question": "Why is browser automation asynchronous?",
                  "options": [
                    {
                      "id": "a",
                      "text": "To make tests run in a random order"
                    },
                    {
                      "id": "b",
                      "text": "Because browser actions take an unknown amount of time, and the program must not freeze while they happen"
                    },
                    {
                      "id": "c",
                      "text": "Because browsers can only run one test at a time"
                    },
                    {
                      "id": "d",
                      "text": "Because TypeScript requires it for every program"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Page loads and clicks take time. Asynchronous code lets the program start the work and be told when it's done."
                },
                {
                  "type": "quiz",
                  "id": "d8-f4-q2",
                  "quizType": "single",
                  "question": "What does the type `Promise<number>` describe?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A result that will arrive later — when it succeeds, it's a number"
                    },
                    {
                      "id": "b",
                      "text": "A number that might be null"
                    },
                    {
                      "id": "c",
                      "text": "A function that takes a number"
                    },
                    {
                      "id": "d",
                      "text": "A list of numbers"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "A Promise stands for a future result; the type in angle brackets is the result's type."
                }
              ]
            },
            {
              "id": "d8-fundamentals-5",
              "title": "F5 · `async` and `await`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Two keywords make asynchronous code read like normal step-by-step code:\n\n| Keyword | Where | Meaning |\n|---|---|---|\n| `async` | Before a function | \"This function does slow work.\" It always returns a Promise |\n| `await` | Before a Promise | \"Pause **this function** until the Promise finishes, then give me the result.\" Other work that was already started keeps running |\n\nOn an arrow function, `async` goes before the brackets: `async () => { … }`, `async ({ page }) => { … }`."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "async function getPageTitle(): Promise<string> {\n  await sleep(100);             // pretend the page takes 100 ms to load\n  return 'My Shop – Home';      // an async function's return value arrives wrapped in a Promise\n}\n\nconst title = await getPageTitle();   // await unwraps Promise<string> into a string\nconsole.log(title);                    // My Shop – Home",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`await` only works inside an `async` function — or at the top level of a module file, like the files in `ts-basics` (the `\"type\": \"module\"` setting from Day 4 makes them modules). That's why every Playwright test body is written `async ({ page }) => { … }`: it needs `await` inside.\n\n### The #1 Playwright bug: a missing `await`\n\nForget `await`, and the program **doesn't wait**. It starts the step and rushes straight on to the next line:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "click('Log in');                 // ❌ starts the click… and doesn't wait for it\ncheckDashboard();                // runs while the click is still happening\n\nawait click('Log in');           // ✅ the click finishes first\nawait checkDashboard();",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "In a real test that leads to random failures, or worse, tests that pass without checking anything. TypeScript usually **doesn't** warn you: calling a function without `await` is perfectly legal code. You'll see it happen in I4.\n\n### `forEach` doesn't wait\n\nDay 7 promised an explanation. `forEach` calls its callback for every item, but it **ignores** the Promises that async callbacks return. It starts them all at once and moves on without waiting. A `for...of` loop with `await` inside does wait, one item at a time:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// ❌ doesn't wait: the next line runs before any page is checked\npages.forEach(async (page) => { await checkPage(page); });\n\n// ✅ waits for each check before starting the next\nfor (const page of pages) {\n  await checkPage(page);\n}",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "If a Playwright method **does something in the browser** or **checks something that may take time** — `goto`, `click`, `fill`, `expect(…).toBeVisible()` — put `await` in front of it. Inside loops, use `for...of`, not `forEach`.",
                  "title": "Rule for the rest of the course"
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q1",
                  "quizType": "single",
                  "question": "What does an `async` function always return?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Whatever type you declare, without a Promise"
                    },
                    {
                      "id": "b",
                      "text": "undefined"
                    },
                    {
                      "id": "c",
                      "text": "A Promise"
                    },
                    {
                      "id": "d",
                      "text": "A string"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "An async function always returns a Promise — `Promise<string>`, `Promise<void>` and so on. Use await to get the value inside."
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q2",
                  "quizType": "single",
                  "question": "A test runs `page.getByRole('button', { name: 'Save' }).click();` with no `await`, then immediately checks for a success message. What's the likely result?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It always passes — Playwright auto-waits anyway"
                    },
                    {
                      "id": "b",
                      "text": "It's unreliable: the check may run before the click has finished"
                    },
                    {
                      "id": "c",
                      "text": "The click happens twice"
                    },
                    {
                      "id": "d",
                      "text": "TypeScript refuses to run the file"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Auto-waiting happens INSIDE the click, but without await your test doesn't wait for the click to finish before moving on."
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q3",
                  "quizType": "single",
                  "question": "You need to check 5 product pages, one after another, in a Playwright test. Which loop is correct?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`products.forEach(async (p) => { await check(p); });`"
                    },
                    {
                      "id": "b",
                      "text": "`products.map((p) => check(p));`"
                    },
                    {
                      "id": "c",
                      "text": "`for (const p of products) { await check(p); }`"
                    },
                    {
                      "id": "d",
                      "text": "`for (const p in products) { await check(p); }`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "for...of with await waits for each check in turn. forEach and map start them all without waiting, and for...in gives indexes, not products."
                }
              ]
            },
            {
              "id": "d8-fundamentals-6",
              "title": "F6 · Errors — `throw`, `try`, `catch`, `finally`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Throwing an error\n\nWhen a function hits a problem it can't handle, it **throws** an error. That stops it immediately — like `return`, but signalling failure:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function parsePrice(text: string): number {\n  const price = Number(text.replace('₹', '').replaceAll(',', ''));\n  if (Number.isNaN(price)) {                          // true when price is NaN\n    throw new Error(`Not a price: \"${text}\"`);        // create an Error with a message, and throw it\n  }\n  return price;\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "(`Number.isNaN(value)` checks for `NaN`. You can't use `=== NaN`, because `NaN` isn't even equal to itself.)\n\nIf nothing catches the error, the program stops and prints it — the same kind of red error message you read on Day 4.\n\n### Catching an error\n\nWrap risky code in `try { … }`. If anything inside throws, the program jumps straight to `catch`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "try {\n  const price = parsePrice('call for price');   // throws…\n  console.log(price);                           // …so this line is skipped\n} catch (error) {\n  if (error instanceof Error) {\n    console.log(`Could not read price: ${error.message}`);\n  }\n} finally {\n  console.log('Price check finished');          // always runs, error or not\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Block | Runs… |\n|---|---|\n| `try` | First. It stops at the first error |\n| `catch (error)` | Only if something in `try` threw; `error` holds what was thrown |\n| `finally` (optional) | Always, at the end — good for clean-up |\n\n**Why `instanceof Error`?** JavaScript lets you throw *anything* — not just Error objects — so in strict mode, the caught `error` has the type `unknown` (Day 6). `error instanceof Error` checks that it really is an Error object, which **narrows** it (Day 7) so you can read `.message`. You'll also see `(error as Error).message` in other people's code; `as` tells TypeScript \"trust me\", without checking.\n\n### Errors in async code\n\nA `throw` inside an `async` function **rejects** the Promise that function returned. And when an awaited Promise is rejected, `await` turns that back into a thrown error — so the same `try/catch` works:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "try {\n  await findButton('Sign up');      // if this Promise is rejected…\n} catch (error) {\n  // …we land here\n}",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "This is how Playwright tests fail. When an assertion like `await expect(page).toHaveTitle('Shop')` doesn't match, it keeps retrying until its timeout (5 seconds by default, Day 2), then **throws** an error. Playwright's runner catches it, marks the test as failed, records the message and moves on to the next test. So in tests you rarely write `try/catch` yourself — letting the error reach the runner is exactly what you want. You'll build a runner that does this in I6."
                },
                {
                  "type": "quiz",
                  "id": "d8-f6-q1",
                  "quizType": "single",
                  "question": "`try { A(); B(); } catch (e) { C(); } finally { D(); }` — A() throws an error. Which run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A, B, C, D"
                    },
                    {
                      "id": "b",
                      "text": "A, D"
                    },
                    {
                      "id": "c",
                      "text": "A, C"
                    },
                    {
                      "id": "d",
                      "text": "A, C, D"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "A throws, so B is skipped and catch runs C. finally always runs D."
                },
                {
                  "type": "quiz",
                  "id": "d8-f6-q2",
                  "quizType": "single",
                  "question": "Why does TypeScript make you check `error instanceof Error` before reading `error.message` in a catch?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Because a catch block can't read properties at all"
                    },
                    {
                      "id": "b",
                      "text": "Because Error objects don't have a message"
                    },
                    {
                      "id": "c",
                      "text": "It doesn't — error is always an Error"
                    },
                    {
                      "id": "d",
                      "text": "Because anything can be thrown, so the caught value has the type unknown"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "JavaScript allows throwing any value, so TypeScript types it as unknown until you check it."
                }
              ]
            },
            {
              "id": "d8-fundamentals-7",
              "title": "F7 · Modules — `export` and `import`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "As a project grows, you split it into files: tests in one place, test data in another, helpers in a third. Each file is a **module**. A module **exports** what it wants to share, and other modules **import** it."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// file: data/products.ts\nexport type Product = { name: string; price: number };\nexport const products: Product[] = [{ name: 'Monitor', price: 8999 }];\nexport function findProduct(name: string): Product | undefined {\n  return products.find((p) => p.name === name);\n}",
                  "mode": "read"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// file: shop.test.ts\nimport { products, findProduct } from './data/products.ts';\n\nconsole.log(products.length);\nconsole.log(findProduct('Monitor')?.price);",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Anything **not** exported stays private to its file.\n\n### Paths\n\n| Import from | Means |\n|---|---|\n| `'./products.ts'` | A file in the **same** folder |\n| `'./data/products.ts'` | A file in the `data` sub-folder |\n| `'../data/products.ts'` | Go **up** one folder first, then into `data` |\n| `'@playwright/test'` (no `./`) | An installed **package** in `node_modules` |\n\n### Named and default exports\n\n| Kind | Export | Import |\n|---|---|---|\n| **Named** — any number per file | `export function login() {}` | `import { login } from './auth.ts';` — with braces, same name |\n| **Default** — at most one per file | `export default function login() {}` | `import login from './auth.ts';` — no braces, any name |\n\nMost Playwright code uses named exports; you'll meet a default export on Day 10, in `playwright.config.ts`.\n\n### Importing types: `import type`\n\nTypes exist only for TypeScript's checks. They're removed before the code runs (Day 4). So when you import a **type**, say so:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { products, type Product } from './data/products.ts';   // one value, one type\nimport type { Product } from './data/products.ts';             // only types",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "If you import a type **without** marking it, TypeScript's check passes, but running the file with Node.js fails: `SyntaxError: The requested module './data/products.ts' does not provide an export named 'Product'`. Node looked for `Product` in the running code, and there's no such thing. (Inside Playwright tests both forms work, but marking types is a good habit.)"
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "In the `ts-basics` playground, imports of your own files include `.ts`: `'./data/products.ts'`. That's what Node.js requires (and why the `check` command has `--allowImportingTsExtensions`). In Playwright projects you normally leave the extension out — `'../data/products'` — because Playwright finds the file for you.",
                  "title": "File extensions"
                },
                {
                  "type": "markdown",
                  "content": "### Decoding the first line of every Playwright test"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\n//      └── named imports ─┘      └── an installed package (no ./)",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "*\"From the installed package `@playwright/test`, bring in the two named exports `test` and `expect`.\"*"
                },
                {
                  "type": "quiz",
                  "id": "d8-f7-q1",
                  "quizType": "single",
                  "question": "`utils/data.ts` contains `export const adminUser = { … };`. How do you import it into `tests/admin.spec.ts` in a Playwright project?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`import { adminUser } from '@playwright/test';`"
                    },
                    {
                      "id": "b",
                      "text": "`import { adminUser } from '../utils/data';`"
                    },
                    {
                      "id": "c",
                      "text": "`import adminUser from 'utils/data';`"
                    },
                    {
                      "id": "d",
                      "text": "`import { adminUser } from './utils/data';`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "It's a named export, so use braces. From the tests folder, go up one level (../) and then into utils. './utils' would look inside tests."
                },
                {
                  "type": "quiz",
                  "id": "d8-f7-q2",
                  "quizType": "single",
                  "question": "A file has `const secretKey = 'abc';` with no `export`. Can another file import it?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Yes, with import { secretKey }"
                    },
                    {
                      "id": "b",
                      "text": "Only if both files are in the same folder"
                    },
                    {
                      "id": "c",
                      "text": "No — only exported names can be imported"
                    },
                    {
                      "id": "d",
                      "text": "Yes, but only as a default import"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "Anything not exported is private to its module."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [
            {
              "type": "markdown",
              "content": "All files today go in `ts-basics/day8`. Run with `node day8/<file>.ts`, check with `npm run check -- day8/<file>.ts`."
            }
          ],
          "lessons": [
            {
              "id": "d8-implementation-1",
              "title": "I1 · A toolbox of helper functions",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Turn price text from a page, like '₹1,499', into the number 1499\nfunction parsePrice(text: string): number {\n  const cleaned = text.trim().replace('₹', '').replaceAll(',', '');\n  return Number(cleaned);\n}\n\n// Build a test email address. domain has a default value.\nfunction buildEmail(name: string, id: number, domain: string = 'example.com'): string {\n  return `${name.toLowerCase()}.${id}@${domain}`;\n}\n\n// Show a duration in readable form. Two different return statements.\nfunction formatDuration(ms: number): string {\n  if (ms < 1000) {\n    return `${ms} ms`;           // return ends the function here…\n  }\n  return `${ms / 1000} s`;       // …so this line only runs for 1000 ms or more\n}\n\n// A function that does something but gives nothing back: void\nfunction logStep(step: number, action: string): void {\n  console.log(`Step ${step}: ${action}`);\n}\n\n// Calling the functions\nconst price = parsePrice(' ₹1,499 ');\nconsole.log(price + 1);                               // a real number now\nconsole.log(parsePrice('₹12,450') > 10000);\nconsole.log(buildEmail('Asha', 1));                   // uses the default domain\nconsole.log(buildEmail('Ravi', 2, 'test.org'));       // overrides it\nconsole.log(formatDuration(450), '|', formatDuration(2500));\nlogStep(1, 'Open the login page');",
                  "mode": "editor",
                  "file": "ts-basics/day8/helpers-demo.ts",
                  "run": "node day8/helpers-demo.ts",
                  "expectedOutput": "1500\ntrue\nasha.1@example.com\nravi.2@test.org\n450 ms | 2.5 s\nStep 1: Open the login page",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "1500\ntrue\nasha.1@example.com\nravi.2@test.org\n450 ms | 2.5 s\nStep 1: Open the login page"
                },
                {
                  "type": "markdown",
                  "content": "`console.log` can take several arguments; it prints them separated by spaces.\n\nOn Day 6 you cleaned one price with a chain of text methods. Now `parsePrice` does it for **any** price text — and if the site changes its currency format, you fix one function.\n\n**Try it:** call `buildEmail('Meera')` and check the file. Read the error, then fix the call."
                }
              ]
            },
            {
              "id": "d8-implementation-2",
              "title": "I2 · Extra practice (optional): refactor the password checker into functions",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Day 7's password checker lived inside a loop. Moved into functions, the same logic is easier to read, test and reuse:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// One job per function: small, named, reusable checks\n\n// Does the text contain at least one digit?\nfunction hasDigit(text: string): boolean {\n  return text.split('').some((ch) => '0123456789'.includes(ch));\n}\n\n// Does the text contain at least one upper-case letter?\nfunction hasUpperCase(text: string): boolean {\n  return text.split('').some((ch) => ch !== ch.toLowerCase());\n}\n\n// Return every rule the password breaks (an empty list = valid)\nfunction validatePassword(password: string): string[] {\n  if (password === '') {\n    return ['a password'];                  // early return: nothing else to check\n  }\n  const problems: string[] = [];\n  if (password.length < 8) {\n    problems.push('at least 8 characters');\n  }\n  if (!hasDigit(password)) {\n    problems.push('a digit');\n  }\n  if (!hasUpperCase(password)) {\n    problems.push('an upper-case letter');\n  }\n  return problems;\n}\n\n// Test data: each case says what we EXPECT\ntype PasswordCase = { input: string; shouldBeValid: boolean };\nconst cases: PasswordCase[] = [\n  { input: 'Secret@123', shouldBeValid: true },\n  { input: 'short1A', shouldBeValid: false },\n  { input: 'alllowercase1', shouldBeValid: false },\n  { input: '', shouldBeValid: false },\n  { input: 'Valid2Password', shouldBeValid: true },\n];\n\nlet passed = 0;\nfor (const testCase of cases) {\n  const problems = validatePassword(testCase.input);\n  const isValid = problems.length === 0;\n  const ok = isValid === testCase.shouldBeValid;   // did reality match the expectation?\n  if (ok) {\n    passed++;\n  }\n  const details = isValid ? 'valid' : `needs ${problems.join(', ')}`;\n  console.log(`${ok ? 'PASS' : 'FAIL'} \"${testCase.input}\" -> ${details}`);\n}\nconsole.log(`${passed}/${cases.length} cases behaved as expected`);",
                  "mode": "editor",
                  "file": "ts-basics/day8/password-check.ts",
                  "run": "node day8/password-check.ts",
                  "expectedOutput": "PASS \"Secret@123\" -> valid\nPASS \"short1A\" -> needs at least 8 characters\nPASS \"alllowercase1\" -> needs an upper-case letter\nPASS \"\" -> needs a password\nPASS \"Valid2Password\" -> valid\n5/5 cases behaved as expected",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "PASS \"Secret@123\" -> valid\nPASS \"short1A\" -> needs at least 8 characters\nPASS \"alllowercase1\" -> needs an upper-case letter\nPASS \"\" -> needs a password\nPASS \"Valid2Password\" -> valid\n5/5 cases behaved as expected"
                },
                {
                  "type": "markdown",
                  "content": "`text.split('')` splits text into single characters, so `.some(…)` can ask \"is any character a digit?\".\n\n**Try it:** add `{ input: 'Abcdefg1', shouldBeValid: true }` — predict the output line first, then run."
                }
              ]
            },
            {
              "id": "d8-implementation-3",
              "title": "I3 · A pretend browser test with `async`/`await`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "These pretend browser actions behave like Playwright's: each one takes time and returns a Promise. Read the test at the bottom — it looks very much like a real Playwright test:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A pretend browser. Like Playwright's, every action takes time and returns a Promise.\nfunction sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\nlet currentUrl = 'about:blank';\n\nasync function goto(url: string): Promise<void> {\n  await sleep(150);                       // loading a page takes a while\n  currentUrl = url;\n  console.log(`goto ${url}`);\n}\n\nasync function fill(field: string, value: string): Promise<void> {\n  await sleep(50);\n  console.log(`fill ${field} = ${value}`);\n}\n\nasync function click(button: string): Promise<void> {\n  await sleep(100);\n  if (button === 'Log in') {\n    currentUrl = '/dashboard';           // clicking Log in opens the dashboard\n  }\n  console.log(`click ${button}`);\n}\n\nasync function getUrl(): Promise<string> {\n  await sleep(10);\n  return currentUrl;                      // async functions can give back a value too\n}\n\n// The \"test\": each step waits for the one before it\nconsole.log('TEST: user can log in');\nawait goto('/login');\nawait fill('Email', 'asha@example.com');\nawait fill('Password', 'Secret@123');\nawait click('Log in');\n\nconst url = await getUrl();               // await unwraps Promise<string> into a string\nconsole.log(url === '/dashboard' ? 'PASSED: on /dashboard' : `FAILED: on ${url}`);",
                  "mode": "editor",
                  "file": "ts-basics/day8/fake-browser.ts",
                  "run": "node day8/fake-browser.ts",
                  "expectedOutput": "TEST: user can log in\ngoto /login\nfill Email = asha@example.com\nfill Password = Secret@123\nclick Log in\nPASSED: on /dashboard",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "TEST: user can log in\ngoto /login\nfill Email = asha@example.com\nfill Password = Secret@123\nclick Log in\nPASSED: on /dashboard"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** delete the `await` in front of `click('Log in')` and run it again. The check runs before the click has finished, and the \"test\" fails — the missing-await bug, reproduced."
                }
              ]
            },
            {
              "id": "d8-implementation-4",
              "title": "I4 · See the missing-`await` bug and the `forEach` trap",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\nasync function click(label: string): Promise<void> {\n  await sleep(200);                               // clicking takes a moment\n  console.log(`  clicked \"${label}\"`);\n}\n\n// 1. Without await: the program doesn't wait for the click\nconsole.log('WITHOUT await:');\nclick('Log in');                                  // ❌ no await\nconsole.log('  checking the dashboard   <- too early!');\nawait sleep(300);                                 // (give the forgotten click time to finish)\n\n// 2. With await: each step finishes before the next starts\nconsole.log('WITH await:');\nawait click('Log in');                            // ✅\nconsole.log('  checking the dashboard   <- right order');\n\n// 3. forEach doesn't wait for async callbacks\nconsole.log('forEach:');\nconst pages: string[] = ['home', 'cart'];\npages.forEach(async (page) => {\n  await sleep(100);\n  console.log(`  checked ${page}`);\n});\nconsole.log('  all pages checked?     <- printed first!');\nawait sleep(200);\n\n// 4. for...of with await does wait\nconsole.log('for...of:');\nfor (const page of pages) {\n  await sleep(100);\n  console.log(`  checked ${page}`);\n}\nconsole.log('  all pages checked      <- right order');",
                  "mode": "editor",
                  "file": "ts-basics/day8/missing-await.ts",
                  "run": "node day8/missing-await.ts",
                  "expectedOutput": "WITHOUT await:\n  checking the dashboard   <- too early!\n  clicked \"Log in\"\nWITH await:\n  clicked \"Log in\"\n  checking the dashboard   <- right order\nforEach:\n  all pages checked?     <- printed first!\n  checked home\n  checked cart\nfor...of:\n  checked home\n  checked cart\n  all pages checked      <- right order",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "WITHOUT await:\n  checking the dashboard   <- too early!\n  clicked \"Log in\"\nWITH await:\n  clicked \"Log in\"\n  checking the dashboard   <- right order\nforEach:\n  all pages checked?     <- printed first!\n  checked home\n  checked cart\nfor...of:\n  checked home\n  checked cart\n  all pages checked      <- right order"
                },
                {
                  "type": "markdown",
                  "content": "Now check the file with `npm run check -- day8/missing-await.ts`. It passes: TypeScript is **silent** about the missing `await`, because the code is legal. That's what makes this bug dangerous. (In real projects, a linter rule called `no-floating-promises` can flag it; the Playwright docs recommend it.)"
                }
              ]
            },
            {
              "id": "d8-implementation-5",
              "title": "I5 · Catching errors",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\n// A pretend \"find a button\" step that fails for unknown buttons\nasync function findButton(name: string): Promise<string> {\n  await sleep(100);\n  if (name !== 'Log in') {\n    throw new Error(`Button \"${name}\" not found`);   // stop and report a problem\n  }\n  return `<button>${name}</button>`;\n}\n\ntry {\n  const button = await findButton('Log in');\n  console.log(`Found: ${button}`);\n  await findButton('Sign up');                       // this one throws…\n  console.log('This line is skipped');\n} catch (error) {\n  // …so the program jumps here. error has the type unknown: check it first.\n  if (error instanceof Error) {\n    console.log(`Caught: ${error.message}`);\n  }\n} finally {\n  console.log('Clean-up always runs');               // runs whether it failed or not\n}\n\nconsole.log('The program carries on');",
                  "mode": "editor",
                  "file": "ts-basics/day8/errors.ts",
                  "run": "node day8/errors.ts",
                  "expectedOutput": "Found: <button>Log in</button>\nCaught: Button \"Sign up\" not found\nClean-up always runs\nThe program carries on",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Found: <button>Log in</button>\nCaught: Button \"Sign up\" not found\nClean-up always runs\nThe program carries on"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** remove the `try {`, the whole `catch` block and the `finally` block (keep the lines inside `try`), then run. There's nothing to catch the error, so the program stops with a red error message and `The program carries on` is never printed."
                }
              ]
            },
            {
              "id": "d8-implementation-6",
              "title": "I6 · Build your own mini test runner — with modules",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Time to put everything together. You'll build a tiny version of Playwright Test, split across three modules:"
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart LR\n  R[\"lib/runner.ts<br/>exports test, expectEqual, run\"] --> T[\"shop.test.ts<br/>the tests\"]\n  D[\"data/products.ts<br/>exports Product, products\"] --> T"
                },
                {
                  "type": "markdown",
                  "content": "**1. The runner** — registers tests, runs them one by one, and turns thrown errors into failed tests:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A tiny test runner: the same idea as Playwright Test, in about 30 lines\n\n// A test body: a function that takes nothing and returns a Promise\ntype TestBody = () => Promise<void>;\ntype RegisteredTest = { title: string; body: TestBody };\n\nconst registeredTests: RegisteredTest[] = [];\n\n// test(): register a test. It does NOT run yet.\nexport function test(title: string, body: TestBody): void {\n  registeredTests.push({ title, body });        // short for { title: title, body: body }\n}\n\n// expectEqual(): a tiny assertion. It throws when the values differ.\nexport function expectEqual(actual: unknown, expected: unknown): void {\n  if (actual !== expected) {\n    throw new Error(`Expected ${String(expected)}, received ${String(actual)}`);\n  }\n}\n\n// run(): run every registered test, one after another, and report\nexport async function run(): Promise<void> {\n  let passed = 0;\n  for (const registered of registeredTests) {\n    try {\n      await registered.body();                  // run the test and wait for it\n      passed++;\n      console.log(`  ✓ ${registered.title}`);\n    } catch (error) {                           // a failed check lands here\n      const message = error instanceof Error ? error.message : String(error);\n      console.log(`  ✘ ${registered.title}`);\n      console.log(`      ${message}`);\n    }\n  }\n  console.log(`${passed} passed, ${registeredTests.length - passed} failed`);\n}",
                  "mode": "editor",
                  "file": "ts-basics/day8/lib/runner.ts"
                },
                {
                  "type": "markdown",
                  "content": "**2. The test data** — shared by any test file that needs it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Test data, shared by any test file that imports it\nexport type Product = { name: string; price: number; inStock: boolean };\n\nexport const products: Product[] = [\n  { name: 'Monitor', price: 8999, inStock: true },\n  { name: 'Webcam', price: 2499, inStock: false },\n  { name: 'Headset', price: 1799, inStock: true },\n];",
                  "mode": "editor",
                  "file": "ts-basics/day8/data/products.ts"
                },
                {
                  "type": "markdown",
                  "content": "**3. The tests** — import both, register three tests, and run:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Import the runner's functions, and the shared test data\nimport { test, expectEqual, run } from './lib/runner.ts';\nimport { products, type Product } from './data/products.ts';\n\nfunction sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\ntest('cart total adds up', async () => {\n  let total = 0;\n  for (const product of products) {\n    total += product.price;\n  }\n  expectEqual(total, 13297);\n});\n\ntest('headset is found by name', async () => {\n  await sleep(100);                                  // pretend the search takes time\n  const found: Product | undefined = products.find((p) => p.name === 'Headset');\n  expectEqual(found?.price, 1799);\n});\n\ntest('every product is in stock', async () => {\n  const allInStock = products.every((p) => p.inStock);\n  expectEqual(allInStock, true);                     // the Webcam isn't: this test fails\n});\n\nawait run();",
                  "mode": "editor",
                  "file": "ts-basics/day8/shop.test.ts",
                  "run": "node day8/shop.test.ts",
                  "expectedOutput": "  ✓ cart total adds up\n  ✓ headset is found by name\n  ✘ every product is in stock\n      Expected true, received false\n2 passed, 1 failed",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "  ✓ cart total adds up\n  ✓ headset is found by name\n  ✘ every product is in stock\n      Expected true, received false\n2 passed, 1 failed"
                },
                {
                  "type": "markdown",
                  "content": "The failing test threw an error, the runner caught it, reported it, and **carried on**. That's exactly how Playwright Test behaves. Compare:\n\n| Your mini runner | Playwright Test (Day 9) |\n|---|---|\n| `import { test, expectEqual, run } from './lib/runner.ts'` | `import { test, expect } from '@playwright/test'` |\n| `test('title', async () => { … })` | `test('title', async ({ page }) => { … })` |\n| `expectEqual(total, 13297)` | `expect(total).toBe(13297)` |\n| Tests are registered first, then run by `run()` | Tests are registered in `*.spec.ts` files, then run by `npx playwright test` |\n| `try/catch` turns a thrown error into a failed test | The same — and it adds a report, screenshots and traces |\n| Runs tests one after another | Runs test files in parallel, with a fresh browser page for every test (tests inside one file run in order by default) |\n\n**Try it:** change the `import { products, type Product }` line to `import { products, Product }` (without `type`) and run the file. Read the `SyntaxError`, then put `type` back."
                },
                {
                  "type": "quiz",
                  "id": "d8-i6-q1",
                  "quizType": "single",
                  "question": "In the mini runner, why does `run()` use `for...of` instead of `registeredTests.forEach(…)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "No reason — they behave the same here"
                    },
                    {
                      "id": "b",
                      "text": "`for...of` catches errors automatically"
                    },
                    {
                      "id": "c",
                      "text": "`for...of` works with await, so each test finishes before the next starts"
                    },
                    {
                      "id": "d",
                      "text": "`forEach` doesn't exist on arrays"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "forEach ignores the Promises returned by async callbacks, so the tests would all start at once and the summary would print before any of them finished."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d8-practice-1",
              "title": "Quiz · Day 8 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d8-pr-q1",
                  "quizType": "single",
                  "question": "`function area(w: number, h: number = 5): number { return w * h; }` — what is `area(4)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "4"
                    },
                    {
                      "id": "b",
                      "text": "20"
                    },
                    {
                      "id": "c",
                      "text": "NaN"
                    },
                    {
                      "id": "d",
                      "text": "An error: 2 arguments expected"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "h has the default value 5, so area(4) is 4 × 5."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q2",
                  "quizType": "single",
                  "question": "Which arrow function returns the price with 18% tax?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const withTax = p: number => p * 1.18;`"
                    },
                    {
                      "id": "b",
                      "text": "`const withTax = (p: number) => p * 1.18;`"
                    },
                    {
                      "id": "c",
                      "text": "`const withTax = (p: number) => { p * 1.18 };`"
                    },
                    {
                      "id": "d",
                      "text": "`const withTax => (p: number) p * 1.18;`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "With braces, you must write return yourself — the version with `{ p * 1.18 }` in braces returns undefined. A typed parameter needs brackets around it."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q3",
                  "quizType": "single",
                  "question": "`async function getCount(): Promise<number> { return 3; }` — what is `getCount()` (called without await)?",
                  "options": [
                    {
                      "id": "a",
                      "text": "An error"
                    },
                    {
                      "id": "b",
                      "text": "A Promise that will deliver 3"
                    },
                    {
                      "id": "c",
                      "text": "undefined"
                    },
                    {
                      "id": "d",
                      "text": "3"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "An async function always returns a Promise. `await getCount()` would give 3."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q4",
                  "quizType": "multiple",
                  "question": "Which lines need `await` in a Playwright test? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const total = 499 + 1299;`"
                    },
                    {
                      "id": "b",
                      "text": "`page.goto('/login')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.getByRole('button', { name: 'Log in' }).click()`"
                    },
                    {
                      "id": "d",
                      "text": "`expect(page).toHaveTitle('Shop')`"
                    }
                  ],
                  "correct": [
                    "b",
                    "c",
                    "d"
                  ],
                  "explanation": "Browser actions and web-first assertions take time and return Promises. Plain arithmetic is instant."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q5",
                  "quizType": "single",
                  "question": "When does the `finally` block run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Only when an error was thrown"
                    },
                    {
                      "id": "b",
                      "text": "Always, after try (and catch, if it ran)"
                    },
                    {
                      "id": "c",
                      "text": "Only if catch didn't return"
                    },
                    {
                      "id": "d",
                      "text": "Only when no error was thrown"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "finally always runs — which makes it the place for clean-up."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q6",
                  "quizType": "single",
                  "question": "`import { test, expect } from '@playwright/test';` — what does the missing `./` tell you?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It's a default import"
                    },
                    {
                      "id": "b",
                      "text": "It's an installed package in node_modules"
                    },
                    {
                      "id": "c",
                      "text": "The file is in the same folder"
                    },
                    {
                      "id": "d",
                      "text": "The import is optional"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Paths starting with ./ or ../ are your own files. A bare name like @playwright/test is an installed package."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q7",
                  "quizType": "single",
                  "question": "A file imports `import { LoginCase } from './data.ts';` where LoginCase is a type. `npm run check` passes, but `node` fails with a SyntaxError. What's the fix?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Rename the file to data.js"
                    },
                    {
                      "id": "b",
                      "text": "Remove the .ts extension"
                    },
                    {
                      "id": "c",
                      "text": "Add `export default` to LoginCase"
                    },
                    {
                      "id": "d",
                      "text": "`import type { LoginCase } from './data.ts';`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Types are removed before the code runs, so Node can't find LoginCase. Node removes `import type` lines along with the other types, so it never looks for it."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q8",
                  "quizType": "single",
                  "question": "In a Playwright test, how does a failing `await expect(…)` make the test fail?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It returns false, and the runner checks the return value"
                    },
                    {
                      "id": "b",
                      "text": "It prints a warning and continues the test"
                    },
                    {
                      "id": "c",
                      "text": "It stops the whole test run"
                    },
                    {
                      "id": "d",
                      "text": "It throws an error; the runner catches it and marks the test as failed"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Assertions throw on failure. The runner catches the error, records it and moves on to the next test."
                }
              ]
            },
            {
              "id": "d8-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d8-pr-p1",
                  "title": "Predict — default and optional parameters",
                  "level": "easy",
                  "prompt": "What is printed?",
                  "code": "function label(name: string, count: number = 1, unit?: string): string {\n  return `${count} ${unit ?? 'x'} ${name}`;\n}\nconsole.log(label('Mouse'));\nconsole.log(label('Cable', 3, 'm'));",
                  "answer": "1 x Mouse\n3 m Cable\n\nIn the first call, count uses its default (1) and unit is undefined, so `??` gives 'x'. In the second, both are passed.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d8-pr-p2",
                  "title": "Predict — a forgotten await",
                  "level": "challenge",
                  "prompt": "What is printed, and in what order? (`sleep(ms)` waits for ms milliseconds.)",
                  "code": "const sleep = (ms: number) => new Promise((resolve) => setTimeout(resolve, ms));\nasync function step(name: string, ms: number): Promise<void> {\n  await sleep(ms);\n  console.log(name);\n}\nconsole.log('A');\nstep('B', 200);\nawait step('C', 100);\nconsole.log('D');",
                  "answer": "A\nC\nD\nB\n\nA prints at once. step('B', 200) starts but isn't awaited, so the program moves straight on. It awaits step('C', 100): after 100 ms, C prints, then D. B's 200 ms finish last.",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d8-pr-p3",
                  "title": "Predict — try, catch and finally",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "function check(value: number): string {\n  try {\n    if (value < 0) {\n      throw new Error('negative');\n    }\n    return 'ok';\n  } catch (error) {\n    return 'caught';\n  } finally {\n    console.log(`checked ${value}`);\n  }\n}\nconsole.log(check(5));\nconsole.log(check(-1));",
                  "answer": "checked 5\nok\nchecked -1\ncaught\n\nfinally runs even when try or catch returns, and it runs before the returned value reaches console.log. So each \"checked\" line appears before the result.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d8-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex1",
                  "title": "Cart total with an optional discount",
                  "level": "easy",
                  "prompt": "Create `day8/total.ts` with a function `calculateTotal(prices: number[], discountPercent: number = 0): number` that adds up the prices and subtracts the discount percentage.\n\nCall it three times and print:\n```\nTotal: 2000\nWith 10% off: 1800\nEmpty cart: 0\n```\nusing the cart `[499, 1299, 202]` for the first two lines and an empty array `[]` for the third.",
                  "file": "ts-basics/day8/total.ts",
                  "run": "node day8/total.ts",
                  "hints": [
                    "Add up with a for...of loop into `let total = 0`.",
                    "The discount amount is `total * discountPercent / 100`.",
                    "The default of 0 means the first call needs only one argument."
                  ],
                  "solution": "// Add up prices and apply an optional discount (in percent)\nfunction calculateTotal(prices: number[], discountPercent: number = 0): number {\n  let total = 0;\n  for (const price of prices) {\n    total += price;\n  }\n  return total - (total * discountPercent) / 100;\n}\n\nconst cart: number[] = [499, 1299, 202];\nconsole.log(`Total: ${calculateTotal(cart)}`);\nconsole.log(`With 10% off: ${calculateTotal(cart, 10)}`);\nconsole.log(`Empty cart: ${calculateTotal([])}`);",
                  "expectedOutput": "Total: 2000\nWith 10% off: 1800\nEmpty cart: 0",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/total.ts",
                    "expected": "Total: 2000\nWith 10% off: 1800\nEmpty cart: 0"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex2",
                  "title": "Fill the cart, one item at a time",
                  "level": "medium",
                  "prompt": "Create `day8/add-items.ts`:\n\n1. Copy the `sleep` function from I3.\n2. Write `async function addToCart(item: string): Promise<void>` that waits 100 ms and then prints `added <item>`.\n3. Write `async function fillCart(items: string[]): Promise<number>` that adds every item **in order** and returns how many it added.\n4. Call `fillCart(['Monitor', 'Webcam', 'Headset'])` and print the result:\n```\nadded Monitor\nadded Webcam\nadded Headset\n3 items in the cart\n```\nThen try it with `forEach` instead of `for...of` inside `fillCart`, to see what goes wrong. Change it back afterwards.",
                  "file": "ts-basics/day8/add-items.ts",
                  "run": "node day8/add-items.ts",
                  "hints": [
                    "Inside fillCart: `for (const item of items) { await addToCart(item); }`.",
                    "At the top level of the file: `const count = await fillCart([...]);`."
                  ],
                  "solution": "function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\nasync function addToCart(item: string): Promise<void> {\n  await sleep(100);\n  console.log(`added ${item}`);\n}\n\nasync function fillCart(items: string[]): Promise<number> {\n  for (const item of items) {\n    await addToCart(item);\n  }\n  return items.length;\n}\n\nconst count = await fillCart(['Monitor', 'Webcam', 'Headset']);\nconsole.log(`${count} items in the cart`);",
                  "expectedOutput": "added Monitor\nadded Webcam\nadded Headset\n3 items in the cart",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/add-items.ts",
                    "expected": "added Monitor\nadded Webcam\nadded Headset\n3 items in the cart"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex3",
                  "title": "Reject bad quantities",
                  "level": "medium",
                  "prompt": "Create `day8/quantities.ts`:\n\n1. Write `parseQuantity(text: string): number`. It converts the text with `Number(…)` and **throws** an Error:\n   - `Not a number: \"<text>\"` when the result is NaN\n   - `Quantity must be at least 1, got <n>` when it's less than 1\n2. Loop over `const inputs: string[] = ['2', 'abc', '0', '5'];`. Use `try/catch` so that one bad input doesn't stop the loop, and add up the good quantities:\n```\nOK: 2\nRejected: Not a number: \"abc\"\nRejected: Quantity must be at least 1, got 0\nOK: 5\nTotal quantity: 7\n```",
                  "file": "ts-basics/day8/quantities.ts",
                  "run": "node day8/quantities.ts",
                  "hints": [
                    "`Number.isNaN(quantity)` is true for NaN.",
                    "Put the `try { … } catch (error) { … }` inside the for...of loop.",
                    "In catch: `` if (error instanceof Error) { console.log(`Rejected: ${error.message}`); } ``"
                  ],
                  "solution": "// Turn text into a whole-number quantity, or throw an error\nfunction parseQuantity(text: string): number {\n  const quantity = Number(text);\n  if (Number.isNaN(quantity)) {\n    throw new Error(`Not a number: \"${text}\"`);\n  }\n  if (quantity < 1) {\n    throw new Error(`Quantity must be at least 1, got ${quantity}`);\n  }\n  return quantity;\n}\n\nconst inputs: string[] = ['2', 'abc', '0', '5'];\nlet total = 0;\n\nfor (const input of inputs) {\n  try {\n    const quantity = parseQuantity(input);\n    total += quantity;\n    console.log(`OK: ${quantity}`);\n  } catch (error) {\n    if (error instanceof Error) {\n      console.log(`Rejected: ${error.message}`);\n    }\n  }\n}\nconsole.log(`Total quantity: ${total}`);",
                  "expectedOutput": "OK: 2\nRejected: Not a number: \"abc\"\nRejected: Quantity must be at least 1, got 0\nOK: 5\nTotal quantity: 7",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/quantities.ts",
                    "expected": "OK: 2\nRejected: Not a number: \"abc\"\nRejected: Quantity must be at least 1, got 0\nOK: 5\nTotal quantity: 7"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex4",
                  "title": "Optional challenge: add skip to the mini runner",
                  "level": "challenge",
                  "prompt": "Playwright lets you **skip** a test with `test.skip(…)`. Add the same feature to a copy of the runner.\n\nCreate `day8/runner-plus.ts` — **one** file holding the runner code from `lib/runner.ts` (without the `export` keywords) plus the tests:\n\n1. Add a `skipped: boolean` property to each registered test.\n2. Add a function `skip(title, body)` that registers a test with `skipped: true`.\n3. In `run()`, don't run skipped tests: print `  - <title> (skipped)` and count them.\n4. Register these three tests:\n   - `test('discount is 10%', …)` checking that `1000 * 0.9` equals `900`\n   - `skip('payment by UPI', …)` with any body — it must not run\n   - `test('free shipping over 999', …)` where `const shipping = 1200 > 999 ? 0 : 49;` and the test (wrongly) expects `49`\n5. Run them. The output must be:\n```\n  ✓ discount is 10%\n  - payment by UPI (skipped)\n  ✘ free shipping over 999\n      Expected 49, received 0\n1 passed, 1 failed, 1 skipped\n```",
                  "file": "ts-basics/day8/runner-plus.ts",
                  "run": "node day8/runner-plus.ts",
                  "hints": [
                    "`type RegisteredTest = { title: string; body: TestBody; skipped: boolean };`",
                    "Keep three counters in run(): passed, failed and skipped.",
                    "In the loop: `if (registered.skipped) { skipped++; console.log(…); continue; }`"
                  ],
                  "solution": "type TestBody = () => Promise<void>;\ntype RegisteredTest = { title: string; body: TestBody; skipped: boolean };\n\nconst registeredTests: RegisteredTest[] = [];\n\nfunction test(title: string, body: TestBody): void {\n  registeredTests.push({ title, body, skipped: false });\n}\n\n// Register a test that should NOT run (like Playwright's test.skip)\nfunction skip(title: string, body: TestBody): void {\n  registeredTests.push({ title, body, skipped: true });\n}\n\nfunction expectEqual(actual: unknown, expected: unknown): void {\n  if (actual !== expected) {\n    throw new Error(`Expected ${String(expected)}, received ${String(actual)}`);\n  }\n}\n\nasync function run(): Promise<void> {\n  let passed = 0;\n  let failed = 0;\n  let skipped = 0;\n  for (const registered of registeredTests) {\n    if (registered.skipped) {\n      skipped++;\n      console.log(`  - ${registered.title} (skipped)`);\n      continue;\n    }\n    try {\n      await registered.body();\n      passed++;\n      console.log(`  ✓ ${registered.title}`);\n    } catch (error) {\n      failed++;\n      const message = error instanceof Error ? error.message : String(error);\n      console.log(`  ✘ ${registered.title}`);\n      console.log(`      ${message}`);\n    }\n  }\n  console.log(`${passed} passed, ${failed} failed, ${skipped} skipped`);\n}\n\ntest('discount is 10%', async () => {\n  expectEqual(1000 * 0.9, 900);\n});\nskip('payment by UPI', async () => {\n  expectEqual('not built yet', 'done');\n});\ntest('free shipping over 999', async () => {\n  const shipping = 1200 > 999 ? 0 : 49;\n  expectEqual(shipping, 49);\n});\n\nawait run();",
                  "expectedOutput": "  ✓ discount is 10%\n  - payment by UPI (skipped)\n  ✘ free shipping over 999\n      Expected 49, received 0\n1 passed, 1 failed, 1 skipped",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/runner-plus.ts",
                    "expected": "  ✓ discount is 10%\n  - payment by UPI (skipped)\n  ✘ free shipping over 999\n      Expected 49, received 0\n1 passed, 1 failed, 1 skipped"
                  }
                }
              ]
            },
            {
              "id": "d8-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. What's the difference between a parameter and an argument?\n2. When would you give a parameter a default value instead of making it optional?\n3. In your own words: what is a Promise, and what does `await` do with it?\n4. Why is a missing `await` so dangerous — and why doesn't TypeScript catch it?\n5. How does a failing assertion make a Playwright test fail, and why don't tests usually need `try/catch`?\n6. Read `import { test, expect } from '@playwright/test';` aloud as a sentence."
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "The real thing: Playwright Test. You'll write tests with `test` and `expect`, find elements with locators, act on them and assert the results — against practice pages in a real browser.",
                  "title": "Coming up on Day 9"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 19,
        "quizzes": 28,
        "exercises": 7,
        "codeBlocks": 34
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 9,
      "week": 2,
      "title": "Playwright Test Runner Basics",
      "subtitle": "Write real Playwright tests from scratch — find elements with locators, act on them, assert the results, and control and debug your runs",
      "estimatedTime": "3 hours",
      "topics": [
        "Anatomy of a Playwright test",
        "Built-in fixtures",
        "Locators, actions and assertions",
        "Annotations — skip, fixme, fail and slow",
        "Running, debugging and reading the report"
      ],
      "objectives": [
        "Read and write a complete Playwright test, line by line",
        "Use the built-in fixtures page and browserName",
        "Find elements with the recommended locators, and explain strictness",
        "Perform user actions — fill, click, check, selectOption, press",
        "Check results with web-first assertions, and know when to use generic ones",
        "Mark tests with skip, fixme, fail and slow",
        "Debug a failing test using the error message, headed mode, the Inspector and traces"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 1: Browser, Context and Page; a test case turned into Playwright steps",
        "Day 2: auto-waiting and web-first assertions",
        "Day 3: running tests from the terminal, the HTML report",
        "Day 6: object destructuring — `{ page }`",
        "Day 8: async/await, arrow-function callbacks, import/export"
      ],
      "workspace": "pw-course/tests/day9/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d9-prerequisites-1",
              "title": "P1 · Checklist before you start",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "From today you're back in the Playwright project. Run every command from the `pw-course` folder, not from `ts-basics`.\n\n- [ ] Run a single test file in Chromium: `npx playwright test tests/example.spec.ts --project=chromium` (Day 3 · F5)\n- [ ] Read `async ({ page }) => { … }` as *\"an async arrow function that takes the `page` out of the object Playwright passes in\"* (Day 6 · F5, Day 8 · F3, F5)\n- [ ] Explain why every browser action gets `await` (Day 8 · F5)\n- [ ] Import a named export from another file (Day 8 · F7)"
                },
                {
                  "type": "quiz",
                  "id": "d9-p1-q1",
                  "quizType": "single",
                  "question": "In `test('checkout works', async ({ page }) => { … })`, what is `{ page }` doing?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Importing page from @playwright/test"
                    },
                    {
                      "id": "b",
                      "text": "Taking the page property out of the object Playwright passes to the callback"
                    },
                    {
                      "id": "c",
                      "text": "Creating a new page object"
                    },
                    {
                      "id": "d",
                      "text": "Declaring a variable that must be filled in later"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Playwright calls your callback with an object of fixtures; object destructuring picks out page."
                },
                {
                  "type": "quiz",
                  "id": "d9-p1-q2",
                  "quizType": "single",
                  "question": "What goes wrong if you write `page.getByRole('button', { name: 'Save' }).click();` without `await`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A type error stops the test from running"
                    },
                    {
                      "id": "b",
                      "text": "The next line may run before the click has happened"
                    },
                    {
                      "id": "c",
                      "text": "Nothing — Playwright adds the await for you"
                    },
                    {
                      "id": "d",
                      "text": "The click is performed twice"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Without await, the test doesn't wait for the click to finish before moving on (Day 8 · F5)."
                }
              ]
            },
            {
              "id": "d9-prerequisites-2",
              "title": "P2 · HTML roles — how Playwright \"sees\" a page",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Playwright's recommended way to find elements is the way **users and assistive technologies** (screen readers) see them: by **role** and **accessible name**.\n\nMost HTML elements have a built-in role:\n\n| HTML | Role | Accessible name comes from… |\n|---|---|---|\n| `<button>Sign in</button>` | `button` | its text → \"Sign in\" |\n| `<a href=\"/help\">Help</a>` | `link` | its text → \"Help\" |\n| `<h1>Dashboard</h1>` (also `h2`–`h6`) | `heading` | its text → \"Dashboard\" |\n| `<input type=\"text\">` or `type=\"email\"`, with a `<label>` | `textbox` | its label → e.g. \"Email\" |\n| `<input type=\"checkbox\">` | `checkbox` | its label → e.g. \"Remember me\" |\n| `<select>` | `combobox` | its label → e.g. \"Course\" |\n| `<option>` inside a `<select>` | `option` | its text |\n| `<ul>` / `<li>` | `list` / `listitem` | — |\n| `<p role=\"alert\">` | `alert` | an urgent message — role set on purpose by the developer |\n| `<p role=\"status\">` | `status` | a status message, such as \"Saved\" — role set by the developer |\n\nSo `page.getByRole('button', { name: 'Sign in' })` means *\"the button whose accessible name is Sign in\"* — the way you'd describe it in a manual test case."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "A **password** input (`type=\"password\"`) has no role, so `getByRole('textbox')` won't find it. Find it by its label instead: `page.getByLabel('Password')`."
                },
                {
                  "type": "quiz",
                  "id": "d9-p2-q1",
                  "quizType": "single",
                  "question": "Which locator finds `<a href=\"/pricing\">See pricing</a>`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.getByRole('button', { name: 'See pricing' })`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByLabel('See pricing')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.getByRole('link', { name: 'See pricing' })`"
                    },
                    {
                      "id": "d",
                      "text": "`page.getByRole('heading', { name: 'See pricing' })`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "An `<a href>` element has the role link, and its accessible name is its text."
                }
              ]
            },
            {
              "id": "d9-prerequisites-3",
              "title": "P3 · Today's practice website",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Real websites change, go down or need a VPN — not ideal for learning. Today you'll test two small **practice pages** that live inside your project as HTML text. Tests load them with `page.setContent(html)` instead of `page.goto(url)`, just like on Day 1. Everything you learn works the same on real sites.\n\nCreate `tests/day9/practice-pages.ts`. It's a normal module (Day 8) that **exports** two HTML strings. Its name doesn't end in `.spec.ts`, so Playwright won't treat it as a test file."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Practice web pages for Days 9 and 10.\n// Each constant holds the HTML of one page. Tests load it with: await page.setContent(signInPage);\n\n// ---------------------------------------------------------------------------\n// Page 1: QA Academy sign-in\n//   Valid user:   student@qa.academy / Learn@123 → \"Signing in…\", then (0.8 s later) the Dashboard\n//   Empty fields: \"Please enter your email and password\"\n//   Wrong login:  \"Invalid email or password\"\n//   Dashboard:    \"Log out\" button → \"Signed out\" page\n// ---------------------------------------------------------------------------\nexport const signInPage = `\n<!DOCTYPE html>\n<html lang=\"en\">\n<head><meta charset=\"utf-8\"><title>QA Academy - Sign in</title></head>\n<body>\n  <img alt=\"QA Academy logo\" width=\"40\" height=\"40\" src=\"data:image/gif;base64,R0lGODlhAQABAAAAACw=\">\n  <h1>Sign in to QA Academy</h1>\n  <form id=\"signin-form\">\n    <label for=\"email\">Email</label>\n    <input id=\"email\" type=\"email\" placeholder=\"you@example.com\">\n\n    <label for=\"password\">Password</label>\n    <input id=\"password\" type=\"password\" placeholder=\"Your password\">\n\n    <label><input id=\"remember\" type=\"checkbox\"> Remember me</label>\n\n    <button type=\"submit\">Sign in</button>\n  </form>\n  <a href=\"#\" title=\"Reset your password\">Forgot password?</a>\n  <p id=\"message\" role=\"alert\"></p>\n\n  <script>\n    const form = document.getElementById('signin-form');\n    const message = document.getElementById('message');\n\n    form.addEventListener('submit', (event) => {\n      event.preventDefault();\n      const email = document.getElementById('email').value.trim();\n      const password = document.getElementById('password').value;\n\n      if (email === '' || password === '') {\n        message.textContent = 'Please enter your email and password';\n      } else if (email === 'student@qa.academy' && password === 'Learn@123') {\n        message.textContent = 'Signing in…';\n        setTimeout(showDashboard, 800);          // pretend the server is slow\n      } else {\n        message.textContent = 'Invalid email or password';\n      }\n    });\n\n    function showDashboard() {\n      document.title = 'QA Academy - Dashboard';\n      document.body.innerHTML =\n        '<h1>Dashboard</h1>' +\n        '<p>Welcome back, Student!</p>' +\n        '<h2>Your courses</h2>' +\n        '<ul><li>Playwright Basics</li><li>API Testing</li><li>Performance Testing</li></ul>' +\n        '<button>Log out</button>';\n      document.querySelector('button').addEventListener('click', () => {\n        document.title = 'QA Academy - Signed out';\n        document.body.innerHTML = '<h1>Signed out</h1><p>See you soon!</p>';\n      });\n    }\n  </script>\n</body>\n</html>\n`;\n\n// ---------------------------------------------------------------------------\n// Page 2: QA Academy course enrolment\n//   The \"Enrol now\" button is enabled only while \"I accept the terms\" is ticked\n//   Checks, in order: name required → email must contain @ → a course must be chosen\n//   Success: \"Thanks, <first name>! You are enrolled in <course>.\" and one seat fewer\n// ---------------------------------------------------------------------------\nexport const enrolPage = `\n<!DOCTYPE html>\n<html lang=\"en\">\n<head><meta charset=\"utf-8\"><title>QA Academy - Enrol</title></head>\n<body>\n  <h1>Enrol in a course</h1>\n  <form id=\"enrol-form\">\n    <label for=\"name\">Full name</label>\n    <input id=\"name\" placeholder=\"e.g. Asha Verma\">\n\n    <label for=\"enrol-email\">Email</label>\n    <input id=\"enrol-email\" type=\"text\" placeholder=\"you@example.com\">\n\n    <label for=\"course\">Course</label>\n    <select id=\"course\">\n      <option value=\"\">-- choose a course --</option>\n      <option value=\"pw\">Playwright Basics</option>\n      <option value=\"api\">API Testing</option>\n      <option value=\"perf\">Performance Testing</option>\n    </select>\n\n    <label><input id=\"terms\" type=\"checkbox\"> I accept the terms</label>\n\n    <button type=\"submit\" disabled>Enrol now</button>\n  </form>\n  <p data-testid=\"seats\">Seats left: 12</p>\n  <p id=\"result\" role=\"status\"></p>\n\n  <script>\n    let seats = 12;\n    const terms = document.getElementById('terms');\n    const submit = document.querySelector('button[type=submit]');\n    const result = document.getElementById('result');\n\n    // The Enrol button is enabled only while the terms box is ticked\n    terms.addEventListener('change', () => {\n      submit.disabled = !terms.checked;\n    });\n\n    document.getElementById('enrol-form').addEventListener('submit', (event) => {\n      event.preventDefault();\n      const name = document.getElementById('name').value.trim();\n      const email = document.getElementById('enrol-email').value.trim();\n      const course = document.getElementById('course');\n\n      if (name === '') { result.textContent = 'Name is required'; return; }\n      if (!email.includes('@')) { result.textContent = 'Enter a valid email'; return; }\n      if (course.value === '') { result.textContent = 'Please choose a course'; return; }\n\n      seats = seats - 1;\n      document.querySelector('[data-testid=seats]').textContent = 'Seats left: ' + seats;\n      const courseName = course.options[course.selectedIndex].text;\n      const firstName = name.split(' ')[0];\n      result.textContent = 'Thanks, ' + firstName + '! You are enrolled in ' + courseName + '.';\n    });\n  </script>\n</body>\n</html>\n`;",
                  "mode": "editor",
                  "file": "tests/day9/practice-pages.ts"
                },
                {
                  "type": "markdown",
                  "content": "The HTML and `<script>` inside are the website's own code. You don't need to understand them. Read the comments at the top of each page like a **requirements document**: every rule there is something a test can check."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d9-fundamentals-1",
              "title": "F1 · Anatomy of a Playwright test",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 1 you ran TC-101 without understanding every symbol. Now you can read the whole test:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';          // ① bring in test() and expect() from the package\nimport { signInPage } from './practice-pages';             // ② bring in our page's HTML (no .ts needed here)\n\ntest('student can sign in', async ({ page }) => {          // ③ register a test: a title + an async callback\n  await page.setContent(signInPage);                       // ④ Arrange: open the page\n\n  await page.getByLabel('Email').fill('student@qa.academy');     // ⑤ Act: locate an element, then act on it\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();   // ⑥ Assert\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| # | Part | Manual test case equivalent |\n|---|---|---|\n| ③ | `test('title', …)` | Test case ID and title |\n| ④ | Open the page | Precondition / Step 1 |\n| ⑤ | Locate + act | Test steps |\n| ⑥ | `expect(…)` | Expected result |\n\nMany testers structure each test as **Arrange → Act → Assert**: set things up, do the actions, check the outcome.\n\n### Rules the runner follows\n\n- **Test files** end in `.spec.ts` and live in the `testDir` folder (`tests`). Any other `.ts` file, like `practice-pages.ts`, is just a helper module.\n- `test(…)` only **registers** a test — like the mini runner you built on Day 8. `npx playwright test` then runs the registered tests.\n- Every test gets a **brand-new, isolated page**, in its own browser context (Day 1). Nothing — cookies, logins, typed text — leaks from one test to the next. So each test must set up everything it needs, and tests can run in any order.\n- A test **fails** when anything in it throws an error: a failed assertion, or an action that can't find its element in time. Otherwise it **passes**."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "In Playwright projects, imports of your own files usually leave out `.ts`: `'./practice-pages'`. Playwright finds the file for you (Day 8 · F7)."
                },
                {
                  "type": "quiz",
                  "id": "d9-f1-q1",
                  "quizType": "single",
                  "question": "Test A signs in. Test B, in the same file, runs after it and expects to see the dashboard without signing in. What happens?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Test B fails — every test gets a fresh, isolated page, so it starts signed out"
                    },
                    {
                      "id": "b",
                      "text": "Test B passes only if the tests run in one worker"
                    },
                    {
                      "id": "c",
                      "text": "Test B is skipped automatically"
                    },
                    {
                      "id": "d",
                      "text": "Test B passes, because test A already signed in"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Test isolation — every test starts from a clean browser context, so each test must do its own setup."
                }
              ]
            },
            {
              "id": "d9-fundamentals-2",
              "title": "F2 · Fixtures — what the runner hands you",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "The object that Playwright passes to your test callback contains **fixtures**: ready-made tools, set up before the test and cleaned up after it. You ask for the ones you need by name, with destructuring:\n\n| Fixture | What it is |\n|---|---|\n| `page` | A fresh, isolated browser tab for this test. The one you'll use in almost every test |\n| `context` | The isolated browser context that `page` belongs to (Day 1) — for cookies, or opening a second tab |\n| `browser` | The browser itself — shared across tests to save time |\n| `browserName` | The name of the browser running the test: `'chromium'`, `'firefox'` or `'webkit'` |\n| `request` | A tool for calling APIs directly, without a page |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('uses two fixtures', async ({ page, browserName }) => {\n  console.log(`Running on ${browserName}`);\n  await page.setContent('<h1>Hello</h1>');\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Playwright only prepares the fixtures you ask for. On Day 10 you'll see how to create your **own** fixtures."
                },
                {
                  "type": "quiz",
                  "id": "d9-f2-q1",
                  "quizType": "single",
                  "question": "Which fixture tells a test whether it's running in Firefox?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`request`"
                    },
                    {
                      "id": "b",
                      "text": "`page`"
                    },
                    {
                      "id": "c",
                      "text": "`context`"
                    },
                    {
                      "id": "d",
                      "text": "`browserName`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "browserName is 'chromium', 'firefox' or 'webkit'."
                }
              ]
            },
            {
              "id": "d9-fundamentals-3",
              "title": "F3 · Locators — finding elements",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **locator** describes **how to find** an element: *\"the button named Sign in\"*. Creating one doesn't touch the page. Playwright only searches when you **act** or **assert** on it, and it searches **again** every time — so a locator never goes stale when the page changes."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const signInButton = page.getByRole('button', { name: 'Sign in' });   // just a description — no await needed\nawait signInButton.click();                                             // now Playwright finds it and clicks",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Which locator to use\n\nThe Playwright docs recommend these, roughly in this order:\n\n| Locator | Finds… | Example on our pages |\n|---|---|---|\n| `getByRole(role, { name })` | Elements by role and accessible name. **The first choice** | `page.getByRole('button', { name: 'Sign in' })` |\n| `getByLabel(text)` | Form fields by their label | `page.getByLabel('Password')` |\n| `getByPlaceholder(text)` | Inputs by their hint text | `page.getByPlaceholder('you@example.com')` |\n| `getByText(text)` | Non-interactive text — paragraphs, messages | `page.getByText('Welcome back, Student!')` |\n| `getByAltText(text)` | Images by their alt text | `page.getByAltText('QA Academy logo')` |\n| `getByTitle(text)` | Elements by their `title` attribute | `page.getByTitle('Reset your password')` |\n| `getByTestId(id)` | Elements by `data-testid=\"…\"` — a hook developers add for tests | `page.getByTestId('seats')` |\n\nWhy prefer these? They describe the page the way a **user** sees it. A developer can rename a CSS class without breaking your test, but if the *Sign in* button's text changes, the user-facing behaviour really did change."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "`page.locator('#email')` (CSS) and `page.locator('//input[@id=\"email\"]')` (XPath) also work. The docs advise against them as a first choice: they depend on page structure, which changes often. Use them as a last resort.",
                  "title": "CSS and XPath"
                },
                {
                  "type": "markdown",
                  "content": "### Matching rules\n\n| By default… | To change it |\n|---|---|\n| Matching ignores upper/lower case: `name: 'sign in'` finds *Sign in* | `{ exact: true }` for exact, case-sensitive matching |\n| A **part** of the text is enough: `getByText('Welcome')` finds *Welcome back, Student!* | `{ exact: true }` |\n| Extra spaces and line breaks are ignored | — |\n\n### Strictness: one element at a time\n\nAn action like `click()` or `fill()` — and an assertion about one element, like `toBeVisible()` or `toHaveText('…')` — must know **exactly one** element. If a locator matches several, Playwright refuses and fails the test, instead of guessing:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "await page.getByRole('textbox').fill('Asha');   // the enrol page has TWO text boxes…\n// Error: locator.fill: Error: strict mode violation: getByRole('textbox') resolved to 2 elements:\n//     1) <input id=\"name\" placeholder=\"e.g. Asha Verma\"/> aka getByRole('textbox', { name: 'Full name' })\n//     2) <input type=\"text\" id=\"enrol-email\" placeholder=\"you@example.com\"/> aka getByRole('textbox', { name: 'Email' })",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The error even suggests better locators. Your options:\n\n| Fix | Example |\n|---|---|\n| Be more specific (best) | `page.getByRole('textbox', { name: 'Full name' })` |\n| Filter by text | `page.getByRole('option').filter({ hasText: 'API' })` |\n| Pick by position (last resort — breaks if the order changes) | `.first()`, `.last()`, `.nth(1)` (counting from 0) |\n\nAssertions that are about **many** elements, like `toHaveCount`, are happy with several matches."
                },
                {
                  "type": "quiz",
                  "id": "d9-f3-q1",
                  "quizType": "single",
                  "question": "Which locator does the Playwright documentation recommend as the first choice?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.locator('//form/div[3]/button')`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByTestId('submit')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.locator('#submit-btn')`"
                    },
                    {
                      "id": "d",
                      "text": "`page.getByRole('button', { name: 'Submit' })`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Role locators reflect how users and assistive technology see the page. Test ids are a good fallback; CSS and XPath are the last resort."
                },
                {
                  "type": "quiz",
                  "id": "d9-f3-q2",
                  "quizType": "single",
                  "question": "A page has three 'Add to cart' buttons. What happens with `await page.getByRole('button', { name: 'Add to cart' }).click();`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "The test fails with a strict mode violation"
                    },
                    {
                      "id": "b",
                      "text": "The first button is clicked"
                    },
                    {
                      "id": "c",
                      "text": "A random one is clicked"
                    },
                    {
                      "id": "d",
                      "text": "All three are clicked"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Actions need exactly one element. Narrow the locator down — for example to the product card you mean — or, as a last resort, use .first() or .nth()."
                },
                {
                  "type": "quiz",
                  "id": "d9-f3-q3",
                  "quizType": "single",
                  "question": "The page shows `<p>Order confirmed: #1045</p>`. Does `page.getByText('order confirmed')` find it?",
                  "options": [
                    {
                      "id": "a",
                      "text": "No — the text must match exactly"
                    },
                    {
                      "id": "b",
                      "text": "Only with `{ exact: true }`"
                    },
                    {
                      "id": "c",
                      "text": "No — the case is different"
                    },
                    {
                      "id": "d",
                      "text": "Yes — matching ignores case and a part of the text is enough"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "By default, text matching is case-insensitive and matches a substring. `{ exact: true }` would make it strict."
                }
              ]
            },
            {
              "id": "d9-fundamentals-4",
              "title": "F4 · Actions — doing what a user does",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Once you have a locator, you act on it. Every action is `await`ed, and before acting, Playwright **auto-waits** until the element is ready — found (exactly one), visible, stable, enabled and not covered by something else, depending on the action (Day 2). `fill`, for example, also waits until the field is editable.\n\n| Action | What it does | Example |\n|---|---|---|\n| `page.goto(url)` | Opens a URL | `await page.goto('https://example.com');` |\n| `page.setContent(html)` | Loads HTML directly (our practice pages) | `await page.setContent(signInPage);` |\n| `click()` | Clicks | `await page.getByRole('button', { name: 'Sign in' }).click();` |\n| `fill(text)` | Clears a field and enters the text (all at once) | `await page.getByLabel('Email').fill('a@b.com');` |\n| `press(key)` | Presses a key, like Enter or Tab | `await page.getByLabel('Password').press('Enter');` |\n| `check()` / `uncheck()` | Ticks / unticks a checkbox or radio button | `await page.getByLabel('Remember me').check();` |\n| `selectOption(…)` | Chooses from a `<select>` list, by visible text or value | `await page.getByLabel('Course').selectOption('API Testing');` |"
                },
                {
                  "type": "quiz",
                  "id": "d9-f4-q1",
                  "quizType": "single",
                  "question": "Which line chooses 'Playwright Basics' in the Course drop-down list?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`await page.getByLabel('Course').fill('Playwright Basics');`"
                    },
                    {
                      "id": "b",
                      "text": "`await page.getByText('Playwright Basics').fill();`"
                    },
                    {
                      "id": "c",
                      "text": "`await page.getByLabel('Course').check('Playwright Basics');`"
                    },
                    {
                      "id": "d",
                      "text": "`await page.getByLabel('Course').selectOption('Playwright Basics');`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "selectOption chooses from a `<select>` list, by the option's visible text or its value."
                }
              ]
            },
            {
              "id": "d9-fundamentals-5",
              "title": "F5 · Assertions — checking the expected result",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "`expect(…)` checks that something is as expected. There are two kinds, and the difference matters.\n\n### Web-first assertions: `await expect(locator or page)…`\n\nThese check the **page**, and they **keep retrying** until the condition is true, or until the timeout (5 seconds by default) runs out. That's why they're reliable on pages that change (Day 2). They must be `await`ed.\n\n| Assertion | Passes when… |\n|---|---|\n| `toBeVisible()` / `toBeHidden()` | The element is shown / not shown **or not on the page at all** |\n| `toHaveText(text)` | The element's **whole** text equals this. Case matters; extra spaces are ignored. Unlike locators, a part is **not** enough — use `toContainText` or a regular expression like `/Welcome/` for that |\n| `toContainText(text)` | The element's text **contains** this part |\n| `toHaveValue(value)` | An input or select has this value |\n| `toBeEmpty()` | An input has no value |\n| `toBeChecked()` | A checkbox is ticked |\n| `toBeEnabled()` / `toBeDisabled()` | The element can / can't be used |\n| `toBeEditable()` | A field can be typed into |\n| `toHaveCount(n)` | The locator matches exactly `n` elements |\n| `toHaveAttribute(name, value)` | An attribute has this value |\n| `toHaveTitle(title)` | The **page**'s title matches — `expect(page)` |\n| `toHaveURL(url)` | The **page**'s URL matches — `expect(page)` |\n\nPut `.not` in front to check the opposite: `await expect(rememberMe).not.toBeChecked();`\n\n`toHaveText` also accepts an array for a list of elements: `await expect(page.getByRole('listitem')).toHaveText(['Playwright Basics', 'API Testing', 'Performance Testing']);` checks every item, in order.\n\n### Generic assertions: `expect(value)…`\n\nThese check an ordinary **value** you already have — a number, text, an array. They check **once**, immediately, and don't need `await`:\n\n| Assertion | Passes when… |\n|---|---|\n| `toBe(value)` | The same value (like `===`) |\n| `toEqual(value)` | Equal in content — for comparing objects and arrays |\n| `toContain(item)` | A text contains a part, or an array contains an item |\n| `toBeGreaterThan(n)` / `toBeLessThan(n)` | Number comparisons |\n| `toBeTruthy()` | The value is truthy (Day 7) |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const total = 499 + 1299;\nexpect(total).toBe(1798);                          // a plain value: no await, no retrying\nexpect(['chromium', 'firefox']).toContain('firefox');",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "`const text = await locator.textContent(); expect(text).toBe('Saved');` reads the text **once**. If the page updates a moment later, the test fails. Use `await expect(locator).toHaveText('Saved');` — it keeps checking. Rule of thumb: **if it's on the page, use a web-first assertion.**",
                  "title": "Don't read the page into a variable, then check it"
                },
                {
                  "type": "quiz",
                  "id": "d9-f5-q3",
                  "quizType": "single",
                  "question": "The status shows `Thanks, Asha! You are enrolled in API Testing.` Which assertion passes?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`await expect(page.getByRole('status')).toHaveText('Thanks, Asha!');`"
                    },
                    {
                      "id": "b",
                      "text": "`await expect(page.getByRole('status')).toContainText('thanks, asha');`"
                    },
                    {
                      "id": "c",
                      "text": "`await expect(page.getByRole('status')).toContainText('Thanks, Asha!');`"
                    },
                    {
                      "id": "d",
                      "text": "`await expect(page.getByRole('status')).toHaveText('thanks, asha! you are enrolled in api testing.');`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "toHaveText with text needs the whole text, with matching case. toContainText accepts a part — but case still matters. (Locators are the lenient ones: getByText('thanks, asha') would find it.)"
                },
                {
                  "type": "quiz",
                  "id": "d9-f5-q1",
                  "quizType": "single",
                  "question": "A success message appears 2 seconds after clicking Save. Which assertion is reliable?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`expect(page.getByRole('status')).toBe('Saved');`"
                    },
                    {
                      "id": "b",
                      "text": "`expect(await page.getByRole('status').textContent()).toBe('Saved');`"
                    },
                    {
                      "id": "c",
                      "text": "`await page.waitForTimeout(1000); expect(await page.getByRole('status').textContent()).toBe('Saved');`"
                    },
                    {
                      "id": "d",
                      "text": "`await expect(page.getByRole('status')).toHaveText('Saved');`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "The web-first assertion retries for up to 5 seconds. Reading textContent once checks too early, and a 1-second fixed wait is still too short."
                },
                {
                  "type": "quiz",
                  "id": "d9-f5-q2",
                  "quizType": "multiple",
                  "question": "Which assertions keep retrying until they pass or time out? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`await expect(page).toHaveTitle('Dashboard')`"
                    },
                    {
                      "id": "b",
                      "text": "`expect(total).toBe(1798)`"
                    },
                    {
                      "id": "c",
                      "text": "`expect(['a', 'b']).toContain('a')`"
                    },
                    {
                      "id": "d",
                      "text": "`await expect(locator).toBeChecked()`"
                    }
                  ],
                  "correct": [
                    "a",
                    "d"
                  ],
                  "explanation": "Assertions on a page or locator are web-first and retry. Assertions on plain values check once."
                }
              ]
            },
            {
              "id": "d9-fundamentals-6",
              "title": "F6 · Annotations — controlling which tests run",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Real suites contain tests that aren't ready, don't apply to every browser, or document a known bug. **Annotations** mark them:\n\n| Annotation | Effect | When to use it |\n|---|---|---|\n| `test.skip('title', …)` | The test doesn't run; it's reported as skipped | The feature isn't built yet |\n| `test.skip(condition, 'reason')` inside a test | Skips only when the condition is true | \"Not supported in WebKit yet\" |\n| `test.fixme('title', …)` | Doesn't run; marked \"to fix\" | The test is broken and needs work |\n| `test.fail()` inside a test | Runs it and **expects it to fail**. If it unexpectedly passes, Playwright reports it as **failed** | Documenting a known bug until it's fixed |\n| `test.slow()` inside a test | Gives it three times the normal time limit | A genuinely long journey |\n| `test.only('title', …)` | Runs **only** this test (and any other `only`s) | Focusing while you work on one test |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('date picker works', async ({ page, browserName }) => {\n  test.skip(browserName === 'webkit', 'Date picker not supported on WebKit yet');\n  // …steps run only in Chromium and Firefox\n});",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "A `test.only` accidentally left in a file means the whole suite silently runs just that one test. The generated config's `forbidOnly: !!process.env.CI` setting makes CI servers refuse to run when a `test.only` is present (Day 10).",
                  "title": "`test.only` is for your own computer"
                },
                {
                  "type": "quiz",
                  "id": "d9-f6-q1",
                  "quizType": "single",
                  "question": "Checkout has a known bug (BUG-88). You want a test that documents it, runs every time, and alerts you once the bug is fixed. Which annotation?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`test.fail`"
                    },
                    {
                      "id": "b",
                      "text": "`test.fixme`"
                    },
                    {
                      "id": "c",
                      "text": "`test.only`"
                    },
                    {
                      "id": "d",
                      "text": "`test.skip`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "test.fail runs the test and expects it to fail. When BUG-88 is fixed, the test passes unexpectedly and Playwright flags it. skip and fixme don't run the test at all."
                }
              ]
            },
            {
              "id": "d9-fundamentals-7",
              "title": "F7 · Running and debugging",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "You met the run commands on Day 3. The ones you'll use most today:\n\n| Goal | Command |\n|---|---|\n| Run one file in Chromium | `npx playwright test tests/day9/signin.spec.ts --project=chromium` |\n| Watch the browser | add `--headed` |\n| Only tests whose title contains a phrase | add `-g \"TC-203\"` |\n| Open the HTML report | `npx playwright show-report` |\n\nWhen a test fails, work through these steps in order:\n\n| Step | How | What you get |\n|---|---|---|\n| 1. **Read the error** | In the terminal | What was expected, what was received, which line (Day 3 · I7) |\n| 2. **Watch it** | `--headed` | See the page as the test runs |\n| 3. **Travel back in time** | `--trace on`, then open the trace from the HTML report | A recording of every action, with page snapshots, network and console (Day 2) |\n\nOn your own computer, two more tools are worth trying: `--debug` opens the **Playwright Inspector**, where you run one line at a time and see which element each locator finds; `--ui` opens **UI Mode**, with a timeline, watch mode and a locator picker."
                },
                {
                  "type": "quiz",
                  "id": "d9-f7-q1",
                  "quizType": "single",
                  "question": "A test failed on the CI server last night. You can't watch it run there. What's the most useful thing to look at?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Add page.pause() and re-run on CI"
                    },
                    {
                      "id": "b",
                      "text": "The trace recorded for the failed run, opened from the HTML report (if your config records traces — Day 10)"
                    },
                    {
                      "id": "c",
                      "text": "The test's title"
                    },
                    {
                      "id": "d",
                      "text": "Run it headed on the CI server"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "A trace records every action with snapshots, so you can inspect exactly what happened after the fact. Headed runs and pauses need someone watching."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [
            {
              "type": "markdown",
              "content": "All test files today go in `tests/day9`. Run commands from the `pw-course` folder."
            }
          ],
          "lessons": [
            {
              "id": "d9-implementation-1",
              "title": "I1 · Your first test from scratch",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Create a test file and type the test yourself — don't paste it. Typing it builds the muscle memory."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\ntest('student can sign in', async ({ page }) => {\n  // Arrange: open the page\n  await page.setContent(signInPage);\n\n  // Act: do what a user would do\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  // Assert: check the expected result\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n  await expect(page).toHaveTitle('QA Academy - Dashboard');\n});",
                  "mode": "editor",
                  "file": "tests/day9/first-test.spec.ts",
                  "run": "npx playwright test tests/day9/first-test.spec.ts --project=chromium --headed",
                  "expectedOutput": "Running 1 test using 1 worker\n\n  ✓  1 [chromium] › tests/day9/first-test.spec.ts:4:5 › student can sign in (1.1s)\n\n  1 passed (1.9s)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9/first-test.spec.ts --project=chromium --headed"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 1 test using 1 worker\n\n  ✓  1 [chromium] › tests/day9/first-test.spec.ts:4:5 › student can sign in (1.1s)\n\n  1 passed (1.9s)"
                },
                {
                  "type": "markdown",
                  "content": "The dashboard appears 0.8 seconds after the click, yet the test has no waits. `toBeVisible()` simply kept checking until the heading appeared.\n\n**Try it:** change `'Learn@123'` to `'learn@123'` and run again with `--headed`. Watch the window during the 5-second retry — what message does the page show? Then read the error: which assertion failed? Undo the change afterwards."
                }
              ]
            },
            {
              "id": "d9-implementation-2",
              "title": "I2 · A sign-in suite from test cases",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Five manual test cases, five tests. Each one starts from a fresh page:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\n// TC-201 · valid credentials open the dashboard\ntest('TC-201 valid sign-in shows the dashboard', async ({ page }) => {\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  await expect(page.getByRole('alert')).toHaveText('Signing in…');             // shown straight away\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible(); // appears 0.8 s later\n  await expect(page.getByText('Welcome back, Student!')).toBeVisible();\n  await expect(page.getByRole('listitem')).toHaveCount(3);                      // three courses listed\n});\n\n// TC-202 · empty fields\ntest('TC-202 empty fields show a message', async ({ page }) => {\n  await page.setContent(signInPage);\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  await expect(page.getByRole('alert')).toHaveText('Please enter your email and password');\n  await expect(page).toHaveTitle('QA Academy - Sign in');                       // still on the sign-in page\n});\n\n// TC-203 · wrong password\ntest('TC-203 wrong password is rejected', async ({ page }) => {\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('wrong-password');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  await expect(page.getByRole('alert')).toHaveText('Invalid email or password');\n  await expect(page.getByLabel('Email')).toHaveValue('student@qa.academy');   // the email is kept\n});\n\n// TC-204 · the Remember me checkbox\ntest('TC-204 remember me can be ticked and unticked', async ({ page }) => {\n  await page.setContent(signInPage);\n  const rememberMe = page.getByRole('checkbox', { name: 'Remember me' });\n\n  await expect(rememberMe).not.toBeChecked();\n  await rememberMe.check();\n  await expect(rememberMe).toBeChecked();\n  await rememberMe.uncheck();\n  await expect(rememberMe).not.toBeChecked();\n});\n\n// TC-205 · sign out\ntest('TC-205 student can sign out', async ({ page }) => {\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByLabel('Password').press('Enter');                            // Enter submits the form too\n\n  await page.getByRole('button', { name: 'Log out' }).click();                 // auto-waits for the dashboard\n  await expect(page.getByRole('heading', { name: 'Signed out' })).toBeVisible();\n});",
                  "mode": "editor",
                  "file": "tests/day9/signin.spec.ts",
                  "run": "npx playwright test tests/day9/signin.spec.ts --project=chromium",
                  "expectedOutput": "Running 5 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day9/signin.spec.ts:5:5 › TC-201 valid sign-in shows the dashboard (1.1s)\n  ✓  2 [chromium] › tests/day9/signin.spec.ts:18:5 › TC-202 empty fields show a message (179ms)\n  ✓  3 [chromium] › tests/day9/signin.spec.ts:27:5 › TC-203 wrong password is rejected (178ms)\n  ✓  4 [chromium] › tests/day9/signin.spec.ts:38:5 › TC-204 remember me can be ticked and unticked (212ms)\n  ✓  5 [chromium] › tests/day9/signin.spec.ts:50:5 › TC-205 student can sign out (1.5s)\n\n  5 passed (3.9s)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 5 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day9/signin.spec.ts:5:5 › TC-201 valid sign-in shows the dashboard (1.1s)\n  ✓  2 [chromium] › tests/day9/signin.spec.ts:18:5 › TC-202 empty fields show a message (179ms)\n  ✓  3 [chromium] › tests/day9/signin.spec.ts:27:5 › TC-203 wrong password is rejected (178ms)\n  ✓  4 [chromium] › tests/day9/signin.spec.ts:38:5 › TC-204 remember me can be ticked and unticked (212ms)\n  ✓  5 [chromium] › tests/day9/signin.spec.ts:50:5 › TC-205 student can sign out (1.5s)\n\n  5 passed (3.9s)"
                },
                {
                  "type": "markdown",
                  "content": "The number of workers, the order of the lines and the times will differ on your machine: the tests run in parallel.\n\nFour things to notice:\n\n- **TC-201** checks the in-between state *\"Signing in…\"* and then the dashboard. Web-first assertions handle both without a single wait.\n- **TC-204** stores a locator in a `const` and reuses it. Remember: a locator is only a description, so it's searched afresh each time.\n- **TC-205** clicks *Log out* straight after pressing Enter. `click()` auto-waits until that button exists — about 0.8 seconds later.\n- Every test starts with `setContent`: tests are isolated, so none of them can rely on another having signed in.\n\n**Try it:** run only TC-203 with `-g \"TC-203\"`."
                }
              ]
            },
            {
              "id": "d9-implementation-3",
              "title": "I3 · Locators in action",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "One test per page, exercising every recommended locator and the matching rules from F3:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { signInPage, enrolPage } from './practice-pages';\n\ntest('recommended locators on the sign-in page', async ({ page }) => {\n  await page.setContent(signInPage);\n\n  // By role (and accessible name): the first choice\n  await expect(page.getByRole('heading', { name: 'Sign in to QA Academy' })).toBeVisible();\n  await expect(page.getByRole('link', { name: 'Forgot password?' })).toBeVisible();\n\n  // By label: form fields\n  await expect(page.getByLabel('Password')).toBeEmpty();\n\n  // By placeholder: fields with hint text\n  await expect(page.getByPlaceholder('you@example.com')).toBeEditable();\n\n  // By text: non-interactive text\n  await expect(page.getByText('Sign in to QA')).toBeVisible();                 // part of the text is enough\n\n  // By alt text (images) and by title attribute\n  await expect(page.getByAltText('QA Academy logo')).toBeVisible();\n  await expect(page.getByTitle('Reset your password')).toHaveText('Forgot password?');\n});\n\ntest('matching rules, strictness and lists on the enrol page', async ({ page }) => {\n  await page.setContent(enrolPage);\n\n  // By test id: data-testid=\"seats\"\n  await expect(page.getByTestId('seats')).toHaveText('Seats left: 12');\n\n  // Names match case-insensitively and by substring… unless exact: true\n  await expect(page.getByRole('button', { name: 'enrol' })).toBeVisible();\n  await expect(page.getByRole('button', { name: 'enrol', exact: true })).toHaveCount(0);\n\n  // Two text boxes match: count them, or narrow down to one\n  const textboxes = page.getByRole('textbox');\n  await expect(textboxes).toHaveCount(2);\n  await expect(textboxes.first()).toHaveAttribute('id', 'name');\n  await expect(textboxes.nth(1)).toHaveAttribute('placeholder', 'you@example.com');\n\n  // Options inside the course list; filter by text\n  const options = page.getByRole('option');\n  await expect(options).toHaveCount(4);\n  await expect(options.filter({ hasText: 'API' })).toHaveText('API Testing');\n});",
                  "mode": "editor",
                  "file": "tests/day9/locators.spec.ts",
                  "run": "npx playwright test tests/day9/locators.spec.ts --project=chromium"
                },
                {
                  "type": "markdown",
                  "content": "`toHaveCount(0)` is how you assert that something does **not** exist.\n\n**Try it:** add `await page.getByRole('textbox').fill('Asha');` at the end of the second test and run it. Read the strict mode violation, then replace the locator with one of the two suggestions from the error message."
                }
              ]
            },
            {
              "id": "d9-implementation-4",
              "title": "I4 · Actions and assertions on the enrol page",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { enrolPage } from './practice-pages';\n\ntest('enrol button is disabled until the terms are accepted', async ({ page }) => {\n  await page.setContent(enrolPage);\n  const terms = page.getByRole('checkbox', { name: 'I accept the terms' });\n  const enrolButton = page.getByRole('button', { name: 'Enrol now' });\n\n  await expect(enrolButton).toBeDisabled();\n  await terms.check();\n  await expect(enrolButton).toBeEnabled();\n  await terms.uncheck();\n  await expect(enrolButton).toBeDisabled();\n});\n\ntest('student can enrol in a course', async ({ page }) => {\n  await page.setContent(enrolPage);\n\n  await page.getByLabel('Full name').fill('Asha Verma');\n  await page.getByLabel('Email').fill('asha@example.com');\n  await page.getByLabel('Course').selectOption('API Testing');                 // choose by visible text\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n\n  await expect(page.getByRole('status')).toHaveText('Thanks, Asha! You are enrolled in API Testing.');\n  await expect(page.getByTestId('seats')).toHaveText('Seats left: 11');\n  await expect(page.getByLabel('Course')).toHaveValue('api');                   // the option's value\n});\n\ntest('an email without @ is rejected', async ({ page }) => {\n  await page.setContent(enrolPage);\n\n  await page.getByLabel('Full name').fill('Ravi Kumar');\n  await page.getByLabel('Email').fill('ravi.example.com');\n  await page.getByLabel('Course').selectOption('Playwright Basics');\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n\n  await expect(page.getByRole('status')).toContainText('valid email');\n  await expect(page.getByTestId('seats')).toHaveText('Seats left: 12');        // no seat was taken\n});",
                  "mode": "editor",
                  "file": "tests/day9/enrol.spec.ts",
                  "run": "npx playwright test tests/day9/enrol.spec.ts --project=chromium --headed"
                },
                {
                  "type": "markdown",
                  "content": "`selectOption('API Testing')` chose the option by its visible text, but `toHaveValue` checks the option's **value**, `'api'` — the hidden code from the page's HTML (`<option value=\"api\">`)."
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "The third test checks the **negative** outcome too: the seat count must **not** change. Checking that nothing else happened is as important as checking the message."
                }
              ]
            },
            {
              "id": "d9-implementation-5",
              "title": "I5 · Annotations",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { signInPage, enrolPage } from './practice-pages';\n\ntest('runs normally', async ({ page }) => {\n  await page.setContent(signInPage);\n  await expect(page.getByRole('heading', { name: 'Sign in to QA Academy' })).toBeVisible();\n});\n\ntest.skip('social sign-in with Google', async ({ page }) => {\n  // Not built yet: this test is skipped and never runs\n});\n\ntest('the logo is shown', async ({ page, browserName }) => {\n  test.skip(browserName === 'webkit', 'Logo check not supported on WebKit yet');\n  await page.setContent(signInPage);\n  await expect(page.getByAltText('QA Academy logo')).toBeVisible();\n});\n\ntest.fixme('remember me keeps the student signed in', async ({ page }) => {\n  // Known to be broken: don't run it until it's fixed\n});\n\ntest('BUG-42: an email without a dot is accepted', async ({ page }) => {\n  test.fail();       // we EXPECT this test to fail until BUG-42 is fixed\n  await page.setContent(enrolPage);\n  await page.getByLabel('Full name').fill('Meera Iyer');\n  await page.getByLabel('Email').fill('meera@example');\n  await page.getByLabel('Course').selectOption('API Testing');\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n  await expect(page.getByRole('status')).toHaveText('Enter a valid email', { timeout: 1000 });\n});\n\ntest('full sign-in journey', async ({ page }) => {\n  test.slow();       // give this test 3× the normal time\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n});",
                  "mode": "editor",
                  "file": "tests/day9/annotations.spec.ts",
                  "run": "npx playwright test tests/day9/annotations.spec.ts --project=chromium",
                  "expectedOutput": "Running 6 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day9/annotations.spec.ts:4:5 › runs normally (188ms)\n  -  2 [chromium] › tests/day9/annotations.spec.ts:9:6 › social sign-in with Google\n  ✓  3 [chromium] › tests/day9/annotations.spec.ts:13:5 › the logo is shown (126ms)\n  -  4 [chromium] › tests/day9/annotations.spec.ts:19:6 › remember me keeps the student signed in\n  ✘  5 [chromium] › tests/day9/annotations.spec.ts:23:5 › BUG-42: an email without a dot is accepted (1.3s)\n  ✓  6 [chromium] › tests/day9/annotations.spec.ts:34:5 › full sign-in journey (1.5s)\n\n  2 skipped\n  4 passed (3.8s)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 6 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day9/annotations.spec.ts:4:5 › runs normally (188ms)\n  -  2 [chromium] › tests/day9/annotations.spec.ts:9:6 › social sign-in with Google\n  ✓  3 [chromium] › tests/day9/annotations.spec.ts:13:5 › the logo is shown (126ms)\n  -  4 [chromium] › tests/day9/annotations.spec.ts:19:6 › remember me keeps the student signed in\n  ✘  5 [chromium] › tests/day9/annotations.spec.ts:23:5 › BUG-42: an email without a dot is accepted (1.3s)\n  ✓  6 [chromium] › tests/day9/annotations.spec.ts:34:5 › full sign-in journey (1.5s)\n\n  2 skipped\n  4 passed (3.8s)"
                },
                {
                  "type": "markdown",
                  "content": "Look closely at test 5: it shows ✘ because it failed — but it's counted among the **4 passed**, because `test.fail()` said it should fail. The page accepted `meera@example`, which is BUG-42. Once the developers fix the bug, this test will pass unexpectedly, be reported as a failure, and remind you to remove `test.fail()`.\n\n`{ timeout: 1000 }` shortens this one assertion's retry time from 5 seconds to 1 second, so the expected failure doesn't slow the run down.\n\n**Try it:** run the file in all three browsers (drop `--project=chromium`). In WebKit, \"the logo is shown\" is skipped."
                }
              ]
            },
            {
              "id": "d9-implementation-6",
              "title": "I6 · Debug a failing test",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This test fails. Run it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\ntest('wrong password shows an error', async ({ page }) => {\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('not-my-password');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  await expect(page.getByRole('alert')).toHaveText('Incorrect email or password');\n});",
                  "mode": "editor",
                  "file": "tests/day9/broken.spec.ts",
                  "run": "npx playwright test tests/day9/broken.spec.ts --project=chromium",
                  "expectError": true,
                  "expectedOutput": "Running 1 test using 1 worker\n\n  ✘  1 [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error (5.3s)\n\n  1) [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error ────────────\n\n    Error: expect(locator).toHaveText(expected) failed\n\n    Locator:  getByRole('alert')\n    Expected: \"Incorrect email or password\"\n    Received: \"Invalid email or password\"\n    Timeout:  5000ms\n\n    Call log:\n      - Expect \"toHaveText\" getByRole('alert') with timeout 5000ms\n      - waiting for getByRole('alert')\n        14 × locator resolved to <p id=\"message\" role=\"alert\">Invalid email or password</p>\n           - unexpected value \"Invalid email or password\"\n\n\n       8 |   await page.getByRole('button', { name: 'Sign in' }).click();\n       9 |\n    > 10 |   await expect(page.getByRole('alert')).toHaveText('Incorrect email or password');\n         |                                         ^\n      11 | });\n      12 |\n        at /home/you/pw-course/tests/day9/broken.spec.ts:10:41\n\n    Error Context: test-results/day9-broken-wrong-password-shows-an-error-chromium/error-context.md\n\n  1 failed\n    [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error ─────────────",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 1 test using 1 worker\n\n  ✘  1 [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error (5.3s)\n\n  1) [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error ────────────\n\n    Error: expect(locator).toHaveText(expected) failed\n\n    Locator:  getByRole('alert')\n    Expected: \"Incorrect email or password\"\n    Received: \"Invalid email or password\"\n    Timeout:  5000ms\n\n    Call log:\n      - Expect \"toHaveText\" getByRole('alert') with timeout 5000ms\n      - waiting for getByRole('alert')\n        14 × locator resolved to <p id=\"message\" role=\"alert\">Invalid email or password</p>\n           - unexpected value \"Invalid email or password\"\n\n\n       8 |   await page.getByRole('button', { name: 'Sign in' }).click();\n       9 |\n    > 10 |   await expect(page.getByRole('alert')).toHaveText('Incorrect email or password');\n         |                                         ^\n      11 | });\n      12 |\n        at /home/you/pw-course/tests/day9/broken.spec.ts:10:41\n\n    Error Context: test-results/day9-broken-wrong-password-shows-an-error-chromium/error-context.md\n\n  1 failed\n    [chromium] › tests/day9/broken.spec.ts:4:5 › wrong password shows an error ─────────────"
                },
                {
                  "type": "markdown",
                  "content": "Work through the debugging steps from F7:\n\n1. **Read the error.** The locator found the alert (`locator resolved to <p … role=\"alert\">`), so the locator is fine. Its text is `Invalid…`, not `Incorrect…`. The assertion retried 14 times over 5 seconds, and the text never changed.\n   The **Error Context** file at the end holds a text snapshot of the page at the moment of failure — handy when the error alone doesn't tell you what the page showed.\n2. **Decide: app bug or test bug?** The requirements at the top of `practice-pages.ts` say *\"Wrong login: Invalid email or password\"*. The **test** is wrong.\n3. **Record a trace**, to practise the tool you'll use for failures you can't reproduce:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9/broken.spec.ts --project=chromium --trace on",
                    "npx playwright show-report"
                  ]
                },
                {
                  "type": "markdown",
                  "content": "In the report, open the failed test and click the trace. Click each action in the timeline and look at the page snapshot before and after it. At the `toHaveText` step, the snapshot shows the real message.\n\n4. On your own computer, also try `--debug`: the Inspector opens paused at the first line. Step through with **Step over** and watch each locator light up in the browser.\n\n5. **Fix the test:** change the expected text to `'Invalid email or password'` and run again — green."
                }
              ]
            },
            {
              "id": "d9-implementation-7",
              "title": "I7 · Read the HTML report",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Your config writes an HTML report after every run. It doesn't open by itself (Day 3 · I5), so open it:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright show-report"
                  ]
                },
                {
                  "type": "markdown",
                  "content": "| In the report | Use it to… |\n|---|---|\n| Filters: **Passed**, **Failed**, **Flaky**, **Skipped** | Jump straight to what needs attention |\n| Search box | Find tests by title — e.g. `TC-20` |\n| A test's page: steps | See every action and assertion with its duration; failed steps are red |\n| Errors | The same message as the terminal, with the code line |\n| Attachments: screenshots, videos, traces | Evidence — when the config records them |\n\nPress **Ctrl+C** in the terminal to stop the report server when you're done."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d9-practice-1",
              "title": "Quiz · Day 9 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d9-pr-q1",
                  "quizType": "single",
                  "question": "Why does `practice-pages.ts` not run as a test file?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Because it has no test() calls"
                    },
                    {
                      "id": "b",
                      "text": "Because its name doesn't end in .spec.ts"
                    },
                    {
                      "id": "c",
                      "text": "Because it's in the tests folder"
                    },
                    {
                      "id": "d",
                      "text": "Because it exports strings"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Playwright looks for test files by name — by default, names ending in .spec.ts or .test.ts."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q2",
                  "quizType": "single",
                  "question": "Which is the best locator for `<label for=\"phone\">Mobile number</label><input id=\"phone\">`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.getByText('Mobile number')`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByLabel('Mobile number')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.locator('input').nth(3)`"
                    },
                    {
                      "id": "d",
                      "text": "`page.locator('#phone')`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Form fields are best found by their label. getByText would find the label itself, not the input."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q3",
                  "quizType": "single",
                  "question": "What does `const button = page.getByRole('button', { name: 'Pay' });` do on its own?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Clicks the Pay button"
                    },
                    {
                      "id": "b",
                      "text": "Searches the page and stores the element"
                    },
                    {
                      "id": "c",
                      "text": "Fails if the button isn't on the page yet"
                    },
                    {
                      "id": "d",
                      "text": "Only creates a description of how to find the button — nothing is searched yet"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Locators are lazy: Playwright searches when you act or assert, and searches again each time."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q4",
                  "quizType": "single",
                  "question": "How do you assert that NO error message is shown?",
                  "options": [
                    {
                      "id": "a",
                      "text": "You can't — Playwright only checks things that exist"
                    },
                    {
                      "id": "b",
                      "text": "`await expect(page.getByRole('alert')).toBeHidden();`"
                    },
                    {
                      "id": "c",
                      "text": "`expect(page.getByRole('alert')).toBe(null);`"
                    },
                    {
                      "id": "d",
                      "text": "`expect(await page.getByRole('alert').isVisible()).toBe(false);`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "toBeHidden passes when the element is missing or invisible, and it retries. The isVisible version checks only once. (toHaveCount(0) also works when the element disappears completely — but our sign-in page always contains an empty alert paragraph, so there only toBeHidden works.)"
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q5",
                  "quizType": "single",
                  "question": "Which assertion checks that a drop-down currently has the option with value 'pw' selected?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`await expect(page.getByLabel('Course')).toHaveValue('pw');`"
                    },
                    {
                      "id": "b",
                      "text": "`await expect(page.getByLabel('Course')).toBeChecked();`"
                    },
                    {
                      "id": "c",
                      "text": "`await expect(page.getByLabel('Course')).toHaveText('pw');`"
                    },
                    {
                      "id": "d",
                      "text": "`expect(page.getByLabel('Course')).toBe('pw');`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "toHaveValue checks an input's or select's current value."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q6",
                  "quizType": "single",
                  "question": "A test is marked `test.fixme(…)`. What does Playwright do with it?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Runs it three times"
                    },
                    {
                      "id": "b",
                      "text": "Runs it and expects it to fail"
                    },
                    {
                      "id": "c",
                      "text": "Doesn't run it, and reports it as skipped"
                    },
                    {
                      "id": "d",
                      "text": "Runs it with a longer timeout"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "fixme (like skip) doesn't run the test. test.fail is the one that runs and expects a failure."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q7",
                  "quizType": "multiple",
                  "question": "Before `click()`, what does Playwright automatically wait for? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "The network has been idle for 2 seconds"
                    },
                    {
                      "id": "b",
                      "text": "The element is not covered by another element"
                    },
                    {
                      "id": "c",
                      "text": "The element is visible"
                    },
                    {
                      "id": "d",
                      "text": "The element is enabled"
                    }
                  ],
                  "correct": [
                    "b",
                    "c",
                    "d"
                  ],
                  "explanation": "Before a click, Playwright waits for the element to be visible, stable, enabled and not covered (receiving events). Network idleness is not part of the checks."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q8",
                  "quizType": "single",
                  "question": "An assertion failed with `Expected: \"Saved\"` and `Received: \"Saving…\"` after 5000 ms. What's the most likely explanation?",
                  "options": [
                    {
                      "id": "a",
                      "text": "The test forgot to await the page.goto"
                    },
                    {
                      "id": "b",
                      "text": "Playwright doesn't support the … character"
                    },
                    {
                      "id": "c",
                      "text": "The locator found the wrong element"
                    },
                    {
                      "id": "d",
                      "text": "The element was found, but its text never became 'Saved' within 5 seconds"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "The locator found the element (it received text), but the text never became 'Saved' while the assertion retried. Investigate the app or the expected text — the trace will show which."
                }
              ]
            },
            {
              "id": "d9-practice-2",
              "title": "Spot the bug",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "written",
                  "id": "d9-pr-bug1",
                  "title": "Four bugs in one test",
                  "level": "medium",
                  "prompt": "This test was written in a hurry. Find **four** bugs, and say what each one would cause:\n```ts\nimport { test, expect } from '@playwright/test';\nimport { enrolPage } from './practice-pages';\n\ntest.only('student can enrol', async (page) => {\n  await page.setContent(enrolPage);\n  await page.getByLabel('Full name').fill('Asha Verma');\n  await page.getByLabel('Email').fill('asha@example.com');\n  await page.getByLabel('Course').fill('API Testing');\n  await page.getByLabel('I accept the terms').check();\n  page.getByRole('button', { name: 'Enrol now' }).click();\n  await expect(page.getByRole('status')).toHaveText('Thanks, Asha! You are enrolled in API Testing.');\n});\n```",
                  "modelAnswer": "1. **`test.only`** — only this test would run in the whole suite. Remove `.only`.\n2. **`async (page) =>`** — without braces, `page` would be the whole fixtures object, not the page. Playwright refuses to even load the file: *First argument must use the object destructuring pattern*. (VS Code also underlines `setContent`.) Write `async ({ page }) =>`.\n3. **`.fill('API Testing')` on a `<select>`** — you can't type into a drop-down list; the action fails with an error saying the element is not an input. Use `.selectOption('API Testing')`.\n4. **Missing `await` before the click** — the test moves on without waiting for the click. Here the web-first assertion happens to retry until the message appears, so the test may even pass — which is exactly why missing awaits are dangerous. If the click were the **last** line, the test could end while the click was still in progress, and the click would be cut off without anyone noticing. Add `await`.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d9-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex1",
                  "title": "The sign-in page, at first sight",
                  "level": "easy",
                  "prompt": "Create `tests/day9/signin-page.spec.ts` with **one** test, `TC-206 sign-in page shows everything a new visitor needs`. Without typing or clicking anything, assert that:\n\n1. The page title is `QA Academy - Sign in`\n2. The heading `Sign in to QA Academy` is visible\n3. The logo (alt text `QA Academy logo`) is visible\n4. The email field (placeholder `you@example.com`) is empty\n5. The *Remember me* checkbox is **not** ticked\n6. The *Forgot password?* link is visible",
                  "file": "tests/day9/signin-page.spec.ts",
                  "run": "npx playwright test tests/day9/signin-page.spec.ts --project=chromium",
                  "hints": [
                    "The page title: `await expect(page).toHaveTitle('…')`.",
                    "Use a different locator type for 3, 4 and 5: getByAltText, getByPlaceholder, getByRole('checkbox', …).",
                    "Not ticked: `.not.toBeChecked()`."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\ntest('TC-206 sign-in page shows everything a new visitor needs', async ({ page }) => {\n  await page.setContent(signInPage);\n\n  await expect(page).toHaveTitle('QA Academy - Sign in');\n  await expect(page.getByRole('heading', { name: 'Sign in to QA Academy' })).toBeVisible();\n  await expect(page.getByAltText('QA Academy logo')).toBeVisible();\n  await expect(page.getByPlaceholder('you@example.com')).toBeEmpty();\n  await expect(page.getByRole('checkbox', { name: 'Remember me' })).not.toBeChecked();\n  await expect(page.getByRole('link', { name: 'Forgot password?' })).toBeVisible();\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/signin-page.spec.ts --project=chromium"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex2",
                  "title": "Enrolment validation",
                  "level": "medium",
                  "prompt": "Create `tests/day9/enrol-validation.spec.ts` with two tests, from these test cases:\n\n| ID | Steps | Expected |\n|---|---|---|\n| TC-301 | Leave **Full name** empty; fill a valid email; choose *API Testing*; accept the terms; click *Enrol now* | Status says `Name is required`; seats stay at 12 |\n| TC-302 | Fill name and a valid email; **don't choose a course**; accept the terms; click *Enrol now* | Status says `Please choose a course`; seats stay at 12 |\n\nName the tests `TC-301 name is required` and `TC-302 a course must be chosen`.",
                  "file": "tests/day9/enrol-validation.spec.ts",
                  "run": "npx playwright test tests/day9/enrol-validation.spec.ts --project=chromium",
                  "hints": [
                    "Start from the third test in I4 and change the inputs.",
                    "The status message is `page.getByRole('status')`; the seats are `page.getByTestId('seats')`."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { enrolPage } from './practice-pages';\n\ntest('TC-301 name is required', async ({ page }) => {\n  await page.setContent(enrolPage);\n  await page.getByLabel('Email').fill('asha@example.com');\n  await page.getByLabel('Course').selectOption('API Testing');\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n\n  await expect(page.getByRole('status')).toHaveText('Name is required');\n  await expect(page.getByTestId('seats')).toHaveText('Seats left: 12');\n});\n\ntest('TC-302 a course must be chosen', async ({ page }) => {\n  await page.setContent(enrolPage);\n  await page.getByLabel('Full name').fill('Asha Verma');\n  await page.getByLabel('Email').fill('asha@example.com');\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n\n  await expect(page.getByRole('status')).toHaveText('Please choose a course');\n  await expect(page.getByTestId('seats')).toHaveText('Seats left: 12');\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/enrol-validation.spec.ts --project=chromium"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex3",
                  "title": "From test case to test — the dashboard",
                  "level": "medium",
                  "prompt": "Automate this manual test case in `tests/day9/dashboard.spec.ts`:\n\n> **TC-210 · Dashboard lists the student's courses**\n> 1. Open the sign-in page\n> 2. Enter `student@qa.academy` / `Learn@123`\n> 3. Click *Sign in*\n>\n> **Expected:** the page title is `QA Academy - Dashboard`; the greeting `Welcome back, Student!` is shown; the heading `Your courses` is shown; the list shows exactly *Playwright Basics*, *API Testing*, *Performance Testing*, in that order.\n\nUse the test title `TC-210 dashboard lists the student's courses` — because the title contains an apostrophe, put it in double quotes: `\"…\"`.",
                  "file": "tests/day9/dashboard.spec.ts",
                  "run": "npx playwright test tests/day9/dashboard.spec.ts --project=chromium",
                  "hints": [
                    "`Your courses` is an `<h2>` — still the role heading.",
                    "Check a whole list, in order: `await expect(page.getByRole('listitem')).toHaveText([ … ]);`"
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\ntest(\"TC-210 dashboard lists the student's courses\", async ({ page }) => {\n  // Steps 1–3: sign in\n  await page.setContent(signInPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  // Expected: the dashboard, with a greeting and three courses in order\n  await expect(page).toHaveTitle('QA Academy - Dashboard');\n  await expect(page.getByText('Welcome back, Student!')).toBeVisible();\n  await expect(page.getByRole('heading', { name: 'Your courses' })).toBeVisible();\n  await expect(page.getByRole('listitem')).toHaveText(['Playwright Basics', 'API Testing', 'Performance Testing']);\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/dashboard.spec.ts --project=chromium"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex4",
                  "title": "Triage with annotations",
                  "level": "medium",
                  "prompt": "Create `tests/day9/triage.spec.ts` with three tests on the enrol page:\n\n1. `TC-303 the course list has four options` — asserts there are 4 options in the list (3 courses and the \"choose\" prompt). It must be **skipped in Firefox** with the reason `Course list is being redesigned for Firefox`.\n2. `TC-304 a student can pay by card` — the payment page isn't built yet. Mark it **fixme**, with an empty body.\n3. `BUG-51: an email with nothing after @ is accepted` — enrol with name `Ravi Kumar`, email `ravi@`, course *Playwright Basics*, terms accepted. Assert that the status says `Enter a valid email` (with a 1-second timeout). The app currently accepts `ravi@`, so mark the test as **expected to fail**.\n\nRun it in Chromium: you should see 2 passed and 1 skipped.",
                  "file": "tests/day9/triage.spec.ts",
                  "run": "npx playwright test tests/day9/triage.spec.ts --project=chromium",
                  "hints": [
                    "Ask for the browserName fixture: `async ({ page, browserName }) => { test.skip(browserName === 'firefox', '…'); … }`.",
                    "`test.fixme('title', async ({ page }) => {});`",
                    "`test.fail();` goes on the first line inside the BUG-51 test."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { enrolPage } from './practice-pages';\n\ntest('TC-303 the course list has four options', async ({ page, browserName }) => {\n  test.skip(browserName === 'firefox', 'Course list is being redesigned for Firefox');\n  await page.setContent(enrolPage);\n  await expect(page.getByRole('option')).toHaveCount(4);   // three courses + the \"choose\" prompt\n});\n\ntest.fixme('TC-304 a student can pay by card', async ({ page }) => {\n  // Payment page not built yet\n});\n\ntest('BUG-51: an email with nothing after @ is accepted', async ({ page }) => {\n  test.fail();\n  await page.setContent(enrolPage);\n  await page.getByLabel('Full name').fill('Ravi Kumar');\n  await page.getByLabel('Email').fill('ravi@');\n  await page.getByLabel('Course').selectOption('Playwright Basics');\n  await page.getByLabel('I accept the terms').check();\n  await page.getByRole('button', { name: 'Enrol now' }).click();\n  await expect(page.getByRole('status')).toHaveText('Enter a valid email', { timeout: 1000 });\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/triage.spec.ts --project=chromium"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex5",
                  "title": "Optional challenge: data-driven sign-in tests",
                  "level": "challenge",
                  "prompt": "On Day 7 you looped over test data by hand. Playwright can **create one test per row**: put `test(…)` inside a `for...of` loop. Each row becomes a separate test, with its own title, its own fresh page and its own pass/fail result.\n\nCreate `tests/day9/signin-data.spec.ts`:\n\n1. A type `SignInCase` with `id`, `email`, `password` and `expectedMessage` (all strings).\n2. An array `cases` with these five rows:\n\n| id | email | password | expectedMessage |\n|---|---|---|---|\n| TC-221 | *(empty)* | *(empty)* | Please enter your email and password |\n| TC-222 | student@qa.academy | *(empty)* | Please enter your email and password |\n| TC-223 | student@qa.academy | learn@123 | Invalid email or password |\n| TC-224 | someone@qa.academy | Learn@123 | Invalid email or password |\n| TC-225 | student@qa.academy | Learn@123 | Signing in… |\n\n3. A `for...of` loop that registers one test per row. Give each a **unique** title built from the row, such as `` `${c.id} sign-in with \"${c.email}\" / \"${c.password}\"` ``. The test fills both fields, clicks *Sign in*, and asserts the alert's text.\n\nRun it: you should see 5 separate tests.",
                  "file": "tests/day9/signin-data.spec.ts",
                  "run": "npx playwright test tests/day9/signin-data.spec.ts --project=chromium",
                  "hints": [
                    "The loop goes at the top level of the file, and `test(…)` goes inside it.",
                    "Titles must be unique — Playwright refuses two tests with the same title in one file.",
                    "`fill('')` on an empty field is fine."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { signInPage } from './practice-pages';\n\ntype SignInCase = { id: string; email: string; password: string; expectedMessage: string };\n\nconst cases: SignInCase[] = [\n  { id: 'TC-221', email: '', password: '', expectedMessage: 'Please enter your email and password' },\n  { id: 'TC-222', email: 'student@qa.academy', password: '', expectedMessage: 'Please enter your email and password' },\n  { id: 'TC-223', email: 'student@qa.academy', password: 'learn@123', expectedMessage: 'Invalid email or password' },\n  { id: 'TC-224', email: 'someone@qa.academy', password: 'Learn@123', expectedMessage: 'Invalid email or password' },\n  { id: 'TC-225', email: 'student@qa.academy', password: 'Learn@123', expectedMessage: 'Signing in…' },\n];\n\n// One test per row: each gets its own title, its own fresh page, and its own result\nfor (const c of cases) {\n  test(`${c.id} sign-in with \"${c.email}\" / \"${c.password}\"`, async ({ page }) => {\n    await page.setContent(signInPage);\n    await page.getByLabel('Email').fill(c.email);\n    await page.getByLabel('Password').fill(c.password);\n    await page.getByRole('button', { name: 'Sign in' }).click();\n    await expect(page.getByRole('alert')).toHaveText(c.expectedMessage);\n  });\n}",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/signin-data.spec.ts --project=chromium"
                  }
                }
              ]
            },
            {
              "id": "d9-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. Map each part of a Playwright test to a part of a manual test case.\n2. Why does every test start by opening the page, even if the test before it already did?\n3. Why is `getByRole` preferred over a CSS selector like `#submit`?\n4. What's the difference between `await expect(locator).toHaveText('x')` and `expect(await locator.textContent()).toBe('x')`?\n5. When would you use `test.skip`, `test.fixme` and `test.fail`?\n6. A test fails on CI but passes on your machine. Which tool do you reach for first, and why?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "From single test files to a **framework**: the config file in depth, grouping with `describe`, shared setup with hooks, tags, test data and helper layers, npm scripts — and a mini-project that brings it all together.",
                  "title": "Coming up on Day 10"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 21,
        "quizzes": 22,
        "exercises": 6,
        "codeBlocks": 13
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 10,
      "week": 2,
      "title": "Framework Structure Overview",
      "subtitle": "Grow from single test files into a maintainable framework — configuration, grouping and hooks, tags, test data, page objects and fixtures",
      "estimatedTime": "3 hours",
      "topics": [
        "Framework layers — tests, pages, fixtures, test data and utilities",
        "playwright.config.ts in depth",
        "test.describe, hooks, tags and steps",
        "Test data and helper modules",
        "Page objects and custom fixtures (introduction)"
      ],
      "objectives": [
        "Explain what each folder in a Playwright framework is for, and where a new file belongs",
        "Read every setting in playwright.config.ts, and change timeouts, baseURL, screenshots and traces",
        "Group tests with test.describe and share set-up with beforeEach and the other hooks",
        "Tag tests and run a chosen subset with --grep; structure reports with test.step",
        "Keep test data and helpers in their own modules",
        "Read and write a simple page object, and use it through a custom fixture",
        "Organise a small suite for the enrol page using all of the above"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 3: the generated project, npm scripts",
        "Day 6: type aliases and objects",
        "Day 8: functions, async/await, export and import",
        "Day 9: test anatomy, fixtures, locators, actions, assertions, annotations, and the practice pages in tests/day9/practice-pages.ts",
        "Day 2: projects (one per browser) and network mocking with page.route",
        "Day 5: the ternary operator"
      ],
      "workspace": "pw-course/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d10-prerequisites-1",
              "title": "P1 · Checklist before you start",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "- [ ] Your Day 9 tests pass: `npx playwright test tests/day9 --project=chromium` (Day 9)\n- [ ] `tests/day9/practice-pages.ts` exists and exports `signInPage` and `enrolPage` (Day 9 · P3)\n- [ ] Explain what a fixture is, and what `{ page }` does (Day 9 · F2)\n- [ ] Export something from one file and import it into another (Day 8 · F7)"
                },
                {
                  "type": "quiz",
                  "id": "d10-p1-q1",
                  "quizType": "single",
                  "question": "`test-data/users.ts` exports `student`. What's the import line in `tests/day10/login.spec.ts`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`import { student } from '../test-data/users';`"
                    },
                    {
                      "id": "b",
                      "text": "`import { student } from '../../test-data/users';`"
                    },
                    {
                      "id": "c",
                      "text": "`import { student } from './test-data/users';`"
                    },
                    {
                      "id": "d",
                      "text": "`import student from 'test-data';`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "From tests/day10, go up two folders (../../) to reach the project folder, then into test-data."
                }
              ]
            },
            {
              "id": "d10-prerequisites-2",
              "title": "P2 · Why test code needs structure",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Picture your suite six months from now: 300 tests. The *Sign in* button's text changes to *Log in*. If every test finds that button itself, you edit 300 files. If one file knows how to use the sign-in page, you edit one line.\n\nYou already organise manual testing this way:\n\n| Manual testing | Automation framework |\n|---|---|\n| Test cases, grouped into suites | `tests/` — spec files, grouped with `test.describe` |\n| Shared steps (\"Log in as a student\") | `pages/` — page objects that know how to use each page |\n| Preconditions (\"user is on the sign-in page\") | Hooks and **fixtures** — set-up that runs before each test |\n| Test-data sheets | `test-data/` — modules of users, products, messages |\n| Test plan: which suites run where, how often | `playwright.config.ts` — browsers, retries, timeouts, reports |\n| Tags on test cases: *smoke*, *regression* | Tags: `{ tag: '@smoke' }` |\n\nA **framework** is simply this: an agreed structure, so that everyone knows where things live and each fact is written down **once**."
                },
                {
                  "type": "quiz",
                  "id": "d10-p2-q1",
                  "quizType": "single",
                  "question": "The Sign in button is renamed to Log in. In a well-structured framework, how many places need changing?",
                  "options": [
                    {
                      "id": "a",
                      "text": "None — Playwright finds the new name automatically"
                    },
                    {
                      "id": "b",
                      "text": "One — the page object for the sign-in page"
                    },
                    {
                      "id": "c",
                      "text": "Every test that signs in"
                    },
                    {
                      "id": "d",
                      "text": "Only the config file"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Keeping each page's locators in one page object means a UI change is fixed in one place."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d10-fundamentals-1",
              "title": "F1 · The layers of a framework",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Here's the structure you'll build today, inside `pw-course`:"
                },
                {
                  "type": "code",
                  "language": "text",
                  "content": "pw-course/\n├── playwright.config.ts   ← how tests run: browsers, timeouts, reports\n├── package.json           ← npm scripts: test, test:smoke, report…\n├── tests/                 ← the tests themselves (*.spec.ts)\n│   ├── day9/\n│   └── day10/\n├── pages/                 ← page objects: how to use each page\n│   ├── SignInPage.ts\n│   └── EnrolPage.ts\n├── fixtures/              ← custom fixtures: ready-made set-up for tests\n│   └── index.ts\n├── test-data/             ← users, products, expected messages\n│   ├── users.ts\n│   └── messages.ts\n└── utils/                 ← small helpers used by several layers\n    └── practice-site.ts",
                  "mode": "read"
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart TD\n  T[\"tests/*.spec.ts<br/>WHAT to check\"] --> F[\"fixtures/<br/>set-up, ready-made objects\"]\n  T --> P[\"pages/<br/>HOW to use each page\"]\n  T --> D[\"test-data/<br/>WITH which data\"]\n  F --> P\n  P --> U[\"utils/<br/>shared helpers\"]\n  F --> U\n  C[\"playwright.config.ts<br/>WHERE and HOW tests run\"] -.-> T"
                },
                {
                  "type": "markdown",
                  "content": "| Layer | Holds | Rule of thumb |\n|---|---|---|\n| `tests/` | Test files — titles, steps, assertions | Reads like a test case: *what* is checked, not *how* to find each element |\n| `pages/` | One page object per page or component | The only place that knows a page's locators |\n| `fixtures/` | Custom fixtures (F7) | Set-up that many tests need, handed to them by name |\n| `test-data/` | Plain data: users, products, messages | No browser code at all — just values |\n| `utils/` | Small general helpers — dates, random emails, API calls | Used by several layers |\n| `playwright.config.ts` | Browsers, timeouts, retries, reports, base URL | One place for decisions that apply to every test |\n\nFolder names vary between teams — some say `page-objects/` or `data/` — but the idea is always the same: **each kind of thing has its own place**. Only `tests/` is special to Playwright, because the config's `testDir` points at it; the other folders are plain modules that tests import."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "Test files: `feature.spec.ts` (`signin.spec.ts`). Page objects: the page name in PascalCase — every word capitalised — ending in `Page` (`SignInPage.ts`). Everything else: short, lower-case names with hyphens (`practice-site.ts`).",
                  "title": "Naming conventions"
                },
                {
                  "type": "quiz",
                  "id": "d10-f1-q1",
                  "quizType": "single",
                  "question": "Where does a list of 20 product names and prices, used by several search tests, belong?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`tests/`"
                    },
                    {
                      "id": "b",
                      "text": "`test-data/`"
                    },
                    {
                      "id": "c",
                      "text": "`playwright.config.ts`"
                    },
                    {
                      "id": "d",
                      "text": "`pages/`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "It's plain data with no browser code, used by several tests: test-data."
                }
              ]
            },
            {
              "id": "d10-fundamentals-2",
              "title": "F2 · `playwright.config.ts` in depth",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 3 you changed one line of this file. Now you can read all of it. Here's the generated config, without its comment lines, plus a few settings you'll add today:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { defineConfig, devices } from '@playwright/test';\n\nexport default defineConfig({\n  testDir: './tests',\n  fullyParallel: true,\n  forbidOnly: !!process.env.CI,\n  retries: process.env.CI ? 2 : 0,\n  workers: process.env.CI ? 1 : undefined,\n  timeout: 30_000,\n  expect: { timeout: 5_000 },\n  reporter: [['list'], ['html', { open: 'never' }]],\n  use: {\n    baseURL: 'https://qa-academy.test',\n    trace: 'on-first-retry',\n    screenshot: 'only-on-failure',\n  },\n  projects: [\n    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },\n    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },\n    { name: 'webkit', use: { ...devices['Desktop Safari'] } },\n  ],\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "The file **default-exports** (Day 8 · F7) one big settings object, wrapped in `defineConfig(…)` so that VS Code can check and autocomplete every setting.\n\n### The top-level settings\n\n| Setting | Meaning |\n|---|---|\n| `testDir: './tests'` | Where Playwright looks for `*.spec.ts` files |\n| `fullyParallel: true` | Run tests **inside** each file in parallel too, not just different files |\n| `forbidOnly: !!process.env.CI` | On a CI server, fail the run if a `test.only` was left in (Day 9 · F6) |\n| `retries: process.env.CI ? 2 : 0` | Re-run a failed test up to 2 times on CI; never on your computer (Day 5 · F7) |\n| `workers: process.env.CI ? 1 : undefined` | How many tests run at once. `undefined` lets Playwright choose based on your computer |\n| `timeout: 30_000` | Maximum time for **each test**, including its `beforeEach` hooks and fixture set-up: 30 seconds (the default). `afterEach`, `beforeAll` and `afterAll` hooks each get their own limit of the same length |\n| `expect: { timeout: 5_000 }` | How long each web-first assertion keeps retrying: 5 seconds (the default) |\n| `reporter` | Which reports to produce: here, a list in the terminal plus the HTML report |\n\n`30_000` is just `30000` written with an underscore to make it easier to read — the underscore is ignored.\n\n### `process.env.CI` — one config, two environments\n\n`process.env` holds the computer's **environment variables**: named settings the system passes to every program. CI servers such as GitHub Actions set a variable called `CI`. So `process.env.CI ? 2 : 0` (a ternary, Day 5 · F7) means *\"on a CI server, 2 retries; on my computer, 0\"*. In `forbidOnly`, `!` (Day 5 · F7) turns a value into its opposite true/false, and `!!` does that twice — so a variable that is set (truthy, Day 7 · F1) becomes `true`, and a missing one becomes `false`.\n\n### `use` — settings for every test\n\n`use` holds settings that affect the browser and page each test gets:\n\n| Option | Example | Effect |\n|---|---|---|\n| `baseURL` | `'https://qa-academy.test'` | `page.goto('/signin')` opens `https://qa-academy.test/signin`. Change one line to test another environment |\n| `trace` | `'on-first-retry'` | Record a trace when a test is retried. Also: `'on'`, `'off'`, `'retain-on-failure'`. Locally, where `retries` is 0, nothing is recorded — use `--trace on` (Day 2) when you need one |\n| `screenshot` | `'only-on-failure'` | Attach a screenshot to each failed test in the report. Also: `'on'`, `'off'` |\n| `video` | `'retain-on-failure'` | Keep a video of failed tests. Also: `'on'`, `'off'`, `'on-first-retry'` |\n| `actionTimeout` | `10_000` | Maximum time for each action, such as `click()`. The default is no limit of its own — only the test's 30 s |\n\n### `projects` — the same tests, several ways\n\nEach **project** runs the whole suite once with its own settings — usually one per browser (Day 2 · F4). `devices['Desktop Chrome']` is a ready-made bundle of settings (browser, screen size and more). Here `...` is the **spread** operator: it copies every property of one object into another — so all of those settings land in the project's `use`. It looks like the rest parameter from Day 8 · F2, but does a different job. You can also add mobile projects, such as `devices['Pixel 7']`.\n\n### Settings at three levels\n\nSettings apply from the most general to the most specific, and the more specific one wins:\n\n| Level | Where | Example |\n|---|---|---|\n| Whole suite | `use` at the top of the config | `screenshot: 'only-on-failure'` |\n| One project | `use` inside a project | `...devices['Desktop Safari']` |\n| One file or group | `test.use({ … })` in a spec file | `test.use({ viewport: { width: 375, height: 667 } });` |"
                },
                {
                  "type": "quiz",
                  "id": "d10-f2-q1",
                  "quizType": "single",
                  "question": "Tests pass on your computer but you want 2 retries on the CI server only. Which setting does that?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`forbidOnly: 2`"
                    },
                    {
                      "id": "b",
                      "text": "`retries: process.env.CI ? 2 : 0`"
                    },
                    {
                      "id": "c",
                      "text": "`workers: process.env.CI ? 2 : 0`"
                    },
                    {
                      "id": "d",
                      "text": "`retries: 2`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The ternary picks 2 when the CI environment variable is set, and 0 otherwise."
                },
                {
                  "type": "quiz",
                  "id": "d10-f2-q2",
                  "quizType": "single",
                  "question": "With `baseURL: 'https://staging.shop.test'` in the config, what does `await page.goto('/cart')` open?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`/cart` on the last page visited"
                    },
                    {
                      "id": "b",
                      "text": "`https://playwright.dev/cart`"
                    },
                    {
                      "id": "c",
                      "text": "An error: goto needs a full URL"
                    },
                    {
                      "id": "d",
                      "text": "`https://staging.shop.test/cart`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "A path starting with / is added to the baseURL. Switching environments then means changing one line."
                },
                {
                  "type": "quiz",
                  "id": "d10-f2-q3",
                  "quizType": "single",
                  "question": "A web-first assertion gives up after 5 seconds, and a whole test after 30 seconds. Which two settings control these?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`actionTimeout` and `retries`"
                    },
                    {
                      "id": "b",
                      "text": "`workers` and `timeout`"
                    },
                    {
                      "id": "c",
                      "text": "`expect.timeout` and `timeout`"
                    },
                    {
                      "id": "d",
                      "text": "`timeout` and `globalTimeout`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "expect: { timeout } is per assertion; the top-level timeout is per test."
                }
              ]
            },
            {
              "id": "d10-fundamentals-3",
              "title": "F3 · Grouping and shared set-up: `describe` and hooks",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### `test.describe` — a test suite\n\n`test.describe('title', () => { … })` groups related tests, like a suite in a test-management tool. The group title appears before each test's title in reports: `Sign-in › wrong password is rejected`."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test.describe('Sign-in', () => {\n  test('valid student reaches the dashboard', async ({ page }) => { /* … */ });\n  test('wrong password is rejected', async ({ page }) => { /* … */ });\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Hooks — code that runs around tests\n\nIf every test in a group starts with the same steps, move them into a **hook**:\n\n| Hook | Runs… | Typical use |\n|---|---|---|\n| `test.beforeEach(async ({ page }) => { … })` | Before **every** test in its file or group | Open the page; sign in |\n| `test.afterEach(async ({ page }) => { … })` | After every test — passed or failed | Clean-up; extra logging |\n| `test.beforeAll(async () => { … })` | Once, before the first test in its file or group — **once per worker** | Expensive one-time set-up, such as creating test data through an API |\n| `test.afterAll(async () => { … })` | Once, after the last test — once per worker | Removing that test data |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test.describe('Sign-in', () => {\n  test.beforeEach(async ({ page }) => {\n    await page.goto('/signin');            // every test in this group starts here\n  });\n\n  test('empty form shows a message', async ({ page }) => {\n    await page.getByRole('button', { name: 'Sign in' }).click();\n    await expect(page.getByRole('alert')).toHaveText('Please enter your email and password');\n  });\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`beforeEach` and `afterEach` can use fixtures like `page`; they get the **same** page as the test. `beforeAll` and `afterAll` can't use `page`, because they don't belong to any single test."
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "\"Sign in once in `beforeAll`, then let all tests use that session\" sounds efficient, but tests run in parallel and in separate contexts (Day 9 · F1). Each test should set up what it needs, in `beforeEach` or a fixture. (For fast sign-in, Playwright can save and reuse a signed-in state — a topic for later.)",
                  "title": "Don't share state between tests through `beforeAll`"
                },
                {
                  "type": "quiz",
                  "id": "d10-f3-q1",
                  "quizType": "single",
                  "question": "A describe block has a beforeEach and 4 tests. How many times does the beforeEach run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "1"
                    },
                    {
                      "id": "b",
                      "text": "5"
                    },
                    {
                      "id": "c",
                      "text": "4"
                    },
                    {
                      "id": "d",
                      "text": "It depends on the number of workers"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "beforeEach runs before every test in its group: 4 tests, 4 runs. (beforeAll is the one that depends on workers.)"
                },
                {
                  "type": "quiz",
                  "id": "d10-f3-q2",
                  "quizType": "single",
                  "question": "A test fails halfway through. Which hook still runs for that test?",
                  "options": [
                    {
                      "id": "a",
                      "text": "None — hooks stop after a failure"
                    },
                    {
                      "id": "b",
                      "text": "Only beforeAll"
                    },
                    {
                      "id": "c",
                      "text": "afterEach"
                    },
                    {
                      "id": "d",
                      "text": "Only beforeEach"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "afterEach runs after every test, passed or failed — that's what makes it suitable for clean-up."
                }
              ]
            },
            {
              "id": "d10-fundamentals-4",
              "title": "F4 · Tags, steps and better assertion messages",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Tags — choosing which tests run\n\nGive tests **tags** in the details object — the optional argument between the title and the callback:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('valid student reaches the dashboard', { tag: '@smoke' }, async ({ page }) => { /* … */ });\ntest('wrong password is rejected', { tag: ['@validation', '@auth'] }, async ({ page }) => { /* … */ });\n\ntest.describe('Enrolment', { tag: '@enrol' }, () => {\n  // every test in here gets @enrol\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Tags start with `@`. (Some teams write them in the title instead: `test('checkout works @smoke', …)` — that works too.) Then choose tests from the command line:\n\n| Command | Runs |\n|---|---|\n| `npx playwright test --grep @smoke` | Only tests tagged `@smoke` |\n| `npx playwright test --grep-invert @slow` | Everything **except** `@slow` tests |\n\nTo run tests with **either** of two tags, separate them with a vertical bar, which means \"or\":"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test --grep \"@smoke|@auth\""
                  ]
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Typical tags: `@smoke` (a quick check that the build is usable), `@regression` (the full suite), `@slow`, and a feature name such as `@checkout`. They're the same labels you'd put on manual test cases."
                },
                {
                  "type": "markdown",
                  "content": "### `test.step` — readable reports\n\nLong tests read better in the report when they're split into named steps:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "await test.step('Enter credentials and submit', async () => {\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "In the HTML report and the trace, the actions appear nested under *Enter credentials and submit*. When a test fails, you see immediately which step broke."
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "A second argument to `expect` adds a custom message to the report when the assertion fails: `` await expect(page.getByTestId('seats'), 'seat count should drop after enrolling').toHaveText('Seats left: 11'); ``. And `expect.soft(…)` records a failure but lets the test continue, so you see **all** the problems on a page at once; the test is still marked failed at the end.",
                  "title": "Two extras for assertions"
                },
                {
                  "type": "quiz",
                  "id": "d10-f4-q1",
                  "quizType": "single",
                  "question": "Which command runs every test EXCEPT the ones tagged @slow?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`npx playwright test --skip @slow`"
                    },
                    {
                      "id": "b",
                      "text": "`npx playwright test --grep @slow`"
                    },
                    {
                      "id": "c",
                      "text": "`npx playwright test -g !@slow`"
                    },
                    {
                      "id": "d",
                      "text": "`npx playwright test --grep-invert @slow`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "--grep-invert runs the tests that do NOT match."
                }
              ]
            },
            {
              "id": "d10-fundamentals-5",
              "title": "F5 · Test data and helpers as modules",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Hard-coded values scattered through tests cause two problems: the same value is written in many places, and it's hard to see which data a test uses. Move them into **modules** (Day 8 · F7):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// test-data/users.ts\nexport type User = { email: string; password: string; name: string };\n\nexport const student: User = { email: 'student@qa.academy', password: 'Learn@123', name: 'Student' };",
                  "mode": "read"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// tests/day10/signin.spec.ts\nimport { student } from '../../test-data/users';\n\nawait page.getByLabel('Email').fill(student.email);",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Now the student's email is written once. The `User` type means TypeScript checks every user you add (Day 6).\n\n| Put in `test-data/` | Put in `utils/` |\n|---|---|\n| Users, products, addresses | A function that builds a unique email: `uniqueEmail('asha')` |\n| Expected messages and texts | A function that formats a date the way the app shows it |\n| Lists for data-driven tests (Day 9 · ex5) | Anything that *does* something rather than *is* something |"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "Test data files end up in version control, where everyone can read them. Real passwords, API keys and tokens belong in **environment variables** — `process.env.STUDENT_PASSWORD` — set on the CI server or in a local `.env` file that is never committed. The generated config has commented-out lines for reading a `.env` file with a package called `dotenv`. Our practice password is fine to keep in the code: it's for a pretend site.",
                  "title": "Passwords and secrets"
                },
                {
                  "type": "quiz",
                  "id": "d10-f5-q1",
                  "quizType": "single",
                  "question": "Where should the password for your company's real staging admin account be stored?",
                  "options": [
                    {
                      "id": "a",
                      "text": "In `test-data/users.ts`, so every test can import it"
                    },
                    {
                      "id": "b",
                      "text": "In `playwright.config.ts`, under use"
                    },
                    {
                      "id": "c",
                      "text": "In the test's title"
                    },
                    {
                      "id": "d",
                      "text": "In an environment variable, read with `process.env`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Secrets stay out of code and version control. Environment variables are set on the CI server or in an uncommitted .env file."
                }
              ]
            },
            {
              "id": "d10-fundamentals-6",
              "title": "F6 · Page objects",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **page object** gathers everything tests need to know about one page — its locators and the actions a user can take there — into one module. Tests then say *what* they do (`signInPage.signIn(email, password)`) instead of *how* (three locators and three actions).\n\nPage objects are usually written as a **class**. You haven't met classes yet, so here's the minimum you need:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import type { Page, Locator } from '@playwright/test';\n\nexport class SignInPage {                         // ① a class: a blueprint for objects\n  readonly page: Page;                            // ② properties, each with a type\n  readonly emailField: Locator;\n\n  constructor(page: Page) {                       // ③ runs once, when a test writes: new SignInPage(page)\n    this.page = page;                             //    \"this\" = the object being built\n    this.emailField = page.getByLabel('Email');\n  }\n\n  async goto(): Promise<void> {                   // ④ a method: a function that belongs to the object\n    await this.page.goto('/signin');\n  }\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| # | Part | Meaning |\n|---|---|---|\n| ① | `class SignInPage { … }` | A **blueprint**. It describes what every sign-in page object has and can do |\n| ② | Properties | Values each object holds, like an object's properties (Day 6). `readonly` means they're set once, in the constructor, and never replaced |\n| ③ | `constructor` | Set-up code that runs when an object is created with `new SignInPage(page)`. Inside a class, `this` means \"the object itself\" |\n| ④ | Methods | Functions that belong to the object. They use `this.` to reach its properties |\n\nA class name also works as a **type**: `let signInPage: SignInPage;` means \"an object built from the SignInPage class\".\n\nUsing it in a test:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const signInPage = new SignInPage(page);    // build an object from the blueprint\nawait signInPage.goto();\nawait signInPage.emailField.fill('student@qa.academy');",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "`Page` and `Locator` are types from Playwright — the types of `page` and of what `getByRole` & co. return — so they're imported with `import type` (Day 8 · F7)."
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Locators and user actions: `signIn(email, password)`, `enrol(details)`. Assertions usually stay in the **tests**, because *what* to check differs from test to test.",
                  "title": "What goes in a page object?"
                },
                {
                  "type": "quiz",
                  "id": "d10-f6-q1",
                  "quizType": "single",
                  "question": "What does `new SignInPage(page)` do?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Opens a new browser tab"
                    },
                    {
                      "id": "b",
                      "text": "Creates an object from the SignInPage class, running its constructor with this page"
                    },
                    {
                      "id": "c",
                      "text": "Navigates to the sign-in page"
                    },
                    {
                      "id": "d",
                      "text": "Imports the SignInPage file"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "new builds an object from the class, and the constructor sets up its properties. Navigating happens only when you call goto()."
                }
              ]
            },
            {
              "id": "d10-fundamentals-7",
              "title": "F7 · Custom fixtures",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "On Day 9 you used Playwright's built-in fixtures, like `page`. You can add **your own**, so that tests can simply ask for a ready-to-use page object:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('wrong password is rejected', async ({ signInPage }) => {    // ← our own fixture\n  await signInPage.signIn('student@qa.academy', 'wrong');\n  await expect(signInPage.message).toHaveText('Invalid email or password');\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "You define custom fixtures once, by **extending** Playwright's `test`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test as base } from '@playwright/test';            // import test, but call it \"base\" here\nimport { SignInPage } from '../pages/SignInPage';\n\ntype QaAcademyFixtures = { signInPage: SignInPage };\n\nexport const test = base.extend<QaAcademyFixtures>({\n  signInPage: async ({ page }, use) => {\n    const signInPage = new SignInPage(page);      // set-up…\n    await signInPage.goto();\n    await use(signInPage);                        // …hand it to the test, and wait while the test runs…\n    // …anything here is clean-up, after the test\n  },\n});\n\nexport { expect } from '@playwright/test';        // pass expect through, so tests import both from here",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Piece | Meaning |\n|---|---|\n| `test as base` | Import `test`, but name it `base` in this file — because we're about to create our own `test` |\n| `base.extend<QaAcademyFixtures>({ … })` | A new `test` with everything the original has, plus the fixtures described by the type in angle brackets |\n| `async ({ page }, use) => { … }` | How to build the fixture. It can ask for other fixtures, like `page` |\n| `await use(value)` | Hands `value` to the test and waits until the test finishes. Code before it is set-up; code after it is clean-up |\n| `export { expect } from …` | Re-exports Playwright's `expect` unchanged |\n\nTests then import from **your** fixtures file instead of from `@playwright/test`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '../../fixtures';",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Fixtures versus `beforeEach`: both do set-up, but a fixture is **reusable across files**, runs **only for tests that ask for it**, and keeps its set-up and clean-up together. Hooks are fine for set-up that belongs to one file."
                },
                {
                  "type": "quiz",
                  "id": "d10-f7-q1",
                  "quizType": "single",
                  "question": "In a fixture, what does `await use(signInPage)` do?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Imports the SignInPage class"
                    },
                    {
                      "id": "b",
                      "text": "Calls signInPage's goto() method"
                    },
                    {
                      "id": "c",
                      "text": "Registers a new test"
                    },
                    {
                      "id": "d",
                      "text": "Hands signInPage to the test and waits until the test finishes, before any clean-up runs"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Everything before use() is set-up, everything after is clean-up, and the test runs in between."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [
            {
              "type": "markdown",
              "content": "Today's files go into several folders of `pw-course`. Run commands from the `pw-course` folder."
            }
          ],
          "lessons": [
            {
              "id": "d10-implementation-1",
              "title": "I1 · Update your config",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Open `playwright.config.ts` and make it look like this. Your file also contains commented-out lines from the generator; keep them or delete them, as you prefer. The new settings are `timeout`, `expect`, `baseURL` and `screenshot`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { defineConfig, devices } from '@playwright/test';\n\nexport default defineConfig({\n  // Where the tests are\n  testDir: './tests',\n\n  // Run the tests inside each file in parallel, too\n  fullyParallel: true,\n\n  // On a CI server, fail the run if a test.only was left in the code\n  forbidOnly: !!process.env.CI,\n\n  // Retry failed tests twice on CI, never on your own computer\n  retries: process.env.CI ? 2 : 0,\n\n  // On CI, one worker at a time; locally, let Playwright decide\n  workers: process.env.CI ? 1 : undefined,\n\n  // Time limits\n  timeout: 30_000,                 // each test, with its beforeEach hooks: 30 s\n  expect: { timeout: 5_000 },      // each web-first assertion: 5 s\n\n  // Reports: a list in the terminal, and an HTML report that doesn't open by itself\n  reporter: [['list'], ['html', { open: 'never' }]],\n\n  // Settings shared by every test in every project\n  use: {\n    baseURL: 'https://qa-academy.test',   // page.goto('/signin') opens https://qa-academy.test/signin\n    trace: 'on-first-retry',              // record a trace when a test is retried\n    screenshot: 'only-on-failure',        // attach a screenshot to every failed test\n  },\n\n  // One project per browser: every test runs once in each\n  projects: [\n    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },\n    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },\n    { name: 'webkit', use: { ...devices['Desktop Safari'] } },\n  ],\n});",
                  "mode": "editor",
                  "file": "playwright.config.ts"
                },
                {
                  "type": "markdown",
                  "content": "`timeout` and `expect.timeout` are set to their default values, so nothing changes yet — but now the limits are visible, and easy to change. Run your Day 9 tests to check that nothing broke:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9 --project=chromium"
                  ]
                }
              ]
            },
            {
              "id": "d10-implementation-2",
              "title": "I2 · Create the framework folders",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Create three folders next to `tests`:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "mkdir pages fixtures test-data utils"
                  ]
                },
                {
                  "type": "markdown",
                  "content": "**A stand-in for the real website.** Our practice pages aren't on the internet. This helper answers the browser's requests to `https://qa-academy.test` with them, using `page.route` — the network mocking you saw on Day 2 · I6. Tests can then use `page.goto('/signin')`, just as they would with a real site:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import type { Page } from '@playwright/test';\nimport { signInPage, enrolPage } from '../tests/day9/practice-pages';\n\n// Stands in for a real web server: answers the browser's requests to\n// https://qa-academy.test with our practice pages. A real project doesn't need this —\n// its tests open the real application.\nexport async function serveQaAcademy(page: Page): Promise<void> {\n  await page.route('https://qa-academy.test/signin', (route) =>\n    route.fulfill({ contentType: 'text/html', body: signInPage }));\n  await page.route('https://qa-academy.test/enrol', (route) =>\n    route.fulfill({ contentType: 'text/html', body: enrolPage }));\n}",
                  "mode": "editor",
                  "file": "utils/practice-site.ts"
                },
                {
                  "type": "markdown",
                  "content": "**Test data**: the users and the messages, each written once:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Test users, in one place. Tests import what they need.\nexport type User = { email: string; password: string; name: string };\n\nexport const student: User = {\n  email: 'student@qa.academy',\n  password: 'Learn@123',\n  name: 'Student',\n};\n\nexport const wrongPassword: User = {\n  email: 'student@qa.academy',\n  password: 'not-my-password',\n  name: 'Student',\n};",
                  "mode": "editor",
                  "file": "test-data/users.ts"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Every message the sign-in page can show, spelled exactly once\nexport const signInMessages = {\n  empty: 'Please enter your email and password',\n  invalid: 'Invalid email or password',\n  signingIn: 'Signing in…',\n};",
                  "mode": "editor",
                  "file": "test-data/messages.ts"
                }
              ]
            },
            {
              "id": "d10-implementation-3",
              "title": "I3 · A suite with `describe`, hooks, tags and steps",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "The Day 9 sign-in tests, reorganised. Compare them with `tests/day9/signin.spec.ts`: the set-up is written once, the data comes from `test-data`, and the tests are grouped and tagged."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { serveQaAcademy } from '../../utils/practice-site';\nimport { student, wrongPassword } from '../../test-data/users';\nimport { signInMessages } from '../../test-data/messages';\n\ntest.describe('Sign-in', () => {\n  // Runs before EACH test in this group: every test starts on the sign-in page\n  test.beforeEach(async ({ page }) => {\n    await serveQaAcademy(page);\n    await page.goto('/signin');                      // baseURL + '/signin'\n  });\n\n  test('valid student reaches the dashboard', { tag: ['@smoke', '@auth'] }, async ({ page }) => {\n    await test.step('Enter credentials and submit', async () => {\n      await page.getByLabel('Email').fill(student.email);\n      await page.getByLabel('Password').fill(student.password);\n      await page.getByRole('button', { name: 'Sign in' }).click();\n    });\n\n    await test.step('See the dashboard', async () => {\n      await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n      await expect(page.getByText(`Welcome back, ${student.name}!`)).toBeVisible();\n    });\n  });\n\n  test('empty form shows a message', { tag: '@validation' }, async ({ page }) => {\n    await page.getByRole('button', { name: 'Sign in' }).click();\n    await expect(page.getByRole('alert')).toHaveText(signInMessages.empty);\n  });\n\n  test('wrong password is rejected', { tag: ['@validation', '@auth'] }, async ({ page }) => {\n    await page.getByLabel('Email').fill(wrongPassword.email);\n    await page.getByLabel('Password').fill(wrongPassword.password);\n    await page.getByRole('button', { name: 'Sign in' }).click();\n    await expect(page.getByRole('alert')).toHaveText(signInMessages.invalid);\n  });\n});",
                  "mode": "editor",
                  "file": "tests/day10/signin.spec.ts",
                  "run": "npx playwright test tests/day10/signin.spec.ts --project=chromium",
                  "expectedOutput": "Running 3 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day10/signin.spec.ts:13:7 › Sign-in › valid student reaches the dashboard @smoke @auth (1.2s)\n  ✓  2 [chromium] › tests/day10/signin.spec.ts:26:7 › Sign-in › empty form shows a message @validation (250ms)\n  ✓  3 [chromium] › tests/day10/signin.spec.ts:31:7 › Sign-in › wrong password is rejected @validation @auth (258ms)\n\n  3 passed (2.5s)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 3 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day10/signin.spec.ts:13:7 › Sign-in › valid student reaches the dashboard @smoke @auth (1.2s)\n  ✓  2 [chromium] › tests/day10/signin.spec.ts:26:7 › Sign-in › empty form shows a message @validation (250ms)\n  ✓  3 [chromium] › tests/day10/signin.spec.ts:31:7 › Sign-in › wrong password is rejected @validation @auth (258ms)\n\n  3 passed (2.5s)"
                },
                {
                  "type": "markdown",
                  "content": "The group title *Sign-in ›* and the tags appear in every line. Now pick tests by tag:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day10/signin.spec.ts --project=chromium --grep @smoke",
                    "npx playwright test tests/day10/signin.spec.ts --project=chromium --grep-invert @validation"
                  ]
                },
                {
                  "type": "markdown",
                  "content": "Open the report (`npx playwright show-report`) and click the first test: its actions are grouped under the two step names."
                }
              ]
            },
            {
              "id": "d10-implementation-4",
              "title": "I4 · Watch the hooks run in order",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test } from '@playwright/test';\n\ntest.beforeAll(async () => {\n  console.log('beforeAll  - once, before the first test in this file');\n});\n\ntest.beforeEach(async () => {\n  console.log('  beforeEach - before every test');\n});\n\ntest.afterEach(async () => {\n  console.log('  afterEach  - after every test, passed or failed');\n});\n\ntest.afterAll(async () => {\n  console.log('afterAll   - once, after the last test in this file');\n});\n\ntest('first test', async () => {\n  console.log('    first test');\n});\n\ntest.describe('a group', () => {\n  test.beforeEach(async () => {\n    console.log('    group beforeEach - only for tests in this group, after the outer one');\n  });\n\n  test('second test', async () => {\n    console.log('      second test');\n  });\n});",
                  "mode": "editor",
                  "file": "tests/day10/hooks.spec.ts",
                  "run": "npx playwright test tests/day10/hooks.spec.ts --project=chromium --workers=1",
                  "expectedOutput": "Running 2 tests using 1 worker\n\nbeforeAll  - once, before the first test in this file\n  beforeEach - before every test\n    first test\n  afterEach  - after every test, passed or failed\n  ✓  1 [chromium] › tests/day10/hooks.spec.ts:19:5 › first test (1ms)\n  beforeEach - before every test\n    group beforeEach - only for tests in this group, after the outer one\n      second test\n  afterEach  - after every test, passed or failed\nafterAll   - once, after the last test in this file\n  ✓  2 [chromium] › tests/day10/hooks.spec.ts:28:7 › a group › second test (0ms)\n\n  2 passed (623ms)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 2 tests using 1 worker\n\nbeforeAll  - once, before the first test in this file\n  beforeEach - before every test\n    first test\n  afterEach  - after every test, passed or failed\n  ✓  1 [chromium] › tests/day10/hooks.spec.ts:19:5 › first test (1ms)\n  beforeEach - before every test\n    group beforeEach - only for tests in this group, after the outer one\n      second test\n  afterEach  - after every test, passed or failed\nafterAll   - once, after the last test in this file\n  ✓  2 [chromium] › tests/day10/hooks.spec.ts:28:7 › a group › second test (0ms)\n\n  2 passed (623ms)"
                },
                {
                  "type": "markdown",
                  "content": "The outer `beforeEach` runs for **both** tests; the group's `beforeEach` only for the test inside the group, after the outer one. `--workers=1` makes the order easy to read. With several workers, each worker runs its own `beforeAll` and `afterAll`.\n\n**Try it:** run it again without `--workers=1`. With `fullyParallel`, the two tests may land on different workers — count how many times `beforeAll` is printed. (Usually twice: once per worker.)"
                }
              ]
            },
            {
              "id": "d10-implementation-5",
              "title": "I5 · Your first page object and custom fixture",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "**1. The page object** — everything about the sign-in page, in one place:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import type { Page, Locator } from '@playwright/test';\n\n// A page object: everything tests need to know about the sign-in page, in one place\nexport class SignInPage {\n  // Properties: the page, and the locators for its elements\n  readonly page: Page;\n  readonly emailField: Locator;\n  readonly passwordField: Locator;\n  readonly signInButton: Locator;\n  readonly message: Locator;\n\n  // The constructor runs once, when a test writes: new SignInPage(page)\n  constructor(page: Page) {\n    this.page = page;\n    this.emailField = page.getByLabel('Email');\n    this.passwordField = page.getByLabel('Password');\n    this.signInButton = page.getByRole('button', { name: 'Sign in' });\n    this.message = page.getByRole('alert');\n  }\n\n  // Methods: the actions a user can take here\n  async goto(): Promise<void> {\n    await this.page.goto('/signin');\n  }\n\n  async signIn(email: string, password: string): Promise<void> {\n    await this.emailField.fill(email);\n    await this.passwordField.fill(password);\n    await this.signInButton.click();\n  }\n}",
                  "mode": "editor",
                  "file": "pages/SignInPage.ts"
                },
                {
                  "type": "markdown",
                  "content": "**2. The fixture** — hands every test that asks for it a signed-out sign-in page, ready to use:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test as base } from '@playwright/test';\nimport { SignInPage } from '../pages/SignInPage';\nimport { serveQaAcademy } from '../utils/practice-site';\n\n// The extra fixtures our tests can ask for\ntype QaAcademyFixtures = {\n  signInPage: SignInPage;\n};\n\n// A new test() that has everything the normal one has, plus our fixtures\nexport const test = base.extend<QaAcademyFixtures>({\n  signInPage: async ({ page }, use) => {\n    await serveQaAcademy(page);               // set-up: make the practice site available\n    const signInPage = new SignInPage(page);\n    await signInPage.goto();\n    await use(signInPage);                    // hand it to the test, and wait until the test ends\n    // anything after use() is clean-up, and runs after the test\n  },\n});\n\n// Re-export expect, so tests import both from one place\nexport { expect } from '@playwright/test';",
                  "mode": "editor",
                  "file": "fixtures/index.ts"
                },
                {
                  "type": "markdown",
                  "content": "**3. The tests** — short, and readable by anyone on the team:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '../../fixtures';\nimport { student, wrongPassword } from '../../test-data/users';\nimport { signInMessages } from '../../test-data/messages';\n\ntest('valid student reaches the dashboard', { tag: '@smoke' }, async ({ signInPage, page }) => {\n  await signInPage.signIn(student.email, student.password);\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n});\n\ntest('wrong password is rejected', async ({ signInPage }) => {\n  await signInPage.signIn(wrongPassword.email, wrongPassword.password);\n  await expect(signInPage.message).toHaveText(signInMessages.invalid);\n});\n\ntest('empty form shows a message', async ({ signInPage }) => {\n  await signInPage.signIn('', '');\n  await expect(signInPage.message).toHaveText(signInMessages.empty);\n});",
                  "mode": "editor",
                  "file": "tests/day10/signin-pom.spec.ts",
                  "run": "npx playwright test tests/day10/signin-pom.spec.ts --project=chromium",
                  "expectedOutput": "Running 3 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day10/signin-pom.spec.ts:5:5 › valid student reaches the dashboard @smoke (1.1s)\n  ✓  2 [chromium] › tests/day10/signin-pom.spec.ts:10:5 › wrong password is rejected (249ms)\n  ✓  3 [chromium] › tests/day10/signin-pom.spec.ts:15:5 › empty form shows a message (247ms)\n\n  3 passed (2.5s)",
                  "expectedOutputTarget": "terminal"
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 3 tests using 2 workers\n\n  ✓  1 [chromium] › tests/day10/signin-pom.spec.ts:5:5 › valid student reaches the dashboard @smoke (1.1s)\n  ✓  2 [chromium] › tests/day10/signin-pom.spec.ts:10:5 › wrong password is rejected (249ms)\n  ✓  3 [chromium] › tests/day10/signin-pom.spec.ts:15:5 › empty form shows a message (247ms)\n\n  3 passed (2.5s)"
                },
                {
                  "type": "markdown",
                  "content": "The first test asks for **two** fixtures, `signInPage` and `page`: they share the same browser page, because the `signInPage` fixture was built from `page`.\n\nCompare the three versions of \"wrong password is rejected\": Day 9 (everything inline), I3 (hooks and test data) and this one. Each step moved a kind of knowledge to its own layer, and the test itself got shorter and closer to the test case.\n\n**Try it:** the product owner renames the button to *Log in*. Which single line would you change? (Answer: `signInButton` in `SignInPage.ts`.)"
                }
              ]
            },
            {
              "id": "d10-implementation-6",
              "title": "I6 · npm scripts for the framework",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Give your smoke run a short name (Day 3 · I8):"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npm pkg set scripts.test:smoke=\"playwright test --grep @smoke\""
                  ]
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npm run test:smoke -- --list"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Listing tests:\n  [chromium] › day10/signin-pom.spec.ts:5:5 › valid student reaches the dashboard\n  [chromium] › day10/signin.spec.ts:13:7 › Sign-in › valid student reaches the dashboard\n  [firefox] › day10/signin-pom.spec.ts:5:5 › valid student reaches the dashboard\n  [firefox] › day10/signin.spec.ts:13:7 › Sign-in › valid student reaches the dashboard\n  [webkit] › day10/signin-pom.spec.ts:5:5 › valid student reaches the dashboard\n  [webkit] › day10/signin.spec.ts:13:7 › Sign-in › valid student reaches the dashboard\nTotal: 6 tests in 2 files"
                },
                {
                  "type": "markdown",
                  "content": "`--list` shows what **would** run, without running it: a quick way to check that your tags pick the right tests. Two smoke tests × three browsers = six runs."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d10-practice-1",
              "title": "Quiz · Day 10 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d10-pr-q1",
                  "quizType": "single",
                  "question": "What does `fullyParallel: true` add, compared with the default?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Tests inside the same file can also run in parallel, not only different files"
                    },
                    {
                      "id": "b",
                      "text": "Tests retry in parallel"
                    },
                    {
                      "id": "c",
                      "text": "Hooks run in parallel with tests"
                    },
                    {
                      "id": "d",
                      "text": "Tests run in all browsers at once"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "By default, files run in parallel but the tests inside one file run in order. fullyParallel lets them spread across workers too."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q2",
                  "quizType": "single",
                  "question": "Where should the locator for the 'Enrol now' button live in a framework?",
                  "options": [
                    {
                      "id": "a",
                      "text": "In test-data/messages.ts"
                    },
                    {
                      "id": "b",
                      "text": "In playwright.config.ts"
                    },
                    {
                      "id": "c",
                      "text": "In the EnrolPage page object"
                    },
                    {
                      "id": "d",
                      "text": "In every test that clicks it"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "Page objects are the one place that knows a page's locators."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q3",
                  "quizType": "single",
                  "question": "`test.describe('Cart', { tag: '@cart' }, () => { … })` contains 5 tests. Which tests does `--grep @cart` run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "All 5 — the group's tag applies to every test inside it"
                    },
                    {
                      "id": "b",
                      "text": "Only tests that also have @cart in their own title"
                    },
                    {
                      "id": "c",
                      "text": "Only the first test"
                    },
                    {
                      "id": "d",
                      "text": "None — tags only work on single tests"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "A tag on a describe block is given to every test in it."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q4",
                  "quizType": "single",
                  "question": "Why can't `test.beforeAll` use the page fixture?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Because it doesn't belong to any single test, and every test gets its own page"
                    },
                    {
                      "id": "b",
                      "text": "Because beforeAll can't be async"
                    },
                    {
                      "id": "c",
                      "text": "Because beforeAll runs after the tests"
                    },
                    {
                      "id": "d",
                      "text": "It can — page works everywhere"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "page is created fresh for each test. beforeAll runs once for a group of tests, so there's no single page to hand it."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q5",
                  "quizType": "single",
                  "question": "A test imports `{ test, expect } from '../../fixtures'` instead of `'@playwright/test'`. Why?",
                  "options": [
                    {
                      "id": "a",
                      "text": "The fixtures file's test has the team's custom fixtures, such as signInPage"
                    },
                    {
                      "id": "b",
                      "text": "It makes the tests run faster"
                    },
                    {
                      "id": "c",
                      "text": "It's required for tags to work"
                    },
                    {
                      "id": "d",
                      "text": "@playwright/test can only be imported once per project"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "The extended test has all the built-in fixtures plus the custom ones; expect is re-exported for convenience."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q6",
                  "quizType": "multiple",
                  "question": "Which belong in a page object? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "The locator for the Email field"
                    },
                    {
                      "id": "b",
                      "text": "A method `signIn(email, password)`"
                    },
                    {
                      "id": "c",
                      "text": "The number of retries on CI"
                    },
                    {
                      "id": "d",
                      "text": "The list of 50 test users"
                    }
                  ],
                  "correct": [
                    "a",
                    "b"
                  ],
                  "explanation": "Page objects hold a page's locators and actions. Users go in test-data; retries go in the config."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q8",
                  "quizType": "single",
                  "question": "In a fixture, where does clean-up code go?",
                  "options": [
                    {
                      "id": "a",
                      "text": "In playwright.config.ts"
                    },
                    {
                      "id": "b",
                      "text": "In the test itself"
                    },
                    {
                      "id": "c",
                      "text": "Before `await use(…)`"
                    },
                    {
                      "id": "d",
                      "text": "After `await use(…)`"
                    }
                  ],
                  "correct": [
                    "d"
                  ],
                  "explanation": "Code after use() runs after the test has finished — the natural place for clean-up."
                }
              ]
            },
            {
              "id": "d10-practice-2",
              "title": "Where does it belong?",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "written",
                  "id": "d10-pr-sort",
                  "title": "Sort the framework pieces",
                  "level": "easy",
                  "prompt": "For each item, name the place it belongs: `tests/`, `pages/`, `fixtures/`, `test-data/`, `utils/`, `playwright.config.ts`, or an environment variable.\n\n1. A function that returns today's date in the format the app shows, e.g. `25 Sep 2026`\n2. The locators and actions for the checkout page\n3. \"Run every test in Chromium and on a Pixel 7 phone\"\n4. The expected error texts of the registration form\n5. `test('TC-512 guest can check out', …)`\n6. Set-up that gives tests a signed-in dashboard page, used by 40 tests in 12 files\n7. The API key for the payment provider's test account\n8. A 1-minute time limit for every test",
                  "modelAnswer": "1. `utils/` — a helper that *does* something, used in several places.\n2. `pages/` — a `CheckoutPage` page object.\n3. `playwright.config.ts` — two projects, one with `devices['Pixel 7']`.\n4. `test-data/` — plain values, written once.\n5. `tests/` — a spec file, such as `tests/checkout.spec.ts`.\n6. `fixtures/` — a custom fixture, reusable across files (a `beforeEach` would have to be repeated in each of the 12 files).\n7. An environment variable — secrets never go in the code.\n8. `playwright.config.ts` — `timeout: 60_000`.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d10-practice-3",
              "title": "Mini-project · Organise the enrolment suite",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Build the same structure for the **enrol page**, in five steps (Step 4 is an optional challenge)."
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-ex1",
                  "title": "Step 1: enrolment test data",
                  "level": "easy",
                  "prompt": "Create `test-data/enrolments.ts` that exports:\n\n1. A type `Enrolment` with `fullName`, `email` and `course` (all strings).\n2. Two enrolments:\n   - `asha`: `Asha Verma`, `asha@example.com`, `API Testing`\n   - `noAtSign`: `Ravi Kumar`, `ravi.example.com`, `Playwright Basics`\n3. An object `enrolMessages` with three properties: `nameRequired` (`Name is required`), `invalidEmail` (`Enter a valid email`) and `chooseCourse` (`Please choose a course`).\n\nIt has no tests of its own — Steps 2 and 3 use it, and VS Code underlines any type mistakes as you type.",
                  "file": "test-data/enrolments.ts",
                  "hints": [
                    "Copy the shape of test-data/users.ts.",
                    "`export const enrolMessages = { nameRequired: 'Name is required', … };`"
                  ],
                  "solution": "// Test data for the enrolment page\nexport type Enrolment = { fullName: string; email: string; course: string };\n\nexport const asha: Enrolment = {\n  fullName: 'Asha Verma',\n  email: 'asha@example.com',\n  course: 'API Testing',\n};\n\nexport const noAtSign: Enrolment = {\n  fullName: 'Ravi Kumar',\n  email: 'ravi.example.com',\n  course: 'Playwright Basics',\n};\n\nexport const enrolMessages = {\n  nameRequired: 'Name is required',\n  invalidEmail: 'Enter a valid email',\n  chooseCourse: 'Please choose a course',\n};",
                  "check": {
                    "kind": "typecheckPasses",
                    "file": "test-data/enrolments.ts"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-ex2",
                  "title": "Step 2: the EnrolPage page object",
                  "level": "medium",
                  "prompt": "Create `pages/EnrolPage.ts` with a class `EnrolPage`, modelled on `SignInPage`:\n\n1. Properties (all `readonly`): `page`, and locators `nameField`, `emailField`, `courseList`, `termsCheckbox`, `enrolButton`, `status` and `seats`. Use the locators from Day 9 · I4.\n2. A method `goto()` that opens `/enrol`.\n3. A method `enrol(enrolment: Enrolment)` that fills the name and email, selects the course, ticks the terms and clicks *Enrol now*. Import the `Enrolment` type from Step 1 with `import type`.",
                  "file": "pages/EnrolPage.ts",
                  "hints": [
                    "`import type { Enrolment } from '../test-data/enrolments';`",
                    "`this.courseList = page.getByLabel('Course');` and later `await this.courseList.selectOption(enrolment.course);`",
                    "The seats text has a test id: `page.getByTestId('seats')`."
                  ],
                  "solution": "import type { Page, Locator } from '@playwright/test';\nimport type { Enrolment } from '../test-data/enrolments';\n\n// Page object for the course enrolment page\nexport class EnrolPage {\n  readonly page: Page;\n  readonly nameField: Locator;\n  readonly emailField: Locator;\n  readonly courseList: Locator;\n  readonly termsCheckbox: Locator;\n  readonly enrolButton: Locator;\n  readonly status: Locator;\n  readonly seats: Locator;\n\n  constructor(page: Page) {\n    this.page = page;\n    this.nameField = page.getByLabel('Full name');\n    this.emailField = page.getByLabel('Email');\n    this.courseList = page.getByLabel('Course');\n    this.termsCheckbox = page.getByRole('checkbox', { name: 'I accept the terms' });\n    this.enrolButton = page.getByRole('button', { name: 'Enrol now' });\n    this.status = page.getByRole('status');\n    this.seats = page.getByTestId('seats');\n  }\n\n  async goto(): Promise<void> {\n    await this.page.goto('/enrol');\n  }\n\n  // Fill the whole form, accept the terms and submit\n  async enrol(enrolment: Enrolment): Promise<void> {\n    await this.nameField.fill(enrolment.fullName);\n    await this.emailField.fill(enrolment.email);\n    await this.courseList.selectOption(enrolment.course);\n    await this.termsCheckbox.check();\n    await this.enrolButton.click();\n  }\n}",
                  "check": {
                    "kind": "typecheckPasses",
                    "file": "pages/EnrolPage.ts"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-ex3",
                  "title": "Step 3: the enrolment suite",
                  "level": "medium",
                  "prompt": "Create `tests/day10/enrol.spec.ts`:\n\n1. A `test.describe('Enrolment', { tag: '@enrol' }, …)` group.\n2. Inside it, declare `let enrolPage: EnrolPage;` and a `beforeEach` that calls `serveQaAcademy(page)`, creates the page object and opens the page.\n3. Four tests:\n   - `student can enrol in a course` (tag `@smoke`): enrol `asha`; in a `test.step` named `Confirmation and seat count`, check the status says `Thanks, Asha! You are enrolled in API Testing.` and the seats say `Seats left: 11`\n   - `an email without @ is rejected`: enrol `noAtSign`; check the status shows `enrolMessages.invalidEmail` and seats stay at 12\n   - `a name is required`: enrol `{ fullName: '', email: 'asha@example.com', course: 'API Testing' }`; check for `enrolMessages.nameRequired`\n   - `enrol button needs the terms`: the button starts disabled, and is enabled after ticking the terms\n\nRun it, then run only its smoke test:\n```\nnpx playwright test tests/day10/enrol.spec.ts --project=chromium\nnpx playwright test tests/day10/enrol.spec.ts --project=chromium --grep @smoke\n```",
                  "file": "tests/day10/enrol.spec.ts",
                  "run": "npx playwright test tests/day10/enrol.spec.ts --project=chromium",
                  "hints": [
                    "A variable declared in the describe block, and assigned in beforeEach, is visible to every test in the group.",
                    "In beforeEach: `enrolPage = new EnrolPage(page); await enrolPage.goto();`",
                    "The tests don't need `{ page }` at all: `async () => { await enrolPage.enrol(asha); … }`."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { EnrolPage } from '../../pages/EnrolPage';\nimport { serveQaAcademy } from '../../utils/practice-site';\nimport { asha, noAtSign, enrolMessages } from '../../test-data/enrolments';\n\ntest.describe('Enrolment', { tag: '@enrol' }, () => {\n  let enrolPage: EnrolPage;\n\n  test.beforeEach(async ({ page }) => {\n    await serveQaAcademy(page);\n    enrolPage = new EnrolPage(page);\n    await enrolPage.goto();\n  });\n\n  test('student can enrol in a course', { tag: '@smoke' }, async () => {\n    await enrolPage.enrol(asha);\n\n    await test.step('Confirmation and seat count', async () => {\n      await expect(enrolPage.status).toHaveText('Thanks, Asha! You are enrolled in API Testing.');\n      await expect(enrolPage.seats).toHaveText('Seats left: 11');\n    });\n  });\n\n  test('an email without @ is rejected', async () => {\n    await enrolPage.enrol(noAtSign);\n    await expect(enrolPage.status).toHaveText(enrolMessages.invalidEmail);\n    await expect(enrolPage.seats).toHaveText('Seats left: 12');\n  });\n\n  test('a name is required', async () => {\n    await enrolPage.enrol({ fullName: '', email: 'asha@example.com', course: 'API Testing' });\n    await expect(enrolPage.status).toHaveText(enrolMessages.nameRequired);\n  });\n\n  test('enrol button needs the terms', async () => {\n    await expect(enrolPage.enrolButton).toBeDisabled();\n    await enrolPage.termsCheckbox.check();\n    await expect(enrolPage.enrolButton).toBeEnabled();\n  });\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day10/enrol.spec.ts --project=chromium"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-ex4",
                  "title": "Step 4 (optional challenge): an enrolPage fixture",
                  "level": "challenge",
                  "prompt": "Add an `enrolPage` fixture to `fixtures/index.ts`, next to `signInPage`, so that a test can simply write:\n```ts\ntest('enrolling through the fixture', async ({ enrolPage }) => {\n  await enrolPage.enrol(asha);\n  await expect(enrolPage.seats).toHaveText('Seats left: 11');\n});\n```\n1. Add `enrolPage: EnrolPage` to the fixtures type.\n2. Add the fixture: serve the site, create the page object, open the page, `use` it.\n3. Put the test above in `tests/day10/enrol-fixture.spec.ts` (importing `test` and `expect` from your fixtures, and `asha` from the test data), and run it.",
                  "file": "fixtures/index.ts",
                  "hints": [
                    "The type becomes `{ signInPage: SignInPage; enrolPage: EnrolPage }`.",
                    "Copy the signInPage fixture and change the class and the names."
                  ],
                  "solution": "import { test as base } from '@playwright/test';\nimport { SignInPage } from '../pages/SignInPage';\nimport { EnrolPage } from '../pages/EnrolPage';\nimport { serveQaAcademy } from '../utils/practice-site';\n\n// The extra fixtures our tests can ask for\ntype QaAcademyFixtures = {\n  signInPage: SignInPage;\n  enrolPage: EnrolPage;\n};\n\n// A new test() that has everything the normal one has, plus our fixtures\nexport const test = base.extend<QaAcademyFixtures>({\n  signInPage: async ({ page }, use) => {\n    await serveQaAcademy(page);\n    const signInPage = new SignInPage(page);\n    await signInPage.goto();\n    await use(signInPage);\n  },\n\n  enrolPage: async ({ page }, use) => {\n    await serveQaAcademy(page);\n    const enrolPage = new EnrolPage(page);\n    await enrolPage.goto();\n    await use(enrolPage);\n  },\n});\n\nexport { expect } from '@playwright/test';",
                  "check": {
                    "kind": "typecheckPasses",
                    "file": "fixtures/index.ts"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "terminal",
                  "id": "d10-ex5",
                  "title": "Step 5: a script for the suite",
                  "level": "easy",
                  "prompt": "Add an npm script `test:enrol` that runs only the `@enrol` tests in Chromium, then run it.",
                  "solution": "npm pkg set scripts.test:enrol=\"playwright test --grep @enrol --project=chromium\"\nnpm run test:enrol",
                  "check": {
                    "kind": "commandsRun",
                    "commands": [
                      "npm pkg set scripts.test:enrol=\"playwright test --grep @enrol --project=chromium\"",
                      "npm run test:enrol"
                    ]
                  }
                }
              ]
            },
            {
              "id": "d10-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. In one sentence each: what goes in `tests/`, `pages/`, `fixtures/`, `test-data/` and `utils/`?\n2. Which config settings would you change to (a) test a different environment, (b) get a video of each failed test, (c) give slow tests 60 seconds?\n3. When would you use a `beforeEach` hook, and when a custom fixture?\n4. Why do assertions usually stay in tests rather than in page objects?\n5. Compare the three versions of \"wrong password is rejected\" (Day 9, I3 and I5). What changed, and why is each step an improvement?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "You started with *what Playwright is* and finished with an organised framework: TypeScript fundamentals, a real test runner, locators and web-first assertions, configuration, hooks, tags, test data, page objects and fixtures. Next steps: more page objects for bigger journeys, reusing a signed-in state, API testing with the `request` fixture, visual comparisons, and running your suite on a CI server.",
                  "title": "Two weeks done"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 19,
        "quizzes": 19,
        "exercises": 6,
        "codeBlocks": 22
      }
    }
  ]
}
