{
  "schemaVersion": "1.0",
  "week": 2,
  "title": "Week 2 — TypeScript & the Test Runner",
  "days": [
    {
      "schemaVersion": "1.0",
      "day": 6,
      "week": 2,
      "title": "TypeScript I: Arrays, Objects & Types for Test Data",
      "subtitle": "Lists and records — how you'll store test data — plus union and literal types and the `any` trap",
      "estimatedTime": "2.5 hours",
      "topics": [
        "Data Types (arrays, tuples, objects)",
        "Type aliases",
        "Union & literal types"
      ],
      "objectives": [
        "Store lists in arrays and tuples and read items by index",
        "Describe objects with type aliases, including optional properties",
        "Use union and literal types, and explain why `any` should be avoided",
        "Find and fix type errors in test-data code"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 5: let/const, primitive types, template literals, operators, the ts-basics playground"
      ],
      "workspace": "pw-course/ts-basics/day6/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d6-prerequisites-1",
              "title": "P1 · Quick recap from Day 5",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Three quick checks from Day 5 before we add lists and records:"
                },
                {
                  "type": "quiz",
                  "id": "d6-p1-q1",
                  "quizType": "single",
                  "question": "What does `console.log(`${2 + 3} items`);` print?",
                  "options": [
                    {
                      "id": "a",
                      "text": "${2 + 3} items"
                    },
                    {
                      "id": "b",
                      "text": "5 items"
                    },
                    {
                      "id": "c",
                      "text": "23 items"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Inside backticks, `${ }` evaluates the expression: 2 + 3 = 5."
                },
                {
                  "type": "quiz",
                  "id": "d6-p1-q2",
                  "quizType": "single",
                  "question": "Which line does the type checker reject? `const a = 1; let b = 'x';`",
                  "options": [
                    {
                      "id": "a",
                      "text": "`a = 2;`"
                    },
                    {
                      "id": "b",
                      "text": "`b = 'y';`"
                    },
                    {
                      "id": "c",
                      "text": "Neither"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`a` is a const and cannot be reassigned. `b` is a let holding a string, so 'y' is fine."
                },
                {
                  "type": "quiz",
                  "id": "d6-p1-q3",
                  "quizType": "single",
                  "question": "`const ok = 10 % 3 === 1 && !false;` — what is ok?",
                  "options": [
                    {
                      "id": "a",
                      "text": "true"
                    },
                    {
                      "id": "b",
                      "text": "false"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "10 % 3 is 1, so the comparison is true; !false is true; true && true is true."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d6-fundamentals-1",
              "title": "F1 · Arrays and tuples",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Arrays — ordered lists"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// An array of strings. Two ways to write the type: string[] or Array<string>\nconst browsers: string[] = ['chromium', 'firefox', 'webkit'];\n\nconsole.log(browsers[0]);        // first item — counting starts at 0!\nconsole.log(browsers[2]);        // third item\nconsole.log(browsers.length);    // how many items\n\nbrowsers.push('msedge');         // add an item at the end\nconsole.log(browsers);\nconsole.log(browsers.includes('firefox'));   // is \"firefox\" in the list?\n\nconst retryDelays: number[] = [1000, 2000, 4000];\nconsole.log(retryDelays[1]);",
                  "mode": "editor",
                  "file": "ts-basics/day6/arrays.ts",
                  "run": "node day6/arrays.ts",
                  "expectedOutput": "chromium\nwebkit\n3\n[ 'chromium', 'firefox', 'webkit', 'msedge' ]\ntrue\n2000",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "chromium\nwebkit\n3\n[ 'chromium', 'firefox', 'webkit', 'msedge' ]\ntrue\n2000"
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "`browsers` is a `const`, yet we could `push` into it. `const` means *the box can't be replaced with a different array* — the contents of the array can still change."
                },
                {
                  "type": "markdown",
                  "content": "### Tuples — fixed-length lists with a type per position"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// [username, password] — always exactly a string then a string\nconst credentials: [string, string] = ['asha@example.com', 'Secret@123'];\n\n// [product name, price, in stock?]\nconst item: [string, number, boolean] = ['Keyboard', 1499, true];\nitem[1] = 'free';   // ❌ error — position 1 must be a number",
                  "mode": "read"
                },
                {
                  "type": "quiz",
                  "id": "d6-f4-q1",
                  "quizType": "single",
                  "question": "`const cities = ['Delhi', 'Kolkata', 'Hyderabad'];` — what is `cities[1]`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'Delhi'"
                    },
                    {
                      "id": "b",
                      "text": "'Kolkata'"
                    },
                    {
                      "id": "c",
                      "text": "'Hyderabad'"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Array positions (indexes) start at 0. Index 0 is 'Delhi', index 1 is 'Kolkata'."
                }
              ]
            },
            {
              "id": "d6-fundamentals-2",
              "title": "F2 · Objects — records with named properties",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Objects — named properties\n\nObjects group related values under **property names** — perfect for test data:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A type alias describes the shape of an object once, so we can reuse it\ntype User = {\n  name: string;\n  email: string;\n  age: number;\n  isAdmin: boolean;\n  phone?: string;          // ? = optional property (may be missing)\n};\n\nconst testUser: User = {\n  name: 'Asha Verma',\n  email: 'asha@example.com',\n  age: 29,\n  isAdmin: false,\n};\n\nconsole.log(testUser.name);        // read a property with a dot\nconsole.log(testUser['email']);    // or with square brackets\ntestUser.age = 30;                 // change a property\nconsole.log(testUser);\nconsole.log(testUser.phone);       // optional and not set → undefined",
                  "mode": "editor",
                  "file": "ts-basics/day6/objects.ts",
                  "run": "node day6/objects.ts",
                  "expectedOutput": "Asha Verma\nasha@example.com\n{\n  name: 'Asha Verma',\n  email: 'asha@example.com',\n  age: 30,\n  isAdmin: false\n}\nundefined",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Asha Verma\nasha@example.com\n{\n  name: 'Asha Verma',\n  email: 'asha@example.com',\n  age: 30,\n  isAdmin: false\n}\nundefined"
                },
                {
                  "type": "markdown",
                  "content": "If you forget a required property or use the wrong type, the checker complains:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const badUser: User = { name: 'Ravi', email: 'ravi@example.com', age: '31' };\n// ❌ Type 'string' is not assignable to type 'number'   (age)\n// ❌ Property 'isAdmin' is missing",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Objects are how you'll store **test data**: one object per user, product or form submission — just like a row in your test-data spreadsheet, with named columns."
                },
                {
                  "type": "quiz",
                  "id": "d6-f4-q3",
                  "quizType": "single",
                  "question": "In `type Product = { name: string; price: number; discount?: number };`, what does the `?` mean?",
                  "options": [
                    {
                      "id": "a",
                      "text": "discount must be a question"
                    },
                    {
                      "id": "b",
                      "text": "discount is optional — an object may leave it out"
                    },
                    {
                      "id": "c",
                      "text": "discount can be any type"
                    },
                    {
                      "id": "d",
                      "text": "discount is required"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "A `?` after a property name makes it optional. If it's missing, reading it gives `undefined`."
                }
              ]
            },
            {
              "id": "d6-fundamentals-3",
              "title": "F3 · Union, literal and special types",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Union types — \"this OR that\""
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let orderId: string | number;   // may be a string or a number\norderId = 1045;                 // ✅\norderId = 'ORD-1045';           // ✅\norderId = true;                 // ❌ boolean is not allowed",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Literal types — only these exact values"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type BrowserName = 'chromium' | 'firefox' | 'webkit';\n\nlet target: BrowserName = 'firefox';   // ✅\ntarget = 'safari';                     // ❌ '\"safari\"' is not assignable to type 'BrowserName'",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Playwright uses literal types a lot — for example `browserName` is typed as `'chromium' | 'firefox' | 'webkit'`, so VS Code can autocomplete and catch typos.\n\n### `any` and `unknown` — escape hatches\n\n| Type | Meaning | Advice |\n|---|---|---|\n| `any` | \"Turn off type checking for this value\" — anything goes | **Avoid.** It hides bugs — it turns TypeScript back into plain JavaScript |\n| `unknown` | \"Could be anything — check before using it\" | Safer when you truly don't know the type (e.g. data from a file) |"
                },
                {
                  "type": "quiz",
                  "id": "d6-f4-q2",
                  "quizType": "single",
                  "question": "Given `type Status = 'passed' | 'failed' | 'skipped';`, which assignment causes a type error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`let s: Status = 'passed';`"
                    },
                    {
                      "id": "b",
                      "text": "`let s: Status = 'skipped';`"
                    },
                    {
                      "id": "c",
                      "text": "`let s: Status = 'flaky';`"
                    },
                    {
                      "id": "d",
                      "text": "`let s: Status = 'failed';`"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "A literal-union type only accepts the exact listed values. 'flaky' isn't one of them."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [],
          "lessons": [
            {
              "id": "d6-implementation-1",
              "title": "I1 · Build a test-data profile",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "You'll store test data for a registration test and print a summary — using `const`, an object type, template literals and string tools."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Describe the shape of our test data once\ntype RegistrationData = {\n  fullName: string;\n  email: string;\n  age: number;\n  country: string;\n  acceptsTerms: boolean;\n};\n\n// One test-data record (like one row in a test-data sheet)\nconst newCustomer: RegistrationData = {\n  fullName: 'Meera Iyer',\n  email: 'meera.iyer@example.com',\n  age: 27,\n  country: 'India',\n  acceptsTerms: true,\n};\n\n// Build some values from the data\nconst firstName = newCustomer.fullName.split(' ')[0];   // split the name at the space, take the first part\nconst expectedWelcome = `Welcome, ${firstName}!`;\n\n// Print a readable summary\nconsole.log('--- Test data: new customer ---');\nconsole.log(`Name:    ${newCustomer.fullName}`);\nconsole.log(`Email:   ${newCustomer.email}`);\nconsole.log(`Adult:   ${newCustomer.age >= 18}`);\nconsole.log(`Terms:   ${newCustomer.acceptsTerms ? 'accepted' : 'NOT accepted'}`);\nconsole.log(`Expected message after sign-up: \"${expectedWelcome}\"`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/test-data.ts",
                  "run": "node day6/test-data.ts"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "node day6/test-data.ts",
                    "npm run check -- day6/test-data.ts"
                  ]
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "--- Test data: new customer ---\nName:    Meera Iyer\nEmail:   meera.iyer@example.com\nAdult:   true\nTerms:   accepted\nExpected message after sign-up: \"Welcome, Meera!\""
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** change `age` to `'27'` (in quotes) and run `npm run check -- day6/test-data.ts`. Read the error, then change it back."
                }
              ]
            },
            {
              "id": "d6-implementation-2",
              "title": "I2 · Type detective — find and fix 5 bugs",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This file has **five** type errors (it uses the operators from Day 5). Don't run it yet — first **check** it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type Product = {\n  name: string;\n  price: number;\n  inStock: boolean;\n};\n\nconst keyboard: Product = {\n  name: 'Mechanical Keyboard',\n  price: '2499',            // bug 1\n  inStock: 'yes',           // bug 2\n};\n\nconst quantity: number = 2;\nconst maxQuantity = 5;\nmaxQuantity = 10;           // bug 3\n\nlet browser: 'chromium' | 'firefox' | 'webkit' = 'chrome';   // bug 4\n\nconst total: string = keyboard.price * quantity;             // bug 5\nconsole.log(`Total for ${quantity}: ${total}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/detective.ts",
                  "run": "npm run check -- day6/detective.ts",
                  "expectError": true
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npm run check -- day6/detective.ts"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "day6/detective.ts(9,3): error TS2322: Type 'string' is not assignable to type 'number'.\nday6/detective.ts(10,3): error TS2322: Type 'string' is not assignable to type 'boolean'.\nday6/detective.ts(15,1): error TS2588: Cannot assign to 'maxQuantity' because it is a constant.\nday6/detective.ts(17,5): error TS2322: Type '\"chrome\"' is not assignable to type '\"chromium\" | \"firefox\" | \"webkit\"'.\nday6/detective.ts(19,7): error TS2322: Type 'number' is not assignable to type 'string'."
                },
                {
                  "type": "markdown",
                  "content": "Fix each line, then check again until the command prints nothing, and finally run it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "type Product = {\n  name: string;\n  price: number;\n  inStock: boolean;\n};\n\nconst keyboard: Product = {\n  name: 'Mechanical Keyboard',\n  price: 2499,              // fix 1: a number, not text\n  inStock: true,            // fix 2: a boolean\n};\n\nconst quantity: number = 2;\nlet maxQuantity = 5;        // fix 3: let, because it changes\nmaxQuantity = 10;\n\nlet browser: 'chromium' | 'firefox' | 'webkit' = 'chromium';   // fix 4: an allowed value\n\nconst total: number = keyboard.price * quantity;               // fix 5: the result is a number\nconsole.log(`Total for ${quantity}: ${total}`);\nconsole.log(`Max quantity: ${maxQuantity}, browser: ${browser}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/detective-fixed.ts",
                  "run": "node day6/detective-fixed.ts",
                  "expectedOutput": "Total for 2: 4998\nMax quantity: 10, browser: chromium",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Total for 2: 4998\nMax quantity: 10, browser: chromium"
                }
              ]
            },
            {
              "id": "d6-implementation-3",
              "title": "I3 · Arrays, tuples and literal types for a test matrix",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Only these three browser names are allowed\ntype BrowserName = 'chromium' | 'firefox' | 'webkit';\n\n// The browsers we test on\nconst browsers: BrowserName[] = ['chromium', 'firefox'];\nbrowsers.push('webkit');                        // add one more\n\n// Login credentials as a tuple: [email, password]\nconst validLogin: [string, string] = ['asha@example.com', 'Secret@123'];\nconst [email, password] = validLogin;          // unpack the tuple into two variables\n\n// Pages to check\nconst pagesToCheck: string[] = ['/home', '/products', '/cart'];\n\nconst runsNeeded = browsers.length * pagesToCheck.length;\n\nconsole.log(`Browsers: ${browsers.join(', ')}`);\nconsole.log(`Logging in as ${email} (password has ${password.length} characters)`);\nconsole.log(`First page: ${pagesToCheck[0]}, last page: ${pagesToCheck[pagesToCheck.length - 1]}`);\nconsole.log(`Total page checks: ${runsNeeded}`);",
                  "mode": "editor",
                  "file": "ts-basics/day6/matrix.ts",
                  "run": "node day6/matrix.ts",
                  "expectedOutput": "Browsers: chromium, firefox, webkit\nLogging in as asha@example.com (password has 10 characters)\nFirst page: /home, last page: /cart\nTotal page checks: 9",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Browsers: chromium, firefox, webkit\nLogging in as asha@example.com (password has 10 characters)\nFirst page: /home, last page: /cart\nTotal page checks: 9"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "`const [email, password] = validLogin;` is called **destructuring** — unpacking values into separate variables. You'll meet its object version, `{ page }`, on Day 8 and then in every Playwright test."
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** add `browsers.push('safari');` and run the checker. Why does it complain?"
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d6-practice-1",
              "title": "Quiz · Day 6 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d6-pr-q4",
                  "quizType": "single",
                  "question": "`const tags = ['@smoke', '@regression'];` — what is `tags.length`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "1"
                    },
                    {
                      "id": "b",
                      "text": "2"
                    },
                    {
                      "id": "c",
                      "text": "3"
                    },
                    {
                      "id": "d",
                      "text": "0"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The array has two items. (The last index is 1, but the length is 2.)"
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q7",
                  "quizType": "multiple",
                  "question": "Which of these are TypeScript-only features (they don't exist in plain JavaScript)? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "Type annotations like `: string`"
                    },
                    {
                      "id": "b",
                      "text": "`const` and `let`"
                    },
                    {
                      "id": "c",
                      "text": "Type aliases like `type User = { … }`"
                    },
                    {
                      "id": "d",
                      "text": "Template literals with backticks"
                    }
                  ],
                  "correct": [
                    "a",
                    "c"
                  ],
                  "explanation": "`const`, `let` and template literals are modern JavaScript. Type annotations and type aliases are what TypeScript adds — and they're removed when the code runs."
                },
                {
                  "type": "quiz",
                  "id": "d6-pr-q8",
                  "quizType": "single",
                  "question": "Why is using `any` everywhere a bad idea?",
                  "options": [
                    {
                      "id": "a",
                      "text": "It makes the program run slower"
                    },
                    {
                      "id": "b",
                      "text": "It switches off type checking for those values, so TypeScript can no longer catch mistakes"
                    },
                    {
                      "id": "c",
                      "text": "It isn't allowed in Playwright"
                    },
                    {
                      "id": "d",
                      "text": "It turns numbers into strings"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`any` tells TypeScript to trust you blindly — you lose the main benefit of TypeScript."
                }
              ]
            },
            {
              "id": "d6-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d6-pr-p3",
                  "title": "Predict — arrays and objects",
                  "level": "medium",
                  "prompt": "What is printed?",
                  "code": "const results = ['pass', 'fail', 'pass'];\nconst summary = { suite: 'Checkout', total: results.length };\nresults.push('skip');\nconsole.log(results.length, summary.total, results[results.length - 1]);",
                  "answer": "`4 3 skip`\n\n`summary.total` stored the length (3) at the moment the object was created; pushing later doesn't change it. The array now has 4 items and the last one is 'skip'.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d6-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex1",
                  "title": "Tester profile card",
                  "level": "easy",
                  "prompt": "Create `day6/profile.ts`. Declare **constants** for your name (string), years of testing experience (number), favourite browser (only `'chromium' | 'firefox' | 'webkit'` allowed) and whether you have used automation before (boolean).\n\nPrint exactly this format using **one template literal per line** (with your own values):\n\n```\nTester: Priya Sharma\nExperience: 3 years\nFavourite browser: firefox\nAutomation before: false\n```",
                  "file": "ts-basics/day6/profile.ts",
                  "run": "node day6/profile.ts",
                  "starter": "// 1. Declare your constants here (with types)\n\n// 2. Print the four lines with template literals",
                  "hints": [
                    "A literal type looks like: `const favouriteBrowser: 'chromium' | 'firefox' | 'webkit' = 'firefox';`",
                    "A template literal: `` console.log(`Tester: ${testerName}`); ``"
                  ],
                  "solution": "// 1. Constants with types\nconst testerName: string = 'Priya Sharma';\nconst yearsOfExperience: number = 3;\nconst favouriteBrowser: 'chromium' | 'firefox' | 'webkit' = 'firefox';\nconst usedAutomation: boolean = false;\n\n// 2. Print with template literals\nconsole.log(`Tester: ${testerName}`);\nconsole.log(`Experience: ${yearsOfExperience} years`);\nconsole.log(`Favourite browser: ${favouriteBrowser}`);\nconsole.log(`Automation before: ${usedAutomation}`);",
                  "expectedOutput": "Tester: Priya Sharma\nExperience: 3 years\nFavourite browser: firefox\nAutomation before: false",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/profile.ts",
                    "expected": "Tester: Priya Sharma\nExperience: 3 years\nFavourite browser: firefox\nAutomation before: false"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex3",
                  "title": "Test-data object with a type alias",
                  "level": "medium",
                  "prompt": "Create `day6/login-data.ts`:\n\n1. Define a type alias `LoginCase` with: `id` (string), `username` (string), `password` (string), `shouldSucceed` (boolean) and an **optional** `expectedError` (string).\n2. Create two constants of that type:\n   - `validCase`: id `TC-01`, username `standard_user`, password `secret_sauce`, shouldSucceed `true`\n   - `lockedCase`: id `TC-02`, username `locked_out_user`, password `secret_sauce`, shouldSucceed `false`, expectedError `Sorry, this user has been locked out.`\n3. Put both in an array `loginCases` typed `LoginCase[]` and print:\n```\n2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out.\n```\nRun `npm run check -- day6/login-data.ts` — it must print nothing.",
                  "file": "ts-basics/day6/login-data.ts",
                  "run": "node day6/login-data.ts",
                  "hints": [
                    "Optional property: `expectedError?: string;`",
                    "Access items with `loginCases[0]` and `loginCases[1]`."
                  ],
                  "solution": "// The shape of one login test case\ntype LoginCase = {\n  id: string;\n  username: string;\n  password: string;\n  shouldSucceed: boolean;\n  expectedError?: string;   // only needed for failing cases\n};\n\nconst validCase: LoginCase = {\n  id: 'TC-01',\n  username: 'standard_user',\n  password: 'secret_sauce',\n  shouldSucceed: true,\n};\n\nconst lockedCase: LoginCase = {\n  id: 'TC-02',\n  username: 'locked_out_user',\n  password: 'secret_sauce',\n  shouldSucceed: false,\n  expectedError: 'Sorry, this user has been locked out.',\n};\n\nconst loginCases: LoginCase[] = [validCase, lockedCase];\n\nconsole.log(`${loginCases.length} login cases`);\nconsole.log(`${loginCases[0].id} ${loginCases[0].username} expects success: ${loginCases[0].shouldSucceed}`);\nconsole.log(`${loginCases[1].id} ${loginCases[1].username} expects error: ${loginCases[1].expectedError}`);",
                  "expectedOutput": "2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out.",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/login-data.ts",
                    "expected": "2 login cases\nTC-01 standard_user expects success: true\nTC-02 locked_out_user expects error: Sorry, this user has been locked out."
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d6-ex5",
                  "title": "Challenge: build a URL and a test title",
                  "level": "challenge",
                  "prompt": "Create `day6/url-builder.ts`. Given:\n```ts\nconst baseUrl = 'https://shop.example.com';\nconst category = 'Laptops & Tablets';\nconst page = 2;\nconst sortBy: 'price' | 'rating' = 'price';\n```\n1. Turn the category into a URL-friendly **slug**: lowercase, `&` removed, spaces replaced by `-`, no double dashes → `laptops-tablets`. (Use `toLowerCase()`, `replace('&', '')`, `split(' ')`, `filter(...)` and `join('-')` — or research your own way.)\n2. Build: `https://shop.example.com/c/laptops-tablets?page=2&sort=price`\n3. Build a test title: `[P2] Laptops & Tablets sorted by price` — `P2` comes from the page number.\n4. Print both lines, the URL first.",
                  "file": "ts-basics/day6/url-builder.ts",
                  "run": "node day6/url-builder.ts",
                  "hints": [
                    "'Laptops & Tablets'.toLowerCase() → 'laptops & tablets'",
                    ".replace('&', '') → 'laptops  tablets' (two spaces!)",
                    ".split(' ') → ['laptops', '', 'tablets'] — filter out the empty string with `.filter((word) => word !== '')`"
                  ],
                  "solution": "const baseUrl = 'https://shop.example.com';\nconst category = 'Laptops & Tablets';\nconst page = 2;\nconst sortBy: 'price' | 'rating' = 'price';\n\n// 1. Make a slug: lowercase → remove & → split into words → drop empty words → join with -\nconst slug = category\n  .toLowerCase()\n  .replace('&', '')\n  .split(' ')\n  .filter((word) => word !== '')\n  .join('-');\n\n// 2. Build the URL with a template literal\nconst url = `${baseUrl}/c/${slug}?page=${page}&sort=${sortBy}`;\n\n// 3. Build the test title\nconst title = `[P${page}] ${category} sorted by ${sortBy}`;\n\n// 4. Print\nconsole.log(url);\nconsole.log(title);",
                  "expectedOutput": "https://shop.example.com/c/laptops-tablets?page=2&sort=price\n[P2] Laptops & Tablets sorted by price",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day6/url-builder.ts",
                    "expected": "https://shop.example.com/c/laptops-tablets?page=2&sort=price\n[P2] Laptops & Tablets sorted by price"
                  }
                }
              ]
            },
            {
              "id": "d6-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. Give one example each of an array, a tuple and an object you'd use as test data.\n2. What is the difference between `string | number` and `'chromium' | 'firefox'`?\n3. Why is `any` a bad habit?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Conditions, loops and functions: your code starts making decisions, repeating work and packaging steps you can reuse — like reusable test steps.",
                  "title": "Coming up on Day 7"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 11,
        "quizzes": 9,
        "exercises": 4,
        "codeBlocks": 10
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 7,
      "week": 2,
      "title": "TypeScript II: Conditions, Loops & Functions",
      "subtitle": "Make decisions, repeat work and package steps into reusable functions — including the callback pattern behind every Playwright test",
      "estimatedTime": "3 hours",
      "topics": [
        "Conditions",
        "Loops",
        "Functions"
      ],
      "objectives": [
        "Use `if / else if / else`, `switch` and the ternary operator to make decisions",
        "Explain truthy and falsy values",
        "Repeat work with `for`, `for...of`, `while` and array methods, using `break` and `continue`",
        "Write functions with typed parameters, return types, optional and default parameters, and arrow functions",
        "Pass functions to other functions (callbacks) — the pattern behind `test('title', async () => {…})`"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 5–6: variables, types, operators, arrays, objects, type aliases"
      ],
      "workspace": "pw-course/ts-basics/day7/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d7-prerequisites-1",
              "title": "P1 · Blocks, indentation and scope",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Curly braces `{ }` group statements into a **block**. Code inside a block is **indented** (moved right) so you can see what belongs together."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "if (isLoggedIn) {            // the block starts\n  console.log('Welcome');    // indented: belongs to the if\n  showDashboard();\n}                            // the block ends\nconsole.log('Done');         // not indented: always runs",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "**Scope** means *where a variable exists*. A `let` or `const` created **inside** a block exists only inside that block:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const suite = 'Checkout';           // outside any block: visible everywhere below\nif (true) {\n  const message = 'inside';         // only exists inside these braces\n  console.log(suite, message);      // ✅ both visible\n}\nconsole.log(message);               // ❌ Cannot find name 'message'",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "In VS Code, select code and press `Shift + Alt + F` (Windows/Linux) or `Shift + Option + F` (Mac) to auto-format indentation."
                }
              ]
            },
            {
              "id": "d7-prerequisites-2",
              "title": "P2 · Truthy and falsy",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Conditions need `true` or `false`. When you give them another value, JavaScript treats it as \"truthy\" or \"falsy\".\n\n**Falsy** — these count as `false`: `false`, `0`, `''` (empty string), `null`, `undefined`, `NaN`.\n**Truthy** — *everything else*: `'hello'`, `42`, `[]`, `{}`, `'false'` (a non-empty string!)."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const errorMessage = '';\nif (errorMessage) {\n  console.log('Error shown:', errorMessage);   // skipped — '' is falsy\n}",
                  "mode": "read"
                },
                {
                  "type": "quiz",
                  "id": "d7-p2-q1",
                  "quizType": "multiple",
                  "question": "Which values are FALSY? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`0`"
                    },
                    {
                      "id": "b",
                      "text": "`'0'`"
                    },
                    {
                      "id": "c",
                      "text": "`''`"
                    },
                    {
                      "id": "d",
                      "text": "`undefined`"
                    }
                  ],
                  "correct": [
                    "a",
                    "c",
                    "d"
                  ],
                  "explanation": "`'0'` is a non-empty string, so it's truthy. The number 0, the empty string and undefined are falsy."
                }
              ]
            },
            {
              "id": "d7-prerequisites-3",
              "title": "P3 · Quick recap from Days 5–6",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d7-p3-q1",
                  "quizType": "single",
                  "question": "`const browsers = ['chromium', 'firefox']; browsers.push('webkit');` — what is `browsers[2]`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'firefox'"
                    },
                    {
                      "id": "b",
                      "text": "'webkit'"
                    },
                    {
                      "id": "c",
                      "text": "undefined"
                    },
                    {
                      "id": "d",
                      "text": "An error, because browsers is const"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`const` stops you replacing the array, not changing its contents. After push, index 2 is 'webkit'."
                },
                {
                  "type": "quiz",
                  "id": "d7-p3-q2",
                  "quizType": "single",
                  "question": "What is `5 > 3 && 2 > 4`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "true"
                    },
                    {
                      "id": "b",
                      "text": "false"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "5 > 3 is true but 2 > 4 is false. With `&&`, both must be true."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d7-fundamentals-1",
              "title": "F1 · Conditions: making decisions",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### `if`, `else if`, `else`"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const statusCode = 404;\n\n// Checked from top to bottom — the FIRST true condition wins, the rest are skipped\nif (statusCode >= 200 && statusCode < 300) {\n  console.log('Success');\n} else if (statusCode >= 400 && statusCode < 500) {\n  console.log('Client error — check the request');\n} else if (statusCode >= 500) {\n  console.log('Server error — raise a bug');\n} else {\n  console.log('Something else');\n}\n\n// A condition can use a truthy/falsy value directly\nconst couponCode = '';\nif (!couponCode) {\n  console.log('No coupon applied');\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/conditions.ts",
                  "run": "node day7/conditions.ts",
                  "expectedOutput": "Client error — check the request\nNo coupon applied",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Client error — check the request\nNo coupon applied"
                },
                {
                  "type": "markdown",
                  "content": "### `switch` — choose between many exact values"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const browserName: string = 'webkit';\nlet realBrowser: string;\n\nswitch (browserName) {\n  case 'chromium':\n    realBrowser = 'Chrome / Edge';\n    break;                          // stop here — otherwise the next case runs too!\n  case 'firefox':\n    realBrowser = 'Firefox';\n    break;\n  case 'webkit':\n    realBrowser = 'Safari';\n    break;\n  default:                          // none of the cases matched\n    realBrowser = 'Unknown';\n}\n\nconsole.log(`${browserName} is like ${realBrowser}`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/switch.ts",
                  "run": "node day7/switch.ts",
                  "expectedOutput": "webkit is like Safari",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "webkit is like Safari"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "Forgetting `break` is a classic bug: execution \"falls through\" into the next case."
                },
                {
                  "type": "markdown",
                  "content": "### Choosing the right tool\n\n| Situation | Use |\n|---|---|\n| Ranges or combined conditions (`>= 200 && < 300`) | `if / else if / else` |\n| One variable compared to many exact values | `switch` |\n| Choose between two values in one line | ternary `cond ? a : b` |"
                },
                {
                  "type": "quiz",
                  "id": "d7-f1-q1",
                  "quizType": "single",
                  "question": "`const score = 75;` — what prints? `if (score >= 90) { console.log('A'); } else if (score >= 70) { console.log('B'); } else if (score >= 50) { console.log('C'); }`",
                  "options": [
                    {
                      "id": "a",
                      "text": "A"
                    },
                    {
                      "id": "b",
                      "text": "B"
                    },
                    {
                      "id": "c",
                      "text": "B and C"
                    },
                    {
                      "id": "d",
                      "text": "C"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The chain stops at the first true condition. 75 >= 70 is true, so 'B' prints and the 'C' check is skipped."
                }
              ]
            },
            {
              "id": "d7-fundamentals-2",
              "title": "F2 · Loops: repeating work",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Automation is about repetition — the same check for every user, every browser, every row. Loops do that.\n\n### `for...of` — for each item in a list (your most-used loop)"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const users = ['asha', 'ravi', 'meera'];\nfor (const user of users) {\n  console.log(`Logging in as ${user}`);\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Classic `for` — when you need a counter"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// start at 1; keep going while attempt <= 3; add 1 after each round\nfor (let attempt = 1; attempt <= 3; attempt++) {\n  console.log(`Attempt ${attempt}`);\n}",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### `while` and `do...while` — repeat until something changes"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "let itemsInCart = 3;\nwhile (itemsInCart > 0) {        // checks BEFORE each round (may run 0 times)\n  itemsInCart--;\n}\n\nlet tries = 0;\ndo {                              // runs at least ONCE, checks AFTER each round\n  tries++;\n} while (tries < 3);",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "If the condition never becomes false (e.g. you forget `itemsInCart--`), the loop never ends. Press `Ctrl + C` in the terminal to stop it.",
                  "title": "Infinite loops"
                },
                {
                  "type": "markdown",
                  "content": "### `break` and `continue`"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const results = ['pass', 'pass', 'skip', 'fail', 'pass'];\n\n// continue: skip the rest of THIS round, go to the next item\nfor (const result of results) {\n  if (result === 'skip') {\n    continue;\n  }\n  console.log('Counted:', result);\n}\n\n// break: stop the whole loop immediately\nfor (let i = 0; i < results.length; i++) {\n  if (results[i] === 'fail') {\n    console.log(`First failure at position ${i}`);\n    break;\n  }\n}",
                  "mode": "editor",
                  "file": "ts-basics/day7/loops.ts",
                  "run": "node day7/loops.ts",
                  "expectedOutput": "Counted: pass\nCounted: pass\nCounted: fail\nCounted: pass\nFirst failure at position 3",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Counted: pass\nCounted: pass\nCounted: fail\nCounted: pass\nFirst failure at position 3"
                },
                {
                  "type": "markdown",
                  "content": "### Array methods that loop for you\n\nArrays have built-in methods that take a small function (you'll write these properly in F3):\n\n| Method | Does | Example | Result |\n|---|---|---|---|\n| `forEach` | runs code for each item | `names.forEach((n) => console.log(n))` | prints each name |\n| `map` | makes a new array by transforming each item | `[1, 2, 3].map((n) => n * 2)` | `[2, 4, 6]` |\n| `filter` | keeps only items that pass a test | `['pass', 'fail'].filter((r) => r === 'fail')` | `['fail']` |\n| `find` | returns the first matching item | `users.find((u) => u.isAdmin)` | first admin or `undefined` |\n| `some` / `every` | is it true for any / all items? | `results.every((r) => r === 'pass')` | `true` or `false` |"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "Later, when a loop contains Playwright actions (`await page.click(…)`), use `for...of`. `forEach` does **not** wait for `await` inside it — a subtle and common bug.",
                  "title": "`forEach` and `await` don't mix"
                },
                {
                  "type": "quiz",
                  "id": "d7-f2-q1",
                  "quizType": "single",
                  "question": "How many times does \"Hi\" print? `for (let i = 0; i < 3; i++) { console.log('Hi'); }`",
                  "options": [
                    {
                      "id": "a",
                      "text": "2"
                    },
                    {
                      "id": "b",
                      "text": "3"
                    },
                    {
                      "id": "c",
                      "text": "4"
                    },
                    {
                      "id": "d",
                      "text": "Forever"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "i takes the values 0, 1, 2 (three rounds). When i becomes 3, `i < 3` is false and the loop stops."
                },
                {
                  "type": "quiz",
                  "id": "d7-f2-q2",
                  "quizType": "single",
                  "question": "What does `[5, 12, 8, 20].filter((n) => n > 10)` return?",
                  "options": [
                    {
                      "id": "a",
                      "text": "[5, 8]"
                    },
                    {
                      "id": "b",
                      "text": "[12, 20]"
                    },
                    {
                      "id": "c",
                      "text": "12"
                    },
                    {
                      "id": "d",
                      "text": "true"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`filter` keeps the items for which the function returns true: 12 and 20."
                }
              ]
            },
            {
              "id": "d7-fundamentals-3",
              "title": "F3 · Functions: reusable steps",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "A **function** is a named, reusable block of steps — like a reusable test step \"Log in as user X\" that many test cases call.\n\n### Declaring and calling"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// name     parameters (inputs, with types)       return type\nfunction calculatePassRate(passed: number, total: number): number {\n  return (passed / total) * 100;              // return sends a value back to the caller\n}\n\n// A function that returns nothing uses the type void\nfunction printHeader(title: string): void {\n  console.log(`===== ${title} =====`);\n}\n\n// Calling (using) the functions\nprintHeader('Nightly run');\nconst rate = calculatePassRate(45, 50);       // arguments: the actual values\nconsole.log(`Pass rate: ${rate}%`);\nconsole.log(`Smoke pass rate: ${calculatePassRate(9, 10)}%`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/functions.ts",
                  "run": "node day7/functions.ts",
                  "expectedOutput": "===== Nightly run =====\nPass rate: 90%\nSmoke pass rate: 90%",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "===== Nightly run =====\nPass rate: 90%\nSmoke pass rate: 90%"
                },
                {
                  "type": "markdown",
                  "content": "Words to know: **parameters** are the input names in the definition (`passed`, `total`); **arguments** are the actual values you pass (`45`, `50`); **`return`** ends the function and hands back a value.\n\n### Optional and default parameters"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// timeout has a DEFAULT value; tag is OPTIONAL (may be undefined)\nfunction describeTest(title: string, timeout: number = 30000, tag?: string): string {\n  const tagText = tag ? ` ${tag}` : '';        // add the tag only if one was given\n  return `${title}${tagText} (timeout ${timeout / 1000}s)`;\n}\n\nconsole.log(describeTest('Login works'));\nconsole.log(describeTest('Checkout completes', 60000));\nconsole.log(describeTest('Search returns results', 30000, '@smoke'));",
                  "mode": "editor",
                  "file": "ts-basics/day7/parameters.ts",
                  "run": "node day7/parameters.ts",
                  "expectedOutput": "Login works (timeout 30s)\nCheckout completes (timeout 60s)\nSearch returns results @smoke (timeout 30s)",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Login works (timeout 30s)\nCheckout completes (timeout 60s)\nSearch returns results @smoke (timeout 30s)"
                },
                {
                  "type": "markdown",
                  "content": "### Rest parameters — any number of arguments"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "function logTags(title: string, ...tags: string[]): void {\n  console.log(title, 'has', tags.length, 'tags');\n}\nlogTags('Checkout', '@smoke', '@regression', '@payments');   // Checkout has 3 tags",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Arrow functions — the short form you'll see everywhere"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Regular function\nfunction double(n: number): number {\n  return n * 2;\n}\n\n// Same thing as an arrow function stored in a constant\nconst doubleArrow = (n: number): number => {\n  return n * 2;\n};\n\n// Even shorter: one expression → no braces, no return keyword\nconst doubleShort = (n: number) => n * 2;",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Functions as values: callbacks\n\nIn TypeScript a function is a value — you can pass it **into another function**. The receiving function decides *when* to run it. A function passed like this is called a **callback**."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// runStep receives a NAME and a FUNCTION, prints the name, then calls the function\nfunction runStep(name: string, action: () => void): void {\n  console.log(`▶ ${name}`);\n  action();                       // run the function we were given\n}\n\n// Pass an arrow function directly as the second argument\nrunStep('Open the home page', () => {\n  console.log('  …navigating to /home');\n});\n\nrunStep('Search for \"mouse\"', () => {\n  console.log('  …typing into the search box');\n});",
                  "mode": "editor",
                  "file": "ts-basics/day7/callbacks.ts",
                  "run": "node day7/callbacks.ts",
                  "expectedOutput": "▶ Open the home page\n  …navigating to /home\n▶ Search for \"mouse\"\n  …typing into the search box",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "▶ Open the home page\n  …navigating to /home\n▶ Search for \"mouse\"\n  …typing into the search box"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "```ts\ntest('Open the home page', async ({ page }) => { … });\n```\n`test` is a function that receives a **title** and a **callback**. Playwright decides when to run your callback — and hands it a ready-made `page`. You just wrote the same pattern yourself!",
                  "title": "This is exactly how Playwright tests are written"
                },
                {
                  "type": "quiz",
                  "id": "d7-f3-q1",
                  "quizType": "single",
                  "question": "`` function greet(name: string, greeting = 'Hello'): string { return `${greeting}, ${name}`; } `` — what does `greet('Ravi')` return?",
                  "options": [
                    {
                      "id": "a",
                      "text": "'Ravi, Hello'"
                    },
                    {
                      "id": "b",
                      "text": "'Hello, Ravi'"
                    },
                    {
                      "id": "c",
                      "text": "'undefined, Ravi'"
                    },
                    {
                      "id": "d",
                      "text": "An error — greeting is missing"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`greeting` has a default value 'Hello', used when no second argument is passed."
                },
                {
                  "type": "quiz",
                  "id": "d7-f3-q2",
                  "quizType": "single",
                  "question": "Which is a correct arrow function that adds two numbers?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`const add = (a: number, b: number) => a + b;`"
                    },
                    {
                      "id": "b",
                      "text": "`const add = function => a + b;`"
                    },
                    {
                      "id": "c",
                      "text": "`add(a, b) => { a + b }`"
                    },
                    {
                      "id": "d",
                      "text": "`const add => (a + b);`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Parameters in brackets, then `=>`, then the result. With a single expression the value is returned automatically."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [],
          "lessons": [
            {
              "id": "d7-implementation-1",
              "title": "I1 · A data-driven validator",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Combine **objects** (Day 6), **functions**, **conditions** and **loops**: a function checks a password against rules, and a loop runs it over a table of test data — exactly like a data-driven test."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Business rule: 8+ characters, at least one digit, at least one uppercase letter\nfunction validatePassword(password: string): string[] {\n  const problems: string[] = [];\n\n  if (password.length < 8) {\n    problems.push('too short');\n  }\n  if (!/[0-9]/.test(password)) {          // /[0-9]/ = \"any digit\" pattern\n    problems.push('needs a digit');\n  }\n  if (!/[A-Z]/.test(password)) {          // /[A-Z]/ = \"any uppercase letter\" pattern\n    problems.push('needs an uppercase letter');\n  }\n  return problems;                        // an empty list means the password is valid\n}\n\n// Test data: each case says what we EXPECT\ntype PasswordCase = {\n  input: string;\n  shouldBeValid: boolean;\n};\n\nconst cases: PasswordCase[] = [\n  { input: 'Secret123', shouldBeValid: true },\n  { input: 'short1A', shouldBeValid: false },\n  { input: 'alllowercase1', shouldBeValid: false },\n  { input: 'NoDigitsHere', shouldBeValid: false },\n  { input: 'Valid2Password', shouldBeValid: true },\n];\n\nlet passed = 0;\nfor (const testCase of cases) {\n  const problems = validatePassword(testCase.input);\n  const isValid = problems.length === 0;\n  const ok = isValid === testCase.shouldBeValid;          // did reality match the expectation?\n  if (ok) {\n    passed++;\n  }\n  const details = isValid ? 'valid' : problems.join(', ');\n  console.log(`${ok ? '✓' : '✗'} \"${testCase.input}\" → ${details}`);\n}\n\nconsole.log(`${passed}/${cases.length} cases behaved as expected`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/password-rules.ts",
                  "run": "node day7/password-rules.ts",
                  "expectedOutput": "✓ \"Secret123\" → valid\n✓ \"short1A\" → too short\n✓ \"alllowercase1\" → needs an uppercase letter\n✓ \"NoDigitsHere\" → needs a digit\n✓ \"Valid2Password\" → valid\n5/5 cases behaved as expected",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "✓ \"Secret123\" → valid\n✓ \"short1A\" → too short\n✓ \"alllowercase1\" → needs an uppercase letter\n✓ \"NoDigitsHere\" → needs a digit\n✓ \"Valid2Password\" → valid\n5/5 cases behaved as expected"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** add a case `{ input: 'Abcdefg1', shouldBeValid: true }` and predict the output first. Then add a new rule — \"must not contain spaces\" (`password.includes(' ')`) — and a case that checks it."
                }
              ]
            },
            {
              "id": "d7-implementation-2",
              "title": "I2 · Generate a smoke-test checklist",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Combine a **function**, a **loop** and **conditions** to produce a smoke-test checklist from a list of pages — the kind of thing you'd run after every deployment."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Each page we want to check after a deployment\ntype PageCheck = {\n  path: string;\n  expectedTitle: string;\n  critical: boolean;      // critical pages block the release if broken\n};\n\nconst pages: PageCheck[] = [\n  { path: '/', expectedTitle: 'Home', critical: true },\n  { path: '/login', expectedTitle: 'Sign in', critical: true },\n  { path: '/help', expectedTitle: 'Help centre', critical: false },\n  { path: '/about', expectedTitle: 'About us', critical: false },\n];\n\n// A function that describes one check as a line of text\nfunction describeCheck(check: PageCheck, index: number): string {\n  const marker = check.critical ? '!' : ' ';                  // mark critical pages\n  return `${index + 1}. [${marker}] open ${check.path} → expect title \"${check.expectedTitle}\"`;\n}\n\n// A function that counts how many checks are critical\nfunction countCritical(checks: PageCheck[]): number {\n  let count = 0;\n  for (const check of checks) {\n    if (check.critical) {\n      count++;\n    }\n  }\n  return count;\n}\n\nconsole.log('Smoke checklist');\nfor (let i = 0; i < pages.length; i++) {\n  console.log(describeCheck(pages[i], i));\n}\nconsole.log(`${countCritical(pages)} of ${pages.length} checks are release-blocking`);",
                  "mode": "editor",
                  "file": "ts-basics/day7/checklist.ts",
                  "run": "node day7/checklist.ts",
                  "expectedOutput": "Smoke checklist\n1. [!] open / → expect title \"Home\"\n2. [!] open /login → expect title \"Sign in\"\n3. [ ] open /help → expect title \"Help centre\"\n4. [ ] open /about → expect title \"About us\"\n2 of 4 checks are release-blocking",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Smoke checklist\n1. [!] open / → expect title \"Home\"\n2. [!] open /login → expect title \"Sign in\"\n3. [ ] open /help → expect title \"Help centre\"\n4. [ ] open /about → expect title \"About us\"\n2 of 4 checks are release-blocking"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** add a page `{ path: '/cart', expectedTitle: 'Your cart', critical: true }` and predict both the new line and the last line before running."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d7-practice-1",
              "title": "Quiz · Day 7 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d7-pr-q1",
                  "quizType": "single",
                  "question": "Which loop always runs its body at least once?",
                  "options": [
                    {
                      "id": "a",
                      "text": "for"
                    },
                    {
                      "id": "b",
                      "text": "while"
                    },
                    {
                      "id": "c",
                      "text": "do...while"
                    },
                    {
                      "id": "d",
                      "text": "for...of"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "`do...while` checks the condition AFTER each round, so the body runs at least once."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q2",
                  "quizType": "single",
                  "question": "What does `continue` do inside a loop?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Stops the loop completely"
                    },
                    {
                      "id": "b",
                      "text": "Skips the rest of the current round and moves to the next one"
                    },
                    {
                      "id": "c",
                      "text": "Restarts the loop from the first item"
                    },
                    {
                      "id": "d",
                      "text": "Pauses the program"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`continue` skips to the next round; `break` exits the loop."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q3",
                  "quizType": "single",
                  "question": "`function f(a: number, b?: number) { return b === undefined ? a : a + b; }` — what is `f(4)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "4"
                    },
                    {
                      "id": "b",
                      "text": "NaN"
                    },
                    {
                      "id": "c",
                      "text": "An error"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "`b` is optional and wasn't passed, so it is undefined and the function returns `a`."
                },
                {
                  "type": "quiz",
                  "id": "d7-pr-q4",
                  "quizType": "single",
                  "question": "What is a callback?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A phone call from the test runner"
                    },
                    {
                      "id": "b",
                      "text": "A function passed into another function, to be called later by that function"
                    },
                    {
                      "id": "c",
                      "text": "A function that calls itself forever"
                    },
                    {
                      "id": "d",
                      "text": "A type of loop"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "In `test('title', async ({ page }) => {…})`, the arrow function is a callback that Playwright calls when it runs the test."
                }
              ]
            },
            {
              "id": "d7-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p1",
                  "title": "Predict — loop with conditions",
                  "level": "easy",
                  "prompt": "What does this print?",
                  "code": "const statuses = [200, 301, 404, 500];\nfor (const code of statuses) {\n  if (code >= 500) {\n    console.log(code, 'server');\n    break;\n  } else if (code >= 400) {\n    console.log(code, 'client');\n    continue;\n  }\n  console.log(code, 'ok');\n}",
                  "answer": "200 ok\n301 ok\n404 client\n500 server\n\n404 hits `continue`, so 'ok' is skipped for it. 500 prints 'server' and `break` ends the loop (it was the last item anyway).",
                  "check": {
                    "kind": "self"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d7-pr-p3",
                  "title": "Predict — scope",
                  "level": "medium",
                  "prompt": "Which line does the type checker reject, and why?",
                  "code": "const suite = 'Login';\nfor (let i = 0; i < 2; i++) {\n  const title = `${suite} test ${i}`;\n  console.log(title);\n}\nconsole.log(title);",
                  "answer": "The last line: `console.log(title);` → \"Cannot find name 'title'\".\n\n`title` is declared with `const` inside the loop's block, so it only exists inside that block. `suite` is fine because it's declared outside.",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d7-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex1",
                  "title": "Test-run reporter",
                  "level": "easy",
                  "prompt": "Create `day7/reporter.ts`. Given\n```ts\nconst results: string[] = ['pass', 'fail', 'pass', 'skip', 'pass', 'fail'];\n```\nuse **one `for...of` loop** and **`if / else if / else`** to count passes, failures and skips, then print:\n```\nPassed: 3\nFailed: 2\nSkipped: 1\nVerdict: FAIL\n```\nThe verdict is `PASS` only when there are **no** failures (use a ternary).",
                  "file": "ts-basics/day7/reporter.ts",
                  "run": "node day7/reporter.ts",
                  "hints": [
                    "Create three `let` counters starting at 0 before the loop.",
                    "`const verdict = failed === 0 ? 'PASS' : 'FAIL';`"
                  ],
                  "solution": "const results: string[] = ['pass', 'fail', 'pass', 'skip', 'pass', 'fail'];\n\n// Counters\nlet passed = 0;\nlet failed = 0;\nlet skipped = 0;\n\n// Count each result\nfor (const result of results) {\n  if (result === 'pass') {\n    passed++;\n  } else if (result === 'fail') {\n    failed++;\n  } else {\n    skipped++;\n  }\n}\n\n// Verdict: PASS only if nothing failed\nconst verdict = failed === 0 ? 'PASS' : 'FAIL';\n\nconsole.log(`Passed: ${passed}`);\nconsole.log(`Failed: ${failed}`);\nconsole.log(`Skipped: ${skipped}`);\nconsole.log(`Verdict: ${verdict}`);",
                  "expectedOutput": "Passed: 3\nFailed: 2\nSkipped: 1\nVerdict: FAIL",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/reporter.ts",
                    "expected": "Passed: 3\nFailed: 2\nSkipped: 1\nVerdict: FAIL"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex2",
                  "title": "Priority mapper with switch",
                  "level": "easy",
                  "prompt": "Create `day7/priority.ts`. Write a function `slaHours(priority: 'P1' | 'P2' | 'P3' | 'P4'): number` that uses a **switch** to return the bug-fix deadline: P1 → 4, P2 → 24, P3 → 72, P4 → 168.\n\nThen loop over `['P1', 'P3', 'P4']` and print:\n```\nP1 must be fixed within 4 hours\nP3 must be fixed within 72 hours\nP4 must be fixed within 168 hours\n```",
                  "file": "ts-basics/day7/priority.ts",
                  "run": "node day7/priority.ts",
                  "hints": [
                    "With `return` inside each case you don't need `break` — `return` leaves the function immediately.",
                    "Type the list so TypeScript accepts it: `const priorities: ('P1' | 'P2' | 'P3' | 'P4')[] = ['P1', 'P3', 'P4'];`"
                  ],
                  "solution": "type Priority = 'P1' | 'P2' | 'P3' | 'P4';\n\n// Return the SLA (in hours) for a bug priority\nfunction slaHours(priority: Priority): number {\n  switch (priority) {\n    case 'P1':\n      return 4;\n    case 'P2':\n      return 24;\n    case 'P3':\n      return 72;\n    case 'P4':\n      return 168;\n  }\n}\n\nconst priorities: Priority[] = ['P1', 'P3', 'P4'];\nfor (const p of priorities) {\n  console.log(`${p} must be fixed within ${slaHours(p)} hours`);\n}",
                  "expectedOutput": "P1 must be fixed within 4 hours\nP3 must be fixed within 72 hours\nP4 must be fixed within 168 hours",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/priority.ts",
                    "expected": "P1 must be fixed within 4 hours\nP3 must be fixed within 72 hours\nP4 must be fixed within 168 hours"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d7-ex3",
                  "title": "Find the failed tests",
                  "level": "medium",
                  "prompt": "Create `day7/failed.ts` with this data:\n```ts\ntype TestResult = { title: string; status: 'passed' | 'failed' | 'skipped'; durationMs: number; };\nconst run: TestResult[] = [\n  { title: 'login works', status: 'passed', durationMs: 1200 },\n  { title: 'search works', status: 'failed', durationMs: 5300 },\n  { title: 'cart updates', status: 'passed', durationMs: 900 },\n  { title: 'checkout completes', status: 'failed', durationMs: 30000 },\n  { title: 'profile edits', status: 'skipped', durationMs: 0 },\n];\n```\n1. Use `filter` to get only the failed tests.\n2. Use `map` to get their titles.\n3. Write an **arrow function** `slowest(results: TestResult[]): TestResult` that loops over the list and returns the test with the largest `durationMs`.\n4. Print:\n```\nFailed (2): search works, checkout completes\nSlowest: checkout completes (30s)\n```",
                  "file": "ts-basics/day7/failed.ts",
                  "run": "node day7/failed.ts",
                  "hints": [
                    "`run.filter((r) => r.status === 'failed')`",
                    "In `slowest`, start with `let max = results[0];` and replace it whenever you find a longer duration."
                  ],
                  "solution": "type TestResult = {\n  title: string;\n  status: 'passed' | 'failed' | 'skipped';\n  durationMs: number;\n};\n\nconst run: TestResult[] = [\n  { title: 'login works', status: 'passed', durationMs: 1200 },\n  { title: 'search works', status: 'failed', durationMs: 5300 },\n  { title: 'cart updates', status: 'passed', durationMs: 900 },\n  { title: 'checkout completes', status: 'failed', durationMs: 30000 },\n  { title: 'profile edits', status: 'skipped', durationMs: 0 },\n];\n\n// 1 + 2: failed tests → their titles\nconst failedTests = run.filter((r) => r.status === 'failed');\nconst failedTitles = failedTests.map((r) => r.title);\n\n// 3: find the test with the largest duration\nconst slowest = (results: TestResult[]): TestResult => {\n  let max = results[0];\n  for (const r of results) {\n    if (r.durationMs > max.durationMs) {\n      max = r;\n    }\n  }\n  return max;\n};\n\nconst worst = slowest(run);\nconsole.log(`Failed (${failedTests.length}): ${failedTitles.join(', ')}`);\nconsole.log(`Slowest: ${worst.title} (${worst.durationMs / 1000}s)`);",
                  "expectedOutput": "Failed (2): search works, checkout completes\nSlowest: checkout completes (30s)",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day7/failed.ts",
                    "expected": "Failed (2): search works, checkout completes\nSlowest: checkout completes (30s)"
                  }
                }
              ]
            },
            {
              "id": "d7-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. When would you choose `switch` over `if / else if`?\n2. Write, from memory, an arrow function that takes a `name: string` and returns `` `Hello, ${name}` ``.\n3. Read this line aloud in plain English: `runStep('Open the home page', () => { … });`"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "`async`/`await` — the one concept behind every `await page.click()` — plus interfaces, destructuring, modules, and a mini test runner you build yourself.",
                  "title": "Coming up on Day 8"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 12,
        "quizzes": 12,
        "exercises": 5,
        "codeBlocks": 16
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 8,
      "week": 2,
      "title": "TypeScript II: Async, Modules & Your Own Test Runner",
      "subtitle": "Master async/await — the key to every Playwright line — then interfaces, destructuring, modules, and a mini test runner you build yourself",
      "estimatedTime": "3 hours",
      "topics": [
        "async/await & Promises",
        "Interfaces, destructuring & spread",
        "Modules (import/export)"
      ],
      "objectives": [
        "Use interfaces, object destructuring and the spread operator",
        "Explain Promises and use `async`/`await`; recognise the \"missing await\" bug",
        "Handle failures with try/catch/finally",
        "Split code into modules with `export` and `import`",
        "Build a mini test runner that mirrors how Playwright Test works"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 7: functions, arrow functions, callbacks, loops"
      ],
      "workspace": "pw-course/ts-basics/day8/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d8-prerequisites-1",
              "title": "P1 · Quick recap from Day 7",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Day 8 leans on functions and callbacks from Day 7. Check yourself:"
                },
                {
                  "type": "quiz",
                  "id": "d8-p1-q1",
                  "quizType": "single",
                  "question": "`const double = (n: number) => n * 2;` — what is `double(4)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "8"
                    },
                    {
                      "id": "b",
                      "text": "'44'"
                    },
                    {
                      "id": "c",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "A one-expression arrow function returns the expression's value: 4 * 2 = 8."
                },
                {
                  "type": "quiz",
                  "id": "d8-p1-q2",
                  "quizType": "single",
                  "question": "`runStep('Log in', () => console.log('typing…'));` — what is the second argument?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A string"
                    },
                    {
                      "id": "b",
                      "text": "A callback function that runStep can call whenever it wants"
                    },
                    {
                      "id": "c",
                      "text": "The result of console.log"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "A function passed to another function is a callback. `runStep` decides when to run it — exactly how Playwright's `test(title, callback)` works."
                },
                {
                  "type": "quiz",
                  "id": "d8-p1-q3",
                  "quizType": "single",
                  "question": "How many times does the body run? `for (const r of ['pass', 'fail']) { … }`",
                  "options": [
                    {
                      "id": "a",
                      "text": "1"
                    },
                    {
                      "id": "b",
                      "text": "2"
                    },
                    {
                      "id": "c",
                      "text": "3"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Once per item in the array."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d8-fundamentals-1",
              "title": "F1 · Interfaces, destructuring and spread",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Interfaces — another way to describe an object\n\nAn **interface** describes an object's shape, just like the `type` aliases from Day 6. For objects, both work; many teams use `interface` for objects and `type` for unions."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "interface Product {\n  name: string;\n  price: number;\n  inStock: boolean;\n}\n\nconst mouse: Product = { name: 'Wireless Mouse', price: 799, inStock: true };",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Object destructuring — unpack properties into variables"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "interface TestContext {\n  page: string;\n  browserName: string;\n  retry: number;\n}\n\nconst context: TestContext = { page: 'Page#1', browserName: 'firefox', retry: 0 };\n\n// Long way\nconst page1 = context.page;\n\n// Destructuring: pull out the properties you need, by name\nconst { page, browserName } = context;\nconsole.log(page1, page, browserName);\n\n// Destructuring directly in a function's parameter list\nfunction runTest({ page, browserName }: TestContext): void {\n  console.log(`Running on ${browserName} with ${page}`);\n}\nrunTest(context);",
                  "mode": "editor",
                  "file": "ts-basics/day8/destructuring.ts",
                  "run": "node day8/destructuring.ts",
                  "expectedOutput": "Page#1 Page#1 firefox\nRunning on firefox with Page#1",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Page#1 Page#1 firefox\nRunning on firefox with Page#1"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Playwright calls your test function with an object full of ready-made tools (**fixtures**): `page`, `context`, `browser`, `browserName`, `request`… Writing `({ page })` destructures that object and takes only the `page`. Writing `({ page, browserName })` takes two.",
                  "title": "Now you can read `async ({ page }) => {…}`"
                },
                {
                  "type": "markdown",
                  "content": "### Spread `...` — copy properties or items into a new object/array"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const defaults = { headless: true, timeout: 30000 };\nconst debugSettings = { ...defaults, headless: false };   // copy defaults, then override headless\n// → { headless: false, timeout: 30000 }\n\nconst coreBrowsers = ['chromium', 'firefox'];\nconst allBrowsers = [...coreBrowsers, 'webkit'];          // → ['chromium', 'firefox', 'webkit']",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "That's what this config line from Day 4 does: `use: { ...devices['Desktop Chrome'] }` — copy all the settings of the \"Desktop Chrome\" preset into `use`."
                },
                {
                  "type": "quiz",
                  "id": "d8-f4-q1",
                  "quizType": "single",
                  "question": "In `test('checkout', async ({ page, browserName }) => { … })`, what does `{ page, browserName }` do?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Creates a new page and a new browser"
                    },
                    {
                      "id": "b",
                      "text": "Destructures the fixtures object Playwright passes in, taking only `page` and `browserName`"
                    },
                    {
                      "id": "c",
                      "text": "Declares two global variables"
                    },
                    {
                      "id": "d",
                      "text": "Imports page and browserName from a file"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Playwright passes one object containing all fixtures; destructuring picks the ones your test needs."
                }
              ]
            },
            {
              "id": "d8-fundamentals-2",
              "title": "F2 · Asynchronous code: Promises, `async` and `await`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "This is the **most important concept for Playwright**. Every browser action takes time, so almost every Playwright line starts with `await`.\n\n### Synchronous vs asynchronous — the coffee-shop analogy\n\n- **Synchronous:** you order a coffee and stand frozen at the counter until it's ready. Nothing else happens.\n- **Asynchronous:** you order, get a **token**, and the barista calls you when it's ready. The token is a **Promise** — a promise of a future result.\n\nIn code, slow operations — loading a page, clicking and waiting for a response, reading a file — return a **Promise**. A Promise is in one of three states:"
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "stateDiagram-v2\n  [*] --> Pending: operation starts\n  Pending --> Fulfilled: success (gives a value)\n  Pending --> Rejected: failure (gives an error)"
                },
                {
                  "type": "markdown",
                  "content": "### `await` — wait for the Promise, then continue\n\n`await` pauses the current function until the Promise is finished, then gives you its value. You can only use `await` inside a function marked **`async`** (or at the top level of a module file)."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// A helper that returns a Promise which finishes after `ms` milliseconds.\n// (You won't write Promises by hand in Playwright — its methods return them for you.)\nconst wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\n// Pretend browser action: takes 300 ms, then returns the page title\nasync function loadPage(url: string): Promise<string> {\n  await wait(300);                     // pause here until 300 ms have passed\n  return `Title of ${url}`;\n}\n\nasync function main(): Promise<void> {\n  console.log('1. Opening page…');\n  const title = await loadPage('/home'); // wait for the result\n  console.log(`2. Loaded: ${title}`);\n  console.log('3. Now we can check the title safely');\n}\n\nmain();",
                  "mode": "editor",
                  "file": "ts-basics/day8/async-basics.ts",
                  "run": "node day8/async-basics.ts",
                  "expectedOutput": "1. Opening page…\n2. Loaded: Title of /home\n3. Now we can check the title safely",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "1. Opening page…\n2. Loaded: Title of /home\n3. Now we can check the title safely"
                },
                {
                  "type": "markdown",
                  "content": "Notice the return type `Promise<string>`: an `async` function **always** returns a Promise. The value inside is a `string`.\n\n### The #1 Playwright bug: a missing `await`"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\nasync function click(label: string): Promise<void> {\n  await wait(200);                     // clicking takes a moment\n  console.log(`   clicked \"${label}\"`);\n}\n\nasync function withoutAwait(): Promise<void> {\n  console.log('WITHOUT await:');\n  click('Log in');                     // ❌ no await — we don't wait for the click\n  console.log('   checking the dashboard…   ← too early!');\n}\n\nasync function withAwait(): Promise<void> {\n  console.log('WITH await:');\n  await click('Log in');               // ✅ wait until the click is done\n  console.log('   checking the dashboard…   ← correct order');\n}\n\nasync function main(): Promise<void> {\n  await withoutAwait();\n  await wait(300);                     // let the forgotten click finish printing\n  await withAwait();\n}\n\nmain();",
                  "mode": "editor",
                  "file": "ts-basics/day8/missing-await.ts",
                  "run": "node day8/missing-await.ts",
                  "expectedOutput": "WITHOUT await:\n   checking the dashboard…   ← too early!\n   clicked \"Log in\"\nWITH await:\n   clicked \"Log in\"\n   checking the dashboard…   ← correct order",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "WITHOUT await:\n   checking the dashboard…   ← too early!\n   clicked \"Log in\"\nWITH await:\n   clicked \"Log in\"\n   checking the dashboard…   ← correct order"
                },
                {
                  "type": "markdown",
                  "content": "Without `await`, the program **doesn't wait** — it rushes on and checks the dashboard before the click happened. In a real test that gives random (\"flaky\") failures or false passes."
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "If a Playwright method **does something in the browser** or **checks something that may take time** — `goto`, `click`, `fill`, `expect(…).toBeVisible()` — put `await` in front of it. VS Code's Playwright extension and linters (ESLint's `no-floating-promises` rule) can warn you when you forget.",
                  "title": "Rule for the rest of the course"
                },
                {
                  "type": "markdown",
                  "content": "### Handling failures: `try / catch / finally`\n\nWhen an awaited Promise is **rejected** (fails), it throws an error. You can catch it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\nasync function findElement(name: string): Promise<string> {\n  await wait(100);\n  if (name !== 'Log in') {\n    throw new Error(`Element \"${name}\" not found`);   // reject the Promise with an error\n  }\n  return `<button>${name}</button>`;\n}\n\nasync function main(): Promise<void> {\n  try {\n    const found = await findElement('Log in');\n    console.log('Found:', found);\n    await findElement('Sign up');                      // this one fails…\n    console.log('This line is skipped');\n  } catch (error) {\n    console.log('Caught:', (error as Error).message);  // …so we jump here\n  } finally {\n    console.log('Clean-up always runs');               // runs whether it failed or not\n  }\n}\n\nmain();",
                  "mode": "editor",
                  "file": "ts-basics/day8/try-catch.ts",
                  "run": "node day8/try-catch.ts",
                  "expectedOutput": "Found: <button>Log in</button>\nCaught: Element \"Sign up\" not found\nClean-up always runs",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "Found: <button>Log in</button>\nCaught: Element \"Sign up\" not found\nClean-up always runs"
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "In Playwright tests you rarely write `try/catch` yourself: when an action or assertion fails, the error travels up to the test runner, which marks the test as **failed** and shows the message. That's what you want."
                },
                {
                  "type": "markdown",
                  "content": "`(error as Error)` is a **type assertion**: in strict mode a caught error has the type `unknown`, and `as Error` tells TypeScript \"treat it as an Error object\" so we can read `.message`."
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q1",
                  "quizType": "single",
                  "question": "What does an `async` function always return?",
                  "options": [
                    {
                      "id": "a",
                      "text": "A string"
                    },
                    {
                      "id": "b",
                      "text": "A Promise"
                    },
                    {
                      "id": "c",
                      "text": "undefined"
                    },
                    {
                      "id": "d",
                      "text": "Whatever type you like, without a Promise"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "An async function always returns a Promise — `Promise<string>`, `Promise<void>`, etc. Use `await` to get the value inside."
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q2",
                  "quizType": "single",
                  "question": "A test does `page.getByRole('button', { name: 'Save' }).click();` (no await) and then immediately checks for a success message. What is the likely result?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Always passes — Playwright auto-waits anyway"
                    },
                    {
                      "id": "b",
                      "text": "Unreliable: the check may run before the click finished, causing random failures or false passes"
                    },
                    {
                      "id": "c",
                      "text": "A syntax error"
                    },
                    {
                      "id": "d",
                      "text": "The click happens twice"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Auto-waiting happens INSIDE the click — but without `await`, your test doesn't wait for the click to finish before moving on."
                },
                {
                  "type": "quiz",
                  "id": "d8-f5-q3",
                  "quizType": "truefalse",
                  "question": "You can use `await` inside any normal (non-async) function.",
                  "options": [
                    {
                      "id": "a",
                      "text": "True"
                    },
                    {
                      "id": "b",
                      "text": "False"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`await` only works inside functions marked `async` (or at the top level of a module). That's why Playwright tests are written as `async ({ page }) => { … }`."
                }
              ]
            },
            {
              "id": "d8-fundamentals-3",
              "title": "F3 · Modules: `export` and `import`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "As a project grows, you split code across files. Each file is a **module**. A module **exports** what others may use; other modules **import** it."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// file: utils/format.ts\nexport function formatPrice(amount: number): string {   // named export\n  return `₹${amount.toFixed(2)}`;\n}\nexport const CURRENCY = 'INR';                            // another named export\n\n// file: tests/cart.ts\nimport { formatPrice, CURRENCY } from '../utils/format';  // import by name, with curly braces",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Kind | Export | Import |\n|---|---|---|\n| **Named** (many per file) | `export function login() {}` | `import { login } from './auth';` |\n| **Default** (one per file) | `export default class LoginPage {}` | `import LoginPage from './LoginPage';` (no braces, any name) |\n\nPath rules: `'./x'` = same folder, `'../x'` = one folder up. A name **without** `./` (like `'@playwright/test'`) means an installed package from `node_modules`.\n\n### Decoding the first line of every Playwright test"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\n//      └─ named imports ─┘      └─ installed package in node_modules",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "\"From the installed package `@playwright/test`, bring in the two things called `test` and `expect`.\""
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "In the `ts-basics` playground (plain Node.js) you must write the extension: `import { x } from './utils.ts'`. Inside Playwright test files you normally leave it out — `import { x } from '../utils/helpers'` — because the Playwright runner resolves it for you.",
                  "title": "File extensions in imports"
                },
                {
                  "type": "quiz",
                  "id": "d8-f6-q1",
                  "quizType": "single",
                  "question": "`utils/data.ts` contains `export const adminUser = { … };`. How do you import it into `tests/admin.spec.ts`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`import adminUser from 'utils/data';`"
                    },
                    {
                      "id": "b",
                      "text": "`import { adminUser } from '../utils/data';`"
                    },
                    {
                      "id": "c",
                      "text": "`require adminUser;`"
                    },
                    {
                      "id": "d",
                      "text": "`import { adminUser } from '@playwright/test';`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "It's a named export, so use curly braces. From tests/ you go up one level (`../`) and into utils/."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [],
          "lessons": [
            {
              "id": "d8-implementation-1",
              "title": "I1 · Simulate a test flow with `async`/`await`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "These pretend \"browser actions\" behave like Playwright's: they take time and return Promises. Read the flow — it looks a lot like a real test."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\n// --- pretend browser API (Playwright gives you real versions of these) ---\nlet currentUrl = 'about:blank';\n\nasync function goto(url: string): Promise<void> {\n  await wait(150);\n  currentUrl = url;\n  console.log(`  goto ${url}`);\n}\n\nasync function fill(field: string, value: string): Promise<void> {\n  await wait(50);\n  console.log(`  fill ${field} = ${value}`);\n}\n\nasync function click(button: string): Promise<void> {\n  await wait(100);\n  if (button === 'Log in') {\n    currentUrl = '/dashboard';           // clicking Log in takes us to the dashboard\n  }\n  console.log(`  click ${button}`);\n}\n\n// --- the \"test\" ---\nasync function loginTest(): Promise<void> {\n  console.log('TEST: user can log in');\n  await goto('/login');\n  await fill('Email', 'asha@example.com');\n  await fill('Password', 'Secret@123');\n  await click('Log in');\n\n  // the \"assertion\"\n  if (currentUrl === '/dashboard') {\n    console.log('✓ PASSED — landed on /dashboard');\n  } else {\n    console.log(`✗ FAILED — expected /dashboard but was ${currentUrl}`);\n  }\n}\n\nloginTest();",
                  "mode": "editor",
                  "file": "ts-basics/day8/fake-browser.ts",
                  "run": "node day8/fake-browser.ts",
                  "expectedOutput": "TEST: user can log in\n  goto /login\n  fill Email = asha@example.com\n  fill Password = Secret@123\n  click Log in\n✓ PASSED — landed on /dashboard",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "TEST: user can log in\n  goto /login\n  fill Email = asha@example.com\n  fill Password = Secret@123\n  click Log in\n✓ PASSED — landed on /dashboard"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** remove the `await` in front of `click('Log in')` and run it again. The \"assertion\" runs before the click finishes and the test **fails** — the missing-await bug from F2, reproduced."
                }
              ]
            },
            {
              "id": "d8-implementation-2",
              "title": "I2 · Organise code into modules",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Move reusable helpers into their own file and import them.\n\nFirst the helper module:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Reusable helpers for our \"tests\"\n\n// Build a unique-looking test email from a name and a number\nexport function buildEmail(name: string, id: number, domain: string = 'example.com'): string {\n  return `${name.toLowerCase()}.${id}@${domain}`;\n}\n\n// Turn milliseconds into readable text: 1500 → \"1.5s\"\nexport function formatDuration(ms: number): string {\n  return ms < 1000 ? `${ms}ms` : `${ms / 1000}s`;\n}\n\n// A default export: the main settings object of this module\nconst settings = {\n  baseUrl: 'https://shop.example.com',\n  defaultTimeout: 30000,\n};\nexport default settings;",
                  "mode": "editor",
                  "file": "ts-basics/day8/helpers.ts"
                },
                {
                  "type": "markdown",
                  "content": "Then a file that uses it:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// default import (no braces) + named imports (with braces)\nimport settings, { buildEmail, formatDuration } from './helpers.ts';\n\nconst users = ['Asha', 'Ravi'];\n\nfor (let i = 0; i < users.length; i++) {\n  console.log(buildEmail(users[i], i + 1));\n}\nconsole.log(buildEmail('Meera', 3, 'test.org'));\n\nconsole.log(`Open ${settings.baseUrl}/login`);\nconsole.log(`Default timeout: ${formatDuration(settings.defaultTimeout)}`);\nconsole.log(`Quick action: ${formatDuration(250)}`);",
                  "mode": "editor",
                  "file": "ts-basics/day8/use-helpers.ts",
                  "run": "node day8/use-helpers.ts"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "node day8/use-helpers.ts",
                    "npm run check -- day8/use-helpers.ts"
                  ]
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "asha.1@example.com\nravi.2@example.com\nmeera.3@test.org\nOpen https://shop.example.com/login\nDefault timeout: 30s\nQuick action: 250ms"
                }
              ]
            },
            {
              "id": "d8-implementation-3",
              "title": "I3 · Build your own mini test runner",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Time to put **everything** from Days 5–8 together. You'll build a tiny version of what Playwright Test does: register tests with a title and an async callback, run them, catch failures and print a report. After this, Playwright's test runner will feel familiar."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// ---------- the mini \"framework\" ----------\ntype TestFunction = () => Promise<void>;          // a test body: an async function\n\ninterface RegisteredTest {\n  title: string;\n  body: TestFunction;\n}\n\nconst registeredTests: RegisteredTest[] = [];\n\n// 1. test(): register a test — it does NOT run it yet (same idea as Playwright)\nfunction test(title: string, body: TestFunction): void {\n  registeredTests.push({ title, body });\n}\n\n// 2. expectEqual(): a tiny assertion — throws an Error when the values differ\nfunction expectEqual(actual: unknown, expected: unknown): void {\n  if (actual !== expected) {\n    throw new Error(`Expected ${JSON.stringify(expected)} but received ${JSON.stringify(actual)}`);\n  }\n}\n\n// 3. run(): execute every registered test, one after another, and report\nasync function run(): Promise<void> {\n  let passed = 0;\n  for (const t of registeredTests) {             // for...of works with await\n    try {\n      await t.body();                            // run the test body and wait for it\n      console.log(`  ✓ ${t.title}`);\n      passed++;\n    } catch (error) {\n      console.log(`  ✘ ${t.title}`);\n      console.log(`      ${(error as Error).message}`);\n    }\n  }\n  console.log(`\\n  ${passed} passed, ${registeredTests.length - passed} failed`);\n}\n\n// ---------- the \"tests\" ----------\nconst wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\ntest('cart total adds up', async () => {\n  const prices = [499, 1299, 250];\n  let total = 0;\n  for (const price of prices) {\n    total += price;\n  }\n  expectEqual(total, 2048);\n});\n\ntest('page title is correct', async () => {\n  await wait(100);                               // pretend the page is loading\n  const title = 'My Shop – Home';\n  expectEqual(title, 'My Shop – Home');\n});\n\ntest('discount is applied', async () => {\n  const price = 1000;\n  const discounted = price * 0.9;\n  expectEqual(discounted, 850);                  // wrong expectation → this test fails\n});\n\nrun();",
                  "mode": "editor",
                  "file": "ts-basics/day8/mini-runner.ts",
                  "run": "node day8/mini-runner.ts",
                  "expectedOutput": "  ✓ cart total adds up\n  ✓ page title is correct\n  ✘ discount is applied\n      Expected 850 but received 900\n\n  2 passed, 1 failed",
                  "expectedOutputTarget": "console"
                },
                {
                  "type": "output",
                  "target": "console",
                  "content": "  ✓ cart total adds up\n  ✓ page title is correct\n  ✘ discount is applied\n      Expected 850 but received 900\n\n  2 passed, 1 failed"
                },
                {
                  "type": "markdown",
                  "content": "Compare your runner with real Playwright code you'll write tomorrow (Day 9):\n\n| Your mini runner | Playwright Test |\n|---|---|\n| `test('title', async () => { … })` | `test('title', async ({ page }) => { … })` |\n| `expectEqual(total, 2048)` | `expect(total).toBe(2048)` / `await expect(locator).toHaveText('…')` |\n| Tests registered first, run later by `run()` | Tests registered in `*.spec.ts` files, run by `npx playwright test` |\n| `try/catch` marks a test as failed and continues | The runner catches failures, marks the test failed and continues |\n| Runs tests one by one | Runs files in parallel with workers, one fresh page per test |"
                },
                {
                  "type": "quiz",
                  "id": "d8-i4-q1",
                  "quizType": "single",
                  "question": "In the mini runner, why does `run()` use `for...of` instead of `registeredTests.forEach(...)`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`forEach` doesn't exist on arrays"
                    },
                    {
                      "id": "b",
                      "text": "`for...of` works with `await`, so each test finishes before the next starts; `forEach` doesn't wait"
                    },
                    {
                      "id": "c",
                      "text": "`for...of` is faster to type"
                    },
                    {
                      "id": "d",
                      "text": "No reason — they behave identically here"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`forEach` ignores the Promise returned by an async callback. `for...of` inside an async function awaits each test in turn."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d8-practice-1",
              "title": "Quiz · Day 8 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d8-pr-q5",
                  "quizType": "single",
                  "question": "`const { name } = { name: 'Asha', role: 'admin' };` — what is `name`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "{ name: 'Asha' }"
                    },
                    {
                      "id": "b",
                      "text": "'Asha'"
                    },
                    {
                      "id": "c",
                      "text": "'admin'"
                    },
                    {
                      "id": "d",
                      "text": "undefined"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Object destructuring pulls out the property called `name`."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q6",
                  "quizType": "multiple",
                  "question": "Which lines in a Playwright test NEED `await`? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.goto('/login')`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByRole('button', { name: 'Log in' }).click()`"
                    },
                    {
                      "id": "c",
                      "text": "`await expect(page).toHaveURL(/dashboard/)` — the `expect(...)` web-first assertion"
                    },
                    {
                      "id": "d",
                      "text": "`const email = 'asha@example.com'`"
                    }
                  ],
                  "correct": [
                    "a",
                    "b",
                    "c"
                  ],
                  "explanation": "Browser actions and web-first assertions return Promises and must be awaited. Assigning a plain string doesn't."
                },
                {
                  "type": "quiz",
                  "id": "d8-pr-q7",
                  "quizType": "single",
                  "question": "What does `{ ...devices['Desktop Chrome'] }` do in playwright.config.ts?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Deletes the Desktop Chrome preset"
                    },
                    {
                      "id": "b",
                      "text": "Copies all properties of the Desktop Chrome preset into a new object"
                    },
                    {
                      "id": "c",
                      "text": "Launches three Chrome windows"
                    },
                    {
                      "id": "d",
                      "text": "Imports Chrome from node_modules"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The spread operator `...` copies properties from one object into another."
                }
              ]
            },
            {
              "id": "d8-practice-2",
              "title": "Predict the output",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "predict",
                  "id": "d8-pr-p2",
                  "title": "Predict — async order",
                  "level": "medium",
                  "prompt": "In which order are the letters printed?",
                  "code": "const wait = (ms: number) => new Promise<void>((r) => setTimeout(r, ms));\n\nasync function step(letter: string, ms: number) {\n  await wait(ms);\n  console.log(letter);\n}\n\nasync function main() {\n  console.log('A');\n  step('B', 200);          // no await!\n  await step('C', 100);\n  console.log('D');\n  await wait(200);\n}\nmain();",
                  "answer": "A\nC\nD\nB\n\n'B' starts but isn't awaited. `await step('C', 100)` waits 100 ms → prints C, then D. B finishes at 200 ms, so it prints last (the final `await wait(200)` keeps the program alive long enough).",
                  "check": {
                    "kind": "self"
                  }
                }
              ]
            },
            {
              "id": "d8-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex4",
                  "title": "Retry a flaky action",
                  "level": "hard",
                  "prompt": "Create `day8/retry.ts`. The function `flakyCheck()` below fails the first **two** times it is called and succeeds on the third — like a flaky service.\n\nWrite `async function retry(action: () => Promise<string>, maxAttempts: number): Promise<string>` that:\n- calls `await action()` inside `try/catch`,\n- prints `Attempt N failed: <message>` on failure and tries again,\n- returns the result as soon as it succeeds,\n- throws `new Error('Gave up after N attempts')` if every attempt fails.\n\nRun it with `maxAttempts = 3` and print the final result.\n\nExpected output:\n```\nAttempt 1 failed: Service unavailable\nAttempt 2 failed: Service unavailable\nResult: OK on call 3\n```",
                  "file": "ts-basics/day8/retry.ts",
                  "run": "node day8/retry.ts",
                  "starter": "const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\nlet calls = 0;\nasync function flakyCheck(): Promise<string> {\n  calls++;\n  await wait(50);\n  if (calls < 3) {\n    throw new Error('Service unavailable');\n  }\n  return `OK on call ${calls}`;\n}\n\n// TODO: write retry() here\n\nasync function main(): Promise<void> {\n  const result = await retry(flakyCheck, 3);\n  console.log(`Result: ${result}`);\n}\nmain();",
                  "hints": [
                    "Use a classic `for (let attempt = 1; attempt <= maxAttempts; attempt++)` loop.",
                    "`return` inside the `try` exits both the loop and the function on success.",
                    "After the loop ends, every attempt has failed — `throw` the \"Gave up\" error there."
                  ],
                  "solution": "const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));\n\nlet calls = 0;\nasync function flakyCheck(): Promise<string> {\n  calls++;\n  await wait(50);\n  if (calls < 3) {\n    throw new Error('Service unavailable');\n  }\n  return `OK on call ${calls}`;\n}\n\n// Try an async action up to maxAttempts times\nasync function retry(action: () => Promise<string>, maxAttempts: number): Promise<string> {\n  for (let attempt = 1; attempt <= maxAttempts; attempt++) {\n    try {\n      return await action();                                        // success → leave immediately\n    } catch (error) {\n      console.log(`Attempt ${attempt} failed: ${(error as Error).message}`);\n    }\n  }\n  throw new Error(`Gave up after ${maxAttempts} attempts`);         // every attempt failed\n}\n\nasync function main(): Promise<void> {\n  const result = await retry(flakyCheck, 3);\n  console.log(`Result: ${result}`);\n}\nmain();",
                  "expectedOutput": "Attempt 1 failed: Service unavailable\nAttempt 2 failed: Service unavailable\nResult: OK on call 3",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/retry.ts",
                    "expected": "Attempt 1 failed: Service unavailable\nAttempt 2 failed: Service unavailable\nResult: OK on call 3"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d8-ex5",
                  "title": "Challenge: upgrade the mini runner",
                  "level": "challenge",
                  "prompt": "Copy `day8/mini-runner.ts` to `day8/mini-runner-v2.ts` and add three features that Playwright Test also has:\n\n1. `beforeEach(fn)` — registers **one** async function that runs before **every** test (store it in a variable).\n2. `test.skip`-style skipping: a function `skip(title, body)` that registers a test which is **not run** and is reported as `- title (skipped)`.\n3. A summary line: `2 passed, 1 failed, 1 skipped`.\n\nUse these tests:\n```ts\nlet cart: number[] = [];\nbeforeEach(async () => { cart = []; });            // fresh cart for every test\ntest('adds one item', async () => { cart.push(499); expectEqual(cart.length, 1); });\ntest('cart starts empty', async () => { expectEqual(cart.length, 0); });\nskip('applies coupon', async () => { expectEqual(1, 2); });\ntest('total is right', async () => { cart.push(100, 200); expectEqual(cart.length, 3); });\n```\nExpected output:\n```\n  ✓ adds one item\n  ✓ cart starts empty\n  - applies coupon (skipped)\n  ✘ total is right\n      Expected 3 but received 2\n\n  2 passed, 1 failed, 1 skipped\n```\nNotice how `beforeEach` gives each test a clean cart — the same idea as Playwright's fresh page per test.",
                  "file": "ts-basics/day8/mini-runner-v2.ts",
                  "run": "node day8/mini-runner-v2.ts",
                  "hints": [
                    "Add a `skipped: boolean` property to `RegisteredTest`.",
                    "Store the hook: `let beforeEachHook: TestFunction | undefined;` and call it with `if (beforeEachHook) { await beforeEachHook(); }`.",
                    "The skipped test must still appear in its original position, so keep it in the same array."
                  ],
                  "solution": "type TestFunction = () => Promise<void>;\n\ninterface RegisteredTest {\n  title: string;\n  body: TestFunction;\n  skipped: boolean;\n}\n\nconst registeredTests: RegisteredTest[] = [];\nlet beforeEachHook: TestFunction | undefined;      // no hook until beforeEach() is called\n\nfunction test(title: string, body: TestFunction): void {\n  registeredTests.push({ title, body, skipped: false });\n}\n\nfunction skip(title: string, body: TestFunction): void {\n  registeredTests.push({ title, body, skipped: true });\n}\n\nfunction beforeEach(fn: TestFunction): void {\n  beforeEachHook = fn;\n}\n\nfunction expectEqual(actual: unknown, expected: unknown): void {\n  if (actual !== expected) {\n    throw new Error(`Expected ${JSON.stringify(expected)} but received ${JSON.stringify(actual)}`);\n  }\n}\n\nasync function run(): Promise<void> {\n  let passed = 0;\n  let failed = 0;\n  let skippedCount = 0;\n\n  for (const t of registeredTests) {\n    if (t.skipped) {                                // don't run skipped tests\n      console.log(`  - ${t.title} (skipped)`);\n      skippedCount++;\n      continue;\n    }\n    try {\n      if (beforeEachHook) {\n        await beforeEachHook();                     // fresh state before every test\n      }\n      await t.body();\n      console.log(`  ✓ ${t.title}`);\n      passed++;\n    } catch (error) {\n      console.log(`  ✘ ${t.title}`);\n      console.log(`      ${(error as Error).message}`);\n      failed++;\n    }\n  }\n  console.log(`\\n  ${passed} passed, ${failed} failed, ${skippedCount} skipped`);\n}\n\n// ---------- the tests ----------\nlet cart: number[] = [];\nbeforeEach(async () => { cart = []; });\ntest('adds one item', async () => { cart.push(499); expectEqual(cart.length, 1); });\ntest('cart starts empty', async () => { expectEqual(cart.length, 0); });\nskip('applies coupon', async () => { expectEqual(1, 2); });\ntest('total is right', async () => { cart.push(100, 200); expectEqual(cart.length, 3); });\n\nrun();",
                  "expectedOutput": "  ✓ adds one item\n  ✓ cart starts empty\n  - applies coupon (skipped)\n  ✘ total is right\n      Expected 3 but received 2\n\n  2 passed, 1 failed, 1 skipped",
                  "check": {
                    "kind": "stdoutEquals",
                    "cwd": "pw-course/ts-basics",
                    "run": "node day8/mini-runner-v2.ts",
                    "expected": "  ✓ adds one item\n  ✓ cart starts empty\n  - applies coupon (skipped)\n  ✘ total is right\n      Expected 3 but received 2\n\n  2 passed, 1 failed, 1 skipped"
                  }
                }
              ]
            },
            {
              "id": "d8-practice-4",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. In one sentence: what does `await` do, and what happens if you forget it before `page.click()`?\n2. Read this line aloud in plain English: `test('login', async ({ page }) => { … });`\n3. What is the difference between a named export and a default export?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "You've built a mini test runner. Tomorrow you'll use the real one: `test`, `expect`, fixtures, locators, assertions, `describe` and hooks — and write a complete suite for a practice web page.",
                  "title": "Coming up on Day 9"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 11,
        "quizzes": 12,
        "exercises": 3,
        "codeBlocks": 12
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 9,
      "week": 2,
      "title": "Playwright Test Runner I: Your First Real Tests",
      "subtitle": "The anatomy of a test, fixtures, locators, actions, assertions, describe and hooks — and a complete suite for a practice sign-in page",
      "estimatedTime": "3 hours",
      "topics": [
        "Playwright Test Runner Basics",
        "Locators, actions & assertions",
        "describe & hooks"
      ],
      "objectives": [
        "Explain every part of a Playwright test — import, test(), title, async callback, fixtures, await, expect",
        "Use the built-in fixtures `page`, `context`, `browser`, `browserName` and `request`",
        "Find elements with user-facing locators and perform basic actions (click, fill, check, selectOption, press)",
        "Write web-first assertions and generic assertions, and know which ones need `await`",
        "Group tests with `test.describe` and share setup with `beforeEach`, `afterEach`, `beforeAll`, `afterAll`"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 3–4: the pw-course project, running tests, playwright.config.ts",
        "Day 7–8: callbacks, arrow functions, destructuring, async/await, import/export"
      ],
      "workspace": "pw-course/tests/day9/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d9-prerequisites-1",
              "title": "P1 · Checklist before you start",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "You're ready for today if you can tick all of these. If not, revisit the lesson in brackets.\n\n- [ ] I can open the terminal in `pw-course` and run `npx playwright test --project=chromium` (Day 3 · I4, Day 4 · I2)\n- [ ] My `playwright.config.ts` uses `reporter: [['list'], ['html', { open: 'never' }]]` (Day 4 · I1)\n- [ ] I can read `async ({ page }) => { … }` as \"an async arrow function that destructures `page`\" (Day 7 · F3, Day 8 · F1)\n- [ ] I know why every browser action needs `await` (Day 8 · F2)\n- [ ] I can import a named export from another file (Day 8 · F3)"
                }
              ]
            },
            {
              "id": "d9-prerequisites-2",
              "title": "P2 · HTML roles — how Playwright \"sees\" a page",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Playwright's recommended way to find elements is the way **users and assistive technologies** (screen readers) see them: by **role** and **accessible name**.\n\nMost HTML elements have a built-in role:\n\n| HTML | Role | Accessible name comes from… |\n|---|---|---|\n| `<button>Sign in</button>` | `button` | its text → \"Sign in\" |\n| `<a href=\"/help\">Help</a>` | `link` | its text → \"Help\" |\n| `<h1>Dashboard</h1>` (also h2–h6) | `heading` | its text → \"Dashboard\" |\n| `<input type=\"text\">` / `type=\"email\"` with a `<label>` | `textbox` | its label → e.g. \"Email\" |\n| `<input type=\"checkbox\">` | `checkbox` | its label → e.g. \"Remember me\" |\n| `<select>` | `combobox` | its label → e.g. \"Course\" |\n| `<ul>` / `<li>` | `list` / `listitem` | — |\n| `<p role=\"alert\">` | `alert` | its text |\n\nSo `page.getByRole('button', { name: 'Sign in' })` means: *the button whose accessible name is \"Sign in\"* — exactly how you'd describe it in a manual test case."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "A **password** input (`type=\"password\"`) has no ARIA role, so `getByRole('textbox')` won't find it — find it by its label instead: `page.getByLabel('Password')`."
                },
                {
                  "type": "quiz",
                  "id": "d9-p2-q1",
                  "quizType": "single",
                  "question": "Which locator finds `<a href=\"/pricing\">See pricing</a>`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.getByRole('button', { name: 'See pricing' })`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByRole('link', { name: 'See pricing' })`"
                    },
                    {
                      "id": "c",
                      "text": "`page.getByRole('heading', { name: 'pricing' })`"
                    },
                    {
                      "id": "d",
                      "text": "`page.getByLabel('See pricing')`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "An `<a href>` element has the role `link`, and its accessible name is its text."
                }
              ]
            },
            {
              "id": "d9-prerequisites-3",
              "title": "P3 · The practice website for Days 9–10",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Real websites change, go down, or need a VPN — not ideal for learning. For the next two days you'll test two small **practice pages** that live inside your project as HTML text, and load them with `page.setContent(html)` instead of `page.goto(url)`. Everything you learn works the same on real sites.\n\nCreate the file `tests/day9/practice-pages.ts`. It's a normal module (Day 8!) that **exports** two HTML strings. Because its name doesn't end in `.spec.ts`, Playwright won't treat it as a test file."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// Practice web pages for Days 9–10.\n// Each constant holds the HTML of one page. Tests load it with: await page.setContent(loginPage);\n\n// ---------------------------------------------------------------------------\n// Page 1: QA Academy sign-in\n//   Valid user:   student@qa.academy / Learn@123  → \"Signing in…\" then (0.8 s later) the Dashboard\n//   Empty fields: \"Please enter your email and password\"\n//   Wrong login:  \"Invalid email or password\"\n// ---------------------------------------------------------------------------\nexport const loginPage = `\n<!DOCTYPE html>\n<html lang=\"en\">\n<head><title>QA Academy - Sign in</title></head>\n<body>\n  <h1>Sign in to QA Academy</h1>\n  <form id=\"login-form\">\n    <label for=\"email\">Email</label>\n    <input id=\"email\" type=\"email\" placeholder=\"you@example.com\">\n\n    <label for=\"password\">Password</label>\n    <input id=\"password\" type=\"password\" placeholder=\"Your password\">\n\n    <label><input id=\"remember\" type=\"checkbox\"> Remember me</label>\n\n    <button type=\"submit\">Sign in</button>\n  </form>\n  <p id=\"message\" role=\"alert\" data-testid=\"login-message\"></p>\n\n  <script>\n    const form = document.getElementById('login-form');\n    const message = document.getElementById('message');\n\n    form.addEventListener('submit', (event) => {\n      event.preventDefault();\n      const email = document.getElementById('email').value.trim();\n      const password = document.getElementById('password').value;\n\n      if (email === '' || password === '') {\n        message.textContent = 'Please enter your email and password';\n      } else if (email === 'student@qa.academy' && password === 'Learn@123') {\n        message.textContent = 'Signing in…';\n        setTimeout(showDashboard, 800);          // pretend the server is slow\n      } else {\n        message.textContent = 'Invalid email or password';\n      }\n    });\n\n    function showDashboard() {\n      document.title = 'QA Academy - Dashboard';\n      document.body.innerHTML =\n        '<h1>Dashboard</h1>' +\n        '<p>Welcome back, Student!</p>' +\n        '<ul><li>Weeks 1-2 - Fundamentals</li><li>Week 3 - Locators</li><li>Week 4 - Page Objects</li></ul>' +\n        '<button id=\"logout\">Log out</button>';\n      document.getElementById('logout').addEventListener('click', () => {\n        document.title = 'QA Academy - Signed out';\n        document.body.innerHTML = '<h1>Signed out</h1><p>See you soon!</p>';\n      });\n    }\n  </script>\n</body>\n</html>\n`;\n\n// ---------------------------------------------------------------------------\n// Page 2: QA Academy course enrolment (used in the Day 10 mini-project)\n// ---------------------------------------------------------------------------\nexport const enrolPage = `\n<!DOCTYPE html>\n<html lang=\"en\">\n<head><title>QA Academy - Enrol</title></head>\n<body>\n  <h1>Enrol in a course</h1>\n  <form id=\"enrol-form\">\n    <label for=\"name\">Full name</label>\n    <input id=\"name\" placeholder=\"e.g. Asha Verma\">\n\n    <label for=\"enrol-email\">Email</label>\n    <input id=\"enrol-email\" type=\"text\" placeholder=\"you@example.com\">\n\n    <label for=\"course\">Course</label>\n    <select id=\"course\">\n      <option value=\"\">-- choose a course --</option>\n      <option value=\"pw\">Playwright Basics</option>\n      <option value=\"api\">API Testing</option>\n      <option value=\"perf\">Performance Testing</option>\n    </select>\n\n    <label><input id=\"terms\" type=\"checkbox\"> I accept the terms</label>\n\n    <button type=\"submit\" disabled>Enrol now</button>\n  </form>\n  <p data-testid=\"seats\">Seats left: 12</p>\n  <p id=\"result\" role=\"status\"></p>\n\n  <script>\n    let seats = 12;\n    const terms = document.getElementById('terms');\n    const submit = document.querySelector('button[type=submit]');\n    const result = document.getElementById('result');\n\n    // The Enrol button is enabled only while the terms box is ticked\n    terms.addEventListener('change', () => {\n      submit.disabled = !terms.checked;\n    });\n\n    document.getElementById('enrol-form').addEventListener('submit', (event) => {\n      event.preventDefault();\n      const name = document.getElementById('name').value.trim();\n      const email = document.getElementById('enrol-email').value.trim();\n      const course = document.getElementById('course');\n\n      if (name === '') { result.textContent = 'Name is required'; return; }\n      if (!email.includes('@')) { result.textContent = 'Enter a valid email'; return; }\n      if (course.value === '') { result.textContent = 'Please choose a course'; return; }\n\n      seats = seats - 1;\n      document.querySelector('[data-testid=seats]').textContent = 'Seats left: ' + seats;\n      const courseName = course.options[course.selectedIndex].text;\n      const firstName = name.split(' ')[0];\n      result.textContent = 'Thanks, ' + firstName + '! You are enrolled in ' + courseName + '.';\n    });\n  </script>\n</body>\n</html>\n`;",
                  "mode": "editor",
                  "file": "tests/day9/practice-pages.ts"
                },
                {
                  "type": "callout",
                  "variant": "tester",
                  "content": "Read the comments at the top of each page like a requirements document. Your tests will check each rule — just as you'd derive manual test cases from requirements."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d9-fundamentals-1",
              "title": "F1 · Anatomy of a Playwright test",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';          // 1. import the tools\n\ntest('valid user can sign in', async ({ page }) => {      // 2. test(title, async callback)\n  await page.setContent(loginPage);                        // 3. ARRANGE — get to the start state\n  await page.getByLabel('Email').fill('student@qa.academy');   // 4. ACT — do what a user does\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  await expect(page.getByRole('heading')).toHaveText('Dashboard');   // 5. ASSERT — check the result\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "| Part | Meaning |\n|---|---|\n| `import { test, expect } from '@playwright/test'` | Bring in the test-declaring function and the assertion function |\n| `test('valid user can sign in', …)` | Register a test with a **title** that reads like a test-case name |\n| `async ({ page }) => { … }` | The test body: an **async** callback. Playwright calls it and passes **fixtures**; we destructure `page` |\n| `await page.…` | Each browser step returns a Promise — `await` it |\n| `await expect(…).toHaveText(…)` | An **assertion** — the expected result |\n\nThe **Arrange → Act → Assert** pattern (also called *Given / When / Then*) keeps tests readable: set up, perform the action, check the outcome."
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Write titles as behaviour: `'shows an error for a wrong password'`, not `'test2'`. The title is what you'll read in reports at 2 a.m. when something breaks.",
                  "title": "Good test titles"
                },
                {
                  "type": "quiz",
                  "id": "d9-f1-q1",
                  "quizType": "single",
                  "question": "Why is the test body written as `async ({ page }) => { … }`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`async` makes the test run faster"
                    },
                    {
                      "id": "b",
                      "text": "It's an async callback, so it can use `await`; Playwright passes a fixtures object and `{ page }` destructures the page from it"
                    },
                    {
                      "id": "c",
                      "text": "`page` is a global variable that must be declared this way"
                    },
                    {
                      "id": "d",
                      "text": "It's required by the TypeScript compiler for all functions"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The callback must be `async` to use `await`, and `{ page }` picks the page fixture out of the object Playwright passes in."
                }
              ]
            },
            {
              "id": "d9-fundamentals-2",
              "title": "F2 · Fixtures: tools the runner hands you",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "**Fixtures** are ready-made objects that Playwright creates **before** your test and cleans up **after** it. You ask for them by name in the callback's parameters.\n\n| Fixture | What you get | Typical use |\n|---|---|---|\n| `page` | A fresh tab in a fresh context | 95% of UI tests |\n| `context` | The browser context that `page` belongs to | Open a second tab, set cookies/permissions |\n| `browser` | The shared browser instance | Create extra contexts (multi-user tests), `beforeAll` |\n| `browserName` | `'chromium'`, `'firefox'` or `'webkit'` | Browser-specific skips or expectations |\n| `request` | An API client — no browser needed | API tests, creating test data via API |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('fixtures demo', async ({ page, context, browserName }) => {\n  console.log(`Running on ${browserName}`);\n  const secondTab = await context.newPage();   // another tab in the SAME context as page\n  // …\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Playwright only creates the fixtures you ask for — and because each test gets its **own** `page` and `context`, tests are isolated from each other (Day 2 · F2)."
                },
                {
                  "type": "quiz",
                  "id": "d9-f2-q1",
                  "quizType": "single",
                  "question": "A test must check that an admin and a customer see different menus at the same time. Which fixture helps you create two separate sessions?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`browserName`"
                    },
                    {
                      "id": "b",
                      "text": "`browser` — to create two new contexts"
                    },
                    {
                      "id": "c",
                      "text": "`request`"
                    },
                    {
                      "id": "d",
                      "text": "Two `page` fixtures in the same test"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`browser.newContext()` twice gives two isolated sessions (admin and customer) in one test — like Day 2's contexts demo."
                }
              ]
            },
            {
              "id": "d9-fundamentals-3",
              "title": "F3 · Finding elements and acting on them",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Week 3 is all about locators. For now, learn the core set.\n\n### Getting to a page\n\n| Method | Use |\n|---|---|\n| `await page.goto('https://shop.example.com/login')` | Open a URL (with `baseURL` in the config you can write `page.goto('/login')`) |\n| `await page.setContent(html)` | Load HTML directly — used for today's practice pages |\n| `await page.title()` / `page.url()` | Read the current title / URL |\n\n### Locators — \"how to find the element\"\n\nPrefer them in this order (user-facing first):\n\n| Locator | Finds… | Example |\n|---|---|---|\n| `getByRole(role, { name })` | By role + accessible name | `page.getByRole('button', { name: 'Sign in' })` |\n| `getByLabel(text)` | Form field by its label | `page.getByLabel('Email')` |\n| `getByPlaceholder(text)` | Input by placeholder | `page.getByPlaceholder('you@example.com')` |\n| `getByText(text)` | Element by visible text | `page.getByText('Welcome back')` |\n| `getByTestId(id)` | By `data-testid` attribute | `page.getByTestId('login-message')` |\n| `locator(css)` | By CSS selector (fallback) | `page.locator('#email')` |\n\nText matching is **case-insensitive and matches part of the text** by default: `getByText('welcome')` finds \"Welcome back, Student!\". Add `{ exact: true }` for an exact match.\n\nA locator is a **recipe**, not the element itself: creating it doesn't touch the page (so no `await`). The search happens when you act or assert — and that's when auto-waiting kicks in."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "const signIn = page.getByRole('button', { name: 'Sign in' });   // just a recipe — no await\nawait signIn.click();                                             // now it finds, waits, clicks",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "### Actions\n\n| Action | Example |\n|---|---|\n| Click | `await page.getByRole('button', { name: 'Sign in' }).click()` |\n| Type into a field (replaces its value) | `await page.getByLabel('Email').fill('student@qa.academy')` |\n| Press a key | `await page.getByLabel('Password').press('Enter')` |\n| Tick / untick a checkbox | `await page.getByLabel('Remember me').check()` / `.uncheck()` |\n| Choose from a `<select>` | `await page.getByLabel('Course').selectOption('API Testing')` |\n| Clear a field | `await page.getByLabel('Email').clear()` |"
                },
                {
                  "type": "quiz",
                  "id": "d9-f3-q1",
                  "quizType": "single",
                  "question": "Which line needs NO `await`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.getByLabel('Email').fill('a@b.com')`"
                    },
                    {
                      "id": "b",
                      "text": "`const email = page.getByLabel('Email')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.getByRole('button', { name: 'Save' }).click()`"
                    },
                    {
                      "id": "d",
                      "text": "`page.setContent(html)`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Creating a locator only describes how to find the element — it doesn't talk to the browser yet. Actions like fill, click and setContent do, so they're awaited."
                }
              ]
            },
            {
              "id": "d9-fundamentals-4",
              "title": "F4 · Assertions with `expect`",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### Web-first assertions — auto-retrying (use `await`!)\n\nThese keep checking the page until the condition is true or the timeout expires (**5 seconds** by default).\n\n**Learn these five now** — they cover most of what a test checks:\n\n1. `await expect(locator).toBeVisible()` — the element is on screen.\n2. `await expect(locator).toHaveText('Dashboard')` — its text **equals** this (a regex such as `/Dash/` matches part of it).\n3. `await expect(locator).toContainText('Welcome')` — its text **contains** this.\n4. `await expect(page).toHaveTitle(/Dashboard/)` — the tab title matches.\n5. `await expect(page).toHaveURL(/dashboard/)` — the URL matches.\n\nThe others follow the same pattern — open this when you need one:"
                },
                {
                  "type": "reference",
                  "title": "More web-first assertions",
                  "content": "| Assertion | Passes when… |\n|---|---|\n| `await expect(locator).toBeHidden()` | it's hidden or doesn't exist |\n| `await expect(locator).toHaveValue('a@b.com')` | an input's current value is this |\n| `await expect(locator).toBeEmpty()` | an input/element has no value or text |\n| `await expect(locator).toBeChecked()` | a checkbox/radio is ticked |\n| `await expect(locator).toBeEnabled()` / `.toBeDisabled()` | the element is (not) enabled |\n| `await expect(locator).toHaveCount(3)` | the locator matches exactly 3 elements |\n| `await expect(locator).toHaveAttribute('type', 'email')` | an attribute has this value |\n| `await expect(locator).toHaveClass(/active/)` | the class attribute matches |\n| `await expect(locator).toBeFocused()` | the element has keyboard focus |"
                },
                {
                  "type": "markdown",
                  "content": "Add `.not` to invert: `await expect(page.getByRole('alert')).not.toBeEmpty()`, `await expect(button).not.toBeVisible()`.\n\n### Generic assertions — check a plain value once (no `await`)\n\nFor values you already have in a variable — no retrying:\n\n| Assertion | Example |\n|---|---|\n| `toBe` (strict equal, like `===`) | `expect(total).toBe(3)` |\n| `toEqual` (same content — for objects/arrays) | `expect(tags).toEqual(['@smoke', '@login'])` |\n| `toContain` | `expect(title).toContain('Academy')` |\n| `toBeTruthy` / `toBeFalsy` | `expect(isVisible).toBeTruthy()` |\n| `toBeGreaterThan` / `toBeLessThan` | `expect(price).toBeGreaterThan(0)` |\n| `toHaveLength` | `expect(items).toHaveLength(3)` |"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "// ❌ Flaky: reads the text ONCE, maybe before the page updated\nconst text = await page.getByRole('alert').textContent();\nexpect(text).toBe('Invalid email or password');\n\n// ✅ Reliable: retries until the text matches (up to 5 s)\nawait expect(page.getByRole('alert')).toHaveText('Invalid email or password');",
                  "mode": "read"
                },
                {
                  "type": "callout",
                  "variant": "warning",
                  "content": "Prefer **web-first** assertions for anything on the page. Use generic assertions only for plain values (numbers you calculated, arrays, API data)."
                },
                {
                  "type": "markdown",
                  "content": "You can add a custom message that appears in the report when the assertion fails:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "await expect(page.getByRole('heading'), 'user should land on the dashboard').toHaveText('Dashboard');",
                  "mode": "read"
                },
                {
                  "type": "quiz",
                  "id": "d9-f4-q1",
                  "quizType": "single",
                  "question": "After clicking \"Sign in\" with a wrong password, the page's script shows \"Invalid email or password\" — on a slow machine this can take a moment. Which assertion is reliable?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`expect(await page.getByRole('alert').textContent()).toBe('Invalid email or password')`"
                    },
                    {
                      "id": "b",
                      "text": "`await expect(page.getByRole('alert')).toHaveText('Invalid email or password')`"
                    },
                    {
                      "id": "c",
                      "text": "`expect(page.getByRole('alert')).toHaveText('Invalid email or password')` without await"
                    },
                    {
                      "id": "d",
                      "text": "`page.waitForTimeout(800)` then a generic toBe"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The web-first assertion retries until it matches. Option A reads the text once (maybe too early). Option C forgets await. Fixed waits (D) are slow and still flaky."
                },
                {
                  "type": "quiz",
                  "id": "d9-f4-q2",
                  "quizType": "single",
                  "question": "What's the difference between `toHaveText('Welcome')` and `toContainText('Welcome')`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "None"
                    },
                    {
                      "id": "b",
                      "text": "`toHaveText` needs the whole text to match; `toContainText` passes if 'Welcome' appears anywhere in it"
                    },
                    {
                      "id": "c",
                      "text": "`toContainText` is case-sensitive, `toHaveText` isn't"
                    },
                    {
                      "id": "d",
                      "text": "`toHaveText` only works on headings"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "For \"Welcome back, Student!\", `toContainText('Welcome')` passes but `toHaveText('Welcome')` fails (with a plain string, toHaveText compares the full text)."
                }
              ]
            },
            {
              "id": "d9-fundamentals-5",
              "title": "F5 · Grouping tests and sharing setup: `describe` and hooks",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "### `test.describe` — a group (like a test suite / folder in a test-management tool)"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test.describe('Sign in page', () => {\n  test('valid user reaches the dashboard', async ({ page }) => { /* … */ });\n  test('wrong password shows an error', async ({ page }) => { /* … */ });\n});",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "Report titles include the group: `Sign in page › wrong password shows an error`. Groups can be nested.\n\n### Hooks — run code around tests\n\n| Hook | Runs | Gets `page`? | Typical use |\n|---|---|---|---|\n| `test.beforeEach` | before **each** test in the file/group | ✅ yes | open the page, log in |\n| `test.afterEach` | after **each** test | ✅ yes | extra clean-up, logging |\n| `test.beforeAll` | **once** before all tests in the file/group (per worker) | ❌ no — use `browser` | expensive one-time setup, e.g. create test data via API |\n| `test.afterAll` | **once** after all tests (per worker) | ❌ no | delete test data |"
                },
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart TD\n  BA[\"beforeAll (once)\"] --> BE1[\"beforeEach\"] --> T1[\"test 1\"] --> AE1[\"afterEach\"]\n  AE1 --> BE2[\"beforeEach\"] --> T2[\"test 2\"] --> AE2[\"afterEach\"]\n  AE2 --> AA[\"afterAll (once)\"]"
                },
                {
                  "type": "markdown",
                  "content": "Hooks declared **inside** a `describe` apply only to tests in that group; hooks at the top of the file apply to every test in the file."
                },
                {
                  "type": "callout",
                  "variant": "note",
                  "content": "With parallel workers, each worker process runs its **own** `beforeAll`/`afterAll` for the tests it receives. Don't rely on `beforeAll` to share state between tests — each test should be able to run on its own.",
                  "title": "\"Per worker\""
                },
                {
                  "type": "quiz",
                  "id": "d9-f5-q1",
                  "quizType": "single",
                  "question": "A file has `beforeAll`, `beforeEach` and 3 tests, all run by ONE worker. How many times does each hook run?",
                  "options": [
                    {
                      "id": "a",
                      "text": "beforeAll 3, beforeEach 3"
                    },
                    {
                      "id": "b",
                      "text": "beforeAll 1, beforeEach 3"
                    },
                    {
                      "id": "c",
                      "text": "beforeAll 1, beforeEach 1"
                    },
                    {
                      "id": "d",
                      "text": "beforeAll 3, beforeEach 1"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "beforeAll runs once per worker for the file; beforeEach runs before every test."
                },
                {
                  "type": "quiz",
                  "id": "d9-f5-q2",
                  "quizType": "single",
                  "question": "Why does `test.beforeAll(async ({ page }) => { … })` throw an error?",
                  "options": [
                    {
                      "id": "a",
                      "text": "beforeAll cannot be async"
                    },
                    {
                      "id": "b",
                      "text": "The `page` fixture is created per test, so it isn't available in beforeAll — use `browser` instead"
                    },
                    {
                      "id": "c",
                      "text": "beforeAll must be inside a describe"
                    },
                    {
                      "id": "d",
                      "text": "Hooks can't receive fixtures at all"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`page` belongs to a single test. `beforeAll` runs outside any single test, so it can only use worker-level fixtures such as `browser`."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [],
          "lessons": [
            {
              "id": "d9-implementation-1",
              "title": "I1 · Your first test from scratch",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Make sure `tests/day9/practice-pages.ts` exists (Prerequisites · P3). Now create `tests/day9/first.spec.ts`:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { loginPage } from './practice-pages';   // our practice page (no .ts needed in test files)\n\ntest('valid user can sign in', async ({ page }) => {\n  // Arrange: open the sign-in page\n  await page.setContent(loginPage);\n  await expect(page).toHaveTitle('QA Academy - Sign in');\n\n  // Act: sign in like a user would\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n\n  // Assert: the dashboard appears (after ~0.8 s — Playwright waits for it)\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n  await expect(page.getByText('Welcome back')).toBeVisible();\n  await expect(page).toHaveTitle(/Dashboard/);\n});",
                  "mode": "editor",
                  "file": "tests/day9/first.spec.ts",
                  "run": "npx playwright test tests/day9/first.spec.ts --project=chromium --headed"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9/first.spec.ts --project=chromium --headed"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 1 test using 1 worker\n\n  ✓  1 [chromium] › tests/day9/first.spec.ts:4:5 › valid user can sign in (1.2s)\n\n  1 passed (1.9s)"
                },
                {
                  "type": "markdown",
                  "content": "Watch the browser pane: the form fills in, \"Signing in…\" shows briefly, then the Dashboard appears.\n\n**Try it:** change `'Learn@123'` to `'learn@123'` (lowercase L) and run again. Read the error — which line failed, what was expected, what was received? Then change it back."
                }
              ]
            },
            {
              "id": "d9-implementation-2",
              "title": "I2 · A complete suite with `describe`, `beforeEach` and tags",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Now test **every rule** of the sign-in page. Opening the page is the same for every test, so it moves into `beforeEach` (lesson F5). The tests also carry **tags** such as `{ tag: '@smoke' }` — labels you'll use to run subsets on Day 10; for now just notice them."
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { loginPage } from './practice-pages';\n\ntest.describe('Sign in page', () => {\n  // Runs before EACH test in this group: every test starts on a fresh sign-in page\n  test.beforeEach(async ({ page }) => {\n    await page.setContent(loginPage);\n  });\n\n  test('shows the sign-in form', { tag: '@smoke' }, async ({ page }) => {\n    await expect(page.getByRole('heading', { name: 'Sign in to QA Academy' })).toBeVisible();\n    await expect(page.getByLabel('Email')).toBeEmpty();\n    await expect(page.getByPlaceholder('you@example.com')).toHaveAttribute('type', 'email');\n    await expect(page.getByRole('button', { name: 'Sign in' })).toBeEnabled();\n  });\n\n  test('valid user reaches the dashboard', { tag: '@smoke' }, async ({ page }) => {\n    await page.getByLabel('Email').fill('student@qa.academy');\n    await page.getByLabel('Password').fill('Learn@123');\n    await page.getByRole('button', { name: 'Sign in' }).click();\n\n    await expect(page.getByRole('alert')).toHaveText('Signing in…');\n    await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n    await expect(page.getByRole('listitem')).toHaveCount(3);   // three course weeks listed\n  });\n\n  test('wrong password shows an error', { tag: '@regression' }, async ({ page }) => {\n    await page.getByLabel('Email').fill('student@qa.academy');\n    await page.getByLabel('Password').fill('wrong-password');\n    await page.getByRole('button', { name: 'Sign in' }).click();\n\n    await expect(page.getByRole('alert')).toHaveText('Invalid email or password');\n    await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeHidden();\n  });\n\n  test('empty form shows a validation message', { tag: '@regression' }, async ({ page }) => {\n    await page.getByRole('button', { name: 'Sign in' }).click();\n    await expect(page.getByTestId('login-message')).toHaveText('Please enter your email and password');\n  });\n\n  test('pressing Enter submits the form', { tag: '@regression' }, async ({ page }) => {\n    await page.getByLabel('Email').fill('student@qa.academy');\n    await page.getByLabel('Password').fill('Learn@123');\n    await page.getByLabel('Password').press('Enter');   // keyboard instead of mouse\n    await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n  });\n\n  test('remember-me can be ticked and unticked', { tag: '@regression' }, async ({ page }) => {\n    const rememberMe = page.getByRole('checkbox', { name: 'Remember me' });   // a locator in a variable\n    await expect(rememberMe).not.toBeChecked();\n    await rememberMe.check();\n    await expect(rememberMe).toBeChecked();\n    await rememberMe.uncheck();\n    await expect(rememberMe).not.toBeChecked();\n  });\n});",
                  "mode": "editor",
                  "file": "tests/day9/login.spec.ts",
                  "run": "npx playwright test tests/day9/login.spec.ts --project=chromium"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9/login.spec.ts --project=chromium"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 6 tests using 4 workers\n\n  ✓  1 [chromium] › tests/day9/login.spec.ts:10:7 › Sign in page › shows the sign-in form @smoke (240ms)\n  ✓  2 [chromium] › tests/day9/login.spec.ts:27:7 › Sign in page › wrong password shows an error @regression (260ms)\n  ✓  3 [chromium] › tests/day9/login.spec.ts:36:7 › Sign in page › empty form shows a validation message @regression (180ms)\n  ✓  4 [chromium] › tests/day9/login.spec.ts:17:7 › Sign in page › valid user reaches the dashboard @smoke (1.1s)\n  ✓  5 [chromium] › tests/day9/login.spec.ts:41:7 › Sign in page › pressing Enter submits the form @regression (1.0s)\n  ✓  6 [chromium] › tests/day9/login.spec.ts:48:7 › Sign in page › remember-me can be ticked and unticked @regression (150ms)\n\n  6 passed (2.4s)"
                },
                {
                  "type": "markdown",
                  "content": "Notice: `fullyParallel: true` lets tests from the same file run at the same time, so they may finish in a different order each run. That's fine — every test is independent thanks to `beforeEach` and fresh pages."
                },
                {
                  "type": "quiz",
                  "id": "d9-i2-q1",
                  "quizType": "single",
                  "question": "Why is `await page.setContent(loginPage)` in `beforeEach` and not in a `beforeAll`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`beforeAll` is slower"
                    },
                    {
                      "id": "b",
                      "text": "`beforeAll` can't use the `page` fixture, and every test needs its own fresh page anyway"
                    },
                    {
                      "id": "c",
                      "text": "`beforeEach` runs only once"
                    },
                    {
                      "id": "d",
                      "text": "There is no difference"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Each test gets its own new page, so opening the practice page must happen per test — in beforeEach, which also has access to `page`."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d9-practice-1",
              "title": "Quiz · Day 9 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d9-pr-q1",
                  "quizType": "single",
                  "question": "Which file names will Playwright pick up as tests in `tests/`? (Choose the best answer)",
                  "options": [
                    {
                      "id": "a",
                      "text": "Any .ts file"
                    },
                    {
                      "id": "b",
                      "text": "Files ending in .spec.ts or .test.ts"
                    },
                    {
                      "id": "c",
                      "text": "Only example.spec.ts"
                    },
                    {
                      "id": "d",
                      "text": "Files that start with \"test\""
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "That's why `practice-pages.ts` is treated as a normal module, not a test file."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q2",
                  "quizType": "single",
                  "question": "Which assertion checks that an input field contains the text the user typed?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`toHaveText`"
                    },
                    {
                      "id": "b",
                      "text": "`toHaveValue`"
                    },
                    {
                      "id": "c",
                      "text": "`toContainText`"
                    },
                    {
                      "id": "d",
                      "text": "`toHaveTitle`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "An input's typed content is its *value*, not its text. Use `await expect(locator).toHaveValue('…')`."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q6",
                  "quizType": "single",
                  "question": "`getByText('welcome')` — does it match an element with the text \"Welcome back, Student!\"?",
                  "options": [
                    {
                      "id": "a",
                      "text": "No — it's case-sensitive and needs the whole text"
                    },
                    {
                      "id": "b",
                      "text": "Yes — by default text matching is case-insensitive and matches a substring"
                    },
                    {
                      "id": "c",
                      "text": "{'Only if you add `{ exact': 'true }`'}"
                    },
                    {
                      "id": "d",
                      "text": "Only inside a describe"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Default text matching is case-insensitive and partial. `{ exact: true }` makes it case-sensitive and whole-string."
                },
                {
                  "type": "quiz",
                  "id": "d9-pr-q8",
                  "quizType": "single",
                  "question": "What is the MAIN purpose of `beforeEach` in a Playwright test file?",
                  "options": [
                    {
                      "id": "a",
                      "text": "To run the tests faster"
                    },
                    {
                      "id": "b",
                      "text": "To put shared setup (like opening the page) in one place so every test starts from the same state"
                    },
                    {
                      "id": "c",
                      "text": "To share a single page between all tests"
                    },
                    {
                      "id": "d",
                      "text": "To skip tests"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "It removes repetition and guarantees each test begins from a known state — while each test still gets its own fresh page."
                }
              ]
            },
            {
              "id": "d9-practice-2",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex1",
                  "title": "Test the log-out flow",
                  "level": "easy",
                  "prompt": "Create `tests/day9/logout.spec.ts` with one test, `user can log out`:\n\n1. Sign in as `student@qa.academy` / `Learn@123`.\n2. Wait for the Dashboard heading.\n3. Click **Log out**.\n4. Assert the heading **Signed out** is visible, the text **See you soon!** is visible, and the title contains `Signed out`.",
                  "file": "tests/day9/logout.spec.ts",
                  "run": "npx playwright test tests/day9/logout.spec.ts --project=chromium --headed",
                  "hints": [
                    "`page.getByRole('button', { name: 'Log out' })`",
                    "`await expect(page).toHaveTitle(/Signed out/)`"
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { loginPage } from './practice-pages';\n\ntest('user can log out', async ({ page }) => {\n  // Sign in\n  await page.setContent(loginPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('Learn@123');\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();\n\n  // Log out\n  await page.getByRole('button', { name: 'Log out' }).click();\n\n  // Check the signed-out page\n  await expect(page.getByRole('heading', { name: 'Signed out' })).toBeVisible();\n  await expect(page.getByText('See you soon!')).toBeVisible();\n  await expect(page).toHaveTitle(/Signed out/);\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/logout.spec.ts --project=chromium --headed"
                  }
                },
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d9-ex2",
                  "title": "Data-driven sign-in errors",
                  "level": "medium",
                  "prompt": "Use a **loop** (Day 7!) to create one test per row of test data. Create `tests/day9/login-data.spec.ts`:\n\n```ts\nconst invalidLogins = [\n  { email: '', password: '', message: 'Please enter your email and password' },\n  { email: 'student@qa.academy', password: '', message: 'Please enter your email and password' },\n  { email: 'nobody@qa.academy', password: 'Learn@123', message: 'Invalid email or password' },\n  { email: 'student@qa.academy', password: 'learn@123', message: 'Invalid email or password' },\n];\n```\n\nFor each row, register a test titled like `rejects \"nobody@qa.academy\" / \"Learn@123\"` that fills the fields, clicks **Sign in** and checks the alert text. Put all tests inside `test.describe('Invalid sign-in')` with a `beforeEach` that loads the page.\n\nRun with `--project=chromium` — you should see **4** tests.",
                  "file": "tests/day9/login-data.spec.ts",
                  "run": "npx playwright test tests/day9/login-data.spec.ts --project=chromium",
                  "hints": [
                    "Put `for (const data of invalidLogins) { test(`rejects ...`, async ({ page }) => { … }); }` inside the describe.",
                    "Test titles must be unique — including the email and password makes them unique.",
                    "`fill('')` is fine for empty fields."
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { loginPage } from './practice-pages';\n\n// Test data: one row = one test\nconst invalidLogins = [\n  { email: '', password: '', message: 'Please enter your email and password' },\n  { email: 'student@qa.academy', password: '', message: 'Please enter your email and password' },\n  { email: 'nobody@qa.academy', password: 'Learn@123', message: 'Invalid email or password' },\n  { email: 'student@qa.academy', password: 'learn@123', message: 'Invalid email or password' },\n];\n\ntest.describe('Invalid sign-in', () => {\n  test.beforeEach(async ({ page }) => {\n    await page.setContent(loginPage);\n  });\n\n  // Create one test per data row\n  for (const data of invalidLogins) {\n    test(`rejects \"${data.email}\" / \"${data.password}\"`, async ({ page }) => {\n      await page.getByLabel('Email').fill(data.email);\n      await page.getByLabel('Password').fill(data.password);\n      await page.getByRole('button', { name: 'Sign in' }).click();\n      await expect(page.getByRole('alert')).toHaveText(data.message);\n    });\n  }\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day9/login-data.spec.ts --project=chromium"
                  }
                }
              ]
            },
            {
              "id": "d9-practice-3",
              "title": "Reflection",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "1. Name the five parts of a Playwright test (from `import` to `expect`).\n2. Which fixture would you use to open a second tab in the same session? And to make two separate logged-in users?\n3. Why do web-first assertions need `await` while `expect(total).toBe(3)` does not?"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Annotations and tags, what the runner does under the hood (workers, timeouts, retries), filtering from the command line, debugging failures, the HTML report — and your Week 2 mini-project.",
                  "title": "Coming up on Day 10"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 13,
        "quizzes": 13,
        "exercises": 2,
        "codeBlocks": 9
      }
    },
    {
      "schemaVersion": "1.0",
      "day": 10,
      "week": 2,
      "title": "Playwright Test Runner II: Control, Debug & Ship",
      "subtitle": "Annotations and tags, how the runner works under the hood, filtering, debugging, the HTML report — and the Week 2 mini-project",
      "estimatedTime": "3–3.5 hours",
      "topics": [
        "Annotations & tags",
        "Runner internals: workers, timeouts, retries",
        "Debugging & reports",
        "Mini-project"
      ],
      "objectives": [
        "Control tests with `test.only`, `test.skip`, `test.fixme` and tags, and filter them from the CLI",
        "Explain how the runner discovers, parallelises, times out, retries and reports tests",
        "See hooks run in order across workers",
        "Debug a failing test from its error output, the Inspector and the HTML report",
        "Turn written requirements into a complete, tagged, passing test suite"
      ],
      "prerequisitesFromEarlierDays": [
        "Day 9: test anatomy, fixtures, locators, assertions, describe, beforeEach, the practice pages"
      ],
      "workspace": "pw-course/tests/day10/",
      "sections": [
        {
          "id": "prerequisites",
          "title": "Prerequisites",
          "intro": [],
          "lessons": [
            {
              "id": "d10-prerequisites-1",
              "title": "P1 · Quick recap from Day 9",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Quick checks on Day 9 before we take control of the runner:"
                },
                {
                  "type": "quiz",
                  "id": "d10-p1-q1",
                  "quizType": "single",
                  "question": "Which locator finds `<button>Sign in</button>`?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`page.getByRole('button', { name: 'Sign in' })`"
                    },
                    {
                      "id": "b",
                      "text": "`page.getByLabel('Sign in')`"
                    },
                    {
                      "id": "c",
                      "text": "`page.getByRole('link', { name: 'Sign in' })`"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "A button element has the role button; its text is its accessible name."
                },
                {
                  "type": "quiz",
                  "id": "d10-p1-q2",
                  "quizType": "single",
                  "question": "Where does `await page.setContent(loginPage)` belong when every test in a file needs it?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`test.beforeAll`"
                    },
                    {
                      "id": "b",
                      "text": "`test.beforeEach`"
                    },
                    {
                      "id": "c",
                      "text": "Copied into every test"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`beforeEach` runs before every test and has access to `page`; `beforeAll` runs once and cannot use `page`."
                },
                {
                  "type": "quiz",
                  "id": "d10-p1-q3",
                  "quizType": "single",
                  "question": "Which assertion keeps retrying until the text matches?",
                  "options": [
                    {
                      "id": "a",
                      "text": "`expect(await locator.textContent()).toBe('Dashboard')`"
                    },
                    {
                      "id": "b",
                      "text": "`await expect(locator).toHaveText('Dashboard')`"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "Web-first assertions retry (5 s by default); reading the text once and comparing does not."
                }
              ]
            }
          ]
        },
        {
          "id": "fundamentals",
          "title": "Fundamentals",
          "intro": [],
          "lessons": [
            {
              "id": "d10-fundamentals-1",
              "title": "F1 · Controlling which tests run: annotations and tags",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "| Annotation | Effect |\n|---|---|\n| `test.only('…', …)` | Run **only** this test (and other `.only` tests). Handy while writing — never commit it! |\n| `test.skip('…', …)` | Don't run this test; report it as skipped |\n| `test.skip(condition, 'reason')` *(inside a test)* | Skip only when the condition is true, e.g. `test.skip(browserName === 'webkit', 'Feature not supported in Safari yet')` |\n| `test.fixme('…', …)` | Skip because the **test** needs fixing (marks it as \"fixme\" in the report) |\n| `test.fail()` *(inside a test)* | \"This test is expected to fail\" (known bug) — it passes when it fails, and alerts you when it starts passing |\n| `test.slow()` *(inside a test)* | Triple the timeout for this test |\n\nThe config line `forbidOnly: !!process.env.CI` (Day 4) makes CI **fail** if someone left a `test.only` in the code — otherwise CI would silently run only one test.\n\n### Tags\n\nTags label tests so you can run subsets such as smoke or regression packs:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "test('valid user reaches the dashboard', { tag: '@smoke' }, async ({ page }) => { /* … */ });\n\ntest('remember-me box can be ticked', { tag: ['@regression', '@login'] }, async ({ page }) => { /* … */ });\n\ntest.describe('Enrolment', { tag: '@enrol' }, () => { /* every test inside gets @enrol */ });",
                  "mode": "read"
                },
                {
                  "type": "markdown",
                  "content": "(Tags written inside the title, like `'login works @smoke'`, also work.)"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test --grep @smoke            # only tests tagged @smoke",
                    "npx playwright test --grep-invert @slow      # everything EXCEPT @slow",
                    "npx playwright test --grep \"@smoke|@login\"   # @smoke OR @login"
                  ]
                },
                {
                  "type": "quiz",
                  "id": "d10-f6-q1",
                  "quizType": "single",
                  "question": "A teammate pushed code with `test.only` still in it. What happens on CI with the generated config?",
                  "options": [
                    {
                      "id": "a",
                      "text": "CI runs only that one test and reports success"
                    },
                    {
                      "id": "b",
                      "text": "CI fails the run, because `forbidOnly` is true when the CI variable is set"
                    },
                    {
                      "id": "c",
                      "text": "CI ignores `.only`"
                    },
                    {
                      "id": "d",
                      "text": "CI deletes the test"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "`forbidOnly: !!process.env.CI` turns a leftover `.only` into a failure on CI, protecting the full suite from being silently skipped."
                }
              ]
            },
            {
              "id": "d10-fundamentals-2",
              "title": "F2 · What the runner does when you press Run",
              "blocks": [
                {
                  "type": "diagram",
                  "format": "mermaid",
                  "content": "flowchart LR\n  A[\"1. Load config<br/>playwright.config.ts\"] --> B[\"2. Discover tests<br/>*.spec.ts in testDir\"]\n  B --> C[\"3. Multiply by projects<br/>(chromium, firefox, webkit)\"]\n  C --> D[\"4. Distribute to workers<br/>(parallel processes)\"]\n  D --> E[\"5. For each test:<br/>fixtures → hooks → body → teardown\"]\n  E --> F[\"6. Retry failures<br/>(if retries > 0)\"]\n  F --> G[\"7. Report<br/>list, html…\"]"
                },
                {
                  "type": "markdown",
                  "content": "### Timeouts to remember\n\n| Timeout | Default | Change it with |\n|---|---|---|\n| Whole test — the body plus `beforeEach` hooks and fixture setup | **30 s** | `timeout` in the config, `--timeout=60000`, or `test.setTimeout(60000)` |\n| Each web-first assertion (`expect`) | **5 s** | `expect: { timeout: 10000 }` in the config, or `{ timeout: 10000 }` on one assertion |\n| Each action (`click`, `fill`…) | no separate limit — bounded by the test timeout | `use: { actionTimeout: 10000 }` |\n\n### Results you can get\n\n| Status | Meaning |\n|---|---|\n| ✓ **passed** | Everything worked |\n| ✘ **failed** | An action/assertion failed or timed out |\n| **flaky** | Failed first, then **passed on retry** — investigate! |\n| **skipped** | Skipped by `test.skip` / `test.fixme` |\n\nWhen any test fails, the command ends with a non-zero **exit code** — that's how CI knows the build is red."
                },
                {
                  "type": "quiz",
                  "id": "d10-f7-q1",
                  "quizType": "single",
                  "question": "With `retries: 1`, a test fails on the first attempt and passes on the retry. How is it reported?",
                  "options": [
                    {
                      "id": "a",
                      "text": "passed"
                    },
                    {
                      "id": "b",
                      "text": "failed"
                    },
                    {
                      "id": "c",
                      "text": "flaky"
                    },
                    {
                      "id": "d",
                      "text": "skipped"
                    }
                  ],
                  "correct": [
                    "c"
                  ],
                  "explanation": "Passing only on retry means the result isn't consistent. Playwright marks it flaky so you can investigate the root cause."
                }
              ]
            }
          ]
        },
        {
          "id": "implementation",
          "title": "Implementation",
          "intro": [],
          "lessons": [
            {
              "id": "d10-implementation-1",
              "title": "I1 · Filter, list, tag and skip",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Run subsets of your suite:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "# only smoke tests",
                    "npx playwright test tests/day9 --grep @smoke --project=chromium",
                    "# everything except smoke tests",
                    "npx playwright test tests/day9 --grep-invert @smoke --project=chromium",
                    "# titles containing \"error\"",
                    "npx playwright test tests/day9 -g \"error\" --project=chromium",
                    "# what would run in ALL browsers, without running it",
                    "npx playwright test tests/day9/login.spec.ts --list"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Listing tests:\n  [chromium] › day9/login.spec.ts:10:7 › Sign in page › shows the sign-in form @smoke\n  [chromium] › day9/login.spec.ts:17:7 › Sign in page › valid user reaches the dashboard @smoke\n  …\n  [webkit] › day9/login.spec.ts:48:7 › Sign in page › remember-me can be ticked and unticked @regression\nTotal: 18 tests in 1 file"
                },
                {
                  "type": "markdown",
                  "content": "6 tests × 3 projects = 18.\n\nNow try the annotations. Create `tests/day10/annotations.spec.ts` (it imports the practice pages from the Day 9 folder):"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { loginPage } from '../day9/practice-pages';\n\ntest('runs everywhere', async ({ page }) => {\n  await page.setContent(loginPage);\n  await expect(page.getByRole('button', { name: 'Sign in' })).toBeVisible();\n});\n\ntest('skipped on WebKit only', async ({ page, browserName }) => {\n  // Conditional skip: imagine a known Safari-only issue\n  test.skip(browserName === 'webkit', 'Known Safari issue BUG-123');\n  await page.setContent(loginPage);\n  await expect(page.getByLabel('Remember me')).not.toBeChecked();\n});\n\ntest.skip('password reset link', async ({ page }) => {\n  // Not built yet — skipped for everyone\n});\n\ntest.fixme('sign in with Google', async ({ page }) => {\n  // The test itself needs work — shows as \"fixme\" in the report\n});",
                  "mode": "editor",
                  "file": "tests/day10/annotations.spec.ts",
                  "run": "npx playwright test tests/day10/annotations.spec.ts"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day10/annotations.spec.ts"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 12 tests using 4 workers\n\n  ✓   1 [chromium] › tests/day10/annotations.spec.ts:4:5 › runs everywhere (210ms)\n  ✓   2 [chromium] › tests/day10/annotations.spec.ts:9:5 › skipped on WebKit only (190ms)\n  -   3 [chromium] › tests/day10/annotations.spec.ts:16:6 › password reset link\n  -   4 [chromium] › tests/day10/annotations.spec.ts:20:6 › sign in with Google\n  ✓   5 [firefox] › tests/day10/annotations.spec.ts:4:5 › runs everywhere (520ms)\n  …\n  -  11 [webkit] › tests/day10/annotations.spec.ts:9:5 › skipped on WebKit only\n  …\n\n  7 skipped\n  5 passed (3.1s)"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "Change one `test(` to `test.only(` and run the file — only that test runs. Remove `.only` before you commit: on CI it fails the whole run (`forbidOnly`).",
                  "title": "`test.only` while you work"
                }
              ]
            },
            {
              "id": "d10-implementation-2",
              "title": "I2 · See the hooks run in order",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\n\ntest.beforeAll(async () => {\n  console.log('beforeAll  → once, before the tests');\n});\n\ntest.beforeEach(async ({ page }) => {\n  console.log('beforeEach → before each test');\n  await page.setContent('<h1>Hooks demo</h1>');\n});\n\ntest.afterEach(async () => {\n  console.log('afterEach  → after each test');\n});\n\ntest.afterAll(async () => {\n  console.log('afterAll   → once, after the tests');\n});\n\ntest('first test', async ({ page }) => {\n  console.log('test body  → first test');\n  await expect(page.getByRole('heading')).toHaveText('Hooks demo');\n});\n\ntest('second test', async ({ page }) => {\n  console.log('test body  → second test');\n  await expect(page.getByRole('heading')).toBeVisible();\n});",
                  "mode": "editor",
                  "file": "tests/day10/hooks.spec.ts",
                  "run": "npx playwright test tests/day10/hooks.spec.ts --project=chromium --workers=1"
                },
                {
                  "type": "markdown",
                  "content": "`--workers=1` makes the tests run one after another so the order is easy to read:"
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day10/hooks.spec.ts --project=chromium --workers=1"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "Running 2 tests using 1 worker\n\nbeforeAll  → once, before the tests\nbeforeEach → before each test\ntest body  → first test\nafterEach  → after each test\n  ✓  1 [chromium] › tests/day10/hooks.spec.ts:20:5 › first test (160ms)\nbeforeEach → before each test\ntest body  → second test\nafterEach  → after each test\nafterAll   → once, after the tests\n  ✓  2 [chromium] › tests/day10/hooks.spec.ts:25:5 › second test (40ms)\n\n  2 passed (900ms)"
                },
                {
                  "type": "markdown",
                  "content": "**Try it:** run the same command **without** `--workers=1`. If Playwright starts two or more workers (the default is half your CPU cores), each worker runs its own `beforeAll` and `afterAll` — you'll see them printed more than once."
                }
              ]
            },
            {
              "id": "d10-implementation-3",
              "title": "I3 · Debug a failing test",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "Create a test with two deliberate mistakes:"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "content": "import { test, expect } from '@playwright/test';\nimport { loginPage } from '../day9/practice-pages';\n\ntest('wrong expected text', async ({ page }) => {\n  await page.setContent(loginPage);\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  // Mistake 1: the real message is \"Please enter your email and password\"\n  await expect(page.getByRole('alert')).toHaveText('Email is required');\n});\n\ntest('element that does not exist', async ({ page }) => {\n  test.setTimeout(5000);   // fail faster than the default 30 s while we practise\n  await page.setContent(loginPage);\n  // Mistake 2: there is no \"Log in\" button — it's called \"Sign in\"\n  await page.getByRole('button', { name: 'Log in' }).click();\n});",
                  "mode": "editor",
                  "file": "tests/day10/broken.spec.ts",
                  "run": "npx playwright test tests/day10/broken.spec.ts --project=chromium",
                  "expectError": true
                },
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day10/broken.spec.ts --project=chromium"
                  ]
                },
                {
                  "type": "output",
                  "target": "terminal",
                  "content": "  1) [chromium] › tests/day10/broken.spec.ts:4:5 › wrong expected text ─────────────\n\n    Error: expect(locator).toHaveText(expected) failed\n\n    Locator:  getByRole('alert')\n    Expected: \"Email is required\"\n    Received: \"Please enter your email and password\"\n    Timeout:  5000ms\n\n       6 |   await page.getByRole('button', { name: 'Sign in' }).click();\n       7 |   // Mistake 1: the real message is \"Please enter your email and password\"\n    >  8 |   await expect(page.getByRole('alert')).toHaveText('Email is required');\n         |                                         ^\n\n  2) [chromium] › tests/day10/broken.spec.ts:11:5 › element that does not exist ──────\n\n    Test timeout of 5000ms exceeded.\n\n    Error: locator.click: Test timeout of 5000ms exceeded.\n    Call log:\n      - waiting for getByRole('button', { name: 'Log in' })\n\n      14 |   // Mistake 2: there is no \"Log in\" button — it's called \"Sign in\"\n    > 15 |   await page.getByRole('button', { name: 'Log in' }).click();\n         |                                                      ^\n\n  2 failed"
                },
                {
                  "type": "markdown",
                  "content": "Two very different failures:\n\n| Failure | Clue in the output | Typical cause |\n|---|---|---|\n| **Assertion failed** | `Expected: …` vs `Received: …` | Wrong expectation — or a real bug in the app |\n| **Timeout waiting for an element** | `waiting for getByRole('button', { name: 'Log in' })` | Wrong locator, element never appears, or appears too late |\n\n### Your debugging toolbox\n\n| Tool | Command | Best for |\n|---|---|---|\n| Error output | *(always shown)* | First look: which line, expected vs received |\n| HTML report | `npx playwright show-report` | Browsing failures, steps and timings; screenshots/traces when enabled |\n| Headed mode | `--headed` | Watching what really happens |\n| Inspector | `npx playwright test tests/day10/broken.spec.ts --debug` | Stepping through one action at a time |\n| UI Mode | `npx playwright test --ui` | Watch mode, time-travel through each step |\n\nFix both mistakes (`'Please enter your email and password'` and `'Sign in'`), remove the `test.setTimeout` line, and run again until it's green."
                }
              ]
            },
            {
              "id": "d10-implementation-4",
              "title": "I4 · Read the HTML report",
              "blocks": [
                {
                  "type": "terminal",
                  "shell": "bash",
                  "commands": [
                    "npx playwright test tests/day9 tests/day10 --project=chromium",
                    "npx playwright show-report"
                  ]
                },
                {
                  "type": "markdown",
                  "content": "In the report:\n\n1. Use the filters at the top (**Passed / Failed / Flaky / Skipped**) and the search box — try typing `@smoke`.\n2. Click a test to see every step (`setContent`, `fill`, `click`, `expect …`) with its duration.\n3. Failed tests show the same error as the terminal, plus attachments when screenshots or traces are enabled in the config.\n\nPress `Ctrl + C` in the terminal to stop the report server when you're done."
                }
              ]
            }
          ]
        },
        {
          "id": "practice",
          "title": "Practice",
          "intro": [],
          "lessons": [
            {
              "id": "d10-practice-1",
              "title": "Quiz · Day 10 check",
              "blocks": [
                {
                  "type": "quiz",
                  "id": "d10-pr-q3",
                  "quizType": "single",
                  "question": "What is the default timeout for a whole test, and for one web-first assertion?",
                  "options": [
                    {
                      "id": "a",
                      "text": "5 s and 30 s"
                    },
                    {
                      "id": "b",
                      "text": "30 s and 5 s"
                    },
                    {
                      "id": "c",
                      "text": "60 s and 10 s"
                    },
                    {
                      "id": "d",
                      "text": "No limit for both"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "A test may take up to 30 s (its body plus beforeEach hooks and fixture setup; afterEach and beforeAll/afterAll get their own limit of the same length). Each expect retries for up to 5 s."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q4",
                  "quizType": "multiple",
                  "question": "Which of these are valid ways to tag a test as @smoke? (Select all that apply)",
                  "options": [
                    {
                      "id": "a",
                      "text": "`test('login works', { tag: '@smoke' }, async ({ page }) => {…})`"
                    },
                    {
                      "id": "b",
                      "text": "`test('login works @smoke', async ({ page }) => {…})`"
                    },
                    {
                      "id": "c",
                      "text": "`test.smoke('login works', async ({ page }) => {…})`"
                    },
                    {
                      "id": "d",
                      "text": "`test.describe('Login', { tag: '@smoke' }, () => {…})` around the test"
                    }
                  ],
                  "correct": [
                    "a",
                    "b",
                    "d"
                  ],
                  "explanation": "Tags go in the details object, in the title, or on a describe (applies to every test inside). There is no `test.smoke`."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q5",
                  "quizType": "single",
                  "question": "`test.skip(browserName === 'firefox', 'Upload not supported')` is written inside a test. When is the test skipped?",
                  "options": [
                    {
                      "id": "a",
                      "text": "Always"
                    },
                    {
                      "id": "b",
                      "text": "Only when it runs in the firefox project"
                    },
                    {
                      "id": "c",
                      "text": "Never — skip must be outside the test"
                    },
                    {
                      "id": "d",
                      "text": "Only on CI"
                    }
                  ],
                  "correct": [
                    "b"
                  ],
                  "explanation": "The conditional form of `test.skip` skips only when the condition is true."
                },
                {
                  "type": "quiz",
                  "id": "d10-pr-q7",
                  "quizType": "single",
                  "question": "You run `npx playwright test --grep @smoke --project=firefox`. Two files contain 3 and 4 tests; 2 tests in total are tagged @smoke. How many test runs?",
                  "options": [
                    {
                      "id": "a",
                      "text": "2"
                    },
                    {
                      "id": "b",
                      "text": "6"
                    },
                    {
                      "id": "c",
                      "text": "7"
                    },
                    {
                      "id": "d",
                      "text": "21"
                    }
                  ],
                  "correct": [
                    "a"
                  ],
                  "explanation": "Only the 2 @smoke tests are selected, and only one project runs: 2 × 1 = 2."
                }
              ]
            },
            {
              "id": "d10-practice-2",
              "title": "Spot the bug",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-bug1",
                  "title": "Four bugs, one test file",
                  "level": "medium",
                  "prompt": "This file has **four** bugs. Some make tests fail, some make them unreliable, one breaks CI. Find and fix them all, then save the fixed version as `tests/day10/bugs.spec.ts` and run it.\n\n```ts\nimport { test, expect } from '@playwright/test';\nimport { loginPage } from '../day9/practice-pages';\n\ntest.beforeAll(async ({ page }) => {\n  await page.setContent(loginPage);\n});\n\ntest.only('wrong password shows an error', async ({ page }) => {\n  await page.setContent(loginPage);\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('nope');\n  page.getByRole('button', { name: 'Sign in' }).click();\n  expect(page.getByRole('alert')).toHaveText('Invalid email or password');\n});\n```",
                  "file": "tests/day10/bugs.spec.ts",
                  "run": "npx playwright test tests/day10/bugs.spec.ts --project=chromium",
                  "hints": [
                    "Which fixtures are available in beforeAll?",
                    "Which lines talk to the browser but have no `await`?",
                    "What does `forbidOnly` do on CI?"
                  ],
                  "solution": "import { test, expect } from '@playwright/test';\nimport { loginPage } from '../day9/practice-pages';\n\n// Bug 1: beforeAll can't use `page` → use beforeEach (and remove the duplicate setContent in the test)\ntest.beforeEach(async ({ page }) => {\n  await page.setContent(loginPage);\n});\n\n// Bug 2: test.only would fail CI (forbidOnly) and hide all other tests → plain test()\ntest('wrong password shows an error', async ({ page }) => {\n  await page.getByLabel('Email').fill('student@qa.academy');\n  await page.getByLabel('Password').fill('nope');\n  // Bug 3: missing await on the click\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  // Bug 4: missing await on the web-first assertion\n  await expect(page.getByRole('alert')).toHaveText('Invalid email or password');\n});",
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day10/bugs.spec.ts --project=chromium"
                  }
                }
              ]
            },
            {
              "id": "d10-practice-3",
              "title": "Exercises",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "terminal",
                  "id": "d10-ex3",
                  "title": "Run exactly what you need",
                  "level": "easy",
                  "prompt": "Write (and run) the command for each:\n\n1. Only the `@smoke` tests in `tests/day9`, Chromium only.\n2. Every test in `tests/day9` **except** `@smoke`, one worker, Firefox only.\n3. Only tests with `dashboard` in the title, with the browser visible.\n4. List all tests in `tests/day9/login.spec.ts` for WebKit without running them.\n5. Re-run only the tests that failed last time, then open the HTML report.",
                  "solution": "npx playwright test tests/day9 --grep @smoke --project=chromium\nnpx playwright test tests/day9 --grep-invert @smoke --workers=1 --project=firefox\nnpx playwright test -g \"dashboard\" --headed\nnpx playwright test tests/day9/login.spec.ts --project=webkit --list\nnpx playwright test --last-failed\nnpx playwright show-report",
                  "check": {
                    "kind": "commandsRun",
                    "commands": [
                      "npx playwright test tests/day9 --grep @smoke --project=chromium",
                      "npx playwright test tests/day9 --grep-invert @smoke --workers=1 --project=firefox",
                      "npx playwright test -g \"dashboard\" --headed",
                      "npx playwright test tests/day9/login.spec.ts --project=webkit --list",
                      "npx playwright test --last-failed",
                      "npx playwright show-report"
                    ]
                  }
                }
              ]
            },
            {
              "id": "d10-practice-4",
              "title": "Week 2 mini-project · Test the enrolment page",
              "blocks": [
                {
                  "type": "exercise",
                  "exerciseType": "code",
                  "id": "d10-project",
                  "title": "Mini-project: automate the QA Academy enrolment page",
                  "level": "challenge",
                  "prompt": "You've received these requirements for the **enrolment page** (`enrolPage` in `tests/day9/practice-pages.ts`). Like a manual tester, first list your test cases — then automate them in `tests/day10/enrol.spec.ts`.\n\n**Requirements**\n- **R1** The page title is `QA Academy - Enrol` and the heading is `Enrol in a course`.\n- **R2** The **Enrol now** button is **disabled** until *I accept the terms* is ticked, and disabled again when it is unticked.\n- **R3** Submitting without a name shows `Name is required`.\n- **R4** An email without `@` shows `Enter a valid email`.\n- **R5** Submitting without choosing a course shows `Please choose a course`.\n- **R6** A valid enrolment (name `Asha Verma`, email `asha@example.com`, course **API Testing**) shows `Thanks, Asha! You are enrolled in API Testing.`\n- **R7** After a successful enrolment, `Seats left` goes from **12** to **11**.\n\n**Your suite must**\n- use `test.describe('Enrolment page', …)` and a `beforeEach` that loads the page,\n- have at least **7** tests (one per requirement — R6 and R7 may share one test if you prefer 6),\n- tag R1 and R6 with `@smoke`, the rest with `@regression`,\n- use user-facing locators (`getByRole`, `getByLabel`, `getByTestId`) and web-first assertions only,\n- pass on **chromium** and **firefox**.\n\nThen run:\n```\nnpx playwright test tests/day10/enrol.spec.ts --project=chromium --project=firefox\nnpx playwright test tests/day10/enrol.spec.ts --grep @smoke\nnpx playwright show-report\n```",
                  "file": "tests/day10/enrol.spec.ts",
                  "run": "npx playwright test tests/day10/enrol.spec.ts --project=chromium --project=firefox",
                  "hints": [
                    "The course dropdown has the label Course: `page.getByLabel('Course').selectOption('API Testing')` (by visible text or by value 'api').",
                    "The seats text has a test id: `page.getByTestId('seats')`.",
                    "To test R3–R5 the Enrol button must be enabled — tick the terms box first.",
                    "Write a small helper function inside the file (e.g. `fillForm(page, name, email, course)`) to avoid repeating the same fills. Its `page` parameter can be typed with `import { type Page } from '@playwright/test'`."
                  ],
                  "solution": "import { test, expect, type Page } from '@playwright/test';\nimport { enrolPage } from '../day9/practice-pages';\n\n// Helper: fill the form (empty strings leave a field blank) and accept the terms\nasync function fillForm(page: Page, name: string, email: string, course: string): Promise<void> {\n  await page.getByLabel('Full name').fill(name);\n  await page.getByLabel('Email').fill(email);\n  if (course !== '') {\n    await page.getByLabel('Course').selectOption(course);\n  }\n  await page.getByLabel('I accept the terms').check();\n}\n\ntest.describe('Enrolment page', () => {\n  test.beforeEach(async ({ page }) => {\n    await page.setContent(enrolPage);\n  });\n\n  test('R1: shows the correct title and heading', { tag: '@smoke' }, async ({ page }) => {\n    await expect(page).toHaveTitle('QA Academy - Enrol');\n    await expect(page.getByRole('heading', { name: 'Enrol in a course' })).toBeVisible();\n  });\n\n  test('R2: Enrol button follows the terms checkbox', { tag: '@regression' }, async ({ page }) => {\n    const enrol = page.getByRole('button', { name: 'Enrol now' });\n    const terms = page.getByLabel('I accept the terms');\n    await expect(enrol).toBeDisabled();\n    await terms.check();\n    await expect(enrol).toBeEnabled();\n    await terms.uncheck();\n    await expect(enrol).toBeDisabled();\n  });\n\n  test('R3: name is required', { tag: '@regression' }, async ({ page }) => {\n    await fillForm(page, '', 'asha@example.com', 'API Testing');\n    await page.getByRole('button', { name: 'Enrol now' }).click();\n    await expect(page.getByRole('status')).toHaveText('Name is required');\n  });\n\n  test('R4: email must contain @', { tag: '@regression' }, async ({ page }) => {\n    await fillForm(page, 'Asha Verma', 'asha.example.com', 'API Testing');\n    await page.getByRole('button', { name: 'Enrol now' }).click();\n    await expect(page.getByRole('status')).toHaveText('Enter a valid email');\n  });\n\n  test('R5: a course must be chosen', { tag: '@regression' }, async ({ page }) => {\n    await fillForm(page, 'Asha Verma', 'asha@example.com', '');\n    await page.getByRole('button', { name: 'Enrol now' }).click();\n    await expect(page.getByRole('status')).toHaveText('Please choose a course');\n  });\n\n  test('R6: valid enrolment shows a confirmation', { tag: '@smoke' }, async ({ page }) => {\n    await fillForm(page, 'Asha Verma', 'asha@example.com', 'API Testing');\n    await page.getByRole('button', { name: 'Enrol now' }).click();\n    await expect(page.getByRole('status')).toHaveText('Thanks, Asha! You are enrolled in API Testing.');\n  });\n\n  test('R7: seats left goes down by one', { tag: '@regression' }, async ({ page }) => {\n    await expect(page.getByTestId('seats')).toHaveText('Seats left: 12');\n    await fillForm(page, 'Asha Verma', 'asha@example.com', 'Playwright Basics');\n    await page.getByRole('button', { name: 'Enrol now' }).click();\n    await expect(page.getByTestId('seats')).toHaveText('Seats left: 11');\n  });\n});",
                  "rubric": [
                    "All 7 requirements have at least one assertion",
                    "describe + beforeEach used; no test depends on another test",
                    "Correct tags; `--grep @smoke` runs exactly the R1 and R6 tests",
                    "Every browser action and web-first assertion is awaited",
                    "Passes on chromium and firefox"
                  ],
                  "check": {
                    "kind": "testsPass",
                    "cwd": "pw-course",
                    "run": "npx playwright test tests/day10/enrol.spec.ts --project=chromium --project=firefox"
                  }
                }
              ]
            },
            {
              "id": "d10-practice-5",
              "title": "Week 2 wrap-up",
              "blocks": [
                {
                  "type": "markdown",
                  "content": "You can now:\n\n- ✅ Read and write TypeScript: variables, types, operators, conditions, loops, functions, async/await, modules\n- ✅ Write, organise, tag, filter, run and debug Playwright tests\n- ✅ Turn written requirements into an automated test suite"
                },
                {
                  "type": "callout",
                  "variant": "tip",
                  "content": "**Locators in depth** (role, text, CSS, XPath, filtering, chaining, strictness), **actions** (hover, drag-and-drop, uploads, dialogs, frames), more **assertions**, and your first **Page Object Model** — moving the \"how\" out of your tests into `pages/`.",
                  "title": "Coming up in Week 3"
                }
              ]
            }
          ]
        }
      ],
      "stats": {
        "lessons": 12,
        "quizzes": 9,
        "exercises": 3,
        "codeBlocks": 4
      }
    }
  ]
}
