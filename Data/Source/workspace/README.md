# Workspace seed

Files every Terminal workspace starts with, as a new project made with `npm init playwright@latest`
would have them. `npm run build:content` copies them into `Data/Content/workspaces.json`, with each
week's own seed files. `tests/example.spec.ts` is the example test that Playwright 1.63 writes.
